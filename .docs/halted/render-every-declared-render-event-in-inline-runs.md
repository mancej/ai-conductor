# Halt record

Status: resolved
Resolution cause: rekick
Resolved at: 2026-09-10T22:54:46.938Z
Slug: render-every-declared-render-event-in-inline-runs
Class: needs-human
Halting step: unknown
Phase: unknown
Branch: feat/daemon-render-every-declared-render-event-in-inline-runs
Head SHA: 0f3c1af7a002c3ce6aaa3574b3314aebe7fa5e74
Halted at: 2026-09-10T15:03:27.833Z

Push status: this record may be ahead of the remote; push is not guaranteed.

## HALT

```text
Need a corrected task breakdown: the 2026-09-10 ADR-003 amendment requires a single terminal UIRenderer, internal UISubscriber fan-out, ui_renderer:terminal registration, and index/plugin-loader composition, but Tasks 1-3 still require the superseded callback/class forwarding partition and list none of the necessary composition files or acceptance proof.


stall:renderer-unification-breakdown (build: ANSWER — the corrected breakdown is derivable from committed artifacts, so BUILD proceeds without a new plan authoring pass. The governing contract is the plan's own 2026-09-10 operator amendment (.docs/plans/render-every-declared-render-event-in-inline-runs.md:3-14) plus the stories amendment (.docs/stories/render-every-declared-render-event-in-inline-runs.md:11-16), which together bring renderer unification in scope and bind it to APPROVED ADR-003 (.docs/decisions/003-ui-renderer-plugin-point.md). Where the amendment and a Task 1-3 Done-when conflict, the amendment supersedes: Task 1 Done when 3 ('still forwards exactly the three types') and Task 3 Done when 2 ('each sink-forwarded renderable type ... captures zero lines from the dashboard renderer') are void, because the amendment abolishes the callback/class forwarding partition those clauses describe. Every other Done-when in Tasks 1-3 survives verbatim and must still hold at the end. This is the same finding the SHIP halt raised (AB-1, DESIGN, 003-ui-renderer-plugin-point, .docs/halted/render-every-declared-render-event-in-inline-runs.md:22) and that the operator resolved by amending rather than by rewriting the tasks. Execute six ordered units of work. (A) Contract — src/conductor/src/ui/types.ts:17-25. Make UIRenderer.stop() return Promise<void> and change UISubscriber to start(renderers: UIRenderer[]): void / stop(): Promise<void>, exactly as ADR-003 declares. Matched pair: every implementer must move in the same commit — TerminalRenderer.stop (src/conductor/src/ui/terminal-renderer.ts:297), TerminalSubscriber (src/conductor/src/ui/subscriber.ts:29-76), and plugins/json-stdout-subscriber/index.ts:16. (B) Single terminal UIRenderer — fold src/conductor/src/ui/create-renderer.ts into the existing TerminalRenderer.handle (src/conductor/src/ui/terminal-renderer.ts:109), then delete create-renderer.ts. Coverage is a UNION, not a replacement: TerminalRenderer today lacks nine branches that create-renderer.ts has (build_no_progress, build_progress, build_stall, parallel_started, parallel_completed, parallel_failure, pipeline_closeout, unattributed_progress, when_skip) and create-renderer.ts lacks four that TerminalRenderer has (feature_usage_total, halt_marker_write_failed, pipeline_tail_diagnostic, renderer_error); the unified class must carry all thirteen plus the four dedicated loop branches at create-renderer.ts:307-338 (gate_verdict unsatisfied-only, kickback, loop_halt, loop_converged) and the dim fallback default at create-renderer.ts:337-343. This preserves Task 2 Done when 1-5 and Task 3 Done when 1 and 3, and preserves the daemon-facing coverage the existing terminal-renderer suite owns. The fallback set is computed once at construction as renderedEventTypes() minus the types that now have a dedicated branch; the current 'minus FORWARDED_TO_TERMINAL_RENDERER_EVENT_TYPES' subtraction (create-renderer.ts:50-52) is deleted because with one renderer writing one live region there is no second writer to deduplicate against. (C) Internal fan-out — src/conductor/src/ui/subscriber.ts:29-76. start(renderers) keeps subscribing once per type over renderedEventTypes() union NON_RENDERABLE_DASHBOARD_EVENT_TYPES (this is what keeps Task 1 Done when 1 and Done when 2 true; do not shrink either source), and per event dispatches await Promise.all(renderers.map(r => r.handle(event).catch(err => emit renderer_error with the renderer name and original error))) per ADR-003's multi-renderer dispatch. Drop the onRender callback constructor parameter and the terminalRenderer constructor parameter. Matched pair: deleting the exported FORWARDED_TO_TERMINAL_RENDERER_EVENT_TYPES constant (subscriber.ts:24-28) must also update its only two importers, create-renderer.ts:16 (deleted in B) and src/conductor/test/ui/subscriber.test.ts:6. Preserve the isForwardedFromFeature early return (subscriber.ts:59) ahead of the fan-out — its counterparts are src/conductor/src/engine/event-persister.ts:157 and src/conductor/src/daemon-cli.ts:1132, and removing it regresses the daemon double-print fix delivered by #2412/#2375. (D) Composition — the three files the amendment names. src/conductor/src/engine/plugin-loader.ts:226-235: register the TerminalRenderer instance as ui_renderer:terminal, delete the ui_renderer:terminal_renderer alias and the registration of TerminalSubscriber under ui_renderer (a subscriber registered as a renderer kind is precisely the ADR violation), and return new TerminalSubscriber(events). src/conductor/src/engine/cli-builtins.ts:9-22: drop the renderEvent parameter it only forwards. src/conductor/src/index.ts:1388: delete the createRenderer call; src/conductor/src/index.ts:1428-1435: resolve registry.get<UIRenderer>('ui_renderer', config?.ui_renderer ?? 'terminal') and call subscriber.start([renderer]) — select the renderer plugin first, then construct the subscriber wrapper around it. (E) json-stdout plugin — plugins/json-stdout-subscriber/index.ts:16 currently implements the old UISubscriber-as-ui_renderer shape and would be handed to start() as a renderer once (D) lands, so it must convert to UIRenderer in the same change: it already has handle(event), so make it async, keep stop(), and drop the start()/started gating. Its plugin.yml already declares kind: ui_renderer and needs no edit. This is ADR-003 conformance (the ADR names JSON-stdout as the motivating second backend), not new scope. (F) Acceptance proof — the artifacts the current tasks list none of. Move the create-renderer.test.ts cases onto the unified class in src/conductor/test/ui/terminal-renderer.test.ts (keeping the injected-live-region-over-a-capture-stream fixture, no conductor run, no provider, no network). Add: a subscriber unit proving one event reaches every registered renderer exactly once via Promise.all; a subscriber unit proving a renderer that rejects emits renderer_error and does not stop the other renderers from receiving subsequent events; a renderer unit proving each renderable type produces exactly one line (this is the replacement proof for Story 2's no-duplicate negative criterion, whose old wording depended on the deleted partition); and a plugin-loader unit proving ui_renderer:terminal resolves to an object satisfying UIRenderer and that the returned subscriber is not registered under ui_renderer. Update the orphaned importers of the deleted factory: src/conductor/test/ui/create-renderer.test.ts, src/conductor/test/create-renderer-progress.test.ts, src/conductor/test/ui/when-parallel-renderer.test.ts, src/conductor/test/progress-event-coverage.test.ts, src/conductor/test/ui/subscriber-events.test.ts, src/conductor/test/ui/json-stdout-config-selection.test.ts, src/conductor/test/json-stdout-progress.test.ts, src/conductor/test/engine/json-stdout-plugin-loader.test.ts, src/conductor/test/engine/plugin-loader.test.ts, src/conductor/test/engine/daemon-mode-feature-log.integration.test.ts, src/conductor/test/engine/artifact-resolution-wiring.test.ts, src/conductor/test/integration/otel-warning-wiring.test.ts, src/conductor/test/acceptance/step-completion-globs-are-feature-unscoped-so-anot.acceptance.test.ts and src/conductor/test/acceptance/build-post-task-tail-telemetry.acceptance.test.ts. Task 3 Step 4's docs/reference/cli.md paragraph is unchanged and still owed. Sweep boundary: the only sibling site found and deliberately excluded is plugins/recorder-provider/, which registers under a different plugin kind and is untouched by the ui_renderer contract change. Confidence high (~90%): the amendment names the exact topology, the exact three composition files and the exact test obligation, and ADR-003 supplies both interface signatures verbatim, so no load-bearing value had to be guessed. The single residual assumption, stated for the record, is that deleting create-renderer.ts as a single file does not trip build_review's testQuality counterfactual materialization — that failure mode is documented for directory deletions, not file deletions; if the preflight does fail as materialization-failed, reduce (B) to leaving create-renderer.ts on disk unreferenced and delete it in a follow-up feature.) — remediation produced no dispatchable build work; the implicated task(s) are already evidence-complete — human needed
```
