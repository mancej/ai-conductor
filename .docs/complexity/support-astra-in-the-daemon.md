# Complexity: Support Astra in the daemon

Tier: M

Rationale: The change spans operator configuration, Codex model dispatch and fallback, automatic rate-card coverage, and behavioral tests. It extends existing seams without adding a provider, schema migration, or new subsystem.
