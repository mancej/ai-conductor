# Complexity: Runtime values remain literal data

Tier: M

The operator approved Medium scope on 2026-09-06. The change crosses installer configuration, generated Git hooks, a session hook, and repository validation. It requires boundary-level regression coverage and generated-script inspection but no new runtime, persistent schema, service, or external integration. Small would understate the distinct shell/interpreter boundaries; Large would overstate an established argument-passing repair.

Scope excludes Python removal (#2266), retired bin/conduct behavior, and unrelated hook-policy changes.
