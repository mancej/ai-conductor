# Complexity: Qualify the reported cost figure when it covers only metered dispatches

Tier: S

Operator scope: small, confirmed 2026-09-06 (delegated).

One production function changes: the composer of the whole-feature finish usage line. Its inputs already carry both counts the qualification needs, so no rollup arithmetic, no event union member, and no persisted artifact format changes. The remaining edits are assertion updates in the three existing test files that pin the line's exact text, plus the daemon guide paragraph that documents its shape. No new module, dependency, storage, telemetry channel, CLI flag, configuration key, hook, or skill is introduced, and nothing in the change alters what a dispatch costs or how it is classified. Small-tier architecture, conflict-check, and coherence artifacts are not required.
