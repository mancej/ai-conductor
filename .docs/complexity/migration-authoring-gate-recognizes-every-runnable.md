# Complexity: Migration authoring gate recognizes every runnable fence

Tier: S

One bounded safety defect with existing parser, checker, shell-fixture infrastructure, and command-clause authority. Two dependent tasks extract the existing lexical recognizer and use it in the gate with inclusion regressions. No new dependency, external service, migration, configuration, or executable-language expansion. The scratch harnesses need only the new helper copied alongside existing library files. Technical track; Small composer route skips PRD, new architecture, conflict artifact, and coherence artifact.
