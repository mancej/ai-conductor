# Complexity: Simplify evaluator model routing to a two-way risk switch

Tier: S

Operator scope: small, confirmed 2026-09-06 (delegated).

The change rewrites one typed metadata string, regenerates one marked HARNESS.md region with the existing generator, syncs one mirrored reference-table cell, and restates two dispatch bullets plus one override sentence in two shipped skills. It introduces no module, seam, schema, config key, event, metric, CLI flag, or hook, and it changes no engine resolution path — `EXTRA_MODEL_TABLE_ROWS` is data-only and the Claude cell is free-form to the generator. No directory is deleted, no ADR is amended, and the approved generated-model-table ADR already declares a model-policy change to be a single metadata edit plus one regeneration. Small-tier architecture, conflict-check, and coherence artifacts are not required.
