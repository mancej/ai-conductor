# Coherence Mapping: plan-growth-allowance-is-spent-on-work-existing-ta (#2119)

Technical track (no fr rows). Outcomes staged from jstoup111/ai-conductor#2119.

| Row class | Cited id / criterion | Counterpart id(s) | Verdict | Notes / verbatim quote | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-1 | covered | Existing-task-owned finding draws no growth and does not halt an unspent allowance |
| outcome | outcome-2 | story-4 | covered | Bounded via lap allowance; budget named in halt and ledger |
| outcome | outcome-3 | story-6 | covered | Genuinely new tasks still draw growth and halt on true exhaustion |
| outcome | outcome-4 | story-5 | covered | Halt names the budget actually exhausted |
| outcome | outcome-5 | story-5, story-3 | covered | Operator can distinguish growth from unfinished planned work; re-stage delivers the unfinished tasks |
| story | story-1 | task-1, task-2, task-3 | covered | Task Story lines cite this story |
| story | story-2 | task-1, task-10 | covered | Task Story lines cite this story |
| story | story-3 | task-5, task-6 | covered | Task Story lines cite this story |
| story | story-4 | task-4, task-7 | covered | Task Story lines cite this story |
| story | story-5 | task-9 | covered | Task Story lines cite this story |
| story | story-6 | task-8 | covered | Task Story lines cite this story |
| task | task-1 | story-2 | covered | Story line confirmed in plan |
| task | task-2 | story-1 | covered | Story line confirmed in plan |
| task | task-3 | story-1 | covered | Story line confirmed in plan |
| task | task-4 | story-4 | covered | Story line confirmed in plan |
| task | task-5 | story-3 | covered | Story line confirmed in plan |
| task | task-6 | story-3 | covered | Story line confirmed in plan |
| task | task-7 | story-4 | covered | Story line confirmed in plan |
| task | task-8 | story-6 | covered | Story line confirmed in plan |
| task | task-9 | story-5 | covered | Story line confirmed in plan |
| task | task-10 | story-2 | covered | Story line confirmed in plan |
| adr | adr-2026-08-25-as-built-remediable-findings-bounded-build-route | story-1, story-3, story-4 | covered | Decision 9 (amended by this spec) is implemented by the non-appending route, re-stage, and lap-bounding stories |
| adr | adr-2026-08-22-prd-audit-stories-authority-and-bounded-kickback | story-1, story-6 | covered | Amended D5/D6 notes honored: append caps govern appending dispositions; growth untouched for existing-task |
| criterion | Story 1 happy: Given a validated as-built REMEDIABLE finding dispositioned `existing-task` bound to task ids present in the active plan, when remediation routes, then the feature rewinds to `build` and `appendRemediationTasks` is not invoked for that gap | task-3 | covered | the appender is never called for them | diff-local |
| criterion | Story 1 happy: Given an `existing-task` gap admitted in a round, when budgets are settled, then the kickback ledger's `growth.added` and `growth.remaining` are unchanged and `prdAuditAppendCap` is not consulted for that gap | task-3 | covered | Growth added/remaining are byte-identical before and after an all-existing-task round | diff-local |
| criterion | Story 1 happy: Given a prd_audit FIXABLE finding dispositioned `existing-task` bound to its owning plan task id, when remediation routes, then it takes the same non-appending route under gate key `prd_audit` | task-3 | covered | A prd_audit FIXABLE existing-task gap takes the same non-appending route in a test | diff-local |
| criterion | Story 1 negative: Given an `existing-task` gap whose bound id is absent from the active plan, when `resolvePlanTaskReference` fails to resolve it, then the disposition is invalid and the round halts naming the unresolvable id rather than silently appending or dropping the gap | task-2 | covered | an unresolvable one halts naming the id | diff-local |
| criterion | Story 1 negative: Given an `existing-task` gap with an empty task-binding list, when the remediation plan is read, then the gap is rejected as malformed rather than admitted as a free lap | task-1 | covered | rejected as malformed, not admitted as a free lap | diff-local |
| criterion | Story 1 negative: Given a plan whose growth allowance is fully unspent, when every finding in the round is `existing-task`, then no `kickback-cap` halt citing the plan-growth allowance is produced | task-3 | covered | routes to build instead of halting kickback-cap | diff-local |
| criterion | Story 2 happy: Given `.pipeline/remediation.json` containing an `existing-task` gap with resolvable bindings, when `readRemediationPlan` parses it, then the gap survives into the returned plan with its bound task ids intact | task-1 | covered | admits an `existing-task` gap with non-empty bound ids | diff-local |
| criterion | Story 2 happy: Given the `/remediate` skill contract, when the planner reads its disposition list, then `existing-task` is documented with the ownership test (the owning task's Done-when admits the remedy) alongside `publication`'s exclusion rationale | task-10 | covered | names existing-task, its ownership test, its id-binding requirement, and its append exclusion | diff-local |
| criterion | Story 2 negative: Given a remediation plan mixing one valid `existing-task` gap and one gap with an unknown disposition string, when parsed, then only the unknown disposition is dropped and the existing-task gap is still admitted | task-1 | covered | still drops unknown dispositions while keeping the valid existing-task gap | diff-local |
| criterion | Story 2 negative: Given an `existing-task` gap whose bound reference carries a tolerated trailing parenthesized annotation, when resolved, then the annotation is stripped per adr-2026-08-30 D3 and the bare id resolves — a re-derived `Number()` parse is never used | task-2 | covered | resolves after stripping (resolver behavior, no local `Number()` parse) | diff-local |
| criterion | Story 3 happy: Given an admitted `existing-task` gap bound to plan tasks currently marked `done` in `.pipeline/task-status.json`, when the route is taken, then those task ids are re-staged to `pending` via the same re-seed seam the appender uses before the rewind | task-5 | covered | rewrite each resolved bound id's status to pending via the existing re-seed seam | diff-local |
| criterion | Story 3 happy: Given the re-staged task-status, when the next BUILD dispatch starts, then it sees the bound tasks as pending work and executes them | task-5 | covered | done-to-pending transition for every bound id before dispatch | diff-local |
| criterion | Story 3 negative: Given a route where re-staging cannot be performed (task-status.json unreadable or the bound id missing from it), when the route would rewind, then the round halts fail-closed naming the re-stage failure instead of dispatching an empty BUILD | task-6 | covered | halt needs-human naming the re-stage defect and never dispatch | diff-local |
| criterion | Story 3 negative: Given a bound task already `pending`, when re-staging runs, then the route proceeds without error and the task remains pending (idempotent re-stage) | task-5 | covered | Idempotent case (already pending) passes without error | diff-local |
| criterion | Story 4 happy: Given an admitted `existing-task` round, when the route is taken, then exactly one lap is consumed under the owning gate's ledger key (`gates.architecture_review_as_built` or `gates.prd_audit`) | task-4 | covered | increments the owning gate's ledger laps by exactly one | diff-local |
| criterion | Story 4 happy: Given a pending as-built existing-task finding whose lap is authorized, when the binding resolves successfully, then a `pendingAsBuiltRemediationFindings` entry is persisted with the same fail-closed validation and cleared in the step that projects it (adr-2026-08-25 D7 as amended) | task-7 | covered | written on resolution success, cleared on projection | diff-local |
| criterion | Story 4 negative: Given a gate whose lap cap is already consumed, when a new `existing-task` round is requested, then the round halts `kickback-cap` with prose naming the lap cap (`lap cap reached (n/n)`) — not the plan-growth allowance | task-7 | covered | Lap-cap halt detail contains the lap figures and no growth-allowance phrase | diff-local |
| criterion | Story 4 negative: Given an `existing-task` lap that produces no tree-hash change by the next gate evaluation, when the no-op escalation pair evaluates, then it escalates to a halt instead of admitting another lap | task-7 | covered | a zero-progress existing-task lap escalates instead of looping | diff-local |
| criterion | Story 4 negative: Given a validation-group round carrying a `manual_test` FAIL alongside as-built gaps, when routing is decided, then the existing-task route does not run and the gaps ride the consolidated dispatch (adr-2026-08-25 D8) | task-8 | covered | the existing-task route is unreachable when a FAIL is present | diff-local |
| criterion | Story 5 happy: Given an appending round that genuinely exceeds the growth cap, when it halts, then the prose still reports the growth figures exactly as today (`n/cap appended; k requested, r remaining`) and lists every finding | task-9 | covered | Existing growth-halt wording and finding rendering are unchanged for appending rounds | diff-local |
| criterion | Story 5 happy: Given any new halt fixture in this feature, when its assertions run, then they assert the kickback-cap halt class and typed ledger figures and no new machinery parses the halt prose for authorization (adr-2026-08-29 D2) | task-9 | covered | Halt class remains kickback-cap in every new fixture | diff-local |
| criterion | Story 5 negative: Given a round whose growth allowance is unspent and whose only exhausted budget is the lap cap, when it halts, then the prose names the lap cap and does not report a growth-cap exhaustion | task-9 | covered | No fixture can produce a growth-exhausted halt with zero growth drawn in the round | diff-local |
| criterion | Story 5 negative: Given a mixed round where appending gaps exhaust growth while existing-task gaps are within lap allowance, when it halts, then the prose attributes the exhaustion to the appending gaps' growth draw | task-8 | covered | Mixed-round test proves per-budget attribution | diff-local |
| criterion | Story 6 happy: Given a REMEDIABLE finding dispositioned `build` with new tasks, when remediation routes, then tasks are appended, `growth.added` increments, and the shared cap is enforced unchanged | task-8 | covered | still appends, increments growth, and halts on true growth exhaustion | diff-local |
| criterion | Story 6 happy: Given `publication` and `halt` gaps, when a round mixes them with existing-task gaps, then their existing handling (finish route, needs-human halt, no append) is byte-for-byte unchanged | task-8 | covered | keep their existing handling (finish route, needs-human halt, no append) unchanged | diff-local |
| criterion | Story 6 negative: Given appending gaps requesting more tasks than `growth.remaining`, when budgets are checked, then the round halts on the shared growth allowance exactly as today | task-8 | covered | halts on true growth exhaustion | diff-local |
| criterion | Story 6 negative: Given a planner attempting to disposition genuinely new scope as `existing-task` with a fabricated task id, when the id fails to resolve against the active plan, then the disposition is rejected fail-closed rather than granting a growth-free append | task-2 | covered | an unresolvable one halts naming the id | diff-local |
