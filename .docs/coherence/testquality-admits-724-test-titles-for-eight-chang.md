# Coherence: Accurate test-quality scope

Date: 2026-09-06
Tier: L
Track: technical — no PRD/FR row class.
Source: jstoup111/ai-conductor#2231; five verbatim intake outcomes in .pipeline/intake-outcomes.md.
Verdict: covered

The rows below map the approved outcomes, all nine stories, all 18 tasks, all 54 criteria and each changed ADR. Criterion rows use six cells: row class, exact criterion, task ids, verdict, exact Done-when quote, diff locality. Other rows use the five-column header. This mixed form follows the canonical coherence parser.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-2, story-7 | covered | Separate direct changes, behavior bindings and frozen authority. |
| outcome | outcome-2 | story-1, story-2, story-9 | covered | Unchanged sibling titles cannot enter from a file-level marker. |
| outcome | outcome-3 | story-3, story-4, story-5 | covered | Concrete shared context retained; aggregate regression execution stays independent; no exhaustive static discovery claim. |
| outcome | outcome-4 | story-2, story-5, story-6 | covered | Established, out-of-scope and indeterminate states remain distinct with cause-specific recovery. |
| outcome | outcome-5 | story-9 | covered | Portable eight-body/724-title reproduction records actual binding dispositions and honest measurements. |
| story | story-1 | task-2, task-3, task-8 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-2 | task-4, task-5, task-8 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-3 | task-6, task-7, task-8 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-4 | task-13, task-14 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-5 | task-5, task-11, task-12 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-6 | task-1, task-15, task-16 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-7 | task-1, task-10, task-11, task-17 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-8 | task-1, task-8, task-9 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| story | story-9 | task-10, task-18 | covered | Happy and negative criteria map to scoped behavioral checks and named integration ownership. |
| task | task-1 | story-6, story-7, story-8 | covered | Freeze source reads and safe path identities; concrete Done-when checks, no terminal aggregate task. |
| task | task-2 | story-1 | covered | Compare supported test declarations without title-key collisions; concrete Done-when checks, no terminal aggregate task. |
| task | task-3 | story-1 | covered | Handle movement, deletion and merge-base boundaries; concrete Done-when checks, no terminal aggregate task. |
| task | task-4 | story-2 | covered | Attach Covers markers to tests and suites; concrete Done-when checks, no terminal aggregate task. |
| task | task-5 | story-2, story-5 | covered | Represent binding edits and concrete association uncertainty; concrete Done-when checks, no terminal aggregate task. |
| task | task-6 | story-3 | covered | Group shared setup effects without inflating changed tests; concrete Done-when checks, no terminal aggregate task. |
| task | task-7 | story-3 | covered | Discover concrete helper effects from changed and planned test seeds; concrete Done-when checks, no terminal aggregate task. |
| task | task-8 | story-1, story-2, story-3, story-8 | covered | Wire typed scope analysis into frozen input assembly; concrete Done-when checks, no terminal aggregate task. |
| task | task-9 | story-8 | covered | Ship the parser in the installed production dependency set; concrete Done-when checks, no terminal aggregate task. |
| task | task-10 | story-7, story-9 | covered | Project compact scope and content-bound evidence under input v3; concrete Done-when checks, no terminal aggregate task. |
| task | task-11 | story-5, story-7 | covered | Validate candidate dispositions and scoped finding authority; concrete Done-when checks, no terminal aggregate task. |
| task | task-12 | story-5 | covered | Carry fallback judgment through the existing provider and result paths; concrete Done-when checks, no terminal aggregate task. |
| task | task-13 | story-4 | covered | Separate counterfactual file execution from review target selection; concrete Done-when checks, no terminal aggregate task. |
| task | task-14 | story-4 | covered | Preserve disabled and refactor empty-scope fast paths; concrete Done-when checks, no terminal aggregate task. |
| task | task-15 | story-6 | covered | Route scope-incomplete with retained evidence and bounded recovery; concrete Done-when checks, no terminal aggregate task. |
| task | task-16 | story-6 | covered | Recover corrected scope and honor explicit reduced coverage; concrete Done-when checks, no terminal aggregate task. |
| task | task-17 | story-7 | covered | Validate current scope on cache and persisted result reuse; concrete Done-when checks, no terminal aggregate task. |
| task | task-18 | story-9 | covered | Deliver portable regression and scope-efficiency comparison; concrete Done-when checks, no terminal aggregate task. |
| adr | adr-2026-09-06-engine-owned-test-quality-scope | story-1, story-2, story-3, story-4, story-5, story-6, story-7, story-8, story-9 | covered | Decision-by-decision mappings in the plan are semantically reviewed below; existing and no-change entries preserve their owning seams. |
| adr | adr-2026-08-22-build-review-opt-in-rubric-container | story-2, story-4, story-5, story-6 | covered | Decision-by-decision mappings in the plan are semantically reviewed below; existing and no-change entries preserve their owning seams. |
| adr | adr-2026-08-19-engine-stamped-rubric-judged-result-envelope | story-5, story-7 | covered | Decision-by-decision mappings in the plan are semantically reviewed below; existing and no-change entries preserve their owning seams. |
| adr | adr-2026-08-18-content-anchored-finding-reference-schema | story-1, story-5, story-7, story-8 | covered | Decision-by-decision mappings in the plan are semantically reviewed below; existing and no-change entries preserve their owning seams. |
| criterion | Story 1 happy: Given a file containing many unchanged tests and one added or modified test, when scope is assembled, then the changed declaration is identified separately and unchanged sibling titles are absent from the directly changed target list | task-2, task-8 | covered | The declaration unit fixtures return exactly added/body-or-argument-modified declarations while leaving unrelated unchanged sibling titles out of the changed set. | diff-local |
| criterion | Story 1 happy: Given a test declaration whose arguments or assertion body changed while its title stayed the same, when base and HEAD are compared, then the changed declaration is identified | task-2 | covered | The duplicate-title fixture distinguishes the edited occurrence, and a same-title assertion edit is identified as changed. | diff-local |
| criterion | Story 1 happy: Given two tests with identical titles where only one body changed, when scope and finding references are produced, then the changed occurrence remains distinguishable from its unchanged sibling | task-2 | covered | The duplicate-title fixture distinguishes the edited occurrence, and a same-title assertion edit is identified as changed. | diff-local |
| criterion | Story 1 negative: Given a test is only moved or renamed without changing behavior or binding, when scope is assembled, then the move alone creates no quality target or finding | task-3 | covered | Movement/deletion fixtures report no executable target for pure relocation or a deleted declaration while retaining the source change as evidence. | diff-local |
| criterion | Story 1 negative: Given a deleted test or deleted source path, when scope is assembled, then it is not represented as an executable HEAD test and the change remains available as evidence | task-3 | covered | Movement/deletion fixtures report no executable target for pure relocation or a deleted declaration while retaining the source change as evidence. | diff-local |
| criterion | Story 1 negative: Given unrelated tests arrive through a newer merge-base, when the feature diff is reviewed, then those tests do not become feature-owned changed targets | task-3 | covered | The merge-base input fixture excludes tests introduced solely by the base branch from feature-owned changed declarations. | diff-local |
| criterion | Story 2 happy: Given a changed test with an attached valid story-criterion or task Covers marker, when review scope is assembled, then that test carries the matching obligation and marker provenance | task-4, task-8 | covered | Binding fixtures identify the attached criterion/task marker and allow suite inheritance only within its descendant tests. | diff-local |
| criterion | Story 2 happy: Given a suite declares a valid Covers marker, when a descendant test changes, then the descendant may inherit that marker while tests outside the suite do not | task-4 | covered | Binding fixtures identify the attached criterion/task marker and allow suite inheritance only within its descendant tests. | diff-local |
| criterion | Story 2 happy: Given an otherwise unchanged declaration whose Covers marker changes or is removed, when scope is assembled, then the changed association is visible and previous binding authority is not silently reused | task-5 | covered | Marker-only edit/removal fixtures expose the association change and invalidate the former binding without changing the test body. | diff-local |
| criterion | Story 2 negative: Given a marker occurs only on a sibling test, when another test changes, then the changed test cannot inherit that sibling marker | task-4 | covered | The sibling-marker, absent-feature-id and unmarked-test fixtures produce no borrowed binding and no required-test failure. | diff-local |
| criterion | Story 2 negative: Given an absent-feature obligation id or a test with no applicable marker, when scope is assembled, then it is explicitly out of scope with the appropriate note and no invented obligation or demand for a new test | task-4 | covered | The sibling-marker, absent-feature-id and unmarked-test fixtures produce no borrowed binding and no required-test failure. | diff-local |
| criterion | Story 2 negative: Given competing or file-header marker associations cannot be established, when a concrete changed candidate is reviewed, then the ambiguity remains explicit until source-based judgment resolves it | task-5 | covered | Competing/file-header association fixtures retain concrete candidates, while unsupported unmarked source yields no invented review candidate or halt. | diff-local |
| criterion | Story 3 happy: Given changed setup encloses opted-in tests whose bodies are unchanged, when scope is assembled, then a concrete affected group and its setup evidence are available separately from directly changed test declarations | task-6, task-8 | covered | The setup-only fixture yields an affected opted-in group with its setup reference and zero falsely directly changed descendant bodies. | diff-local |
| criterion | Story 3 happy: Given an existing opted-in test named by the active plan reaches a changed project-local helper, when scope is assembled, then the concrete dependency effect is presented for relevance judgment even when the test file is unchanged | task-7 | covered | The plan-seeded helper fixture presents the concrete changed dependency chain for an unchanged opted-in test; a plan path alone creates no binding. | diff-local |
| criterion | Story 3 happy: Given multiple candidates share setup or a helper, when the reviewer receives its input, then shared evidence is available once by pinned reference rather than repeated as every unchanged sibling title | task-6 | covered | The shared-fixture fixture contains one deduplicated evidence reference and does not enumerate unrelated unchanged sibling titles. | diff-local |
| criterion | Story 3 negative: Given a plan merely names a test file but no applicable marker or concrete changed dependency is established, when scope is assembled, then the path alone confers no review authority | task-7 | covered | The plan-seeded helper fixture presents the concrete changed dependency chain for an unchanged opted-in test; a plan path alone creates no binding. | diff-local |
| criterion | Story 3 negative: Given a local dependency cycle, when evidence is collected, then collection terminates without duplicate unbounded traversal | task-7 | covered | Dependency-cycle and hypothetical-consumer fixtures terminate without duplicate traversal or a speculative scope failure. | diff-local |
| criterion | Story 3 negative: Given only a hypothetical unknown dependency could connect a production refactor to tests, when scope is assembled, then that possibility alone creates no candidate or scope failure | task-7 | covered | Dependency-cycle and hypothetical-consumer fixtures terminate without duplicate traversal or a speculative scope failure. | diff-local |
| criterion | Story 4 happy: Given a production-only refactor with no test edits or concrete affected opted-in candidate, when enabled test-quality review runs, then it passes with an explicit empty-scope reason and invokes neither reviewer nor counterfactual preflight | task-14 | covered | coordinateBuildReviewRubrics returns explicit empty-scope PASS with zero reviewer/preflight calls for production-only refactors, pure moves and missing plan test paths when no concrete candidate exists. | diff-local |
| criterion | Story 4 happy: Given tests were moved without changing their behavior or binding and no other candidate exists, when enabled test-quality review runs, then it passes empty scope without demanding markers, new tests or a waiver | task-14 | covered | coordinateBuildReviewRubrics returns explicit empty-scope PASS with zero reviewer/preflight calls for production-only refactors, pure moves and missing plan test paths when no concrete candidate exists. | diff-local |
| criterion | Story 4 happy: Given the rubric is disabled, when review runs, then its analysis-driven reviewer/preflight work is not dispatched and existing disabled behavior is preserved | task-14 | covered | Disabled review makes no reviewer/preflight call, while a concrete uncertain candidate cannot take the no-dispatch empty-scope shortcut. | diff-local |
| criterion | Story 4 negative: Given no test paths are named in the plan, when a production-only refactor is reviewed, then absence of planned tests alone does not produce incomplete scope | task-14 | covered | coordinateBuildReviewRubrics returns explicit empty-scope PASS with zero reviewer/preflight calls for production-only refactors, pure moves and missing plan test paths when no concrete candidate exists. | diff-local |
| criterion | Story 4 negative: Given a concretely identified candidate remains unresolved, when the engine considers empty-scope completion, then it does not substitute an empty-scope PASS for that candidate | task-14 | covered | Disabled review makes no reviewer/preflight call, while a concrete uncertain candidate cannot take the no-dispatch empty-scope shortcut. | diff-local |
| criterion | Story 4 negative: Given review scope is empty or narrower than the test suite, when configured regression verification runs, then review scope does not narrow or bypass that verification | task-13 | covered | The regression-verification seam retains its original selection, and an indeterminate counterfactual with resolved scope and no findings still passes. | diff-local |
| criterion | Story 5 happy: Given an opted-in changed candidate uses unsupported language or dynamic declaration syntax, when the existing reviewer reads its pinned evidence, then it can return a concrete in-scope resolution and quality judgment in that same call | task-12 | covered | The existing review dispatch accepts and persists a source-grounded unsupported-candidate resolution and quality finding in one provider call with engine-stamped result-v3 identity. | diff-local |
| criterion | Story 5 happy: Given the reviewer establishes that a concrete candidate is unrelated or unchanged, when it returns an out-of-scope disposition with evidence, then that candidate is excluded and an otherwise empty result may pass with its reason recorded | task-12 | covered | An out-of-scope candidate result can leave no quality findings with its exclusion reason retained, and an established-target review makes no separate scope-review call. | diff-local |
| criterion | Story 5 happy: Given a routine fully established target set, when review runs successfully, then no separate scope-review provider session is required | task-12 | covered | An out-of-scope candidate result can leave no quality findings with its exclusion reason retained, and an established-target review makes no separate scope-review call. | diff-local |
| criterion | Story 5 negative: Given a result omits or duplicates a required candidate resolution, when the engine validates it, then it rejects the result through bounded malformed-result handling rather than passing or creating a bad-test finding | task-11 | covered | Result validation rejects missing/duplicate candidate answers, foreign obligations and absent/out-of-candidate source regions with the named problem. | diff-local |
| criterion | Story 5 negative: Given a proposed candidate resolution cites a foreign obligation, absent source region or unrelated sibling, when validated, then that proposed authority and any finding relying on it are rejected | task-11 | covered | Result validation rejects missing/duplicate candidate answers, foreign obligations and absent/out-of-candidate source regions with the named problem. | diff-local |
| criterion | Story 5 negative: Given unsupported syntax exists without a concrete opted-in candidate, when scope is assessed, then unsupported syntax alone does not create a halt or review requirement | task-5 | covered | Competing/file-header association fixtures retain concrete candidates, while unsupported unmarked source yields no invented review candidate or halt. | diff-local |
| criterion | Story 6 happy: Given a reviewer remains indeterminate about a concrete candidate, when the result is settled, then it identifies that candidate, its marker/obligation evidence and what clarification is needed through the bounded scope-incomplete path | task-15 | covered | A concrete indeterminate result records candidate/binding/missing-evidence detail on the existing result/event path and cannot emit ordinary PASS without an authorized reduced-coverage disposition. | diff-local |
| criterion | Story 6 happy: Given the operator supplies an authorized binding or source-evidence correction, when review reruns against the corrected evidence, then scope is recomputed and the previous indeterminacy does not force a permanent halt | task-16 | covered | Corrected pinned binding evidence recomputes candidate scope and can clear the previous scope-incomplete condition rather than retaining a permanent failure. | diff-local |
| criterion | Story 6 happy: Given the operator explicitly accepts reduced coverage for scope-incomplete with identity and rationale, when effective review status is computed, then the accepted limitation remains visibly attributed | task-16 | covered | The existing reduced-coverage action accepts only current authorized exhausted scope-incomplete state, renders attribution, and neither absent approval nor a waiver suppresses an independent finding. | diff-local |
| criterion | Story 6 negative: Given a pinned source read or provider call fails, when existing bounded retries are exhausted, then the failure is reported as unavailable evidence rather than an empty file, new test requirement or insensitive-test finding | task-1, task-15 | covered | The source-reader negative fixtures reject path escape and required missing blobs, never substitute live content or an empty file, and expose a bounded evidence-read failure. | diff-local |
| criterion | Story 6 negative: Given unchanged indeterminacy is returned again, when recovery is evaluated, then it does not create an unbounded new retry loop or append speculative implementation tasks | task-15 | covered | The routing fixtures retain valid independent findings, charge only the existing fault allowance for unresolved scope, append no tasks and introduce no new retry loop; missing source/provider evidence is never labeled test-insensitive. | diff-local |
| criterion | Story 6 negative: Given no authorized reduced-coverage decision exists, when an indeterminate candidate remains, then ordinary PASS is not emitted; absence of tests or markers alone never enters this path | task-15 | covered | A concrete indeterminate result records candidate/binding/missing-evidence detail on the existing result/event path and cannot emit ordinary PASS without an authorized reduced-coverage disposition. | diff-local |
| criterion | Story 7 happy: Given source assembly has pinned base, HEAD and approved feature artifacts, when mutable worktree content changes afterward, then the already frozen scope and binding evidence remain unchanged | task-1 | covered | The source-snapshot integration keeps the same frozen plan, stories and test bytes after live files change, and correctly pairs renamed paths containing spaces. | diff-local |
| criterion | Story 7 happy: Given an identical current projection and policy are reviewed again, when a cache entry is reused, then its candidate resolutions and findings are validated against current reference authority | task-17 | covered | Current cache hits and fresh results enforce the same candidate/anchor authority; changed binding/helper/analysis evidence and old projection-v2 entries cannot reuse stale judgment. | diff-local |
| criterion | Story 7 happy: Given a previous disposition has the same supported result-v3 finding identity, when storage is read after the input upgrade, then it remains readable without an automatic identity migration | task-17 | covered | Legacy result-v3 dispositions remain readable with their exact existing finding identities, and a cache-version miss does not corrupt or migrate disposition storage. | diff-local |
| criterion | Story 7 negative: Given a projection-v2 cache entry, when the projection-v3 engine looks it up, then it misses safely and is not treated as malformed disposition storage | task-17 | covered | Current cache hits and fresh results enforce the same candidate/anchor authority; changed binding/helper/analysis evidence and old projection-v2 entries cannot reuse stale judgment. | diff-local |
| criterion | Story 7 negative: Given a binding, referenced helper/setup source or analysis contract changes, when cache lookup occurs, then stale scope judgment cannot be reused | task-10, task-17 | covered | Projection identity changes when binding, helper/setup bytes or the analysis contract changes, while provenance-only changes do not alter semantic identity. | diff-local |
| criterion | Story 7 negative: Given a cached or fresh result uses a coarse fallback anchor to claim an unrelated sibling, when current scope authority is validated, then it is rejected despite matching a file-level identity | task-11, task-17 | covered | Fresh reference validation rejects unrelated sibling findings even under a coarse fallback anchor, while preserving result-v3 title/occurrence identities for legitimate targets. | diff-local |
| criterion | Story 8 happy: Given an installed production engine without development dependencies, when a supported JavaScript/TypeScript candidate is analyzed, then precision analysis is available | task-9 | covered | The production-only packaging fixture imports actual scope analysis and identifies a supported JS/TS candidate without access to development-only dependencies. | diff-local |
| criterion | Story 8 happy: Given a supported source path was renamed or contains spaces, when source identities and changes are read, then the correct pinned sources are associated without splitting the path | task-1 | covered | The source-snapshot integration keeps the same frozen plan, stories and test bytes after live files change, and correctly pairs renamed paths containing spaces. | diff-local |
| criterion | Story 8 happy: Given several candidates reference the same source blob, when scope is assembled, then that blob is retrieved at most once per identity in that assembly | task-8 | covered | Assembly reads each commit/path blob once, never executes top-level consumer side effects, and preserves a concrete parser failure as uncertainty rather than a verified target or empty PASS. | diff-local |
| criterion | Story 8 negative: Given consumer source contains executable top-level side effects, when analysis reads it, then those side effects are never executed | task-8 | covered | Assembly reads each commit/path blob once, never executes top-level consumer side effects, and preserves a concrete parser failure as uncertainty rather than a verified target or empty PASS. | diff-local |
| criterion | Story 8 negative: Given a source reference escapes the repository boundary or names unavailable pinned content, when resolved, then it is rejected or reported unavailable rather than reading unrelated host files or substituting live content | task-1 | covered | The source-reader negative fixtures reject path escape and required missing blobs, never substitute live content or an empty file, and expose a bounded evidence-read failure. | diff-local |
| criterion | Story 8 negative: Given an applicable parser cannot load or source cannot be safely parsed for a concrete candidate, when analysis handles it, then the limitation remains explicit and cannot manufacture verified targets or an empty pass | task-8 | covered | Assembly reads each commit/path blob once, never executes top-level consumer side effects, and preserves a concrete parser failure as uncertainty rather than a verified target or empty PASS. | diff-local |
| criterion | Story 9 happy: Given the frozen 724-title case, when the new analysis runs, then it identifies eight directly added/modified test bodies and separately explains their actual binding dispositions rather than claiming eight authorized targets | task-18 | covered | The portable comparison identifies eight directly changed bodies from the 724-title regression and separately reports each actual binding disposition without local daemon files or GitHub downloads. | diff-local |
| criterion | Story 9 happy: Given increasingly many unrelated unchanged sibling declarations around the same changed tests, when projections are compared, then the changed-target list stays fixed and unrelated sibling titles do not inflate reviewer input | task-10 | covered | deriveBuildReviewRubricProjections emits input v3 with unchanged directly changed target count and no extra unrelated sibling titles as the fixture grows. | diff-local |
| criterion | Story 9 happy: Given old and new analysis run against the same portable fixtures, when comparison results are reported, then they identify source-read counts, projection bytes, target/candidate counts, model-call counts and analysis elapsed time | task-18 | covered | The comparison output records bytes, counts and elapsed analysis time, retains shared/ambiguous evidence, and makes no total-token or end-to-end savings claim without opt-in measured provider evidence. | diff-local |
| criterion | Story 9 negative: Given retained local daemon artifacts or the original feature commits are absent in CI, when ordinary verification runs, then portable fixtures still prove scope selection and no GitHub download is needed | task-18 | covered | The portable comparison identifies eight directly changed bodies from the 724-title regression and separately reports each actual binding disposition without local daemon files or GitHub downloads. | diff-local |
| criterion | Story 9 negative: Given difficult shared or ambiguous candidates require more context, when optimization is assessed, then they are not dropped merely to improve input size or elapsed time | task-18 | covered | The comparison output records bytes, counts and elapsed analysis time, retains shared/ambiguous evidence, and makes no total-token or end-to-end savings claim without opt-in measured provider evidence. | diff-local |
| criterion | Story 9 negative: Given no real-provider comparison was run, when results are reported, then projection bytes are not claimed as measured total tokens or end-to-end latency savings and ordinary tests call no third-party service | task-18 | covered | The comparison output records bytes, counts and elapsed analysis time, retains shared/ambiguous evidence, and makes no total-token or end-to-end savings claim without opt-in measured provider evidence. | diff-local |

## Semantic consistency review

Checked each mapping against the actual accepted stories and task Done-when checks, not just id membership. New ADR decisions 1–5 are fulfilled collectively by pinned source reads, declaration comparison, binding provenance and concrete shared-candidate analysis, with assembly integration in Task 8. D6 uses Task 12 for the real provider boundary and Tasks 10/11 for compact source-bound validation. D7 spans Tasks 14–16: a legitimate empty refactor cannot become scope-incomplete, while a concrete unresolved candidate cannot become a false empty PASS. D8 retains result-v3 identity and invalidates only input/cached scope as Tasks 10–12/17 require. D9 uses existing event and state readers, not a parallel channel. D10 uses the behavior-owning scoped fixtures plus the portable reproduction; it does not add a completed-feature catch-all task. D11 measures counts/bytes/timing without asserting unmeasured savings. D12 is a delivery constraint, with parser packaging separately covered by D3.

The changed historical ADR rows preserve their remaining decisions: registry/default-off/deprecated keys are existing implementation, new candidate-bearing scope refines the opted-in intersection, and engine-stamped envelopes retain their original authority. The closed provider evidence set changes only for validated scopeResolutions. Existing repair diagnosis/allowance is reused, not reset or reimplemented. Plan-task reference grammar and complexity scheduling are unchanged constraints. Content-region title/occurrence identity remains the same; scope validation restricts which concrete candidate can use a coarse reference.

Cross-layer contradiction/oscillation checks: accuracy versus smaller projection is compatible because shared context is retained and only unrelated sibling targets are removed; planned tests versus scope authority is compatible because paths are seeds only; refactor PASS versus unresolved-candidate recovery is compatible because candidate creation requires concrete opted-in evidence; counterfactualSensitivity indeterminate versus scope-incomplete concerns distinct evidence and authority questions; projection-v3 misses versus result-v3 dispositions preserve existing exact identities. Each implication was checked in both directions. No task requires changing another feature's accepted artifact: DECIDE already made the authorized refinements.

All criteria are diff-local tests of the introduced behavior under controlled pinned fixtures and faithful fake adapters. A deployment or future unrelated commit is not required for those assertions. No outside-diff waiver or PRD layer is needed. The portable case may contain eight directly changed bodies and fewer authorized quality targets; the mapping intentionally does not equate them.

## Verify-Claims Ledger

Verified: exact story/task ids and completion quotes are read from the authored artifacts; the engine ADR parser supplied the complete 30-decision pool across four changed ADRs. Semantic coverage is 90% confidence, inferred from the cited behaviors and integration ownership, not proof of implemented behavior. There are no pending load-bearing assumptions or accepted degrading gaps. The read-only land coherence validator will independently verify syntax, references, decision rows and quotes before publication.

Task 15 preserves valid same-rubric findings alongside indeterminate candidate evidence. A derived typed scope-failure view feeds existing fault/coverage consumers; an infrastructure-only replacement would lose findings and is explicitly forbidden. Task 16’s waiver cannot resolve those findings. This closes the mixed-result interaction without a second provider call or result identity change.
