# Coherence: Exported cost equals the feature's own ledger, on any backend

**Plan:** .docs/plans/exported-step-cost-under-records-spend-20x-so-ever.md
**Stories:** .docs/stories/exported-step-cost-under-records-spend-20x-so-ever.md
**Tier:** M (technical track — no PRD, so the `fr` row class is omitted)
**Outcomes:** 8 staged bullets (jstoup111/ai-conductor#2095 bullets 1–5, jstoup111/ai-conductor#2086 bullets 6–8)
**ADR pool:** the two non-deleted ADRs amended in this change set

Consistency pass (§4d): every covered row was re-read for contradiction and oscillation across layers. Outcome 5 ("unavailable rather than a small number") versus outcome 2 ("a 24h view includes completed features") was the closest pair: satisfying 5 by emitting nothing on an unreadable ledger does not remove an already-exported last value, and satisfying 2 by cumulative gauges does not require emitting on an unreadable ledger — both hold in both directions. Story 5's "cost-unmetered tokens are counted" versus Story 2's "cost-unmetered creates no cost bucket" touch the same dispatch and both hold: token buckets and cost buckets are separate lists. No `fail` rows.

| Row class | Cited id | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1 | covered | Story 1 pins the step-close total to the ledger rollup (the finish line's own source) and requires the second-lifetime close to include the first run's dispatches. |
| outcome | outcome-2 | story-1, story-3 | covered | A cumulative gauge re-exported at every step close remains a last-value series after the feature completes; Story 3 stops dead runs from overwriting it with stale values. |
| outcome | outcome-3 | story-1, story-2, story-5 | covered | All three stories require the second engine process lifetime's first close to include the earlier lifetime's dispatches, per feature, per cost bucket, and per token bucket. |
| outcome | outcome-4 | story-1, story-2, story-5 | covered | Values are cumulative and non-decreasing per series (Story 1) and bucket sums equal the feature total (Story 2) or the ledger's token sums (Story 5), so last-value and max_over_time agree; no reset semantics are required of any backend. |
| outcome | outcome-5 | story-1, story-2, story-5 | covered | Missing or malformed ledger emits no point (Story 1 and Story 5 negatives); partial metering exports cost_complete=false and non-finite cost creates no bucket (Story 2 negatives). |
| outcome | outcome-6 | story-1 | covered | Story 1 happy path: the finish-time gauge equals the shipped record's cost_usd computed from the same ledger, and every step-close value is the same rollup. |
| outcome | outcome-7 | story-2, story-5 | covered | Story 2 requires exact per step, model, and source cost buckets whose sum is the feature total; Story 5 extends the same exactness to token counts per step, model, and kind; per-project follows from the project attribute on every point; spend per interval is the difference of cumulative values. |
| outcome | outcome-8 | story-1 | covered | Story 1 keeps the existing finish-time `conductor.feature.cost` emission and log line unchanged, so records shipped before the change stay queryable by the same series name. |
| story | story-1 | task-2, task-3, task-5 | covered | Task 3 emits the ledger-derived snapshot after every step terminal; Task 2 defines the event; Task 5 routes it and keeps the finish gauge on the same instrument. |
| story | story-2 | task-1, task-4, task-10 | covered | Task 1 builds the exact buckets; Task 4 records them as gauges and removes the per-process counter; Task 10 pins the partial/non-finite negatives. |
| story | story-3 | task-6, task-7 | covered | Task 6 shuts the meter provider down on stop; Task 7 pins the bounded, idempotent, non-interleaving negatives. |
| story | story-4 | task-8, task-9 | covered | Task 8 renders renderer_error into the daemon log; Task 9 pins once-per-run boundedness and outcome neutrality. |
| story | story-5 | task-1, task-4, task-11 | covered | Task 1 builds the token buckets; Task 4 records the token gauge and removes the per-process token counter; Task 11 pins partial-usage, unknown-model, and unreadable-ledger negatives. |
| task | task-1 | story-2, story-5 | covered | infrastructure: per-dimension cost and token buckets and readErrors in the rollup that Stories 2 and 5's exactness and Story 1's unreadable-ledger suppression depend on |
| task | task-2 | story-1 | covered | infrastructure: the feature_cost_snapshot union member, its otel-only sink declaration, and the rollup projection Story 1's emission carries |
| task | task-3 | story-1 | covered | Engine emission after each step terminal; suppressed on readErrors. |
| task | task-4 | story-2, story-5 | covered | Cost and token gauges recorded from snapshots; conductor.step.cost and conductor.step.tokens counters removed. |
| task | task-5 | story-1 | covered | Visualizer routing; finish-time and step-close values coincide on conductor.feature.cost. |
| task | task-6 | story-3 | covered | Meter provider shutdown on stop; final flush; spans still readable. |
| task | task-7 | story-3 | covered | Bounded hanging-exporter stop, idempotent double stop, no interleaving across sequential runs. |
| task | task-8 | story-4 | covered | renderer_error rendered in daemon.log; interactive renderer pinned. |
| task | task-9 | story-4 | covered | Once-per-run boundedness and unchanged run outcome under export failure. |
| task | task-10 | story-2 | covered | Partial or non-finite cost never exports as a confident figure. |
| task | task-11 | story-5 | covered | Token buckets stay exact for partial usage, unknown models, and unreadable ledgers. |
| adr | adr-014-otel-observability-exporter | story-1, story-2, story-3, story-4, story-5 | covered | Amended 2026-08-30: cost and token instruments are spine projections (Stories 1, 2, 5), the meter provider is shut down on stop (Story 3), and the bounded warning is rendered (Story 4). Decision 4 (off the hot path) and Decision 5 (failure isolation) are honoured; identity contract unchanged (Story 1 happy path on project/feature attributes). |
| adr | adr-2026-07-22-per-feature-cost-rollup-in-shipped-record | story-1, story-2, story-5 | covered | Amended 2026-08-30: the anticipated OTel consumer swap is delivered — Stories 1, 2, and 5 make every exported cost and token value a projection of the same events.jsonl rollup; the Cost block is untouched. |
| criterion | Story 1 happy: Given a feature whose event ledger holds three fully-metered dispatches costing $1.20, $0.80, and $2.00, when the third step closes, then the exported `conductor.feature.cost` gauge for that feature carries the value 4.00 (within 0.0001) with attributes `project`, `feature`, and `cost_complete=true` | task-3 | covered | "its `costUsd` equals the ledger rollup total" | diff-local |
| criterion | Story 1 happy: Given a feature whose first engine run halted after dispatches totalling $2.10 and whose second run is a fresh process on the same worktree, when the second run's first step closes after a $1.40 dispatch, then the exported whole-feature cost is 3.50, not 1.40 | task-3 | covered | "a new run's first step close emits `costUsd` 3.50 after a $1.40 dispatch" | diff-local |
| criterion | Story 1 happy: Given a feature with several consecutive step closes, when the exported values are read in order, then each value is greater than or equal to the previous one | task-3 | covered | "across three step closes the snapshot values are non-decreasing" | diff-local |
| criterion | Story 1 happy: Given a feature that reaches `finish`, when the finish-time usage total is emitted, then the `conductor.feature.cost` value equals the `cost_usd` the shipped record renders from the same ledger (within 0.0001) and the existing finish-line log text is unchanged | task-5 | covered | "records `conductor.feature.cost` 3.5 again with `cost_complete` derived from its unmetered counts" | diff-local |
| criterion | Story 1 happy: Given the current export identity contract, when any cost point is exported, then it carries `project` and `feature` attributes and no run identifier on any label | task-4 | covered | "every recorded cost point carries `project` and `feature` and no attribute named `run`, `run_id`, or `conductor.run.id`" | diff-local |
| criterion | Story 1 negative: Given a worktree with no `.pipeline/events.jsonl`, when a step closes, then no `conductor.feature.cost` data point is exported (no zero is recorded) and the step's own verdict and the run's progress are unchanged | task-3 | covered | "with no ledger file, and separately with one malformed line, no snapshot is emitted" | diff-local |
| criterion | Story 1 negative: Given an event ledger with one malformed line, when a step closes, then no `conductor.feature.cost` data point is exported for that close and the step completes normally with no thrown error surfaced to the run | task-3 | covered | "returns without emitting when `rollup.readErrors > 0`" | diff-local |
| criterion | Story 1 negative: Given a feature whose ledger holds one fully-metered dispatch and one dispatch that reported no usage at all, when a step closes, then the exported value equals the metered dispatch's cost and carries `cost_complete=false` | task-10 | covered | "one attempt with no usage yields `toFeatureCostSnapshot` `{ costUsd: 1, costComplete: false }` with one bucket" | diff-local |
| criterion | Story 1 negative: Given a feature whose worktree was recreated from its branch so the ledger holds only dispatches after recreation, when a step closes, then the exported value equals that shorter ledger's total (the same figure the finish line and shipped record will report), not a higher earlier value | task-3 | covered | "its `costUsd` equals the ledger rollup total" | diff-local |
| criterion | Story 1 negative: Given a step that fails rather than completes, when the failure is recorded, then the whole-feature cost export for that close still reflects every dispatch the ledger holds, including the failed step's provider attempts | task-3 | covered | "a step that fails still yields a snapshot including its provider attempts" | diff-local |
| criterion | Story 2 happy: Given a feature whose ledger holds `build` dispatches of $1.00 and $0.50 on model `m1` and a `build_review` dispatch of $2.00 on model `m2`, when the latest step closes, then `conductor.feature.step.cost` carries one point per bucket: 1.50 for {step=build, model=m1} and 2.00 for {step=build_review, model=m2}, each also carrying `project`, `feature`, and `source` | task-1 | covered | "yields `byDimension` entries `{step:'build', model:'m1', source:'provider', costUsd:1.5}` and `{step:'build_review', model:'m2', source:'rate-card', costUsd:2}`" | diff-local |
| criterion | Story 2 happy: Given any exported set of `conductor.feature.step.cost` points for a feature at one step close, when their values are summed, then the sum equals that close's `conductor.feature.cost` value (within 0.0001) | task-1 | covered | "the bucket sum equals `costUsd`" | diff-local |
| criterion | Story 2 happy: Given one dispatch whose cost was reported by the provider and one whose cost came from a rate card, when the step closes, then the two contribute to separate buckets distinguished by `source=provider` and `source=rate-card`, and both are included in the whole-feature total | task-1 | covered | "with `costSource: 'rate-card'`" | diff-local |
| criterion | Story 2 happy: Given dispatches served by different providers (for example a provider that reports cost natively and one priced from a rate card), when the step closes, then every dispatch contributes through the same rollup with no provider-specific exception, and the bucket values match a by-hand sum of the ledger | task-1 | covered | "when `classifyMetering` returns `fully-metered`" | diff-local |
| criterion | Story 2 happy: Given a bucket that received no new dispatches since the previous step close, when the next step closes, then that bucket's point is exported again unchanged (cumulative, not per-interval) | task-4 | covered | "a second snapshot with an unchanged bucket re-records the same value (cumulative last-value)" | diff-local |
| criterion | Story 2 happy: Given any dispatch close, when the visualizer's metric instruments are enumerated, then the only cost-carrying instruments are the two feature gauges; no per-process cost counter is exported | task-4 | covered | "the only instruments whose unit is `usd` are the two feature gauges" | diff-local |
| criterion | Story 2 negative: Given a dispatch whose usage carried tokens but no finite cost, when the step closes, then no bucket point is created for that dispatch, its tokens are still counted, and the whole-feature point carries `cost_complete=false` | task-1 | covered | "a cost-unmetered attempt (tokens, no cost) and an attempt with `costUsd: NaN` create no bucket while `costUnmetered.count` increments" | diff-local |
| criterion | Story 2 negative: Given a dispatch whose reported cost is `NaN` or infinite, when the step closes, then it is treated exactly like a cost-unmetered dispatch: no bucket value changes and `cost_complete=false` | task-1 | covered | "an attempt with `costUsd: NaN` create no bucket" | diff-local |
| criterion | Story 2 negative: Given a dispatch whose model is unknown, when the step closes, then its cost is still counted in a bucket keyed by step and source with the `model` attribute omitted, and the whole-feature total includes it | task-1 | covered | "an attempt with no model yields a bucket with `model` undefined and its cost included" | diff-local |
| criterion | Story 2 negative: Given a feature whose ledger contains a dispatch for a step name that never produced a span, when the step closes, then the bucket for that step is still exported (bucketing depends on the ledger, not on open spans) | task-5 | covered | "including for a `step` that never had a `step_started` (bucketing is ledger-driven, not span-driven)" | diff-local |
| criterion | Story 2 negative: Given a feature with dispatches across two engine process lifetimes, when the second run's first step closes, then every bucket value includes the first run's dispatches for that bucket | task-1 | covered | "a ledger holding events from two separate runs of the same step sums both into one bucket" | diff-local |
| criterion | Story 3 happy: Given a visualizer that recorded metrics during a run, when `stop()` resolves, then one final export carrying the latest recorded values has completed before `stop()` resolves | task-6 | covered | "assert the exporter received one export containing the latest value before `stop()` resolved" | diff-local |
| criterion | Story 3 happy: Given a stopped visualizer, when more than one export interval elapses, then no further metric export occurs from that visualizer | task-6 | covered | "advance fake time by three export intervals after `stop()` and assert the exporter's export-call count did not change" | diff-local |
| criterion | Story 3 happy: Given a run whose spans finished before `stop()`, when `stop()` resolves, then those finished spans remain readable from the span exporter (existing behavior preserved) | task-6 | covered | "finished spans are still returned by the span exporter after `stop()`" | diff-local |
| criterion | Story 3 happy: Given two sequential runs of the same feature inside one long-lived process, when the first run has stopped and the second is exporting, then every exported value for that feature after the first stop originates from the second run | task-7 | covered | "yields exports only from the second's exporter instance" | diff-local |
| criterion | Story 3 negative: Given the metric endpoint does not respond, when `stop()` is called, then `stop()` resolves within the configured export timeout, does not throw, and the run's outcome is unaffected | task-7 | covered | "`stop()` resolves within the configured export timeout plus a margin, does not throw, and `warnOnce` fires at most once" | diff-local |
| criterion | Story 3 negative: Given `stop()` already in progress, when `stop()` is called again or a termination signal arrives, then the second call returns the same in-flight promise and no second shutdown is attempted | task-7 | covered | "returns the identical promise and the exporter's `shutdown` is invoked once" | diff-local |
| criterion | Story 3 negative: Given a visualizer whose start failed and never created providers, when `stop()` is called, then it resolves immediately without error | task-7 | covered | "`stop()` on a visualizer whose `start()` never created providers resolves without error" | diff-local |
| criterion | Story 3 negative: Given a run that recorded no metrics at all, when `stop()` is called, then shutdown completes without exporting any data point | task-6 | covered | "a visualizer that recorded nothing stops without any metric export call" | diff-local |
| criterion | Story 4 happy: Given a daemon-dispatched feature whose metric export fails, when the failure is detected, then the feature's daemon log carries one line naming the `otel` renderer and the failure message | task-8 | covered | "logs exactly one line containing `otel` and `export failed: 503`" | diff-local |
| criterion | Story 4 happy: Given an interactive run whose export fails, when the failure is detected, then the terminal renderer surfaces the same renderer error line (existing behavior preserved) | task-8 | covered | "a `renderer_error` event still reaches the existing `TerminalRenderer` case and renders one line naming the renderer" | diff-local |
| criterion | Story 4 happy: Given a renderer error rendered in the daemon log, when the event is also persisted, then the persisted ledger entry and the log line carry the same renderer name and message | task-9 | covered | "the persisted ledger holds a `renderer_error` record with the same renderer name and message as the logged line" | diff-local |
| criterion | Story 4 negative: Given repeated export failures within one run, when they are detected, then the daemon log carries exactly one such line for that run (bounded, not one per export) | task-9 | covered | "the feature log receives exactly one line naming `otel` across at least three export attempts" | diff-local |
| criterion | Story 4 negative: Given an export failure, when it is rendered, then the run continues, the step's verdict is unchanged, and the run's terminal outcome is not altered by the failure | task-9 | covered | "the run's step verdicts and terminal outcome equal those of an identical run whose exporter succeeds" | diff-local |
| criterion | Story 4 negative: Given a renderer error from a renderer other than `otel`, when it is rendered, then the line names that renderer, so an operator can tell which surface failed | task-8 | covered | "`rendererName: 'terminal'` logs a line naming `terminal`" | diff-local |
| criterion | Story 5 happy: Given a feature whose ledger holds `build` dispatches on model `m1` with input tokens 100 and 50 and output tokens 10 and 5, when the latest step closes, then `conductor.feature.step.tokens` carries one point per kind for {step=build, model=m1}: input 150 and output 15, each also carrying `project` and `feature` | task-4 | covered | "yields `conductor.feature.step.tokens` points 150 for `kind=input` and 15 for `kind=output` with attributes `{project, feature, step, model, kind}`" | diff-local |
| criterion | Story 5 happy: Given a dispatch whose usage carried tokens but no cost, when the step closes, then its tokens are included in the token buckets even though it contributes no cost bucket | task-1 | covered | "a cost-unmetered attempt's tokens are included" | diff-local |
| criterion | Story 5 happy: Given a feature with dispatches across two engine process lifetimes, when the second run's first step closes, then every token bucket includes the first run's dispatches for that bucket | task-1 | covered | "a ledger holding events from two separate runs of the same step sums both into one bucket" | diff-local |
| criterion | Story 5 happy: Given any dispatch close, when the visualizer's metric instruments are enumerated, then the only token-carrying instrument is the feature token gauge; no per-process token counter is exported | task-4 | covered | "the collected instrument names contain `conductor.step.dispatches` and no `conductor.step.tokens`" | diff-local |
| criterion | Story 5 happy: Given a bucket that received no new dispatches since the previous step close, when the next step closes, then its token points are exported again unchanged (cumulative, not per-interval) | task-4 | covered | "a second snapshot with an unchanged bucket re-records the same value (cumulative last-value)" | diff-local |
| criterion | Story 5 negative: Given a dispatch that reported no usage at all, when the step closes, then no token point is created for it and the whole-feature point carries `cost_complete=false` | task-11 | covered | "no bucket for the no-usage attempt, while `toFeatureCostSnapshot` reports `costComplete: false`" | diff-local |
| criterion | Story 5 negative: Given a dispatch whose usage carries only `input` and `output` (cache fields absent), when the step closes, then only the `input` and `output` kinds are exported for its bucket and no zero-filled cache points appear | task-11 | covered | "emits exactly one `conductor.feature.step.tokens` point (kind `input`)" | diff-local |
| criterion | Story 5 negative: Given a dispatch whose model is unknown, when the step closes, then its tokens are counted in a bucket keyed by step with the `model` attribute omitted | task-11 | covered | "with the `model` attribute omitted" | diff-local |
| criterion | Story 5 negative: Given an event ledger with one malformed line, when a step closes, then no token point is exported for that close, the same as for cost | task-11 | covered | "the malformed-ledger case emits no snapshot, so no token point can be recorded for that close" | diff-local |
