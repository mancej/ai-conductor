# Coherence Check: Guided setup walks the operator through project and operator configuration

Date: 2026-09-14
Source: jstoup111/ai-conductor#2218
Tier: M; product track; session-default model.
Verdict: PASS — all required layers covered, no waiver required.

## Inputs and Judgment

This review compares the four sanitized staged issue outcomes, 14 functional requirements, the two amended ADRs in the change set (adr-2026-08-28-test-suite-drift-budget-and-verification-mode, adr-2026-08-09-bash-yaml-access-via-conduct-ts-config), 8 accepted stories, 16 plan tasks, and 27 extracted criteria. Every counterpart id was confirmed against its artifact file; every criterion quote was matched as an exact substring of a cited task's `Done when` block.

Consistency pass (§4d): no cross-layer contradiction. The preserved-behavior promises (FR-4, FR-12, Story 2 and Story 8 effective-settings identity, Story 6 refuse-to-clobber) were compared with every task that adds a side effect on the same path: task-1/2/3 change output only when `--test-suite-command` is present; task-7 writes only the user config; task-11–16 add skill prose whose auto-mode branch is the unchanged flagless invocation. No new effect lacks a condition excluding the preserved scenario.

PRD↔stories tie-out (§4e): every FR-1..FR-14 is cited by a story whose scenarios exercise it, and no story cites an undeclared FR.

Architecture tie-out (§4f): adr-2026-08-28 D1, D2, D8 `existing` — verified in `config.ts` and `registry-cli.ts`; D3–D7 `no-change` — engine-internal verifier, evidence, and event decisions this feature does not touch; D9 `task` — task-1 and task-3 checks assert the flag is parsed into the dispatch and the rendered `test_suite.command` equals it with the flagless literal preserved, which is D9's obligation. adr-2026-08-09 decisions 1–3 `existing` — verified in `bin/install` and `cli.ts`; 4–5 `no-change`; decision 6 `task` — task-7 checks assert the `spec_owner` write, empty-value refusal, and unchanged rejection of every other non-`conductor` path, which is decision 6's obligation.

Achievability (§4g): each criterion's cited checks name the mechanism (`detectRegistryCommand`, `runConfigInit`, `renderVerificationBlock`, `writeProjectConfig`, `userConfigSetCommand`, `writeUserConfig`, `validateConfig`, the named Step 1b-i/1b-ii statements, the template annotation test) and the asserting test. Skill-prose criteria (Stories 1, 3.2–3.3, 4.1–4.2, 5, 6.1–6.2, 8.1–8.2) are delivered by statements the host agent executes; their checks assert the statements exist and name the CLI verbs that record answers, which under adr-2026-08-28 D8 is the approved mechanism for a skill-asked value.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-2, story-3, story-4 | covered | Running `bootstrap` (or `register`, if that's where this lands) on a project with no |
| outcome | outcome-2 | story-1, story-2, story-3, story-7 | covered | The same step-by-step flow covers project config fields beyond the two Step 1b-i |
| outcome | outcome-3 | story-6 | covered | Re-running the flow against an already-configured user/project is safe: it does not |
| outcome | outcome-4 | story-8 | covered | An automated/non-interactive invocation (daemon, CI) is unaffected: it keeps using |
| fr | fr-1 | story-1 | covered | Story 1 asks every decidable setting one at a time. |
| fr | fr-2 | story-1 | covered | Story 1 requires the four guidance elements on every question. |
| fr | fr-3 | story-2 | covered | Story 2 records non-default answers and reads them back as effective. |
| fr | fr-4 | story-2 | covered | Story 2 pins the all-defaults output to the pre-change effective settings, adding only comments. |
| fr | fr-5 | story-3 | covered | Story 3 records the real test command and offers a tooling-inferred default. |
| fr | fr-6 | story-4 | covered | Story 4 establishes identity through the walkthrough with no file edit. |
| fr | fr-7 | story-4 | covered | Story 4 keeps identity machine-scoped and the project guard unchanged. |
| fr | fr-8 | story-5 | covered | Story 5 reports unresolved identity and the blocked actions. |
| fr | fr-9 | story-6 | covered | Story 6 preserves existing values byte-for-byte on re-run. |
| fr | fr-10 | story-6 | covered | Story 6 reports established settings instead of re-asking. |
| fr | fr-11 | story-7 | covered | Story 7 explains unasked keys in the recorded configuration. |
| fr | fr-12 | story-8 | covered | Story 8 keeps the unattended path question-free with pre-change effective settings. |
| fr | fr-13 | story-1 | covered | Story 1 re-asks closed-set and free-text rejects without recording. |
| fr | fr-14 | story-2 | covered | Story 2 refuses invalid values before any write. |
| story | story-1 | task-2, task-6, task-11 | covered | Skill interview task plus engine refusal task deliver every asked setting with guidance and re-ask rules. |
| story | story-2 | task-2, task-3, task-4, task-11 | covered | Round-trip, effective-identity, and refusal tasks establish recorded answers and unchanged defaults. |
| story | story-3 | task-1, task-3, task-12 | covered | Flag parse, substitution, and inference-question tasks deliver the real test command. |
| story | story-4 | task-7, task-8, task-13 | covered | config set spec_owner plus the identity step and project-guard proof deliver machine-scoped identity. |
| story | story-5 | task-14 | covered | Unresolved-identity report task names the blocked actions and forbids a placeholder. |
| story | story-6 | task-5, task-13, task-15 | covered | Refuse-to-clobber proof, established-identity report, and re-run branch keep prior values intact. |
| story | story-7 | task-9, task-10 | covered | Template annotation and validator-known tasks explain every unasked key. |
| story | story-8 | task-4, task-16 | covered | Auto-mode branch task and the pre-change fixture keep unattended onboarding question-free. |
| task | task-1 | story-3 | covered | `detectRegistryCommand` returns `testSuiteCommand` for both `--test-suite-command <v>` and `--test-suite-command=<v>`, as asserted by the two new dispatch tests. |
| task | task-2 | story-1, story-2 | covered | `runConfigInit` exits 1 naming `--test-suite-command` and writes no config file for the empty and multi-line fixtures, as asserted by the two refusal tests. |
| task | task-3 | story-3, story-2 | covered | The rendered `test_suite.command` equals the `--test-suite-command` value and `loadConfig` reads it back unchanged, as asserted by the round-trip test. |
| task | task-4 | story-2, story-8 | covered | `runConfigInit` with no flags produces a file whose parsed settings deep-equal the parsed pre-change `config-init-defaults.yml` and whose non-comment, non-blank lines equal the fixture's in order, as asserted by the flagless effective-identity test. |
| task | task-5 | story-6 | covered | `writeProjectConfig` returns `already-exists` and the pre-existing file is byte-identical after a flagged re-run, as asserted by both preservation tests. |
| task | task-6 | story-1 | covered | `createProgram().commands` for `config init` includes the `--test-suite-command` option with its description, as asserted by the help-listing test. |
| task | task-7 | story-4 | covered | `userConfigSetCommand` writes `spec_owner` to the user config via `writeUserConfig` and `config read spec_owner` returns the value, as asserted by the round-trip test. |
| task | task-8 | story-4 | covered | `userConfigSetCommand` opens only `userConfigPath()`; the project config file is byte-identical after the write, as asserted by the project-untouched test. |
| task | task-9 | story-7 | covered | Every key in the test's unasked-key list has a template comment block with `Controls:`, `Allowed:`, `Default:`, and `Changing it:`, as asserted by the annotation-coverage test. |
| task | task-10 | story-7 | covered | A project config assembled from every key named in template comments passes `validateConfig` with no unknown-key error, as asserted by the explained-keys-are-known test. |
| task | task-11 | story-1, story-2 | covered | Step 1b-i of `skills/bootstrap/SKILL.md` has a numbered question for each decidable key carrying `Controls:`, `Allowed:`, `Default:`, and `Changing it:`, as asserted by the question-coverage test. |
| task | task-12 | story-3 | covered | The test-command question in `skills/bootstrap/SKILL.md` names the tooling-file inference table and the empty-and-uninferred re-ask rule, as asserted by the test-command-question test. |
| task | task-13 | story-4, story-6 | covered | Step 1b-ii reads `spec_owner` via `config read`, records it via `config set spec_owner`, and never instructs a file edit, as asserted by the identity-step test. |
| task | task-14 | story-5 | covered | Step 1b-ii's unresolved branch names the three blocked actions and forbids a placeholder value, as asserted by the unresolved-report test. |
| task | task-15 | story-6 | covered | Step 1b-i's re-run branch reads existing keys via `config read`, prints `already set:` lines, and asks no question for them, as asserted by the re-run-branch test. |
| task | task-16 | story-8 | covered | The auto-mode branch of Step 1b-i is the unchanged flagless-defaults invocation with no question text, as asserted by the auto-mode test, and Task 4 pins its output byte-identical. |
| adr | adr-2026-08-28-test-suite-drift-budget-and-verification-mode | story-1, story-2, story-3, story-8 | covered | D1–D8 are shipped by #2021 or impose no change here; new D9 is delivered by task-1 and task-3 (flag parsed into the dispatch, rendered command equals the flag value, flagless output still `npm test`). Dispositions judged individually below. |
| adr | adr-2026-08-09-bash-yaml-access-via-conduct-ts-config | story-4 | covered | Decisions 1–5 are shipped (`config read`/`config set` in cli.ts, validate-then-write) or outside this feature; new decision 6 is delivered by task-7 (spec_owner accepted, validated, user-config-only, other paths still rejected). Dispositions judged individually below. |
| criterion | Story 1 happy: Given an operator is present and the project has no configuration yet, when onboarding reaches configuration, then every project-scoped setting the operator is expected to decide is asked, one question at a time, and no such setting is silently defaulted without being asked. | task-17 | covered | "asks for the scoped command only when the mode answer is `scoped`" | diff-local |
| criterion | Story 1 happy: Given a question is asked, when it is presented, then it states what the setting controls, which values are permitted, which value applies if the operator answers nothing, and what a non-default value changes. | task-11 | covered | "carrying `Controls:`, `Allowed:`, `Default:`, and `Changing it:`" | diff-local |
| criterion | Story 1 negative: Given a question with a closed set of permitted values, when the operator answers with a value outside that set, then the answer is rejected with the permitted values restated, the same question is asked again, and nothing is recorded for it. | task-11, task-2 | covered | "states the closed-set and free-text re-ask rules" | diff-local |
| criterion | Story 1 negative: Given a question whose answer is free text, when the operator answers with an empty or multi-line value, then the answer is rejected, the question is re-asked, and nothing is recorded for it. | task-2, task-11 | covered | "exits 1 naming `--test-suite-command` and writes no config file for the empty and multi-line fixtures" | diff-local |
| criterion | Story 2 happy: Given the operator answers a question with a permitted non-default value, when onboarding records configuration, then the project configuration carries that value and the harness reads it back as the effective value on its next run. | task-3 | covered | "`loadConfig` reads it back unchanged" | diff-local |
| criterion | Story 2 happy: Given the operator accepts the offered value at every question, when onboarding records configuration, then the resulting project configuration parses to the same effective settings onboarding produced before this change, except that the aggregate test command is the offered project-specific default Story 3 establishes rather than the former fixed literal, and every line it adds to the pre-change output is a comment or blank line. | task-4 | covered | "`runConfigInit` with no flags produces a file whose parsed settings deep-equal the parsed pre-change `config-init-defaults.yml` and whose non-comment, non-blank lines equal the fixture's in order, as asserted by the flagless effective-identity test." | diff-local |
| criterion | Story 2 negative: Given a value that would fail configuration validation, when recording is attempted with it, then recording refuses before any file is written, names the rejected value, and the project has no partially written configuration. | task-2 | covered | "writes no config file for the empty and multi-line fixtures" | diff-local |
| criterion | Story 2 negative: Given recording is invoked with a value for a setting it does not accept, when it runs, then it refuses with a message naming the unsupported setting and writes nothing. | task-2 | covered | "exits 1 with `unsupported flag --<name>` and writes no file" | diff-local |
| criterion | Story 3 happy: Given an operator is present, when onboarding asks for the project's test command, then the operator's answer is recorded verbatim as the aggregate test command and is the value the pre-ship gate invokes. | task-3, task-1 | covered | "The rendered `test_suite.command` equals the `--test-suite-command` value" | diff-local |
| criterion | Story 3 happy: Given the project's tooling makes a candidate command evident, when the question is asked, then that candidate is offered as the default answer rather than a fixed single-ecosystem literal. | task-12 | covered | "names the tooling-file inference table" | diff-local |
| criterion | Story 3 negative: Given no candidate can be inferred and the operator answers nothing, when the question resolves, then onboarding re-asks rather than recording a command the project cannot run. | task-12 | covered | "the empty-and-uninferred re-ask rule" | diff-local |
| criterion | Story 3 negative: Given the operator answers with a command containing shell metacharacters, when it is recorded, then it is stored as a single literal value and is not executed, expanded, or split at recording time. | task-3 | covered | "stored as a single YAML scalar and `runConfigInit` spawns no process other than `git rev-parse`" | diff-local |
| criterion | Story 4 happy: Given no operator identity is established on this machine, when onboarding runs with an operator present, then it asks for the identity, offering the authenticated hosting-service login as the default when one is available, and records the answer as a machine-scoped setting without the operator editing any file. | task-13, task-7 | covered | "records it via `config set spec_owner`, and never instructs a file edit" | diff-local |
| criterion | Story 4 happy: Given operator identity is already established on this machine, when onboarding runs, then it reports the established identity and does not ask again. | task-13 | covered | "reports an already-set identity without asking" | diff-local |
| criterion | Story 4 negative: Given the operator submits an empty value to the identity question rather than explicitly declining it, when the answer is processed, then it is rejected, the question is re-asked with the decline option restated, and no identity is recorded. | task-7, task-13 | covered | "An empty or whitespace `spec_owner` value is refused with exit 1 naming the key and no file write" | diff-local |
| criterion | Story 4 negative: Given onboarding records operator identity, when the project's committed configuration is inspected afterwards, then it contains no operator identity, and a committed configuration that does carry one is still rejected at load exactly as before. | task-8 | covered | "`validateConfig` on a project source with `spec_owner` returns the existing anti-leak error" | diff-local |
| criterion | Story 5 happy: Given no identity is established, no authenticated login is available, and the operator declines to supply one, when onboarding finishes configuration, then it states that operator identity is unresolved and names the actions that will refuse until it is established. | task-14 | covered | "names the three blocked actions and forbids a placeholder value" | diff-local |
| criterion | Story 5 negative: Given identity is unresolved at the end of onboarding, when onboarding completes, then it does not report a fully successful setup and does not invent or record a placeholder identity. | task-14 | covered | "reports setup incomplete when identity is unresolved" | diff-local |
| criterion | Story 6 happy: Given a project whose configuration already exists, when onboarding runs again, then every previously set value is preserved byte-for-byte and the operator is told which settings are already established rather than being asked to decide them again. | task-15 | covered | "prints `already set:` lines, and asks no question for them" | diff-local |
| criterion | Story 6 happy: Given operator identity is already established, when onboarding runs again, then it is reported and preserved. | task-13 | covered | "reports an already-set identity without asking" | diff-local |
| criterion | Story 6 negative: Given a project whose configuration already exists, when the recording step is invoked again with different answers, then it refuses to overwrite, reports that the configuration already exists, and the file is unchanged. | task-5 | covered | "the pre-existing file is byte-identical after a flagged re-run" | diff-local |
| criterion | Story 6 negative: Given a project configuration that has been hand-edited since it was recorded, when onboarding runs again, then the hand-edited values are preserved and reported, not replaced with defaults. | task-5 | covered | "The hand-edited `command: make check` value survives the re-run unchanged" | diff-local |
| criterion | Story 7 happy: Given onboarding has recorded a project configuration, when the operator opens it, then every top-level setting the walkthrough did not ask about is accompanied by an authored explanation stating what it controls, its permitted values, its default, and the consequence of changing it, and every nested setting either has its own such explanation or sits under a section explanation that links to the full configuration reference and says the ai-conductor agent can set the value on request. | task-18 | covered | "Every top-level project-settable key derived from `CONFIG_CONSUMER_KEY_SETS` (excluding the user-scoped `conductor` set, `spec_owner`, and the three asked keys) has an authored template comment block whose `Controls:`, `Allowed:`, `Default:`, and `Changing it:` lines name concrete permitted values, the concrete default, and a consequence, the seven key-specific blocks that commit `782b010cf` replaced are restored, and no block contains the placeholder phrases `a value accepted by the configuration validator`, `use the harness default`, or `changes this setting for the project`, as asserted by the authored-explanation test." | diff-local |
| criterion | Story 7 negative: Given the recorded configuration, when its explanations are compared with the keys the harness accepts, then no explained key is unknown to the harness, no top-level decidable-but-unasked key lacks an authored explanation, no nested key lacks both its own explanation and a section reference line, and no explanation is generic placeholder text. | task-18 | covered | "Every `Controls:` token in the template names a key in the derived dotted-path set, and every derived nested key either has its own block or its top-level section block carries a `Reference:` line containing `https://jstoup111.github.io/ai-conductor/reference/configuration` and stating that the ai-conductor agent can set the value on request, as asserted by the coverage test, so a block naming a rejected key, or an accepted key with neither a block nor a section reference, fails." | diff-local |
| criterion | Story 8 happy: Given onboarding runs with no operator present, when it reaches configuration, then it asks no question, records today's defaults, and completes. | task-16 | covered | "the unchanged flagless-defaults invocation with no question text" | diff-local |
| criterion | Story 8 negative: Given onboarding runs with no operator present and no identity is established, when it reaches the identity step, then it neither asks nor records an identity, and the existing fail-closed behavior on later identity-dependent actions is unchanged. | task-16 | covered | "declares itself skipped in auto mode with no `config set` call" | diff-local |
| criterion | Story 8 negative: Given onboarding runs with no operator present, when its output is compared with the pre-change unattended output, then the recorded project configuration parses to the same effective settings and differs only by added comment or blank lines. | task-4 | covered | "`runConfigInit` with the auto-mode flags produces a file whose parsed settings deep-equal the parsed pre-change fixture and whose non-comment, non-blank lines equal the fixture's in order, as asserted by the auto-mode effective-identity test." | diff-local |
