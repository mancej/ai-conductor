# Coherence: `gh` version floor and machine-level environment gate (#2139)

**Plan:** .docs/plans/gh-cli-capability-probe-report-an-unsupported-json.md
**Tier:** M
**Date:** 2026-09-05

One table. Legacy rows carry five cells; criterion rows carry six. Every criterion text is the
authoritative extractor string; every quote was mechanically verified as an exact substring of the
cited task's body. Technical track, so the `fr` row class is omitted rather than stubbed.

The intake's third Desired-outcome bullet — that the minimum `gh` version be stated alongside the
other prerequisites — was removed from the staged outcomes by operator decision on 2026-09-05. It
is ordinary documentation, and the explore, stories, and plan documentation boundaries each forbid
authoring a story or plan task for it, so it could never acquire a counterpart id. The
documentation still ships in this PR and is recorded as a follow-up action in
adr-2026-09-05-gh-cli-version-floor-and-environment-gate. The remaining three outcome rows are
renumbered accordingly; there is no gap row.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote / Notes | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-5 | covered | An unsupported field becomes a typed capability error naming the `gh` CLI and the offending field, replacing today's "cannot verify PR identity and head" |
| outcome | outcome-2 | story-2, story-3 | covered | The floor is checked before any feature is claimed, so no FINISH retry budget can be spent on it; the daemon raises one waiting condition and the DECIDE entry refuses |
| outcome | outcome-3 | story-6 | covered | The finish-record path still refuses and records no outcome on an unverifiable PR, and its behavior for a genuinely missing PR is unchanged |
| story | story-1 | task-1, task-2, task-3, task-4, task-18 | covered | Tasks 1-4 own the parser, suffix rules, floor constant, and config immunity; task 18 owns the real-binary smoke |
| story | story-2 | task-8, task-9, task-10, task-11 | covered | Task 8 raises the condition, task 9 proves nothing is charged to a feature, task 10 clears and resumes, task 11 bounds the logging |
| story | story-3 | task-12, task-13 | covered | Task 12 owns the entry refusal, task 13 proves it mutates nothing and names an absent CLI distinctly |
| story | story-4 | task-5, task-6, task-7 | covered | Task 5 owns injection and the exec guard, task 6 the bound, task 7 the failure mapping |
| story | story-5 | task-14, task-15, task-19 | covered | Task 14 owns the typed error and structured recognition, task 15 the non-committal default, task 19 the finish-record surface wording |
| story | story-6 | task-16, task-17 | covered | Task 16 owns the two opposite-disposition callers, task 17 the park-reconciliation vocabulary |
| task | task-1 | story-1 | covered | infrastructure task; Story line cites story 1 |
| task | task-2 | story-1 | covered | negative-path task; Story line cites story 1 |
| task | task-3 | story-1 | covered | happy-path task; Story line cites story 1 |
| task | task-4 | story-1 | covered | negative-path task; Story line cites story 1 |
| task | task-5 | story-4 | covered | infrastructure task; Story line cites story 4 |
| task | task-6 | story-4 | covered | negative-path task; Story line cites story 4 |
| task | task-7 | story-4 | covered | negative-path task; Story line cites story 4 |
| task | task-8 | story-2 | covered | happy-path task; Story line cites story 2 |
| task | task-9 | story-2 | covered | negative-path task; Story line cites story 2 |
| task | task-10 | story-2 | covered | happy-path task; Story line cites story 2 |
| task | task-11 | story-2 | covered | negative-path task; Story line cites story 2 |
| task | task-12 | story-3 | covered | happy-path task; Story line cites story 3 |
| task | task-13 | story-3 | covered | negative-path task; Story line cites story 3 |
| task | task-14 | story-5 | covered | happy-path task; Story line cites story 5 |
| task | task-15 | story-5 | covered | negative-path task; Story line cites story 5 |
| task | task-16 | story-6 | covered | negative-path task; Story line cites story 6 |
| task | task-17 | story-6 | covered | negative-path task; Story line cites story 6 |
| task | task-18 | story-1 | covered | infrastructure task serving story 1; supplies the real-binary evidence decision 8 requires |
| task | task-19 | story-5 | covered | happy-path task; Story line cites story 5 |
| adr | adr-2026-09-05-gh-cli-version-floor-and-environment-gate | story-1, story-2, story-3, story-4, story-5, story-6 | covered | Obligation table judged decision by decision: D1 task 3 (the floor value lives in one constant), D2 task 4 (no config surface can lower it), D3 tasks 8 and 9 (dispatch prevented, nothing charged to a feature), D4 task 5 (injectable probe honoring the exec guard), D5 task 14 (typed class from structured fields, one recognizer), D6 task 16 (both callers keep their opposite dispositions), D7 task 9 (no `mechanical` halt class written), D8 task 18 (opt-in real-binary smoke), D9 no-change (the residual direct-`gh` call is recorded as out of scope and obliges no code change, which the ADR itself carries), D10 task 17 (park-reconciliation's vocabulary is unchanged) |
| criterion | Story 1 happy: Given an installed `gh` reporting version 2.73.0, when the daemon reaches its dispatch cycle, then dispatch proceeds and no waiting condition is raised. | task-8 | covered | A dispatch cycle with a probe reporting `2.73.0` raises no condition and dispatch proceeds unchanged. | diff-local |
| criterion | Story 1 happy: Given an installed `gh` reporting a version above 2.73.0, when the daemon reaches its dispatch cycle, then dispatch proceeds and no waiting condition is raised. | task-8 | covered | A dispatch cycle with a probe reporting `2.73.0` raises no condition and dispatch proceeds unchanged. | diff-local |
| criterion | Story 1 happy: Given an installed `gh` reporting version 2.73.0, when the DECIDE/engineer entry runs, then it proceeds without raising an environment refusal. | task-12 | covered | The entry refuses with a probe reporting `2.14.1` and proceeds with one reporting `2.73.0`. | diff-local |
| criterion | Story 1 happy: Given the floor is consulted, when its value is read, then it resolves to 2.73.0 from a single declared constant rather than from configuration. | task-3 | covered | The literal `2.73.0` appears in exactly one exported constant in the module. | diff-local |
| criterion | Story 1 negative: Given an installed `gh` reporting version 2.72.9, when the daemon reaches its dispatch cycle, then dispatch is prevented rather than proceeding. | task-3 | covered | `checkGhVersionFloor` returns `ok` at exactly `2.73.0` and above, and `below-floor` for `2.72.9`, `2.18.0`, and `2.14.1`. | diff-local |
| criterion | Story 1 negative: Given an installed `gh` reporting version 2.18.0, when the daemon reaches its dispatch cycle, then dispatch is prevented, because 2.18.0 satisfies only `headRefOid` and not `gh pr edit`. | task-3 | covered | `checkGhVersionFloor` returns `ok` at exactly `2.73.0` and above, and `below-floor` for `2.72.9`, `2.18.0`, and `2.14.1`. | diff-local |
| criterion | Story 1 negative: Given a `settings.json` and a user config that both attempt to set a `gh` version floor key, when the floor is consulted, then the declared constant is still 2.73.0 and neither file changes it. | task-4 | covered | A test asserts `GH_VERSION_FLOOR` is unaffected by project config, user config, and environment variables. | diff-local |
| criterion | Story 1 negative: Given a `gh` reporting exactly 2.73.0-beta or another pre-release suffix, when the floor is compared, then the comparison resolves deterministically and the chosen verdict is recorded rather than the suffix being silently discarded. | task-2 | covered | A `-beta.1` suffix compares strictly below the same release version. | diff-local |
| criterion | Story 2 happy: Given an installed `gh` reporting version 2.14.1, when the daemon reaches its dispatch cycle, then it raises one waiting condition naming the `gh` CLI, the found version, and the required floor. | task-8 | covered | The condition text contains the strings `gh`, `2.14.1`, and `2.73.0`, and names upgrading `gh` as the remedy. | diff-local |
| criterion | Story 2 happy: Given an installed `gh` reporting version 2.14.1, when the daemon reaches its dispatch cycle, then no feature is claimed from the backlog. | task-9 | covered | Across several cycles below the floor, zero features are claimed and no retry or attempt counter advances. | diff-local |
| criterion | Story 2 happy: Given an installed `gh` reporting version 2.14.1 and a backlog of pending features, when several dispatch cycles elapse, then no feature's retry or attempt counter has advanced. | task-9 | covered | Across several cycles below the floor, zero features are claimed and no retry or attempt counter advances. | diff-local |
| criterion | Story 2 happy: Given the waiting condition is raised, when the operator upgrades `gh` to 2.73.0, then the next dispatch cycle clears the condition and dispatch resumes. | task-10 | covered | A probe sequence of `2.14.1` then `2.73.0` clears the condition and dispatches on the second cycle with no operator action beyond the upgrade. | diff-local |
| criterion | Story 2 happy: Given a feature was in flight when the daemon last ran, when a below-floor `gh` prevents the next dispatch, then that feature's existing state is left untouched. | task-10 | covered | Pre-existing feature state is byte-identical before and after the gate engages and clears. | diff-local |
| criterion | Story 2 negative: Given an installed `gh` reporting version 2.14.1, when the daemon reaches its dispatch cycle, then no per-feature HALT marker is written for any feature. | task-9 | covered | No halt marker and no halt-class sidecar is written for any feature. | diff-local |
| criterion | Story 2 negative: Given an installed `gh` reporting version 2.14.1, when the daemon reaches its dispatch cycle, then no per-feature halt-class sidecar records `mechanical` for any feature. | task-9 | covered | No halt marker and no halt-class sidecar is written for any feature. | diff-local |
| criterion | Story 2 negative: Given any HALT is written anywhere on this path, when its class is read, then the class is `needs-human` and never `mechanical`. | task-9 | covered | A search of this feature's diff finds no write of the halt class `mechanical`. | diff-local |
| criterion | Story 2 negative: Given a below-floor `gh` and twenty consecutive dispatch cycles, when the daemon log is read, then the waiting condition is reported without emitting twenty duplicate identical alarms. | task-11 | covered | Twenty consecutive identical below-floor cycles produce fewer than twenty condition log lines. | diff-local |
| criterion | Story 2 negative: Given a below-floor `gh`, when the operator inspects the raised condition, then it states the remedy (upgrade `gh`) rather than only the fault. | task-8 | covered | The condition text contains the strings `gh`, `2.14.1`, and `2.73.0`, and names upgrading `gh` as the remedy. | diff-local |
| criterion | Story 3 happy: Given an installed `gh` reporting 2.73.0 or above, when the DECIDE/engineer entry runs, then it proceeds to its normal entry decision. | task-12 | covered | The entry refuses with a probe reporting `2.14.1` and proceeds with one reporting `2.73.0`. | diff-local |
| criterion | Story 3 happy: Given an installed `gh` reporting 2.14.1, when the DECIDE/engineer entry runs, then it refuses with a message naming the CLI, the found version, and the floor. | task-12 | covered | The refusal message contains `gh`, `2.14.1`, and `2.73.0`. | diff-local |
| criterion | Story 3 happy: Given the floor check and the existing fail-closed DECIDE-entry policy both apply, when the entry runs, then the floor check composes with that policy rather than replacing or duplicating its decision. | task-12 | covered | The existing entry policy's own decision is still reached when the floor check passes. | diff-local |
| criterion | Story 3 negative: Given an installed `gh` reporting 2.14.1, when the DECIDE/engineer entry refuses, then no spec worktree is created and no branch is cut. | task-13 | covered | A below-floor refusal leaves no worktree, no branch, and no claim record. | diff-local |
| criterion | Story 3 negative: Given an installed `gh` reporting 2.14.1, when the DECIDE/engineer entry refuses, then no claim record is written and no intake issue is mutated. | task-13 | covered | A below-floor refusal leaves no worktree, no branch, and no claim record. | diff-local |
| criterion | Story 3 negative: Given `gh` is absent from `PATH`, when the DECIDE/engineer entry runs, then it refuses naming the absent binary rather than reporting a version comparison failure. | task-13 | covered | The `absent` refusal text names the missing binary and does not contain the floor-comparison wording. | diff-local |
| criterion | Story 4 happy: Given the probe is constructed in production, when it runs, then it invokes the real `gh --version` through the guarded runner. | task-5 | covered | The production default calls the shared real-exec guard before any spawn. | diff-local |
| criterion | Story 4 happy: Given a test injects a fake probe, when the daemon dispatch cycle or the DECIDE/engineer entry runs, then the injected probe is used and no process is spawned. | task-5 | covered | The probe accepts an injected runner and returns its output unchanged. | diff-local |
| criterion | Story 4 negative: Given `AI_CONDUCTOR_NO_REAL_EXEC` is set and no probe is injected, when the probe attempts to run, then it throws the existing real-exec guard error rather than spawning `gh`. | task-5 | covered | With the real-exec kill switch set and no injection, the probe throws the existing guard error and spawns nothing. | diff-local |
| criterion | Story 4 negative: Given the daemon dispatch cycle and the DECIDE/engineer entry are exercised by this feature's own tests, when those tests run, then each uses an injected probe and spawns no real `gh --version`. | task-5 | covered | With the real-exec kill switch set and no injection, the probe throws the existing guard error and spawns nothing. | diff-local |
| criterion | Story 4 negative: Given `gh --version` does not return within a bounded time, when the probe runs, then the probe terminates with a distinct timeout verdict rather than hanging the dispatch cycle. | task-6 | covered | A runner that never settles yields the `timeout` verdict and the call returns within the declared bound. | diff-local |
| criterion | Story 4 negative: Given `gh --version` exits non-zero, when the probe runs, then the probe reports an absent-or-unusable verdict rather than treating empty output as a satisfied floor. | task-7 | covered | A non-zero exit yields a refusing verdict, never `ok`. | diff-local |
| criterion | Story 5 happy: Given a `gh` invocation fails with `Unknown JSON field: "headRefOid"`, when the seam handles it, then it produces a typed capability error naming the `gh` CLI and the field `headRefOid`. | task-14 | covered | A rejection with the unsupported-field signal in `stderr` yields `GhCapabilityError` carrying `headRefOid` as a structured field, not only in its message. | diff-local |
| criterion | Story 5 happy: Given a typed capability error reaches an operator-visible surface, when that surface is read, then it states the CLI version problem rather than an inability to verify PR identity. | task-19 | covered | With a `GhCapabilityError`, the finish-record stderr line begins with the CLI capability problem and names `gh` and the field, and the words `cannot verify PR` do not open the line. | diff-local |
| criterion | Story 5 happy: Given the seam classifies a failure, when it reads the evidence, then it reads the error's structured fields (`stderr` and exit code) and not its `message` string. | task-14 | covered | The recognizer exists in exactly one function and reads no `message` property. | diff-local |
| criterion | Story 5 happy: Given a `gh` invocation succeeds, when the seam handles it, then the result is returned unchanged and no capability error is produced. | task-14 | covered | A successful invocation returns its result unchanged with no wrapper allocation observable to callers. | diff-local |
| criterion | Story 5 negative: Given a `gh` invocation fails because the PR genuinely does not exist, when the seam handles it, then no capability error is produced and the original failure is preserved. | task-15 | covered | A not-found rejection and a network rejection are re-thrown unchanged and are not `GhCapabilityError`. | diff-local |
| criterion | Story 5 negative: Given a `gh` invocation fails with a network error, when the seam handles it, then no capability error is produced and the original failure is preserved. | task-15 | covered | A not-found rejection and a network rejection are re-thrown unchanged and are not `GhCapabilityError`. | diff-local |
| criterion | Story 5 negative: Given a downstream consumer receives a capability error, when it decides how to route, then it branches on the error's type and never on the words `Unknown JSON field`. | task-15 | covered | A repository search finds the string `Unknown JSON field` in exactly one production file. | diff-local |
| criterion | Story 5 negative: Given `gh` changes its unsupported-field wording, when the seam handles the new wording, then the seam's own recognition is the only place that requires updating. | task-14 | covered | The recognizer exists in exactly one function and reads no `message` property. | diff-local |
| criterion | Story 5 negative: Given a `gh` failure whose structured fields carry no recognizable unsupported-field signal, when the seam classifies it, then it produces no capability error and the original failure is preserved unchanged. | task-15 | covered | A non-zero exit with empty or unrelated `stderr` produces no capability error. | diff-local |
| criterion | Story 5 negative: Given a `gh` failure with a non-zero exit code but empty or ambiguous `stderr`, when the seam classifies it, then it produces no capability error, because an exit code alone must never assert a capability problem. | task-15 | covered | A non-zero exit with empty or unrelated `stderr` produces no capability error. | diff-local |
| criterion | Story 6 happy: Given the seam wrapper is in place, when a `gh` call succeeds, then every caller observes the same result it observed before the wrapper existed. | task-16 | covered | This feature's diff changes no conditional in either caller. | diff-local |
| criterion | Story 6 negative: Given the finish-record path receives a capability error while verifying PR identity, when it decides, then it refuses to record an outcome, preserving its fail-closed zero-write behavior. | task-16 | covered | The finish-record path records no outcome and creates no file when a capability error is raised. | diff-local |
| criterion | Story 6 negative: Given the finish-record path receives a capability error, when it refuses, then it writes nothing at all rather than writing a partial or marker record. | task-16 | covered | The finish-record path records no outcome and creates no file when a capability error is raised. | diff-local |
| criterion | Story 6 negative: Given the finish completion gate receives a capability error on its PR read, when it decides, then it still passes with a warning, preserving its fail-open behavior. | task-16 | covered | The finish completion gate still passes with a warning when its PR read raises a capability error. | diff-local |
| criterion | Story 6 negative: Given a PR genuinely does not exist, when the finish-record path decides, then it refuses exactly as it does today and records no outcome on an unverifiable PR. | task-16 | covered | The finish-record path's behavior for a genuinely missing PR is unchanged from before this feature. | diff-local |
| criterion | Story 6 negative: Given the park-reconciliation path receives a capability error while reading a merged PR head, when it decides, then it takes no action and deletes no branch or worktree. | task-17 | covered | A capability error during the merged-PR head read yields reason `no-merge-proof`, deletes nothing, and logs the capability error. | diff-local |
| criterion | Story 6 negative: Given the park-reconciliation path takes no action because of a capability error, when its refusal reason is reported, then the reason is still `no-merge-proof` and this feature adds no member to the refusal vocabulary. | task-17 | covered | The refusal reason union has the same members before and after this feature's diff. | diff-local |
| criterion | Story 6 negative: Given a capability error caused a `no-merge-proof` refusal, when the daemon log is read, then the typed capability error is logged so the cause stays recoverable without changing the reason string. | task-17 | covered | A capability error during the merged-PR head read yields reason `no-merge-proof`, deletes nothing, and logs the capability error. | diff-local |