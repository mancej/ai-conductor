# Coherence Mapping: finish-deadlocks-when-the-prose-judge-asks-for-rev

Technical track — no PRD, so the `fr` row class is omitted. Intake outcomes staged from
jstoup111/ai-conductor#2006. Criterion rows cite the plan task that owns each Given/When/Then,
with a verbatim quote from that task's body and a diff-locality disposition. Amended 2026-08-28
after the as-built review: Tasks 11 and 12 close findings AB-1 and AB-2.

| Row class | Id / Criterion | Counterpart id(s) | Verdict | Notes / Quote | Disposition |
| --- | --- | --- | --- | --- | --- |
| outcome | outcome-1 | story-2, story-3 | covered | Automatic authoring pass + re-judgment: Story 2 routes the revision request to authoring; Story 3 delivers the objection and re-judges the new revision. |
| outcome | outcome-2 | story-4 | covered | Story 4 bounds non-converging laps at the existing publication-progress allowance. |
| outcome | outcome-3 | story-4 | covered | Story 4 requires the rendered halt to include the verdict's detail verbatim when present. |
| outcome | outcome-4 | story-2, story-3 | covered | Story 2 keeps the guard firing for genuinely unselectable retries; Story 3 keeps the advance-path dimension guard halting an unmoved authoring pass. |
| outcome | outcome-5 | story-2 | covered | Story 2's coordinator-level criterion drives the exact #2006 state and requires advancing into authoring, never a repeat of the deadlock halt. |
| story | story-1 | task-1, task-2 | covered | Classification happy paths in Task 1; precedence and degradation negatives in Task 2. |
| story | story-2 | task-3, task-4 | covered | Selector/predicate routing in Task 3; deadlock repro and guard preservation in Task 4. |
| story | story-3 | task-5, task-6, task-7 | covered | Guidance in the request (5), rendered prompt (6), identical-revision guard (7). |
| story | story-4 | task-8, task-9, task-11, task-12 | covered | Halt-detail mechanism (8); bounded-lap acceptance (9); typed no-charge progress shape (11); objection on every prose human-required exit (12). |
| story | story-5 | task-10 | covered | Contract text + prompt detail request + pinned decoder routings. |
| task | task-1 | story-1 | covered | Classify judged-deficient prose as revision_required. |
| task | task-2 | story-1 | covered | Precedence and store-degradation negatives. |
| task | task-3 | story-2 | covered | Selector and effect predicates. |
| task | task-4 | story-2 | covered | Deadlock reproduction and guard preservation. |
| task | task-5 | story-3 | covered | Authoring request carries the objection. |
| task | task-6 | story-3 | covered | Revision-lap authoring prompt. |
| task | task-7 | story-3 | covered | Unproductive authoring pass resolves human_required. |
| task | task-8 | story-4 | covered | Retry detail + exhaustion halt message. |
| task | task-9 | story-4 | covered | Lap bounding acceptance tests. |
| task | task-10 | story-5 | covered | Verdict contract documentation and prompt. |
| task | task-11 | story-4 | covered | Typed judged-deficiency progress disposition; no reason-string retry exemption. |
| task | task-12 | story-4 | covered | Originating objection rendered at every prose human-required exit. |
| adr | adr-2026-08-13-a-publication-transition-advances-only-when-it-moves-the-dimension-it-owns | story-2, story-3 | covered | 2026-08-28 amendment adopts narrowed Option C; Story 2 implements the observation-derived routing, Story 3 preserves the advance-path dimension guard. |
| adr | adr-2026-08-01-engine-owned-resumable-finish-publication | story-1, story-2 | covered | D4 escape clause exercised; Story 1 widens what observation expresses, Story 2 keeps routing observation-derived (D1). |
| adr | adr-2026-08-06-publication-progress-is-its-own-disposition | story-4 | covered | 2026-08-28 amendment adds the typed `publication_revision_progress` shape; Task 11 implements it and keeps the lap charged to the unchanged progress allowance. |
| criterion | Story 1 happy: Given a PR whose observed title/body revision digest has a persisted `revision_required` verdict with reason `structurally_incomplete` in the prose-judgment store, when publication state is observed, then the snapshot reports `pr.prose = 'revision_required'` | task-1 | covered | "returns `revision_required` when the observed revision digest has a stored `revision_required` verdict with reason `structurally_incomplete`" | diff-local |
| criterion | Story 1 happy: Given a PR whose observed revision digest has a persisted `revision_required` verdict with reason `placeholder`, when publication state is observed, then the snapshot reports `pr.prose = 'revision_required'` | task-1 | covered | "and again for reason `placeholder`" | diff-local |
| criterion | Story 1 happy: Given a PR whose observed revision digest has a persisted `accepted` verdict, when publication state is observed, then the snapshot reports `pr.prose = 'accepted'` exactly as before | task-1 | covered | "returns `accepted` for a stored `accepted` verdict" | diff-local |
| criterion | Story 1 negative: Given a PR whose body was edited after the deficient verdict was persisted (observed revision digest no longer matches any stored verdict), when publication state is observed, then the snapshot reports `pr.prose = 'stale'` and the new revision is eligible for a fresh judgment | task-2 | covered | "a body edited after the verdict (digest mismatch) yields `stale`" | diff-local |
| criterion | Story 1 negative: Given a PR carrying a halt signal (needs-remediation label, title prefix, banner, or body marker) whose revision digest also has a persisted deficient verdict, when publication state is observed, then the snapshot reports `pr.prose = 'halt'` — halt classification strictly precedes `revision_required` | task-2 | covered | "a halt-signal PR whose digest also has a deficient verdict classifies `halt`" | diff-local |
| criterion | Story 1 negative: Given a persisted `revision_required` verdict with reason `halt` for the observed revision, when publication state is observed, then the snapshot does NOT report `revision_required` (that verdict keeps its human-required routing and never enters the authoring lap) | task-2 | covered | "a stored verdict with reason `halt` yields `stale`, not `revision_required`" | diff-local |
| criterion | Story 1 negative: Given a missing, unreadable, or malformed prose-judgment store, when publication state is observed, then observation does not throw and authored prose reports `stale` (today's behavior — degrades to a fresh judgment, never to a halt) | task-2 | covered | "a malformed store file does not throw and yields `stale` for authored prose" | diff-local |
| criterion | Story 1 negative: Given an engine-floored placeholder body whose digest coincidentally has a stored verdict, when publication state is observed, then the snapshot reports `placeholder` (placeholder detection precedes verdict lookup) | task-2 | covered | "an engine-floored body with a coincidental stored verdict yields `placeholder`" | diff-local |
| criterion | Story 2 happy: Given a snapshot with `pr.prose = 'revision_required'` (identity `one`, branch pushed, release readiness valid), when the next publication transition is selected, then it is `author_pr_prose` | task-3 | covered | "returns `author_pr_prose` for a snapshot with prose `revision_required`" | diff-local |
| criterion | Story 2 happy: Given a judgment that returns `revision_required`/`structurally_incomplete` and persists it, when the resulting `author_pr_prose` retry is reconciled against a fresh observation, then the fresh observation also selects `author_pr_prose` and the retry proceeds instead of resolving `human_required: publication_transition_unmoved` | task-4 | covered | "assert the coordinator advances into `author_pr_prose` (authoring effect invoked) and never resolves `human_required` with reason `publication_transition_unmoved`" | diff-local |
| criterion | Story 2 negative: Given a snapshot with `pr.prose = 'revision_required'` but a halted PR (`pr.halted = true`), when a publication retry is reconciled, then the result is `human_required: halt_state_pr` before any authoring or judgment dispatch | task-4 | covered | "a halted PR still resolves `halt_state_pr` before any prose dispatch" | diff-local |
| criterion | Story 2 negative: Given a snapshot with `pr.prose = 'stale'` (no persisted deficient verdict), when the next transition is selected, then it remains `judge_pr_prose` — the selector routes to authoring only on the persisted-verdict evidence | task-3 | covered | "returns `judge_pr_prose` for `stale` unchanged" | diff-local |
| criterion | Story 2 negative: Given a retry naming a transition the fresh observation genuinely does not select (any non-prose example, e.g. a `write_shipped_record` retry after the record appears), when the retry is reconciled, then it still resolves `human_required: publication_transition_unmoved` — the guard's original purpose is intact | task-4 | covered | "still resolves `human_required: publication_transition_unmoved` for a retry whose named transition the fresh observation genuinely does not select" | diff-local |
| criterion | Story 3 happy: Given a persisted deficient verdict carrying a non-empty `detail`, when the authoring transition dispatches its request, then the request includes that detail as revision guidance and the rendered provider task contains it | task-5, task-6 | covered | "the coordinator's authoring dispatch receives a `PrProseAuthoringRequest` with `revisionGuidance` equal to that detail" | diff-local |
| criterion | Story 3 happy: Given an authoring pass that rewrites the body (new revision digest, no persisted verdict for it), when publication state is re-observed, then prose reports `stale` and the next selected transition is `judge_pr_prose` — the lap continues through the real judge | task-7 | covered | "a genuinely rewritten body (new digest, prose observes `stale`) reports `advanced`" | diff-local |
| criterion | Story 3 negative: Given a persisted deficient verdict with no `detail` (as on PR #1946), when the authoring transition dispatches, then the request carries no guidance field and the authoring pass still runs its full rewrite — absence of detail never blocks the lap | task-5 | covered | "when the stored verdict has no `detail`, the request omits the field and the dispatch still runs" | diff-local |
| criterion | Story 3 negative: Given an authoring pass that returns a byte-identical revision (digest unchanged, deficient verdict still applies), when the post-effect observation is evaluated, then the advance-path dimension guard reports the authoring transition as not having moved `pr.prose` and resolves `human_required` — an unproductive authoring pass cannot silently loop | task-7 | covered | "authoring effect leaves the body byte-identical (post-effect observation still classifies `revision_required` for the same digest)" | diff-local |
| criterion | Story 4 happy: Given a judge that returns `revision_required`/`structurally_incomplete` with detail for every revision the author produces, when FINISH runs, then author→judge laps repeat only until the existing publication-progress allowance is exhausted and the run halts rather than looping | task-9, task-11 | covered | "terminates via the existing publication-progress allowance with a halt, not an unbounded loop" | diff-local |
| criterion | Story 4 happy: Given a prose-related `human_required` halt whose originating verdict carried a `detail`, when the halt reason is rendered, then the rendered text includes that detail verbatim | task-8, task-12 | covered | "the conductor's allowance-exhaustion halt message includes the last retry's detail when present" | diff-local |
| criterion | Story 4 negative: Given a prose-related `human_required` halt whose originating verdict carried no `detail`, when the halt reason is rendered, then the message still names the condition and next action without an empty or placeholder detail clause | task-8, task-12 | covered | "contains no empty detail clause when absent" | diff-local |
| criterion | Story 4 negative: Given a converging lap (second revision accepted), when FINISH runs, then no allowance-exhaustion halt occurs and publication proceeds to the shipped record — the bound only fires on genuine non-convergence | task-9 | covered | "a judge that accepts the second revision publishes through to the shipped record with no allowance halt" | diff-local |
| criterion | Story 5 happy: Given the shipped verdict vocabulary, when the judgment decoder's contract tests run against the documented contract in the finish skill, then they pass with the documented verdict set matching the decoder exactly | task-10 | covered | "assert the documented verdict set in the finish skill matches the decoder exactly" | diff-local |
| criterion | Story 5 happy: Given the judge prompt contract, when a `revision_required` verdict is rendered by the provider, then the contract instructs it to include a concrete objection `detail` describing what is deficient | task-10 | covered | "asks for a concrete objection detail on `revision_required`" | diff-local |
| criterion | Story 5 negative: Given a provider reply that omits `detail` on a `revision_required` verdict, when the reply is decoded, then it decodes successfully as a detail-less revision requirement — the contract requests detail but the decoder never rejects its absence | task-10 | covered | "detail-less `revision_required` is pinned unchanged" | diff-local |
| criterion | Story 5 negative: Given an undecodable provider reply, when it is decoded, then it still maps to `malformed_response` and earns a fresh judgment session — unchanged routing | task-10 | covered | "pins the four unchanged routings plus detail-less decoding" | diff-local |
