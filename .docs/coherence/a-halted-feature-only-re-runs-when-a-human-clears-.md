# Coherence: retry at the raiser, and operator budget recovery (#2190)

**Plan:** .docs/plans/a-halted-feature-only-re-runs-when-a-human-clears-.md
**Tier:** M
**Date:** 2026-09-05

One table. Legacy rows carry five cells; criterion rows carry six. Every criterion text is the
authoritative extractor string; every quote is a case-sensitive substring of the cited task's body.
A criterion may cite a task whose Story line names a sibling story (Tasks 11, 13, 15 serve Stories
4–6 together); the story rows are keyed on Story lines, the criterion rows on the task that proves them.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote / Notes | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-2 | covered | Delivered as bounded retries at the raiser (group member budget, suite-infrastructure lane) with each retry on the spine and in the daemon log; the intake's backoff re-dispatch mechanism was replaced in DECIDE because adr-2026-08-05 §4 forbids a clock-resumed marker, and live-boundary/seal halts are excluded as fail-closed by decided design (operator-approved 2026-09-05) |
| outcome | outcome-2 | story-2, story-1 | covered | Exhaustion of the suite-infrastructure allowance and of the group member budget each halt needs-human naming the attempts spent |
| outcome | outcome-3 | story-5, story-6 | covered | One operator command (kickback-budget raise or reset) records the attributed adjustment in the ledger; no config edit, no commit on main |
| outcome | outcome-4 | story-7, story-5 | covered | Adjustment on the spine (kickback_budget_adjustment_authorized) and in daemon status; the daemon clears the halt and the feature resumes after its last completed step |
| outcome | outcome-5 | story-3 | covered | needs-human, plan-gap, over-scope, and kickback-cap halts are retained by every automatic path; three budget halts are reclassified so no sweep auto-clears them |
| story | story-1 | task-1 | covered | Task 1 owns both member dispatch sites |
| story | story-2 | task-2, task-3, task-4 | covered | Tasks 2–4 own the lane, its exhaustion, and the fail-closed counter |
| story | story-3 | task-5, task-6, task-20 | covered | Task 5 reclassifies; Task 6 proves retention over the unchanged sweep; Task 20 shares that retention with the progress re-kick and episode-end paths |
| story | story-4 | task-10, task-12 | covered | Tasks 10 and 12 own the renderer and inspect; refusals shared with Task 11 |
| story | story-5 | task-7, task-8, task-9, task-11, task-13, task-14, task-16 | covered | Tasks 7–9, 11, 13, 14, 16 own the ledger, evidence, CLI, and crash windows |
| story | story-6 | task-15 | covered | Tasks 13 and 15 own reset for both gate kinds |
| story | story-7 | task-17, task-18, task-19 | covered | Tasks 17–19 own the daemon boundary, status, and resume |
| task | task-1 | story-1 | covered | happy-path task; Story line cites story 1 |
| task | task-2 | story-2 | covered | infrastructure task; Story line cites story 2 |
| task | task-3 | story-2 | covered | happy-path task; Story line cites story 2 |
| task | task-4 | story-2 | covered | negative-path task; Story line cites story 2 |
| task | task-5 | story-3 | covered | happy-path task; Story line cites story 3 |
| task | task-6 | story-3 | covered | negative-path task; Story line cites story 3 |
| task | task-7 | story-5 | covered | infrastructure task; Story line cites story 5 |
| task | task-8 | story-5 | covered | infrastructure task; Story line cites story 5 |
| task | task-9 | story-5 | covered | happy-path task; Story line cites story 5 |
| task | task-10 | story-4 | covered | happy-path task; Story line cites story 4 |
| task | task-11 | story-5 | covered | infrastructure task; Story line cites story 5 |
| task | task-12 | story-4 | covered | happy-path task; Story line cites story 4 |
| task | task-13 | story-5 | covered | happy-path task; Story line cites story 5 |
| task | task-14 | story-5 | covered | negative-path task; Story line cites story 5 |
| task | task-15 | story-6 | covered | happy-path task; Story line cites story 6 |
| task | task-16 | story-5 | covered | negative-path task; Story line cites story 5 |
| task | task-17 | story-7 | covered | happy-path task; Story line cites story 7 |
| task | task-18 | story-7 | covered | happy-path task; Story line cites story 7 |
| task | task-19 | story-7 | covered | negative-path task; Story line cites story 7 |
| task | task-20 | story-3 | covered | negative-path task; Story line cites story 3 |
| adr | adr-2026-08-29-operator-authorized-kickback-budget-recovery | story-4, story-5, story-6, story-7 | covered | Obligation table judged decision by decision: D1 tasks 7–8 (fields, lease), D2 tasks 13/15 (reset vs raise semantics per gate, amended 2026-09-05 for --gate), D3 task 11 (grammar, TTY, identity, refusals), D4 task 13 (owned vs pre-existing park), D5 task 14 (staged adjustment, exactly-once by id), D6 task 17 (daemon clears, CLI never does), D7 task 13 (sibling-ledger event declared in the sink registry), D8 task 10 (one renderer for inspect and the halt body) |
| criterion | Story 1 happy: Given a daemon-mode validation group whose `prd_audit` branch throws a transient provider error (HTTP 500) on its first attempt and completes on its second, when the group runs, then the group joins normally, `prd_audit` reaches a verdict, and no HALT marker is written. | task-1 | covered | yields a group join with no HALT marker | diff-local |
| criterion | Story 1 happy: Given a daemon-mode validation group where every member completes on its first attempt, when the group runs, then the observable outcome is byte-for-byte today's: same verdicts, same events, no extra dispatch. | task-1 | covered | existing validation-group join tests pass unchanged | diff-local |
| criterion | Story 1 negative: Given a daemon-mode validation group whose `prd_audit` branch throws on every attempt up to the serial attempt budget, when the budget is spent, then the group halts with the existing no-verdict halt naming the member, the reason, and the number of attempts spent, and the halt class is `needs-human`. | task-1 | covered | halts with class `needs-human` and a reason naming the member and the attempt count | diff-local |
| criterion | Story 1 negative: Given a daemon-mode validation group whose `manual_test` branch fails with an authentication failure, when the group runs, then the existing auth park-and-poll path is taken unchanged and no attempt-budget entry is consumed (adr-2026-07-04 §2). | task-1 | covered | the auth-failure park-and-poll path still consumes no attempt | diff-local |
| criterion | Story 2 happy: Given a daemon-mode `test_suite` step whose full-suite verifier returns `FAILED` with reason `timeout` on the first run and a clean pass on the second, when the step runs, then the suite is re-run within the step, the step completes, a `step_retry` event records attempt 1 with an infrastructure reason, and no kickback-ledger `count` or `cumulative` field changes. | task-3 | covered | exactly one `step_retry` event carries an infrastructure reason | diff-local |
| criterion | Story 2 happy: Given a `test_suite` verifier result with reason `nonzero_exit`, when the step evaluates it, then the existing code-repair kickback route is taken unchanged and the infrastructure lane is not entered. | task-3 | covered | takes the existing kickback route and leaves `suiteInfrastructureRetries` absent | diff-local |
| criterion | Story 2 negative: Given a daemon-mode `test_suite` step whose verifier returns an infrastructure failure on every run up to the fixed allowance, when the allowance is spent, then the feature halts with class `needs-human` and a reason naming the verifier reason and the number of automatic retries spent, and the evidence path `.pipeline/test-suite-evidence.json`. | task-4 | covered | a body naming the verifier reason and the retries spent | diff-local |
| criterion | Story 2 negative: Given the infrastructure-lane counter file is unreadable or malformed, when the step evaluates an infrastructure failure, then the lane treats the allowance as spent and halts `needs-human` naming the counter as unreadable (fail-closed, adr-2026-08-31 §1 posture), never as a fresh allowance. | task-4 | covered | a malformed counter halts without invoking the verifier again | diff-local |
| criterion | Story 2 negative: Given a feature whose infrastructure allowance was partly spent in a previous dispatch, when a later dispatch hits another infrastructure failure, then the counter continues from the persisted value rather than restarting, so the bound is per feature across dispatches. | task-4 | covered | the counter continues from its persisted value across a re-dispatch | diff-local |
| criterion | Story 3 happy: Given a feature whose `manual_test` FAIL survives the per-gate kickback cap, when the cap halt is written, then its class is `needs-human`, and a subsequent base-advance re-kick sweep skips the feature and logs the retained disposition. | task-5, task-6 | covered | read `HALT.class` as `needs-human` for the manual-test cap | diff-local |
| criterion | Story 3 happy: Given a feature whose `test_suite` failure survives the per-gate kickback cap, or whose per-gate remediation budget is exhausted at a build kickback, when that halt is written, then its class is `needs-human` and the re-kick sweep retains it. | task-5, task-6 | covered | the test_suite cap, and the per-gate remediation-budget halt | diff-local |
| criterion | Story 3 happy: Given a halt of class `needs-human`, `plan-gap`, `over-scope`, or `kickback-cap` and no operator resume authorization in the feature's ledger, when any automatic path runs (base-advance sweep, progress re-kick, episode-end sweep), then the halt is retained and no dispatch occurs. | task-6, task-20 | covered | One exported retention predicate in `daemon-rekick.ts` is the only place an automatic path decides that a classified human halt is retained. | diff-local |
| criterion | Story 3 negative: Given a feature halted by a self-host live-boundary trip or a protected-artifact seal error, when the daemon runs any automatic retry path this feature adds, then nothing in this feature clears or retries it, and the halt body is unchanged. | task-6 | covered | a live-boundary halt body and a seal halt body are unchanged after a sweep pass | diff-local |
| criterion | Story 3 negative: Given a feature halted with class `mechanical` for a reason other than the three reclassified budget halts, when a base advance occurs, then the existing sweep behavior is unchanged: the halt is cleared and the feature re-kicked rebase-first. | task-6 | covered | `mechanical` is cleared | diff-local |
| criterion | Story 3 negative: Given a stale `HALT.class` sidecar reading `mechanical` left by an older engine on one of the three reclassified halts, when the daemon starts, then the existing legacy migration leaves it as-is and the sweep's existing behavior applies, so no in-flight worktree is silently converted. | task-6 | covered | a `legacy` sidecar is left as-is by `migrateLegacyHaltClasses` | diff-local |
| criterion | Story 4 happy: Given a feature worktree whose kickback ledger has `build_review` cumulative 5 of effective limit 5 and `prd_audit` laps 2 of cap 2, when the operator runs `kickback-budget inspect --feature <slug>`, then the output lists both gates with consumed, limit, remaining 0, and the latest semantic reason, exits 0, and works without a TTY. | task-12 | covered | human inspect lists both gates with consumed, limit, remaining, and latest reason | diff-local |
| criterion | Story 4 happy: Given the same feature and `--format json`, when inspect runs, then stdout is a single JSON document containing the same fields per gate and an `adjustments` array (empty when no adjustment was ever recorded). | task-12 | covered | emits a single parseable document with an `adjustments` array | diff-local |
| criterion | Story 4 negative: Given a feature name that does not resolve to a worktree under the main repository, when inspect runs, then it prints a refusal naming the feature and exits non-zero without creating any file. | task-11 | covered | unresolved feature) exits non-zero | diff-local |
| criterion | Story 4 negative: Given a ledger whose adjustment history is malformed but whose gate values validate, when inspect runs, then the gate values are shown and the history is reported as unavailable rather than invented or defaulted (adr-2026-08-31 §2). | task-12 | covered | malformed-history and legacy entries render as unavailable rather than invented | diff-local |
| criterion | Story 4 negative: Given a legacy ledger entry with no effective-limit or history fields, when inspect runs, then consumed and the default limit are shown as authoritative and adjustment detail is reported unavailable. | task-12 | covered | a legacy entry prints defaults as authoritative | diff-local |
| criterion | Story 5 happy: Given a feature halted at the `build_review` cumulative cap with typed cap evidence in its ledger, when the operator runs `raise --gate build_review --by 1 --rationale "one more lap"` from an interactive terminal, then the ledger's `build_review` effective limit increases by 1, `cumulative` is unchanged, an adjustment record with id, kind, before/after, operator, rationale, and timestamp is appended, a `kickback_budget_adjustment_authorized` event is written to the feature's sibling event ledger, a resume authorization bound to the adjustment id and halt generation is installed, and the command exits 0 printing the new budget view. | task-13 | covered | raise applies `effectiveLimit + N` with `cumulative` unchanged, appends exactly one history record and one sibling-ledger event | diff-local |
| criterion | Story 5 happy: Given a feature halted at the `prd_audit` remediation-lap cap (`laps` equals the configured cap), when the operator runs `raise --gate prd_audit --by 1 --rationale "…"`, then the ledger's `prd_audit` entry gains a feature-local effective lap cap of configured+1 that the gate's append-budget resolution honors on resume, and `laps` consumed is unchanged. | task-15 | covered | raise on `prd_audit` yields `effectiveLapCap` of configured plus N with `laps` unchanged | diff-local |
| criterion | Story 5 happy: Given a feature with no operator park, when raise runs, then it creates a temporary park for the exact slug before mutating, and releases only that park after the adjustment is durable. | task-13 | covered | creates an owned park | diff-local |
| criterion | Story 5 happy: Given a feature reaching the `build_review` cumulative cap, the `prd_audit` remediation-lap cap, or the `architecture_review_as_built` remediation-lap cap, when the cap halt is written, then the ledger's gate entry already carries typed cap evidence (gate, consumed, effective limit, latest reason, halt generation) persisted before the marker, so a later raise or reset can bind to it without parsing halt prose. | task-9 | covered | read `capEvidence` with a `haltGeneration` from the ledger after the cap halt | diff-local |
| criterion | Story 5 negative: Given a feature whose live halt is not a cap halt (class `plan-gap`, `over-scope`, `needs-human` without typed cap evidence, or no HALT at all), when raise runs, then it refuses naming the reason, changes nothing in the ledger, and exits non-zero. | task-13 | covered | verify live halt class + `capEvidence` + generation | diff-local |
| criterion | Story 5 negative: Given raise invoked without an interactive terminal, or with an empty `--rationale`, or with `--by 0`, `--by -1`, or a non-integer, or with an unknown `--gate`, when it runs, then it refuses before touching the park or the ledger and exits non-zero. | task-11 | covered | non-TTY mutation, empty rationale, non-positive or non-integer `--by`, unknown gate | diff-local |
| criterion | Story 5 negative: Given the ledger lease is held by a live or ambiguous owner, when raise runs, then it refuses toward halted, leaves the park as it found it, and exits non-zero. | task-13 | covered | a held lease or generation mismatch refuses with the ledger unchanged | diff-local |
| criterion | Story 5 negative: Given the process crashes after the staged adjustment is written but before the event append succeeds, when the operator re-runs any `kickback-budget` command for that feature, then reconciliation discards the pending record and leaves active budget fields unchanged. | task-14 | covered | a pending record with no event is discarded and active fields are unchanged | diff-local |
| criterion | Story 5 negative: Given the process crashes after the event append succeeds but before the apply, when the operator re-runs any `kickback-budget` command, then reconciliation finds the event by adjustment id, completes the apply exactly once, and does not append a second event. | task-14 | covered | a pending record with an event is applied once and the event count is unchanged afterward | diff-local |
| criterion | Story 5 negative: Given a feature that was already operator-parked before raise ran, when raise completes, then the pre-existing park is preserved and the output prints the existing unpark action instead of releasing it. | task-13 | covered | a pre-existing park is preserved with the unpark action printed | diff-local |
| criterion | Story 6 happy: Given a feature halted at the `build_review` cumulative cap with effective limit 6 from an earlier raise, when reset runs for `build_review`, then `cumulative` becomes 0, the effective limit stays 6, an adjustment record and event are written, and a resume authorization is installed. | task-13 | covered | reset zeroes `cumulative` and preserves a raised `effectiveLimit` | diff-local |
| criterion | Story 6 happy: Given a feature halted at the `architecture_review_as_built` lap cap, when reset runs for that gate, then its consumed `laps` becomes 0 and its effective lap cap is unchanged. | task-15 | covered | reset on the as-built gate zeroes `laps` and keeps `effectiveLapCap` | diff-local |
| criterion | Story 6 negative: Given reset for one gate, when it completes, then every other gate's entry, the plan-growth record, the mechanical-fault fields, and `count` are byte-for-byte unchanged, asserted by comparing the ledger before and after. | task-13 | covered | other gates, growth, count, and mechanical fields are byte-for-byte unchanged | diff-local |
| criterion | Story 6 negative: Given the same refusals as Story 5 (non-cap halt, no TTY, empty rationale, unknown gate, held lease), when reset runs, then it refuses identically and changes nothing. | task-11 | covered | each refusal (non-TTY mutation, empty rationale | diff-local |
| criterion | Story 7 happy: Given a feature with a valid, unconsumed resume authorization whose halt generation matches the live cap halt and no operator park, when the daemon reaches its halted-feature boundary, then it clears the halt marker, its class sidecar, and the `needs-remediation` presentation as one operation, resolves the committed halt record, marks the authorization consumed, emits the existing halt-clear evidence on the spine, and dispatches the feature so it resumes after its last completed step. | task-17 | covered | clears the halt through `clearMarker` and `cleanupHaltPresentation`, marks it consumed, emits `halt_cleared` with cause `kickback-budget` | diff-local |
| criterion | Story 7 happy: Given a feature whose authorization was consumed, when the raised or reset gate next runs, then it enforces the new effective limit or zeroed consumption from the ledger, not from repository config. | task-19 | covered | the raised gate halts at the ledger's effective limit while config still holds the default | diff-local |
| criterion | Story 7 happy: Given a feature with an adjustment recorded, when the operator runs `daemon status`, then the feature's row shows the gate, the adjustment kind, and the remaining allowance. | task-18 | covered | a feature with an adjustment prints gate, kind, and remaining allowance | diff-local |
| criterion | Story 7 negative: Given an authorization whose halt generation does not match the live halt (the halt was rewritten since), when the boundary runs, then the authorization is refused as stale, the halt is retained, and nothing is dispatched. | task-17 | covered | stale generation, operator park, partial clear, and unreadable ledger each retain the halt | diff-local |
| criterion | Story 7 negative: Given a feature that is operator-parked, when the boundary runs, then the park wins: the authorization is left unconsumed and the feature stays parked until an explicit unpark. | task-17 | covered | each retain the halt with the authorization unconsumed and no dispatch | diff-local |
| criterion | Story 7 negative: Given the atomic halt clear reports `partial` (marker removed but presentation repair failed), when the boundary runs, then the feature stays halted, the authorization is not consumed, and the partial result is logged. | task-17 | covered | stale generation, operator park, partial clear, and unreadable ledger each retain the halt with the authorization unconsumed and no dispatch | diff-local |
| criterion | Story 7 negative: Given a ledger that cannot be validated at the boundary, when the boundary runs, then the read fails closed: no authorization is honored and the existing `needs-human` retention branch is taken (adr-2026-08-31 §1). | task-17 | covered | unreadable ledger each retain the halt with the authorization unconsumed and no dispatch | diff-local |

## Consistency pass

Cross-layer pairs checked in both directions: outcome-1 ↔ tasks 1–4 (bounded retry satisfies the
outcome's substance; the outcome's stated mechanism was amended in DECIDE, not contradicted by a
task); outcome-5 ↔ task 17 (an authorized clear is not an automatic retry — Story 3's third
criterion names the no-authorization precondition, so no oscillation); adr-2026-08-29 D6 ↔ Story 5
(the CLI installs an authorization and never removes the halt; Task 17 is the only clearer). No
`fail` rows.

## Assumptions

- outcome-1 is recorded `covered` on the operator's 2026-09-05 decision to reshape the retry
  mechanism; had the operator wanted a timer-driven re-dispatch, this row would be `fail`
  against adr-2026-08-05 §4 and the feature would need a superseding ADR.
