# Coherence Mapping: Export the dispatch dimensions the engine already holds

Feature: export-the-telemetry-dimensions-the-engine-already (jstoup111/ai-conductor#1940). Tier M, technical track (no PRD — the fr row class is omitted). Outcome ids are 1-based over the nine staged intake bullets in `.pipeline/intake-outcomes.md`. Four outcome rows are deliberate gaps waived in `.docs/coherence-waivers/export-the-telemetry-dimensions-the-engine-already.md`: outcome-1 and outcome-6 were delivered on main by #1938 and #1972 before this spec, outcome-2 (GitHub user) was excluded by the operator on privacy grounds, and outcome-9 (extensibility) was dropped with the operator's choice of approach A. The adr row covers the one non-deleted `adr-*.md` in this change set (adr-014, amended additively with D10-D11). Criterion rows carry the extractor-exact criterion text, cited task id(s), verdict, a verbatim quote from one cited task's `Done when` block, and the diff-locality disposition.

Consistency pass (§4d): cross-layer pairs sharing a subject were tested in both directions — outcome-8 (bounded growth) ↔ task-3/task-5 (new labels): satisfied together because every new label's value set is closed (D10) and task-3 asserts the closed list; adr D10 span-only rule ↔ task-8 (usage detail): task-8 asserts no metric series carries the span-only keys; the #1936 story's exact `{ step, metering }` dispatch attribute set ↔ task-6: additive extension, both hold (recorded as accepted degrading overlap in the conflict report); story-1 "no key rather than placeholder" ↔ the #1938 story's "feature = unknown verbatim": different attributes, no shared subject. No pair yields two "no" answers.

Achievability pass (§4g): every criterion row's cited Done-when names the mechanism (event-union fields and emit-site spreads for story 1; the `withDimensions` helper and listener dimension tracking for story 2; the real `provider_attempt` handler over `DispatchMeteringTracker` for story 3; `SpanManager.onProviderAttempt` and attribute setting for stories 4-5; the shared `dispatchDimensionsFrom` builder for story 6). Closed value sets: `fallback` is boolean-or-absent, representing fallback, no fallback, and routing-inactive truthfully. Input boundary: lifecycle rows are excluded at the tracker (`invoked !== true`), owned by task-4's test and task-6's assertion. No CANNOT-DELIVER findings.

| Row class | Cited id / criterion | Counterpart / cited task id(s) | Verdict | Notes / quote | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 |  | gap | Already delivered on main before this spec by #1938 (feature is a data-point label on every per-feature instrument); no story in this spec re-implements it. Gap id: outcome-1 (waived) |
| outcome | outcome-2 |  | gap | Excluded by operator decision 2026-09-09 (privacy of "who"; a separate intake will scope it). Gap id: outcome-2 (waived) |
| outcome | outcome-3 | story-3, story-4 | covered | provider label on duration/retries/dispatches (story 2/3), fallback bool on dispatches (story 3), fallback reason on the span (story 4) |
| outcome | outcome-4 | story-2 | covered | model and effort labels on conductor.step.duration and conductor.step.retries as well as dispatches |
| outcome | outcome-5 | story-5 | covered | reasoning_output, turns, duration_ms, cost.source exported on the step span |
| outcome | outcome-6 |  | gap | Already delivered on main by #1972 (metering attribute on conductor.step.dispatches); out of scope per the track marker. Gap id: outcome-6 (waived) |
| outcome | outcome-7 | story-1, story-2, story-4 | covered | tier rides step close events (story 1) and is a label (story 2) and a span attribute (story 4) |
| outcome | outcome-8 | story-2, story-3 | covered | adr-014 D10 is the documented label-vs-trace decision; story 2 Done-when bounds the attribute set to that list |
| outcome | outcome-9 |  | gap | Approach A (direct plumbing) was chosen by the operator over the dimension-table approach; extensibility is deferred to a separate low-priority intake. Gap id: outcome-9 (waived) |
| adr | adr-014-otel-observability-exporter | story-1, story-2, story-3, story-4, story-5, story-6 | covered | Amended additively (D10 placement contract, D11 dimensions on existing events; approved 2026-09-09). Obligation rows judged: D1/D2/D6/D8/D9 no-change (no new subscription site, plugin kind, transport, resource attribute, or daemon-instrument attribute — verified against the plan Files sets); D3/D7 existing (wire.ts seam and daemon-owned MetricsListener shipped in #1516/#1934/#1937); D4 task-3 (attribute merge is an in-memory helper over already-computed fields); D5 task-9 (throwing recorder swallowed on both paths); D10 task-3/task-8 (closed label list asserted, span-only guard asserted); D11 task-2/task-6 (fields on existing members, provider through the existing tracker). Every mapping fulfils its decision |
| story | story-1 | task-1, task-2 | covered | Every criterion of story 1 has a criterion row below citing one of these tasks |
| story | story-2 | task-3, task-5 | covered | Every criterion of story 2 has a criterion row below citing one of these tasks |
| story | story-3 | task-4, task-6 | covered | Every criterion of story 3 has a criterion row below citing one of these tasks |
| story | story-4 | task-7 | covered | Every criterion of story 4 has a criterion row below citing one of these tasks |
| story | story-5 | task-8 | covered | Every criterion of story 5 has a criterion row below citing one of these tasks |
| story | story-6 | task-9, task-10 | covered | Every criterion of story 6 has a criterion row below citing one of these tasks |
| task | task-1 | story-1 | covered | infrastructure: surfaces resolved effort on StepRunResult so story 1 can emit it |
| task | task-2 | story-1 | covered | happy-path |
| task | task-3 | story-2 | covered | infrastructure: DispatchDimensions attribute seam the story-2/3/6 tasks record through |
| task | task-4 | story-3 | covered | infrastructure: preferredProvider and fallbackReason on the dispatch observation |
| task | task-5 | story-2 | covered | happy-path |
| task | task-6 | story-3 | covered | happy-path |
| task | task-7 | story-4 | covered | happy-path |
| task | task-8 | story-5 | covered | happy-path |
| task | task-9 | story-6 | covered | negative-path |
| task | task-10 | story-6 | covered | happy-path |
| criterion | Story 1 happy: Given a step whose invocation resolved `effort: high` and a run whose `complexity_tier` is `M`, when the step completes, then the emitted `step_completed` event carries `effort: 'high'` and `tier: 'M'` | task-2 | covered | "carry `effort` and `tier` when the runner result and state supply them" | diff-local |
| criterion | Story 1 happy: Given a step whose invocation resolved `effort: low`, when the step fails, then the emitted `step_failed` event carries `effort: 'low'` and the run's tier | task-2 | covered | "the emitted `step_completed` and `step_failed` payloads carry `effort` and `tier`" | diff-local |
| criterion | Story 1 negative: Given a step whose runner did not resolve an effort (no policy value), when the step completes, then `step_completed` carries no `effort` key at all rather than a placeholder string | task-2 | covered | "carry neither key when they do not" | diff-local |
| criterion | Story 1 negative: Given a run whose state has no `complexity_tier` yet, when a step completes, then `step_completed` carries no `tier` key rather than a defaulted tier | task-2 | covered | "carry neither key when they do not" | diff-local |
| criterion | Story 1 negative: Given a pre-existing `events.jsonl` line for `step_completed` with neither `effort` nor `tier`, when the event persister and the metrics listener replay it, then both accept the record and neither throws or drops it | task-2 | covered | "asserts both accept it without throwing" | diff-local |
| criterion | Story 2 happy: Given a completed `build` step dispatched on `claude` with model `opus`, effort `high`, tier `M`, when the metrics listener records its duration, then the histogram data point carries attributes `step=build`, `model=opus`, `effort=high`, `provider=claude`, `tier=M` alongside the existing identity attributes | task-5 | covered | "asserts `model`, `effort`, `provider`, `tier` on the duration point" | diff-local |
| criterion | Story 2 happy: Given a `step_retry` for that same step, when the retries counter is incremented, then the data point carries the same `model`, `effort`, `provider`, and `tier` values as the step's duration point | task-5 | covered | "on a subsequent retries point for the same step" | diff-local |
| criterion | Story 2 negative: Given a completed step whose event carries no `model`, when its duration is recorded, then the data point has no `model` attribute and no `unknown` value is emitted | task-3 | covered | "asserts key absence (not `unknown`) for each omitted member" | diff-local |
| criterion | Story 2 negative: Given a completed step whose event carries no `actualProvider` and no matched `provider_attempt`, when its duration is recorded, then the data point has no `provider` attribute | task-5 | covered | "asserts absence of `model` and `provider` when the events carry neither" | diff-local |
| criterion | Story 2 negative: Given a `step_retry` that arrives before any `step_started` for its step, when the retries counter is incremented, then the point carries `step` and identity only and the listener does not throw | task-5 | covered | "records a retries point with `step` and identity only and does not throw" | diff-local |
| criterion | Story 3 happy: Given a step with preferred provider `codex` whose successful `provider_attempt` came from `claude`, when the dispatch is counted, then the data point carries `provider=claude` and `fallback=true` in addition to the existing `step` and `metering` attributes | task-6 | covered | "one `conductor.step.dispatches` point per dispatch with `provider` and `fallback` set to true or false" | diff-local |
| criterion | Story 3 happy: Given a step whose preferred and actual provider are both `claude`, when the dispatch is counted, then the data point carries `provider=claude` and `fallback=false` | task-6 | covered | "`provider` and `fallback` set to true or false" | diff-local |
| criterion | Story 3 negative: Given a `provider_attempt` lifecycle row (`invoked: false`, `provider: 'provider-lifecycle'`), when the metrics listener receives it, then no dispatch is counted and `provider-lifecycle` never appears as a label value | task-6 | covered | "asserts `provider-lifecycle` never appears as a label value" | diff-local |
| criterion | Story 3 negative: Given a step with no preferred provider recorded (routing inactive), when the dispatch is counted, then the data point carries `provider` but no `fallback` attribute | task-6 | covered | "asserts `fallback` absent when no preferred provider was recorded" | diff-local |
| criterion | Story 3 negative: Given a successful `provider_attempt` followed by the matching `step_completed` for the same step and provider, when both are observed, then exactly one dispatch is counted, not two | task-6 | covered | "yields exactly one dispatch point" | diff-local |
| criterion | Story 4 happy: Given a completed step with model `sonnet`, effort `medium`, tier `S`, preferred provider `codex`, actual provider `claude`, when its span closes, then the span carries `conductor.model=sonnet`, `conductor.effort=medium`, `conductor.complexity_tier=S`, `conductor.provider=claude`, `conductor.provider.preferred=codex`, `conductor.fallback=true` | task-7 | covered | "asserts the full attribute set on a fallback dispatch span" | diff-local |
| criterion | Story 4 happy: Given a `provider_attempt` for that step with `fallbackReason: 'codex unavailable'`, when the span closes, then the span carries `conductor.fallback.reason='codex unavailable'` | task-7 | covered | "asserts the full attribute set on a fallback dispatch span" | diff-local |
| criterion | Story 4 negative: Given a `provider_attempt` whose `fallbackReason` is absent, when the span closes, then the span has no `conductor.fallback.reason` attribute | task-7 | covered | "asserts `conductor.fallback.reason` absent when no reason was reported" | diff-local |
| criterion | Story 4 negative: Given a `provider_attempt` for a step with no open span, when the visualizer receives it, then it is a warn-and-no-op and no span is created or thrown | task-7 | covered | "produces one warning and no span" | diff-local |
| criterion | Story 4 negative: Given a step span, when it closes, then `conductor.fallback.reason` and the `TokenUsage` detail appear on no metric data point (span only) | task-8 | covered | "no exported metric series carries `fallback.reason`, `usage.reasoning_output`, `usage.turns`, `usage.duration_ms`, or `cost.source`" | diff-local |
| criterion | Story 5 happy: Given a completed step whose `TokenUsage` has `reasoningOutput: 1200`, `numTurns: 7`, `durationMs: 84000`, `costSource: 'provider'`, when the span closes, then the span carries `conductor.usage.reasoning_output=1200`, `conductor.usage.turns=7`, `conductor.usage.duration_ms=84000`, `conductor.cost.source=provider` | task-8 | covered | "asserts presence for a full usage" | diff-local |
| criterion | Story 5 happy: Given a completed step whose cost came from the rate card, when the span closes, then the span carries `conductor.cost.source=rate-card` | task-8 | covered | "asserts presence for a full usage" | diff-local |
| criterion | Story 5 negative: Given a Codex dispatch whose `TokenUsage` has no `durationMs` and no `costSource`, when the span closes, then neither `conductor.usage.duration_ms` nor `conductor.cost.source` is present and no zero is written | task-8 | covered | "absence of `duration_ms`/`cost.source` for a Codex-shaped usage" | diff-local |
| criterion | Story 5 negative: Given a completed step with no `tokenUsage` at all, when the span closes, then no `conductor.usage.*` or `conductor.cost.source` attribute is present | task-8 | covered | "absence of all five for no usage" | diff-local |
| criterion | Story 5 negative: Given a `TokenUsage` whose `reasoningOutput` is `NaN`, when the span closes, then `conductor.usage.reasoning_output` is omitted | task-8 | covered | "absence of `reasoning_output` for `NaN`" | diff-local |
| criterion | Story 6 happy: Given one recorded event sequence (`step_started`, `provider_attempt`, `step_completed` with model, effort, tier, providers), when it is replayed through the daemon metrics listener and through the interactive visualizer against two in-memory metric exporters, then the duration, retries, and dispatches data points have identical attribute key/value sets on both paths (identity attributes aside) | task-9 | covered | "asserts equal attribute sets per instrument for duration, retries, and dispatches" | diff-local |
| criterion | Story 6 negative: Given the same sequence with `effort` and `tier` removed from `step_completed`, when replayed through both paths, then both omit `effort` and `tier` and neither substitutes a default | task-9 | covered | "asserts both omit `effort` and `tier` when the fixture lacks them" | diff-local |
| criterion | Story 6 negative: Given a metrics handler that throws while merging attributes, when the event is delivered, then the failure is swallowed as best-effort on both paths and the run continues | task-9 | covered | "asserts a throwing recorder is swallowed on both paths and the following event is still delivered" | diff-local |
