# Coherence: Connector Seam — Visualizer Selection Loop (#1516)

**Date:** 2026-08-26
**Tier:** M
**Track:** technical — the `fr` row class is omitted (no PRD). Outcomes are the Desired-outcome
bullets of jstoup111/ai-conductor#1516 in bullet order (staged). The ADR layer engages via the
amended `adr-014-otel-observability-exporter.md` in this change set.

Every `covered` verdict below was confirmed by reading the counterpart artifact file, not inferred.
`outcome-7` (docs) is a gap by design — the documentation boundary forbids doc stories/tasks; the
documentation ships in the same PR via the harness documentation convention. Waived in
`.docs/coherence-waivers/connector-seam-for-event-submissions-is-registered.md`.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote / Notes | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-5 | covered | "receives event submissions during a run, or the harness refuses it at load time" — Story 1 delivers submissions to installed connectors; Story 5 refuses malformed plugins at load with a named defect; the named-but-missing warning removes the silent case. |
| outcome | outcome-2 | story-2 | covered | "receives its events through the same connector seam an installed plugin uses" — Story 2 retrieves OTel from the registry and starts it through the same selection path. |
| outcome | outcome-3 | story-3 | covered | "attribute each submission it receives to the run's engine version, project, branch, and feature" — Story 3's context carries runId, project, feature, branch, engineVersion, pipelineDir. |
| outcome | outcome-4 | story-2 | covered | "Enabling or disabling one emitter leaves every other connector receiving submissions unaffected" — Story 2's independence negative paths assert both directions. |
| outcome | outcome-5 | story-2 | covered | "keeps working unchanged from the operator's point of view, including its enable/disable gate and its stop-time flush" — Story 2's happy paths pin the gate and flush; disabled-noop preserved. |
| outcome | outcome-6 | story-4 | covered | "throws on start or on a submission does not fail or stall the run" — Story 4 isolates start, submission, and stop failures. |
| outcome | outcome-7 | | gap | outcome-7 — docs outcome has no story by design (documentation boundary); delivered in-PR via the harness documentation convention; waived. |
| adr | adr-014-otel-observability-exporter | story-1, story-2, story-3, story-4 | covered | The amended Decision 3 (selection loop, `visualizers` key, context, isolation, shape validation) is implemented by Stories 1-5; Story 2 preserves Decisions 4-6 (hot path, failure isolation, config gate). |
| story | story-1 | task-2, task-5, task-6 | covered | Config key, selection loop delivery, and the warn-skip / empty-selection / teardown negatives. |
| story | story-2 | task-4, task-5, task-6, task-8, task-9 | covered | Built-in registration, selection-path start, otel-in-visualizers warning, flush-preserving migration, independence proofs. |
| story | story-3 | task-1, task-8, task-9 | covered | Context type + round-trip, OTel resource identity, absent-field and ignored-context proofs. |
| story | story-4 | task-7 | covered | Start isolation with `renderer_error`, emit-time regression assertion, never-started stop exclusion, stop-rejection isolation. |
| story | story-5 | task-3 | covered | Shape validation rejection naming plugin and member; valid-sibling registration. |
| task | task-1 | story-3 | covered | Seam contract types and two-parameter start. |
| task | task-2 | story-1 | covered | `visualizers` config key load/validate. |
| task | task-3 | story-5 | covered | Load-time shape validation and partial-failure preservation. |
| task | task-4 | story-2 | covered | `visualizer:otel` built-in factory with the null-when-disabled gate. |
| task | task-5 | story-1 | covered | Selection loop on the production startup path; also exercises story-2's selection-path criterion. |
| task | task-6 | story-1 | covered | Warn-and-skip and otel-in-visualizers negatives; also carries story-2's gate-precedence negative. |
| task | task-7 | story-4 | covered | Per-plugin start isolation, started-list exclusion, stop isolation, emit-time regression. |
| task | task-8 | story-2 | covered | OTel identity via context with flush behavior pinned; also carries story-3's resource-match criterion. |
| task | task-9 | story-3 | covered | Absent-field delivery and emitter independence; also carries story-2's independence negatives. |
| criterion | Story 1 happy: Given a valid `kind: visualizer` plugin installed in the project plugin directory and its name listed in the `visualizers` config key, when a run starts, then the plugin's `start` is invoked and its subscribed handlers receive the run's events | task-5 | covered | "assert the fake's `start` ran with the emitter and a context carrying the run's identity" | diff-local |
| criterion | Story 1 happy: Given the same plugin installed in the global plugin directory only, when a run starts, then it is selected identically (project install shadows global, matching existing discovery precedence) | task-5 | covered | "configured names via `registry.tryGet`" | diff-local |
| criterion | Story 1 happy: Given two connectors both enabled, when a run emits events, then both receive every event type each subscribed to | task-5 | covered | "both receive an emitted event each subscribed to" | diff-local |
| criterion | Story 1 negative: Given a `visualizers` entry naming a plugin that is not registered, when a run starts, then exactly one warning is logged naming the missing plugin and the registered visualizer names, that entry is skipped, and the run proceeds with the remaining connectors | task-6 | covered | "exactly one warning naming `ghost` and listing registered visualizer names" | diff-local |
| criterion | Story 1 negative: Given an empty or absent `visualizers` key and OTel disabled, when a run starts, then no connector is started and run behavior is byte-identical to today | task-6 | covered | "empty/absent `visualizers` with OTel disabled → zero visualizers started" | diff-local |
| criterion | Story 1 negative: Given a connector enabled in config, when the run ends, then its `stop` is awaited before the process exits, and a `stop` rejection is warned and swallowed without failing teardown | task-7 | covered | "a fake whose `stop` rejects still lets the sibling's `stop` run" | diff-local |
| criterion | Story 2 happy: Given an enabled `otel:` config block, when a run starts, then the OTel visualizer is retrieved from the registry as a built-in (registered alongside the other built-ins) and started through the same selection path as installed connectors | task-4 | covered | "add the `visualizer:otel` factory to `registerBuiltins`" | diff-local |
| criterion | Story 2 happy: Given an enabled `otel:` block and no `visualizers` key, when a run starts, then OTel runs — its enablement is governed solely by the `otel:` gate, not by membership in `visualizers` | task-5 | covered | "Existing otel-exporter and otel-disabled-noop integration tests pass unchanged" | diff-local |
| criterion | Story 2 happy: Given a run with OTel enabled, when the run ends normally, then spans and metrics are flushed at stop exactly as before | task-8 | covered | "Every existing OTel test file passes without behavioral edits" | diff-local |
| criterion | Story 2 negative: Given an absent or invalid `otel:` block, when a run starts, then the OTel visualizer is not started and no OTel dependency is exercised (existing disabled-noop behavior preserved) | task-4 | covered | "the disabled-config invocation returns `null` without touching OTel SDK setup" | diff-local |
| criterion | Story 2 negative: Given OTel disabled and an installed connector enabled, when a run starts, then the connector still receives events (disabling one emitter leaves others unaffected) | task-9 | covered | "OTel disabled + fake enabled → fake receives events" | diff-local |
| criterion | Story 2 negative: Given OTel enabled and an installed connector disabled or absent, when a run starts, then OTel behavior is unaffected by the other connector's state | task-9 | covered | "OTel enabled (file transport) + no fake → OTel unaffected" | diff-local |
| criterion | Story 2 negative: Given a `visualizers` entry naming `otel` while the `otel:` block is absent or disabled, when a run starts, then the entry is ignored with a one-time warning pointing at the `otel:` block, OTel is not started, and the run proceeds | task-6 | covered | "`visualizers: ['otel']` with `otel:` absent → one warning pointing at the `otel:` block, OTel not started, run proceeds" | diff-local |
| criterion | Story 3 happy: Given any started connector, when its `start` is invoked, then it receives a context carrying runId, project, feature, branch, engineVersion, and pipelineDir alongside the emitter | task-1 | covered | "a fake observes all six context fields round-tripped" | diff-local |
| criterion | Story 3 happy: Given the OTel visualizer started through the seam, when it builds its resource attributes, then the identity values match those previously supplied via its private constructor context | task-8 | covered | "the OTel resource attributes carry those values" | diff-local |
| criterion | Story 3 negative: Given a run where a context field cannot be derived (e.g. no git branch available), when connectors start, then the field is delivered as explicitly absent (not a fabricated value) and start still succeeds | task-9 | covered | "a fake connector observes `branch === undefined` and start succeeds" | diff-local |
| criterion | Story 3 negative: Given a connector that ignores the context parameter, when it is started, then it runs normally (context is additive; no connector is required to consume it) | task-9 | covered | "a fake connector whose `start` ignores the context entirely runs normally" | diff-local |
| criterion | Story 4 happy: Given three enabled connectors where the second throws synchronously in `start`, when a run starts, then an error event naming the failing connector is emitted, the first and third connectors run normally, and the run completes | task-7 | covered | "a `renderer_error` event is emitted naming the failing connector, fakes 1 and 3 are started" | diff-local |
| criterion | Story 4 happy: Given a connector whose event handler throws on a submission, when events are emitted, then the run continues and other subscribers still receive the event (existing emitter isolation preserved) | task-7 | covered | "a fake whose event handler throws on a submission does not prevent another subscriber from receiving the event" | diff-local |
| criterion | Story 4 negative: Given a connector that throws in `start`, when the run later ends, then teardown does not invoke `stop` on the never-started connector and does not fail | task-7 | covered | "`stopVisualizers` over that list never calls fake 2's `stop`" | diff-local |
| criterion | Story 4 negative: Given a connector whose `stop` rejects at teardown, when the run ends, then the rejection is warned and swallowed and every other connector's `stop` still runs | task-7 | covered | "a fake whose `stop` rejects still lets the sibling's `stop` run" | diff-local |
| criterion | Story 5 happy: Given an installed `kind: visualizer` plugin whose entrypoint exports a conforming shape, when plugins are discovered, then it registers and is retrievable by name | task-3 | covered | "a valid visualizer in the same discovery pass registers and is retrievable" | diff-local |
| criterion | Story 5 negative: Given an installed `kind: visualizer` plugin whose entrypoint lacks the required start/stop shape, when plugins are discovered, then discovery reports a load error naming the plugin and the missing member, and the plugin is not registered | task-3 | covered | "rejected with a `PluginLoadError` naming the plugin and the missing member and is not registered" | diff-local |
| criterion | Story 5 negative: Given one malformed visualizer plugin and one valid one, when plugins are discovered, then the valid plugin still registers and runs (partial-failure policy preserved) | task-3 | covered | "a malformed visualizer does not prevent the valid one from registering" | diff-local |
