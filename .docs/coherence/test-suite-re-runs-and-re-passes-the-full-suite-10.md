# Coherence: Budgeted, mode-aware test_suite verification (#2021)

**Date:** 2026-08-29
**Tier:** L (coherence pass run on the opus tier)
**Track:** technical — the `fr` row class is omitted (no PRD; the requirement layer is the
issue's Desired-outcome bullets plus the governing ADR).
**Outcome source:** the nine Desired-outcome bullets of jstoup111/ai-conductor#2021, staged in
the pipeline intake-outcomes file carried into the spec by the intake marker at land.
**ADR row pool:** the seven non-deleted decision files in this spec's change set — the new
drift-budget/verification-mode ADR plus the six amended governing ADRs.
**Consistency pass:** every covered row re-read in both directions; no static contradiction and
no cross-layer oscillation found. The one candidate pair — outcome-1's "full suite once" against
the governing inspect-every-time constraint — is not opposed: inspections remain per-evaluation
while executions reduce, which is exactly what Story 2 delivers.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-2 | covered | "the feature reaches SHIP having run the full suite once." Story 2 preserves the recorded PASS across within-budget drift without launching the suite. |
| outcome | outcome-2 | story-2, story-4 | covered | Foreign within-budget drift never reaches execution (Story 2), so it cannot fail and cannot consume a kickback (Story 4's ledger invariant). |
| outcome | outcome-3 | story-6, story-7 | covered | The chosen mode is bound into gate satisfaction (Story 6) and recorded in evidence and events (Story 7). |
| outcome | outcome-4 | story-1 | covered | Scoped mode without a usable scoped command is a load-time validation error naming the missing key. |
| outcome | outcome-5 | story-8 | covered | Bootstrap asks both questions and records the answers through the config CLI; auto mode records the strict default explicitly. |
| outcome | outcome-6 | story-5 | covered | Absent settings resolve to aggregate mode with zero tolerance; regression tests lock today's behavior. |
| outcome | outcome-7 | story-7 | covered | Evidence names the attested provenance commit, the mode, the selectors, and every tolerated drift increment. |
| outcome | outcome-8 | story-2, story-3 | covered | Bounded re-verification: within budget preserves (Story 2); exceeded or unbudgetable drift re-runs naming the exhausted part (Story 3). |
| outcome | outcome-9 | story-1, story-3 | covered | The budget vocabulary is the eight fingerprint categories, validated as a closed set (Story 1), never a raw global file count (Story 3's per-category reasons). |
| adr | adr-2026-08-28-test-suite-drift-budget-and-verification-mode | story-1, story-2, story-3, story-4, story-5, story-6, story-7, story-8 | covered | D1/D2/D3 → Stories 1 and 5; D4 → Stories 2 and 3; D5 → Story 6; D6 → Story 7; D7 → Stories 4 and 7; D8 → Story 8. |
| adr | adr-2026-07-25-content-addressed-full-suite-proof | story-2, story-5 | covered | D7 freshness recalculation is retained by Story 2 (inspection on every evaluation, fail-closed on indeterminacy) and Story 5 (absent config unchanged), per the 2026-08-28 amendment note. |
| adr | adr-2026-08-19-tree-attesting-gates-recheck-before-dispatch | story-2 | covered | The predicate still re-reads the fingerprint every evaluation; preservation is a judged consequence of inspection, per the amendment note beside D1. |
| adr | adr-2026-08-01-engine-owned-scoped-test-invocation | story-6 | covered | Story 6 reuses the engine-owned interface unchanged (quoting, empty-selection refusal) and exercises the amended D7/D8 exception only in scoped mode. |
| adr | adr-2026-07-20-post-rebase-delta-aware-invalidation | story-2, story-3 | covered | Observation stays total; the budget is a declared post-observation tolerance with fail-closed defaults and engine-fixed unbudgetable categories, per the amendment note. |
| adr | adr-2026-08-18-rebase-invalidation-refunds-build-review-convergence | story-7 | covered | Story 7 pins the refund to genuine invalidation: budget preservation surfaces as preserved, no refund granted or suppressed. |
| adr | adr-2026-07-27-project-config-scaffolder | story-8 | covered | Story 8 keeps the CLI as sole writer; parameterized instantiation per the amendment note, refuse-to-clobber intact. |
| story | story-1 | task-1, task-2, task-21 | covered | Valid-shape parsing, the six rejection shapes, and the consumer-registry entries. |
| story | story-2 | task-3, task-4, task-5, task-6, task-7, task-8 | covered | Shared classifier, drift measurement, preservation, cumulative ratchet guard, and the fail-closed branches. |
| story | story-3 | task-9, task-10 | covered | Exhausted/unbudgetable naming and the per-PASS epoch reset. |
| story | story-4 | task-12 | covered | Ledger byte-identity on preservation; existing kickback and non-kickback paths asserted unchanged. |
| story | story-5 | task-11 | covered | Absent-config regression lock (verify-only). |
| story | story-6 | task-13, task-14, task-15, task-16 | covered | Selector derivation, mode-bound satisfaction, recorded aggregate route, scoped identity. |
| story | story-7 | task-5, task-17, task-18 | covered | Evidence schema, event fields, rebase basis and refund integrity. |
| story | story-8 | task-19, task-20 | covered | config init flags/presets and the bootstrap question flow. |
| task | task-1 | story-1 | covered | Types, allowlist, valid-shape resolution. |
| task | task-2 | story-1 | covered | The six load-time rejection rules. |
| task | task-3 | story-2 | covered | Refactor serving Story 2's measurement: single exported category classifier (review condition C2). |
| task | task-4 | story-2 | covered | Infrastructure serving Story 2: per-category drift counts with an explicit indeterminate marker. |
| task | task-5 | story-7 | covered | Infrastructure serving Story 7 (and Story 2's version-stale negative): evidence schema bump. |
| task | task-6 | story-2 | covered | Within-budget preservation at the judgement point. |
| task | task-7 | story-2 | covered | Cumulative measurement and the ratchet negative. |
| task | task-8 | story-2 | covered | Indeterminacy and version staleness resolve to re-run. |
| task | task-9 | story-3 | covered | Exceeded and unbudgetable branches with named categories. |
| task | task-10 | story-3 | covered | New-PASS epoch reset. |
| task | task-11 | story-5 | covered | Verify-only regression lock on absent config. |
| task | task-12 | story-4 | covered | Kickback ledger invariants across preservation, failure, timeout, and launch failure. |
| task | task-13 | story-6 | covered | Infrastructure serving Story 6: framework-agnostic selector derivation. |
| task | task-14 | story-6 | covered | Scoped execution and mode-bound gate satisfaction. |
| task | task-15 | story-6 | covered | Recorded aggregate route on empty selection. |
| task | task-16 | story-6 | covered | Scoped template and selector set join the fingerprint identity. |
| task | task-17 | story-7 | covered | Additive event fields and preservation emission. |
| task | task-18 | story-7 | covered | Rebase preserved-with-budget basis; refund stays on invalidation. |
| task | task-19 | story-8 | covered | config init flags, presets, clobber refusal, flagless byte-identity. |
| task | task-20 | story-8 | covered | Bootstrap question flow and auto-mode strict default. |
| task | task-21 | story-1 | covered | Infrastructure serving Story 1: consumer-registry entries for the two new keys (review condition C1). |
| criterion | Story 1 happy: Given a config with `verification.mode: aggregate` and no `drift_budget`, when config loads, then loading succeeds and the resolved verification settings are zero tolerance in every category with the aggregate command | task-1 | covered | "`verification.mode: aggregate` with no budget resolving to zero tolerance" | diff-local |
| criterion | Story 1 happy: Given a config with `verification.drift_budget` naming only budgetable categories with values `none`, a positive integer, or `unlimited`, when config loads, then loading succeeds and each category resolves to exactly the declared bound | task-1 | covered | "resolving to exactly those bounds" | diff-local |
| criterion | Story 1 happy: Given a config with `verification.mode: scoped` and a valid `scoped_command` containing `{selectors}`, when config loads, then loading succeeds with scoped mode active | task-1 | covered | "`mode: scoped` with a valid `scoped_command` resolving to scoped mode" | diff-local |
| criterion | Story 1 negative: Given a config with `verification.mode: scoped` and no `scoped_command`, when config loads, then loading fails with a validation error naming `test_suite.scoped_command` as the missing key | task-2 | covered | "scoped mode without `scoped_command` (message names `test_suite.scoped_command`)" | diff-local |
| criterion | Story 1 negative: Given a config whose `drift_budget` names an unknown category key, when config loads, then loading fails with a validation error naming the unknown key and listing the valid category names | task-2 | covered | "unknown budget category (message names the key and lists valid names)" | diff-local |
| criterion | Story 1 negative: Given a config whose `drift_budget` names an unbudgetable category (`dependencies`, `migrations`, `environment`, or `project_config`), when config loads, then loading fails with a validation error naming that category as unbudgetable | task-2 | covered | "unbudgetable budget category (message names it unbudgetable)" | diff-local |
| criterion | Story 1 negative: Given a config whose `drift_budget` value is zero, negative, or a non-integer string other than `none`/`unlimited`, when config loads, then loading fails with a validation error naming the offending key and value | task-2 | covered | "zero/negative/non-integer bound value (message names key and value)" | diff-local |
| criterion | Story 1 negative: Given a config with `verification.mode` set to any value other than `aggregate` or `scoped`, when config loads, then loading fails with a validation error naming the allowed values | task-2 | covered | "mode outside the two allowed values (message names them)" | diff-local |
| criterion | Story 1 negative: Given an unknown key inside the `verification` block, when config loads, then loading fails with a validation error naming the unknown key | task-2 | covered | "unknown key inside `verification`" | diff-local |
| criterion | Story 2 happy: Given a recorded PASS and a budget of `source: 20`, when 3 source files change and the gate is re-evaluated, then the fingerprint is recomputed, the PASS is preserved without launching the suite command, and the preservation records the drifted category and path count | task-6 | covered | "returns a preserved outcome, never launches the suite command, and appends a ledger entry naming the category and count" | diff-local |
| criterion | Story 2 happy: Given a recorded PASS and budgets on `source` and `tests`, when both categories drift within their bounds, then the PASS is preserved and both categories appear in the drift record | task-6 | covered | "a two-category within-bounds drift preserves and records both" | diff-local |
| criterion | Story 2 happy: Given a preservation has already tolerated 15 source-path changes since the attested PASS, when 4 more source paths change, then drift is measured cumulatively from the attested PASS (19 total) and the PASS is preserved | task-7 | covered | "measures 19 cumulative from the attested PASS and preserves" | diff-local |
| criterion | Story 2 negative: Given a recorded PASS whose evidence carries an unresolvable provenance commit, when the gate is re-evaluated after drift, then the suite re-runs and the stale reason names the indeterminate drift measurement | task-8 | covered | "unresolvable provenance SHA" | diff-local |
| criterion | Story 2 negative: Given a recorded PASS and a git failure during drift measurement, when the gate is re-evaluated, then the suite re-runs rather than preserving | task-8 | covered | "git failure during measurement" | diff-local |
| criterion | Story 2 negative: Given a prior-version evidence file (before the schema bump), when the gate is re-evaluated, then the evidence is treated as stale and the suite re-runs | task-8 | covered | "prior-version evidence file resolves to a re-run" | diff-local |
| criterion | Story 2 negative: Given a preservation has tolerated 19 cumulative source paths under a budget of 20, when 2 more source paths change (21 total), then the suite re-runs — repeated small preservations cannot ratchet past the bound | task-7 | covered | "at 19 tolerated, 2 more paths measure 21 and force a re-run" | diff-local |
| criterion | Story 3 happy: Given a recorded PASS and a budget of `source: 5`, when 6 source paths change, then the suite re-runs and the recorded stale reason names `source` as the exhausted category with its measured count and bound | task-9 | covered | "re-runs with a reason naming source, count 6, bound 5" | diff-local |
| criterion | Story 3 happy: Given a recorded PASS and any drift budget, when a path classified `dependencies` changes, then the suite re-runs and the recorded reason names `dependencies` as unbudgetable | task-9 | covered | "re-runs naming that category as unbudgetable regardless of budget" | diff-local |
| criterion | Story 3 happy: Given a recorded PASS and any drift budget, when a `migrations`, `environment`, or `project_config` input changes, then the suite re-runs and the recorded reason names that category | task-9 | covered | "one path in each unbudgetable category" | diff-local |
| criterion | Story 3 happy: Given a re-run after exhaustion records a new PASS, when subsequent drift occurs, then drift measurement restarts from the new PASS's provenance state | task-10 | covered | "uses the new PASS's provenance SHA and an empty ledger" | diff-local |
| criterion | Story 3 negative: Given drift in both a within-budget category and an exceeded category, when the gate is re-evaluated, then the suite re-runs (one exceeded category defeats preservation) and the reason names the exceeded category | task-9 | covered | "mixed within-budget + exceeded drift re-runs naming the exceeded category" | diff-local |
| criterion | Story 3 negative: Given drift in a category with no declared budget entry, when the gate is re-evaluated, then the default `none` applies and the suite re-runs | task-9 | covered | "drift in an unlisted budgetable category re-runs under the default bound" | diff-local |
| criterion | Story 4 happy: Given a recorded PASS and a budget tolerating source drift, when a foreign main-side source change lands within budget and the gate is re-evaluated, then no suite process runs, no failure can occur, and the kickback ledger for `test_suite` is unchanged | task-12 | covered | "kickback ledger entry byte-identical and no kickback event emitted" | diff-local |
| criterion | Story 4 happy: Given a within-budget preservation, when the pipeline proceeds to later steps, then no `kickback` event for `test_suite` is emitted for that evaluation | task-12 | covered | "emits no kickback event" | diff-local |
| criterion | Story 4 negative: Given foreign drift that exceeds the budget, when the re-run executes and fails with a non-zero exit, then the existing kickback path applies unchanged (one kickback consumed, kickback event emitted) | task-12 | covered | "still consumes exactly one kickback as today" | diff-local |
| criterion | Story 4 negative: Given foreign drift in an unbudgetable category, when the re-run times out or fails to launch, then the existing non-kickback halt path applies unchanged (no kickback consumed) | task-12 | covered | "still takes the existing non-kickback halt path with no kickback consumed" | diff-local |
| criterion | Story 5 happy: Given a config with a `test_suite` block and no `verification` key, when config loads, then loading succeeds and the resolved settings are aggregate mode with zero drift tolerance | task-11 | covered | "resolution is aggregate mode + all-`none` bounds" | diff-local |
| criterion | Story 5 happy: Given no `verification` config and a recorded PASS, when any code or test path changes and the gate is re-evaluated, then the suite re-runs exactly as today with the existing per-category stale reason | task-11 | covered | "a one-path source drift re-runs with the existing per-category stale reason" | diff-local |
| criterion | Story 5 happy: Given no `verification` config and a fingerprint-identical tree, when the gate is re-evaluated, then the existing reuse path applies (no run, evidence reused) | task-11 | covered | "a fingerprint-identical tree reuses without running" | diff-local |
| criterion | Story 5 negative: Given no `verification` config, when a foreign main-side change drifts one source path, then the suite re-runs (zero tolerance — no implicit budget is inferred) | task-11 | covered | "a one-path source drift re-runs" | diff-local |
| criterion | Story 5 negative: Given no `verification` config, when the evidence is inspected after any drift, then no drift-ledger preservation entry is ever appended | task-11 | covered | "no ledger entry is ever appended" | diff-local |
| criterion | Story 6 happy: Given `verification.mode: scoped` and a feature surface containing changed test paths, when the gate executes, then the engine-owned scoped interface runs `scoped_command` with those selectors and a passing exit records a PASS that satisfies the gate | task-14 | covered | "a passing exit writes a PASS (mode scoped, selectors recorded) that satisfies the gate" | diff-local |
| criterion | Story 6 happy: Given a scoped PASS, when the recorded evidence is read, then it names the mode as scoped and lists the exact selector set that ran | task-5 | covered | "a PASS evidence round-trip preserves `mode`, `selectors`, and `driftLedger` entries" | diff-local |
| criterion | Story 6 happy: Given a scoped PASS, when the feature's changed-test selection changes, then the recorded PASS reads stale (the selector set is part of the evidence identity) | task-16 | covered | "A selector-set change stales a scoped PASS" | diff-local |
| criterion | Story 6 negative: Given `verification.mode: scoped` and a feature surface with no changed test paths, when the gate executes, then the evaluation routes to the aggregate verifier and the evidence and events record that the aggregate route was taken (never a silent scoped no-op) | task-15 | covered | "record that the aggregate route was taken" | diff-local |
| criterion | Story 6 negative: Given `verification.mode: scoped` and a selector containing a space or shell metacharacter, when the gate executes, then the selector is delivered intact to the runner (no splitting or injection) | task-14 | covered | "a selector containing a space reaches the faked runner intact" | diff-local |
| criterion | Story 6 negative: Given `verification.mode: aggregate` and a valid `scoped_command`, when the gate executes, then the aggregate command runs and no scoped invocation can satisfy the gate | task-14 | covered | "A scoped PASS satisfies the gate only when resolved mode is scoped" | diff-local |
| criterion | Story 7 happy: Given any recorded PASS, when the evidence file is read, then it contains the mode, the attested provenance commit, and (for scoped) the selector set | task-5 | covered | "preserves `mode`, `selectors`, and `driftLedger` entries" | diff-local |
| criterion | Story 7 happy: Given one or more within-budget preservations, when the evidence file is read, then an append-only drift ledger lists each preservation with its head commit and per-category path counts | task-6 | covered | "The appended ledger entry carries the evaluation head SHA and per-category counts" | diff-local |
| criterion | Story 7 happy: Given a within-budget preservation, when `.pipeline/events.jsonl` is read, then a verification event records the preserved-within-budget outcome, the mode, and the drifted categories | task-17 | covered | "emits a verification event with mode, a preserved-within-budget outcome, and the drifted categories" | diff-local |
| criterion | Story 7 happy: Given a budget-exceeded re-run, when `.pipeline/events.jsonl` is read, then a verification event names the exhausted or unbudgetable category that forced the run | task-17 | covered | "names the exhausted or unbudgetable category" | diff-local |
| criterion | Story 7 happy: Given a post-rebase evaluation preserved within budget, when events are read, then the existing rebase-preserved event carries a budget basis and no gate-invalidation refund is granted for that gate | task-18 | covered | "grants no build-review convergence refund" | diff-local |
| criterion | Story 7 negative: Given a run with no `verification` config, when events are read, then existing event shapes are unchanged and parse with existing consumers (additive fields only) | task-17 | covered | "event shapes are unchanged apart from additive fields" | diff-local |
| criterion | Story 7 negative: Given a new PASS after a re-run, when the evidence is read, then the drift ledger from the previous PASS epoch is not carried forward into the new PASS's ledger | task-10 | covered | "an empty drift ledger with no entries from the prior epoch" | diff-local |
| criterion | Story 8 happy: Given `conduct-ts config init` invoked with a mode flag and a budget preset flag, when it writes the config, then the generated file contains a `test_suite.verification` block matching the flags | task-19 | covered | "writes a config whose verification block matches the preset" | diff-local |
| criterion | Story 8 negative: Given `config init` invoked with flags in a repo whose config already exists, when it runs, then it refuses to clobber exactly as today and the existing config is unchanged | task-19 | covered | "an existing config still refuses clobber with flags present" | diff-local |
| criterion | Story 8 negative: Given `config init` invoked with an unknown preset or invalid mode value, when it runs, then it exits non-zero naming the allowed values and writes nothing | task-19 | covered | "exits non-zero naming allowed values and writes nothing" | diff-local |
| criterion | Story 8 negative: Given `config init` invoked with no verification flags, when it writes the config, then the generated file matches today's template output (no verification block invented) | task-19 | covered | "the output is byte-identical to the bare template copy" | diff-local |
