# Coherence Mapping: build_review post-join remediation adjudication

**Date:** 2026-08-30  
**Tier:** L  
**Track:** technical — the `fr` row class is omitted because this track has no PRD.  
**Outcome source:** the six Desired-outcome bullets for `jstoup111/ai-conductor#2033` staged in
`.pipeline/intake-outcomes.md`.  
**ADR row pool:** the two non-deleted ADR files introduced by this spec change set, including the
superseded predecessor because it remains the incorporated case/effect decision record.  
**Consistency pass:** every covered row was re-read in both directions. No static contradiction or
cross-layer oscillation remains after the operator-approved mixed-lap successor ADR and the accepted
artifact corrections isolated in companion PR #2066. In particular, preserving infrastructure as
independently blocking is compatible with routing one new content work order: the former controls
PASS eligibility while the latter controls the one bounded remediation attempt. The companion must
land before BUILD, so this mapping does not claim the current feature diff contains foreign stems.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-3, story-6 | covered | One settled fan-in, source-complete merge semantics, and one prioritized work order jointly prevent competing or contradictory BUILD instructions. |
| outcome | outcome-2 | story-2, story-7 | covered | Complete prior-case input plus explicit binding and attempted/regressed-case halts prevent a dispositioned substance from restarting budget or litigation. |
| outcome | outcome-3 | story-3, story-4, story-9, story-11 | covered | Exact source coverage, append-only case history, terminal trace rendering, and persisted lifecycle occurrences retain every raw-to-effective outcome. |
| outcome | outcome-4 | story-8 | covered | A valid out-of-scope deferral reserves a stable effect, reuses or files one intake, and cannot finalize without its issue reference. |
| outcome | outcome-5 | story-6, story-9 | covered | Only a new applied action effect creates the one charged BUILD route; finalized non-actions consume no build_review kickback. |
| outcome | outcome-6 | story-3, story-4, story-8, story-9, story-10, story-11 | covered | Contract, state, external-effect, effective-reducer, recovery, and compatibility failures all stop before PASS. |
| adr | adr-2026-08-29-build-review-remediate-case-adjudication | story-2, story-3, story-4, story-5, story-6, story-7, story-8, story-10, story-11 | covered | The successor explicitly incorporates the predecessor's non-conflicting D2-D10 case schema, authority separation, durable effects, convergence, intake, compatibility, and event-spine decisions; these stories implement those clauses. |
| adr | adr-2026-08-29-mixed-build-review-laps-preserve-content-adjudication | story-1, story-2, story-5, story-9 | covered | Stories 1 and 9 implement D1-D3 mixed-lap precedence; Stories 2 and 5 preserve the bounded semantic input and operator-only infrastructure authority. |
| story | story-1 | task-3, task-6, task-8, task-15, task-18, task-20 | covered | Context selection, the one existing judge, transition reduction, orchestration, production navigation, and raw-shape compatibility cover every settlement path. |
| story | story-2 | task-3, task-6, task-15 | covered | Complete bounded context is supplied to one fresh remediate dispatch, with overflow and invalid history stopping before dispatch. |
| story | story-3 | task-1, task-4, task-5, task-6 | covered | Strict parsing, atomic graph validation, engine stamping, and the closed skill contract account for every current source exactly once. |
| story | story-4 | task-2, task-5, task-16 | covered | Leased atomic persistence and append-only reconciliation stay separate from exact operator disposition reads and writes. |
| story | story-5 | task-16 | covered | One race-table task re-reads exact operator authority at every exit and covers foreign, stale, broader, and unreadable records. |
| story | story-6 | task-9, task-10, task-12, task-18 | covered | Durable ordered work, idempotent charge, action-effect settlement, and one Conductor navigation provide the bounded BUILD route without plan growth. |
| story | story-7 | task-5, task-10, task-17 | covered | Explicit case binding, durable attempt evidence, stable charges, and the repeat classifier distinguish resume, new work, repetition, and regression. |
| story | story-8 | task-11, task-13, task-19 | covered | Exact open/closed marker lookup, one reserved intake effect, and post-create recovery prevent loss and duplication. |
| story | story-9 | task-8, task-15, task-18, task-20 | covered | The closed reducer, coordinator, production transition application, and terminal trace renderer derive one effective outcome without mutating raw evidence. |
| story | story-10 | task-2, task-9, task-10, task-12, task-13, task-19 | covered | Atomic state, durable work order, effect-id charge, both effect executors, and the full fault matrix cover every restart boundary. |
| story | story-11 | task-1, task-6, task-7, task-14, task-20 | covered | Additive schema/skill mode, validated default-on switch, exhaustive spine events, and flag-off/legacy integration provide observability and reversibility. |
| task | task-1 | story-3, story-11 | covered | Parses the strict source/case contract and locks legacy no-mode compatibility. |
| task | task-2 | story-4, story-10 | covered | Supplies the leased, atomic, versioned persistence prerequisite for durable and recoverable cases. |
| task | task-3 | story-1, story-2 | covered | Builds the complete current/history projection while excluding operator-resolved and infrastructure sources. |
| task | task-4 | story-3 | covered | Rejects any incomplete or contradictory proposed source-to-case graph before mutation. |
| task | task-5 | story-4, story-7 | covered | Reconciles engine identities and append-only history, including prior-case resolution and binding. |
| task | task-6 | story-1, story-2, story-3, story-11 | covered | Extends the existing remediate capability with the one closed case mode and no new judge. |
| task | task-7 | story-11 | covered | Resolves default-on and strict invalid-config behavior across all production config surfaces. |
| task | task-8 | story-1, story-9 | covered | Encodes pure/mixed transition precedence and PASS/BUILD/mechanical/HALT eligibility. |
| task | task-9 | story-6, story-10 | covered | Persists ordered BUILD context by stable effect id without mutating plan state. |
| task | task-10 | story-6, story-7, story-10 | covered | Makes the existing budget mutation idempotent by stable effect id while retaining cumulative bounds. |
| task | task-11 | story-8 | covered | Extends the tracker port with exact hidden-marker lookup across open and closed issues. |
| task | task-12 | story-6, story-10 | covered | Reserves, publishes, charges, and finalizes one action effect without navigation or plan edits. |
| task | task-13 | story-8, story-10 | covered | Reserves and settles one deferred intake effect with exact lookup-before-create recovery. |
| task | task-14 | story-11 | covered | Adds every lifecycle occurrence to the existing event union, sinks, and persister only. |
| task | task-15 | story-1, story-2, story-9 | covered | Orchestrates the single fresh post-join dispatch and returns a closed transition without navigation. |
| task | task-16 | story-5, story-4 | covered | Re-reads operator authority at every exit and proves autonomous/operator stores never cross-write. |
| task | task-17 | story-7 | covered | Classifies resume/reuse/new/halt from explicit binding and attempt evidence, never prose similarity. |
| task | task-18 | story-1, story-6, story-9 | covered | Wires closed adjudication transitions into the existing Conductor routing and halt seams. |
| task | task-19 | story-10, story-8 | covered | Exercises every reserved-effect crash boundary and the two-executor lease race with faithful fakes. |
| task | task-20 | story-11, story-1, story-9 | covered | Preserves the exact flag-off/legacy route and adds final source-to-case-to-effect rendering. |

## Criterion coverage

Each quoted fragment below is an exact substring of the cited task body after whitespace
normalization. All cited work is inside the planned implementation diff.
| criterion | Story 1 happy: Given two or more enabled rubrics produce valid judged findings for one frozen lap, when the raw join settles, then exactly one `remediate` session receives every operator-unresolved finding from that lap. | task-15 | covered | "exactly one `StepRunner.run('remediate', ...)` call containing all current sources/history" | diff-local |
| criterion | Story 1 happy: Given every raw finding is already covered by a current operator disposition, when the raw join settles, then the effective gate passes without dispatching `remediate`, creating a case, applying an effect, or consuming a kickback. | task-15 | covered | "Operator-resolved-only input returns PASS with zero provider/store/effect calls." | diff-local |
| criterion | Story 1 happy: Given an exhausted infrastructure result has exact current operator reduced coverage and content siblings remain unresolved, when the aggregate is evaluated, then the infrastructure branch stays outside autonomous judgement and exactly one `remediate` session receives the content findings. | task-3, task-15 | covered | "excluding infrastructure and exact operator-resolved sources." | diff-local |
| criterion | Story 1 happy: Given one rubric has an uncovered infrastructure failure while siblings have valid unresolved content findings, when every branch settles, then the current-lap aggregate preserves both kinds, exactly one `remediate` session receives only the content findings, and a new actionable work order may take the existing one semantic BUILD route while infrastructure remains independently blocking. | task-8, task-15, task-18 | covered | "Mixed action consumes the content route while retaining infrastructure in the next blocker set" | diff-local |
| criterion | Story 1 negative: Given one rubric produces an infrastructure failure with mechanical allowance remaining and no sibling has an operator-unresolved content finding, when the group settles, then the lap follows the existing non-publishing mechanical path with no remediation, external effect, or semantic kickback. | task-8, task-18 | covered | "pure and mixed mechanical cases take the transition table's routes and never fabricate PASS." | diff-local |
| criterion | Story 1 negative: Given an exhausted infrastructure result has no exact current operator reduced-coverage decision and content settlement yields no actionable BUILD route, when the effective transition is chosen, then the infrastructure result remains blocking and follows the existing exhaustion path; finalized content outcomes cannot produce autonomous PASS. | task-8 | covered | "mixed non-action follows the mechanical state." | diff-local |
| criterion | Story 1 negative: Given one rubric branch is missing, stale, malformed, or belongs to another lap while a sibling has a valid content finding, when the group settles, then the invalid branch is represented only as an infrastructure blocker, the valid sibling still enters the source-complete content adjudication, and no case outcome can clear the invalid branch. | task-3, task-8 | covered | "mixed infrastructure exclusion" | diff-local |
| criterion | Story 2 happy: Given a settled current-lap FAIL has valid content findings and feature-local prior cases, whether or not a sibling failed mechanically, when remediation is dispatched, then its bounded input contains every current unresolved content finding and every prior case with outcome, source links, effect status, and mechanically derived resolution evidence. | task-3, task-15 | covered | "every current unresolved content source and every feature-local case in one deterministic projection" | diff-local |
| criterion | Story 2 happy: Given no prior case exists, when remediation is dispatched, then the same contract carries an empty prior-case collection and the judge can propose new cases without reading sibling prompts or re-auditing the source tree. | task-3, task-6 | covered | "Empty history serializes as an empty collection without a synthetic case." | diff-local |
| criterion | Story 2 negative: Given the complete bounded case projection would exceed the configured hard byte ceiling, when context assembly runs, then the gate halts `needs-human` naming the overflow and dispatches no partial history. | task-3, task-15 | covered | "Each named overflow/invalid-state fixture returns no dispatchable context and identifies the offending bound/state." | diff-local |
| criterion | Story 2 negative: Given a prior case contains an over-limit field or an unrepresentable resolution/effect state, when context assembly runs, then it fails closed before the provider dispatch rather than omitting or coercing that case. | task-3 | covered | "over-limit field, total-byte overflow, and unrepresentable prior state." | diff-local |
| criterion | Story 3 happy: Given a valid remediation result, when it is validated, then every current unresolved finding has exactly one source outcome—`acted`, `deferred`, `rejected`, or `merged`—every merged source names a canonical case, and every canonical case has exactly one `act`, `defer`, or `reject` judgement. | task-1, task-4 | covered | "exact acted/deferred/rejected/merged coverage" | diff-local |
| criterion | Story 3 happy: Given several rubric findings describe one repair, when the judge merges them, then every raw finding remains traceable to the one canonical case and its single priority/route. | task-4 | covered | "reconstructs every raw finding through exactly one source row and one canonical case." | diff-local |
| criterion | Story 3 negative: Given a result omits a current finding, repeats one finding, references an unknown finding/case, or leaves a merged source without a target, when validation runs, then the whole result is rejected before any case mutation, effect, PASS, or kickback. | task-4 | covered | "omission, duplication, dangling/unknown reference" | diff-local |
| criterion | Story 3 negative: Given one case contains contradictory dispositions/routes, an action has no concrete work, or a deferral has no current-plan exclusion rationale, when validation runs, then valid sibling rows are not partially applied and the gate fails closed with the named contract defects. | task-1, task-4 | covered | "Every named invalid fixture returns one closed validation result and zero admissible mutations." | diff-local |
| criterion | Story 4 happy: Given a valid new case judgement, when reconciliation commits, then the engine stamps its case and effect ids, atomically records the versioned case/source state under the feature worktree, and a later process reads the same state. | task-2, task-5 | covered | "A second process reads the exact versioned case/source/effect state written by the first." | diff-local |
| criterion | Story 4 happy: Given a current source is judged equivalent to a prior case, when reconciliation commits, then the current source trace is appended to that case without rewriting or deleting its earlier traces. | task-5 | covered | "later bindings append the new raw source without rewriting earlier links." | diff-local |
| criterion | Story 4 happy: Given a prior open case is absent from a later mechanically complete lap, when reconciliation runs, then its resolution state is updated from current-lap absence and available action evidence while its history remains inspectable. | task-5 | covered | "A case absent after recorded BUILD attempt evidence becomes resolved; history remains present." | diff-local |
| criterion | Story 4 negative: Given the case store is unreadable, malformed, version-incompatible, or its lease cannot be acquired, when the gate evaluates, then it blocks without treating the store as empty or falling back to raw direct routing. | task-2 | covered | "Foreign, unsupported, malformed, lock-timeout, and failed-replace fixtures return typed failures" | diff-local |
| criterion | Story 4 negative: Given autonomous reconciliation runs, when it persists outcomes, then it never writes the operator disposition store or makes an autonomous outcome render as operator-accepted risk. | task-2, task-5, task-16 | covered | "No write path touches `.pipeline/build-review-dispositions.json`." | diff-local |
| criterion | Story 4 negative: Given two processes concurrently attempt to reconcile the same result, when the lease serializes them, then the final store contains one case/effect identity and no lost or duplicated source link. | task-2, task-5 | covered | "a bounded lease, and temp-file atomic replace." | diff-local |
| criterion | Story 5 happy: Given some raw findings have exact current operator dispositions, when adjudication input is built, then only the remaining unresolved findings enter case judgement and the accepted findings retain their existing publication evidence. | task-3, task-16 | covered | "excluding infrastructure and exact operator-resolved sources." | diff-local |
| criterion | Story 5 happy: Given the operator accepts the final unresolved finding while adjudication or effect preparation is in flight, when the engine reaches a BUILD-route, HALT, or PASS exit, then it re-reads the current disposition and suppresses obsolete autonomous rework for that finding. | task-16 | covered | "Each exact late-acceptance fixture suppresses obsolete work/effect/route/HALT for only its source." | diff-local |
| criterion | Story 5 negative: Given an autonomous prior case looks semantically similar to an operator-accepted finding but the exact operator identity does not match, when the reducer runs, then the autonomous case cannot broaden the operator disposition or mark the distinct finding accepted. | task-16 | covered | "Foreign, stale, reason-only, and broader records suppress nothing and remain outside case state" | diff-local |
| criterion | Story 5 negative: Given the operator disposition store cannot be read or validated at an exit, when the effective verdict is derived, then the gate fails closed rather than assuming no acceptance or publishing autonomous PASS. | task-16 | covered | "unreadable or malformed operator state returns a named fail-closed stop before autonomous PASS or BUILD." | diff-local |
| criterion | Story 6 happy: Given one or more new `act` cases in a valid adjudication, when effects settle, then the engine publishes one prioritized durable BUILD work order, consumes the existing `build_review` kickback once for its stable effect id, emits one BUILD route, and BUILD receives the same work after an ordinary process restart. | task-9, task-10, task-12, task-18 | covered | "One or many new action cases produce one prioritized work order and one first-time charge." | diff-local |
| criterion | Story 6 happy: Given an adjudication contains only deferred, rejected, and merged outcomes, when their required effects settle, then no build_review kickback is consumed and no BUILD work order is dispatched. | task-8, task-18 | covered | "Non-action completion performs no BUILD navigation" | diff-local |
| criterion | Story 6 negative: Given actionable work is present but the per-tree or cumulative build_review cap is exhausted, when routing is evaluated, then no BUILD navigation occurs and the existing `needs-human` cap halt names the cases and current counter evidence. | task-10, task-12 | covered | "write/cap failures record the named state and admit no BUILD route." | diff-local |
| criterion | Story 6 negative: Given a build_review action is published, when all persistence is inspected, then the approved plan, task-status plan membership, and plan-growth ledger are unchanged; any attempted plan append is a failing acceptance result. | task-9, task-12 | covered | "Work-order publication and read leave the active plan, task-status, and plan-growth record byte-identical." | diff-local |
| criterion | Story 7 happy: Given a work-order effect was reserved or charged but BUILD was not yet attempted, when the daemon resumes, then it completes the same effect id and BUILD route without creating a second case, work order, or kickback increment. | task-10, task-17 | covered | "Interrupted effects resume the same id without a second charge" | diff-local |
| criterion | Story 7 happy: Given a later finding is materially distinct from every prior case, when it is adjudicated as a new action, then it may create a new engine-stamped case/effect and consume the next permitted kickback. | task-5, task-10, task-17 | covered | "a materially distinct judgement may stamp one new permitted case/effect." | diff-local |
| criterion | Story 7 negative: Given BUILD already attempted an action case and a later complete lap binds a current finding to that unresolved case, when routing is derived, then no new or free BUILD route occurs and the gate halts `needs-human` with both current and prior evidence even if the tree moved. | task-17 | covered | "already-attempted and regressed action cases return `needs-human` with both source traces and work-order evidence." | diff-local |
| criterion | Story 7 negative: Given an action case was marked resolved and an equivalent finding reappears, when reconciliation runs, then the gate halts as a regression of the prior case rather than resetting its history or consuming another kickback. | task-17 | covered | "resolved-case regression" | diff-local |
| criterion | Story 8 happy: Given a valid `defer` case, when its effect is reserved, then the engine searches the configured intake repository for the exact hidden effect marker across open and closed issues, reuses a match, or files one sanitized issue through the existing intake adapter and records its reference. | task-11, task-13 | covered | "Open match, closed match, and post-create recovery all record one existing URL and make zero duplicate create calls." | diff-local |
| criterion | Story 8 happy: Given two distinct deferral cases with different effect ids but similar prose, when effects run, then each receives its own issue; similarity alone does not cause a false deduplication. | task-11, task-13 | covered | "distinct effect ids create distinct issues despite similar text." | diff-local |
| criterion | Story 8 negative: Given issue lookup/create fails because of timeout, authentication, permission, rate limit, or response failure, when the effect runs, then its state remains reserved/failed, the gate cannot PASS or route mixed actionable work, and a retry searches by the same marker before creating. | task-11, task-13 | covered | "The four named external failures leave reserved/failed state, no issue reference, and no admitted PASS or mixed BUILD route." | diff-local |
| criterion | Story 8 negative: Given the process crashes after GitHub creates the issue but before the local reference is recorded, when the effect resumes, then marker lookup records the existing issue and does not create a second one. | task-13, task-19 | covered | "post-create recovery all record one existing URL and make zero duplicate create calls." | diff-local |
| criterion | Story 9 happy: Given every current raw finding is operator-resolved or finalized as deferred, rejected, or merged, every required effect is applied, and every infrastructure branch is healthy or exactly covered by the operator, when the effective verdict is derived, then it is PASS while the raw FAIL and each source-to-case/effect link remain inspectable. | task-8, task-20 | covered | "reports reconstruct every current source through operator/case/effect to the terminal route." | diff-local |
| criterion | Story 9 happy: Given at least one new action and all sibling deferral effects are applied, when the effective verdict is derived, then it emits the single BUILD route with a report that includes every source outcome and canonical case. | task-8, task-18, task-20 | covered | "one first action charge/event, one BUILD navigation, and durable retry context" | diff-local |
| criterion | Story 9 negative: Given any required case/effect is missing, reserved, failed, contradictory, or unrenderable, when the effective verdict is derived, then it cannot PASS or partially route and names the blocking state. | task-8, task-15 | covered | "Missing, reserved, failed, contradictory, and unrenderable state never returns PASS or partial BUILD routing." | diff-local |
| criterion | Story 9 negative: Given raw evidence contains a below-allowance infrastructure failure or an exhausted failure without exact operator reduced coverage, when effective derivation is attempted, then autonomous case state cannot convert that mechanical blocker into PASS. | task-8 | covered | "the reducer cannot mint PASS evidence or operator coverage." | diff-local |
| criterion | Story 10 happy: Given the process stops after any successful state transition, when a new process resumes, then it derives the next legal transition from durable case, kickback, work-order, and engine state without relying on timestamps or prior process memory. | task-19 | covered | "a fresh coordinator derives the one legal next action from case, ledger, work-order, issue marker, and conductor state." | diff-local |
| criterion | Story 10 happy: Given two processes race to apply the same stable action effect, when they serialize state changes, then exactly one kickback charge and one applied work order result. | task-2, task-19 | covered | "The two-executor race leaves one parsable case store, one applied effect, and one legal next transition." | diff-local |
| criterion | Story 10 negative: Given persistence fails during atomic replace or a durable state combination is impossible, when recovery reads the files, then the engine fails closed with the last complete state rather than accepting a partial write. | task-2, task-19 | covered | "leave the last complete JSON readable." | diff-local |
| criterion | Story 10 negative: Given case state says an effect is reserved while the kickback ledger already records its charge, when recovery runs, then it reconciles and resumes that same effect without incrementing again; it never treats the mismatch as a fresh case. | task-10, task-12, task-19 | covered | "Replay after reservation or charge reuses the same ids and counters" | diff-local |
| criterion | Story 11 happy: Given adjudication is enabled, when a case proceeds through judgement and effects, then start, completion/failure, reconciliation, effect, and semantic-repeat occurrences travel through the existing `ConductorEvent` union, declared sinks, and `.pipeline/events.jsonl` persister. | task-14, task-21 | covered | "adjudication start/completion/failure, reconciliation, effect reserved/applied/failed, and semantic-repeat halt" | diff-local |
| criterion | Story 11 happy: Given an existing prd_audit, as-built, finish, or build-stall remediation artifact has the legacy shape, when it is read and routed, then its parsed plan and effects are unchanged by the additive build_review case mode. | task-1, task-6, task-20 | covered | "Existing legacy remediation parser tests retain byte-equivalent parsed plans." | diff-local |
| criterion | Story 11 happy: Given `build_review.adjudication.enabled` is false, when raw content FAIL occurs, then the engine follows the pre-feature direct BUILD route and neither reads nor writes remediation case state. | task-7, task-20 | covered | "Flag-off produces the old raw-reasons BUILD route/count/stale cascade and performs zero case context/store/effect reads" | diff-local |
| criterion | Story 11 negative: Given a new event type lacks an explicit sink declaration or renderer decision, when the project compiles/tests, then exhaustiveness fails rather than silently dropping the occurrence. | task-14 | covered | "The sink exhaustiveness test fails when a fixture omits any new type" | diff-local |
| criterion | Story 11 negative: Given a case-mode discriminator is unknown, absent where required, or mixed with legacy fields, when the artifact parser runs, then build_review fails closed while a genuine legacy artifact still uses the legacy parser. | task-1, task-20 | covered | "unknown/missing/mixed case-mode fixtures stop with deterministic diagnostics." | diff-local |
| criterion | Story 11 negative: Given the config block or `enabled` value is malformed or has unknown keys, when configuration is validated, then startup rejects it with a specific path/type diagnostic rather than guessing a rollout mode. | task-7 | covered | "Wrong types and unknown nested keys are rejected with the exact `build_review.adjudication.*` path." | diff-local |
