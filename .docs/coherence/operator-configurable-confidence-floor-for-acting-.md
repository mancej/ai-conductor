# Coherence Mapping: Operator-configurable confidence floor for acting on build_review findings

**Date:** 2026-09-06  
**Tier:** M  
**Track:** technical — the `fr` row class is omitted because this track has no PRD.  
**Outcome source:** none required. `jstoup111/ai-conductor#2383` carries no `## Desired outcome`
section, so the staged `.pipeline/intake-outcomes.md` holds a Source-Ref and zero bullets, and the
outcome layer reads as not-required rather than as a gap.  
**ADR row pool:** one non-deleted ADR file in this spec change set — the governing adjudication ADR,
amended in this pass with D4.1-D4.6 and D5.1-D5.3. Its citable decision ids are D1 through D5;
the sub-items are not separately citable.  
**Consistency pass:** every covered row was re-read in both directions against the revised
(grader-placed) design. No contradiction or oscillation remains; the two conflicts found against the
earlier adjudicator-placed stories do not exist in the rewritten stories, as recorded in
`.docs/conflicts/operator-configurable-confidence-floor-for-acting-.md`.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
| --- | --- | --- | --- | --- |
| story | story-1 | task-1, task-3, task-4 | covered | Parser range check, contract text, and the existing digest-discard behavior deliver a validated optional confidence. |
| story | story-2 | task-2 | covered | Identity invariance and disposition rebinding are pinned by tests against the unchanged identity input. |
| story | story-3 | task-7, task-8, task-9 | covered | The suppressed bucket, the conductor pass path, and the source exclusion deliver suppression before the gate fails and before remediate. |
| story | story-4 | task-10, task-11 | covered | The additive outer-verdict field and its daemon-log projection make every suppression visible. |
| story | story-5 | task-12, task-13 | covered | Store entries and the context history section keep suppressed findings available to the judge without demanding an outcome. |
| story | story-6 | task-14, task-15 | covered | The settlement predicate and its bounds skip only exact-id finalized recurrence and never write the store. |
| story | story-7 | task-16 | covered | The completed-adjudication event, ledger invariance, and trace rendering record a skipped dispatch. |
| story | story-8 | task-5, task-6 | covered | The per-rubric key is validated as a bounded integer and declares its consumer. |
| task | task-1 | story-1 | covered | Implements Story 1; type happy-path. |
| task | task-2 | story-2 | covered | Implements Story 2; type negative-path. |
| task | task-3 | story-1 | covered | Supporting purpose: states the contract the Story 1 grader follows; production skill text, not documentation. |
| task | task-4 | story-1 | covered | Supporting purpose: proves the existing digest-discard behavior covers Story 1 without new code. |
| task | task-5 | story-8 | covered | Implements Story 8; type happy-path. |
| task | task-6 | story-8 | covered | Supporting purpose: satisfies the config-key registry obligation created by Story 8. |
| task | task-7 | story-3 | covered | Implements Story 3; type happy-path. |
| task | task-8 | story-3 | covered | Implements Story 3; type happy-path. |
| task | task-9 | story-3 | covered | Implements Story 3; type negative-path. |
| task | task-10 | story-4 | covered | Implements Story 4; type happy-path. |
| task | task-11 | story-4 | covered | Implements Story 4; type happy-path. |
| task | task-12 | story-5 | covered | Implements Story 5; type happy-path. |
| task | task-13 | story-5 | covered | Implements Story 5; type negative-path. |
| task | task-14 | story-6 | covered | Implements Story 6; type happy-path. |
| task | task-15 | story-6 | covered | Implements Story 6; type negative-path. |
| task | task-16 | story-7 | covered | Implements Story 7; type happy-path. |
| adr | adr-2026-08-29-mixed-build-review-laps-preserve-content-adjudication | story-1, story-2, story-3, story-4, story-5, story-6, story-7, story-8 | covered | D4.1-D4.6 and D5.1-D5.3 were appended in this DECIDE pass and are implemented by these stories. Section 4f judgement on the plan's five obligation rows: D1, D2 and D3 are correctly no-change because suppression narrows the finding set before D1, adds no dispatch to D2, and leaves D3's precedence intact; D4 is correctly task-dispositioned because task-7's Done-when asserts the suppressed bucket with only unresolved blocking, which is D4.3 itself; D5 is correctly task-dispositioned because task-14's Done-when asserts exact-id removal before dispatch, which is D5.1 itself. |
| criterion | Story 1 happy: Given a rubric result whose finding carries `"confidence": 72`, when the engine parses the result, then the finding is accepted and its confidence is retained as the integer 72. | task-1 | covered | The finding parser accepts an integer confidence of 0, 72, and 100 and returns each unchanged on the parsed finding. | diff-local |
| criterion | Story 1 happy: Given a rubric result whose finding carries `"confidence": 0`, when the engine parses the result, then the finding is accepted, because 0 is a valid confidence and not an absent value. | task-1 | covered | The finding parser accepts an integer confidence of 0, 72, and 100 and returns each unchanged on the parsed finding. | diff-local |
| criterion | Story 1 happy: Given a rubric result whose finding carries `"confidence": 100`, when the engine parses the result, then the finding is accepted. | task-1 | covered | The finding parser accepts an integer confidence of 0, 72, and 100 and returns each unchanged on the parsed finding. | diff-local |
| criterion | Story 1 happy: Given a rubric result whose finding omits `confidence`, when the engine parses the result, then the finding is accepted with no confidence recorded, because the field is optional. | task-1 | covered | The finding parser accepts a finding with no confidence and records none. | diff-local |
| criterion | Story 1 negative: Given a finding carrying `"confidence": 101`, when the engine parses the result, then the whole rubric result is malformed and is handled exactly as a result with any other invalid finding field. | task-1 | covered | The finding parser treats 101, -1, 72.5, and a string confidence as a malformed result through the existing invalid-field path. | diff-local |
| criterion | Story 1 negative: Given a finding carrying `"confidence": -1`, when the engine parses the result, then the whole rubric result is malformed. | task-1 | covered | The finding parser treats 101, -1, 72.5, and a string confidence as a malformed result through the existing invalid-field path. | diff-local |
| criterion | Story 1 negative: Given a finding carrying `"confidence": 72.5`, when the engine parses the result, then the whole rubric result is malformed, because confidence must be an integer. | task-1 | covered | The finding parser treats 101, -1, 72.5, and a string confidence as a malformed result through the existing invalid-field path. | diff-local |
| criterion | Story 1 negative: Given a finding carrying `"confidence": "high"`, when the engine parses the result, then the whole rubric result is malformed, because a string is not accepted. | task-1 | covered | The finding parser treats 101, -1, 72.5, and a string confidence as a malformed result through the existing invalid-field path. | diff-local |
| criterion | Story 1 negative: Given a cached rubric result produced before the contract stated confidence, when the skill text has since changed, then the cached result is discarded on skill-digest mismatch and the grader re-runs under the current contract. | task-4 | covered | A cached rubric result produced under the previous skill digest is discarded on skill-digest mismatch and the grader re-runs. | diff-local |
| criterion | Story 2 happy: Given two findings identical in rubric, contract version, concern kind and anchor but carrying confidence 30 and 90, when their identities are computed, then both produce the same finding id. | task-2 | covered | Two findings differing only in confidence share one identity id. | diff-local |
| criterion | Story 2 happy: Given a finding with confidence 30 and the same finding with no confidence, when their identities are computed, then both produce the same finding id. | task-2 | covered | Two findings differing only in confidence share one identity id. | diff-local |
| criterion | Story 2 negative: Given an operator disposition accepting a finding graded at confidence 90, when the next lap re-grades the same finding at confidence 40, then the disposition still binds and the finding is recorded as accepted. | task-2 | covered | An operator disposition recorded against a finding continues to bind after the finding is re-graded at a different confidence. | diff-local |
| criterion | Story 2 negative: Given the canonical identity payload for a finding, when it is inspected, then it contains no confidence field. | task-2 | covered | The canonical identity payload contains no confidence field. | diff-local |
| criterion | Story 2 negative: Given a finding whose anchor differs by one character from an accepted finding, when its identity is computed, then it produces a different id regardless of confidence, because confidence neither adds to nor substitutes for the anchor. | task-2 | covered | A one-character anchor change yields a different id regardless of confidence. | diff-local |
| criterion | Story 3 happy: Given `build_review.rubrics.testQuality.min_confidence` is 70 and a lap's only finding carries confidence 40, when the effective verdict is derived, then the finding is placed in the suppressed set, the unresolved set is empty, and the effective verdict is PASS. | task-7 | covered | A finding below its rubric's floor is suppressed; at or above it, or with no confidence, it is unresolved. | diff-local |
| criterion | Story 3 happy: Given the floor is 70 and a finding carries confidence 70, when the effective verdict is derived, then the finding is unresolved, because the floor is a minimum and not an exclusive bound. | task-7 | covered | A finding below its rubric's floor is suppressed; at or above it, or with no confidence, it is unresolved. | diff-local |
| criterion | Story 3 happy: Given the floor is 70 and a finding carries confidence 95, when the effective verdict is derived, then the finding is unresolved and the effective verdict is FAIL, as today. | task-7 | covered | A finding below its rubric's floor is suppressed; at or above it, or with no confidence, it is unresolved. | diff-local |
| criterion | Story 3 happy: Given the floor is unset and a finding carries confidence 1, when the effective verdict is derived, then the finding is unresolved, because the default floor of 0 never suppresses. | task-7 | covered | A finding below its rubric's floor is suppressed; at or above it, or with no confidence, it is unresolved. | diff-local |
| criterion | Story 3 happy: Given the floor is 70 and a lap has findings at confidence 40 and 90, when the effective verdict is derived, then only the 40 is suppressed and the verdict is FAIL on the 90. | task-7 | covered | A lap mixing a sub-floor and an above-floor finding suppresses only the sub-floor one and fails on the other. | diff-local |
| criterion | Story 3 negative: Given the floor is 70 and a finding omits confidence, when the effective verdict is derived, then the finding is unresolved, because an absent confidence is never suppressed. | task-7 | covered | A finding below its rubric's floor is suppressed; at or above it, or with no confidence, it is unresolved. | diff-local |
| criterion | Story 3 negative: Given a lap whose every finding is suppressed, when the conductor evaluates the build_review gate, then it records the step done without dispatching remediate and without charging a kickback. | task-8 | covered | A lap whose every finding is suppressed passes the build_review gate with no remediate dispatch and no kickback charged. | diff-local |
| criterion | Story 3 negative: Given a lap with one suppressed and one unresolved finding, when adjudication runs, then the judge's current sources contain only the unresolved finding. | task-9 | covered | On a mixed lap the adjudication sources exclude suppressed findings. | diff-local |
| criterion | Story 3 negative: Given a suppressed finding, when the operator disposition store is read after the lap, then it is byte-identical to its state before the lap, because suppression never becomes operator authority. | task-9 | covered | The operator disposition store is unchanged by a suppressed lap. | diff-local |
| criterion | Story 3 negative: Given a rubric with an uncovered infrastructure failure alongside a suppressed finding, when the effective verdict is derived, then the verdict is still FAIL on the infrastructure failure, because suppression clears content only. | task-7 | covered | An uncovered infrastructure failure still fails the effective verdict when every content finding is suppressed. | diff-local |
| criterion | Story 4 happy: Given a finding is suppressed on a lap, when the lap's outer verdict event is read, then it carries a suppressed-findings list naming the finding id, its rubric, its reported confidence, and the floor applied. | task-10 | covered | The outer verdict event carries an additive suppressed-findings list with finding id, rubric, confidence, and floor for every suppressed finding. | diff-local |
| criterion | Story 4 happy: Given two findings are suppressed on a lap, when the outer verdict event is read, then both appear as separate entries. | task-10 | covered | The outer verdict event carries an additive suppressed-findings list with finding id, rubric, confidence, and floor for every suppressed finding. | diff-local |
| criterion | Story 4 happy: Given a finding is suppressed, when the daemon log for the lap is read, then a line names the suppressed finding and its confidence against the floor. | task-11 | covered | The daemon log projection of that event renders one line per suppressed finding. | diff-local |
| criterion | Story 4 negative: Given a lap that suppresses nothing, when the outer verdict event is read, then its suppressed-findings list is absent or empty. | task-10 | covered | A lap that suppresses nothing carries an absent or empty list. | diff-local |
| criterion | Story 4 negative: Given a finding is suppressed, when the event stream is read, then no kickback event is attributable to it. | task-10 | covered | A suppression is never emitted or rendered as a kickback event. | diff-local |
| criterion | Story 4 negative: Given a suppressed finding, when its record is inspected, then it was stamped when the effective verdict was derived rather than reconstructed later from stored state. | task-10 | covered | Entries are stamped when the effective verdict is derived, not reconstructed from stored state. | diff-local |
| criterion | Story 5 happy: Given a finding is suppressed on lap one, when the case store is read after the lap, then it holds a suppression entry keyed by the finding's id carrying its rubric, summary, confidence, floor, and the lap last seen. | task-12 | covered | The case store persists one suppression entry per suppressed finding id, updated in place on recurrence and never pruned. | diff-local |
| criterion | Story 5 happy: Given a suppression entry exists and a later lap dispatches the judge, when the adjudication context is assembled, then the entry appears in a non-blocking history section separate from current sources. | task-13 | covered | The adjudication context carries suppression entries in a history section distinct from current sources. | diff-local |
| criterion | Story 5 happy: Given a suppressed finding recurs on a later lap, when the case store is read, then its entry's last-seen lap is updated and no second entry is created. | task-12 | covered | The case store persists one suppression entry per suppressed finding id, updated in place on recurrence and never pruned. | diff-local |
| criterion | Story 5 negative: Given a suppression entry exists, when the judge returns a case-v1 result that gives that entry no outcome, then the result is still valid, because suppression entries are not current sources and the source-complete validator does not demand an outcome for them. | task-13 | covered | A judgement that assigns no outcome to a suppression entry still validates. | diff-local |
| criterion | Story 5 negative: Given a suppressed finding stops recurring, when later laps run, then its entry remains in the store and is not pruned. | task-12 | covered | The case store persists one suppression entry per suppressed finding id, updated in place on recurrence and never pruned. | diff-local |
| criterion | Story 5 negative: Given a related case is resolved, when the store is read, then both the resolved case and every suppression entry remain present. | task-12 | covered | Resolved cases and suppression entries remain in the store across laps. | diff-local |
| criterion | Story 5 negative: Given a suppression entry, when the operator disposition store is read, then the entry does not appear there. | task-12 | covered | A suppression entry never appears in the operator disposition store. | diff-local |
| criterion | Story 6 happy: Given lap one deferred finding B and its deferral effect is applied, when lap two reports B with the identical content-anchored id, then the live source set is empty after operator resolution and settlement, and no remediate dispatch occurs. | task-14 | covered | A finding whose exact id links to a finalized deferred, rejected, or merged case is removed from the live source set before dispatch. | diff-local |
| criterion | Story 6 happy: Given lap one rejected finding C, when lap two reports C with the identical id, then no remediate dispatch occurs. | task-14 | covered | A finding whose exact id links to a finalized deferred, rejected, or merged case is removed from the live source set before dispatch. | diff-local |
| criterion | Story 6 happy: Given a finding was recorded with a merged source outcome on a finalized case, when it recurs with the identical id, then no remediate dispatch occurs. | task-14 | covered | A finding whose exact id links to a finalized deferred, rejected, or merged case is removed from the live source set before dispatch. | diff-local |
| criterion | Story 6 happy: Given lap two reports settled finding B and new finding D, when adjudication runs, then the judge is dispatched with D as its only current source. | task-14 | covered | A lap with one settled and one new finding dispatches the judge with the new finding as its only current source. | diff-local |
| criterion | Story 6 negative: Given lap one deferred finding B, when lap two reports B with an anchor that differs by one character, then B is a live source and the judge is dispatched, because only an exact id is settled. | task-15 | covered | A drifted id, a reserved or failed effect, and an open action case each leave the finding live. | diff-local |
| criterion | Story 6 negative: Given lap one deferred finding B but its deferral effect is still reserved, when lap two reports B, then B is not settled and the lap follows the existing unfinished-effect route. | task-15 | covered | A drifted id, a reserved or failed effect, and an open action case each leave the finding live. | diff-local |
| criterion | Story 6 negative: Given lap one produced an open action case for finding A that BUILD has not attempted, when lap two reports A, then A is not settled and the existing action route applies. | task-15 | covered | A drifted id, a reserved or failed effect, and an open action case each leave the finding live. | diff-local |
| criterion | Story 6 negative: Given the settlement predicate runs, when the case store is read afterwards, then no case was written, resolved, or pruned by the predicate. | task-15 | covered | The predicate performs no write to the case store. | diff-local |
| criterion | Story 6 negative: Given the case store is unreadable, when the settlement predicate would run, then the lap fails closed exactly as it does today for an unreadable store. | task-15 | covered | An unreadable case store fails the lap closed exactly as before. | diff-local |
| criterion | Story 7 happy: Given the settlement predicate empties the live set, when the lap finalizes, then a remediation adjudication completed event is emitted for the lap carrying the settled case ids and no new effect ids. | task-16 | covered | A skipped dispatch emits the completed-adjudication event with settled case ids and an empty effect list. | diff-local |
| criterion | Story 7 happy: Given a skipped dispatch, when the daemon log is read, then the lap shows a completed adjudication line rather than no adjudication line. | task-16 | covered | The rendered trace lists each settled case with its finalized outcome, and the daemon log shows a completed adjudication line. | diff-local |
| criterion | Story 7 negative: Given the settlement predicate empties the live set, when the event stream is read, then no adjudication started event was emitted for a dispatch that did not happen. | task-16 | covered | No started event is emitted for a skipped dispatch. | diff-local |
| criterion | Story 7 negative: Given the settlement predicate empties the live set, when the kickback ledger is read, then it is unchanged. | task-16 | covered | The kickback ledger is unchanged by a skipped dispatch. | diff-local |
| criterion | Story 7 negative: Given a skipped dispatch, when the trace for the lap is rendered, then each settled case appears with its finalized outcome. | task-16 | covered | The rendered trace lists each settled case with its finalized outcome, and the daemon log shows a completed adjudication line. | diff-local |
| criterion | Story 8 happy: Given a project config setting `build_review.rubrics.testQuality.min_confidence` to 70, when config loads, then it is accepted and the resolved rubric policy carries 70. | task-5 | covered | The key is accepted at 0, 70 and 100 and the resolved rubric policy carries the configured integer. | diff-local |
| criterion | Story 8 happy: Given a project config that omits the key, when config loads, then the resolved floor is 0 and no warning is produced. | task-5 | covered | An absent key resolves to a floor of 0. | diff-local |
| criterion | Story 8 happy: Given a project config setting the key to 0, when config loads, then it is accepted and nothing is ever suppressed. | task-5 | covered | The key is accepted at 0, 70 and 100 and the resolved rubric policy carries the configured integer. | diff-local |
| criterion | Story 8 happy: Given a project config setting the key to 100, when config loads, then it is accepted. | task-5 | covered | The key is accepted at 0, 70 and 100 and the resolved rubric policy carries the configured integer. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to 101, when config loads, then loading fails with a validation error naming the exact `build_review.rubrics.testQuality.min_confidence` path and its permitted range. | task-5 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to -5, when config loads, then loading fails with a validation error naming the exact path. | task-5 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to 70.5, when config loads, then loading fails with a validation error, because the value must be an integer. | task-5 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting the key to the string "70", when config loads, then loading fails with a validation error, because the value is not coerced. | task-5 | covered | Config load fails with an error naming the exact key path and the permitted range 0 to 100 for 101, for -5, for 70.5, and for a string value. | diff-local |
| criterion | Story 8 negative: Given a config setting a misspelled `min_confidance`, when config loads, then loading fails with the existing unknown-key error naming the rubric policy block. | task-5 | covered | A misspelled sibling key still fails with the existing unknown-key error naming the rubric policy block. | diff-local |
| criterion | Story 8 negative: Given the config-key consumer registry, when its totality test runs, then `build_review.rubrics.min_confidence` declares a resolvable production consumer and the test passes. | task-6 | covered | The key declares a resolvable production consumer in the config-key consumer registry. | diff-local |
