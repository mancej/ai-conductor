# Coherence Mapping: coverage claims bound to `Done when` (#2088)

**Date:** 2026-08-31  
**Tier:** M  
**Track:** technical — the `fr` row class is omitted because this track has no PRD.  
**Outcome source:** the five Desired-outcome bullets for `jstoup111/ai-conductor#2088` staged in
`.pipeline/intake-outcomes.md`.  
**ADR row pool:** the one ADR authored by this spec plus the four ADRs it amends additively (all
non-deleted files in this change set).  
**Quote rule:** every criterion row quotes a check from the cited task's `Done when` block — the
rule this spec introduces (adr-2026-08-31 D2) — which is a strict subset of the body-substring rule
the current land validator enforces.  
**Consistency pass:** every covered row was re-read in both directions. The one cross-layer pair
worth naming — outcome-5 (staleness caught) against Story 4 (judge default off) — is not an
oscillation: Story 4 defers the guarantee, it does not negate it, and outcome-5's row records the
condition. No static contradiction was found after conflict-check's six resolutions.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-3, story-6 | covered | Land rejects an ungrounded binding on both carriers (Story 1, Story 3); the judge refuses needs-human before `build` on `does-not-assert` (Story 6). |
| outcome | outcome-2 | story-1, story-6 | covered | Land rejection detail and the halt record both carry the criterion, the bound task id, and that task's actual `Done when` checks verbatim. |
| outcome | outcome-3 | story-1, story-6, story-7 | covered | A quote in any one cited task's `Done when` grounds the row (Story 1); all-`asserts` completes `done` (Story 6); one task may carry several criteria and a legacy task is never forced (Story 7). |
| outcome | outcome-4 | story-2, story-3 | covered | The plan's own `## Coverage Check` table carries criterion-level rows (Story 2) and tier S is checked at land with the same rules (Story 3). |
| outcome | outcome-5 | story-5, story-6 | covered | Covered by re-judgement: a `Done when` edit changes the claim digest and re-dispatches (Story 5); a cleared halt re-admits the step and re-judges the amended pair (Story 6). Holds when `coverage_binding.judge.enabled` is true; the shipped default is off, so until the follow-up flip a post-land staleness is caught on the next enabled run, not at land. |
| story | story-1 | task-3, task-4, task-18 | covered | Each cited task's **Story:** line names Story 1; confirmed in the plan. |
| story | story-2 | task-1, task-2, task-19 | covered | Each cited task's **Story:** line names Story 2; confirmed in the plan. |
| story | story-3 | task-5, task-6 | covered | Each cited task's **Story:** line names Story 3; confirmed in the plan. |
| story | story-4 | task-7, task-8, task-9 | covered | Each cited task's **Story:** line names Story 4; confirmed in the plan. |
| story | story-5 | task-11, task-12, task-13, task-14 | covered | Each cited task's **Story:** line names Story 5; confirmed in the plan. |
| story | story-6 | task-15, task-16 | covered | Each cited task's **Story:** line names Story 6; confirmed in the plan. |
| story | story-7 | task-10 | covered | Each cited task's **Story:** line names Story 7; confirmed in the plan. |
| story | story-8 | task-17 | covered | Each cited task's **Story:** line names Story 8; confirmed in the plan. |
| task | task-1 | story-2 | covered | **Story:** Story 2 in the plan; the task's Done when asserts criteria of that story. |
| task | task-2 | story-2 | covered | **Story:** Story 2 in the plan; the task's Done when asserts criteria of that story. |
| task | task-3 | story-1 | covered | **Story:** Story 1 in the plan; the task's Done when asserts criteria of that story. |
| task | task-4 | story-1 | covered | **Story:** Story 1 in the plan; the task's Done when asserts criteria of that story. |
| task | task-5 | story-3 | covered | **Story:** Story 3 in the plan; the task's Done when asserts criteria of that story. |
| task | task-6 | story-3 | covered | **Story:** Story 3 in the plan; the task's Done when asserts criteria of that story. |
| task | task-7 | story-4 | covered | **Story:** Story 4 in the plan; the task's Done when asserts criteria of that story. |
| task | task-8 | story-4 | covered | **Story:** Story 4 in the plan; the task's Done when asserts criteria of that story. |
| task | task-9 | story-4 | covered | **Story:** Story 4 in the plan; the task's Done when asserts criteria of that story. |
| task | task-10 | story-7 | covered | **Story:** Story 7 in the plan; the task's Done when asserts criteria of that story. |
| task | task-11 | story-5 | covered | **Story:** Story 5 in the plan; the task's Done when asserts criteria of that story. |
| task | task-12 | story-5 | covered | **Story:** Story 5 in the plan; the task's Done when asserts criteria of that story. |
| task | task-13 | story-5 | covered | **Story:** Story 5 in the plan; the task's Done when asserts criteria of that story. |
| task | task-14 | story-5 | covered | **Story:** Story 5 in the plan; the task's Done when asserts criteria of that story. |
| task | task-15 | story-6 | covered | **Story:** Story 6 in the plan; the task's Done when asserts criteria of that story. |
| task | task-16 | story-6 | covered | **Story:** Story 6 in the plan; the task's Done when asserts criteria of that story. |
| task | task-17 | story-8 | covered | **Story:** Story 8 in the plan; the task's Done when asserts criteria of that story. |
| task | task-18 | story-1 | covered | **Story:** Story 1 in the plan; the task's Done when asserts criteria of that story. |
| task | task-19 | story-2 | covered | **Story:** Story 2 in the plan; the task's Done when asserts criteria of that story. |
| adr | adr-2026-08-31-coverage-binding-judge-step | story-1, story-2, story-3, story-4, story-5, story-6, story-7, story-8 | covered | New ADR authored by this spec; D1–D3 → Stories 1–3, D4/D7 → Story 4, D5 → Story 5, D6 → Story 6, D8 → Story 7, D9 → Story 8. |
| adr | adr-2026-07-22-coherence-gate-placement-and-validation-split | story-3, story-4 | covered | Amended 2026-08-31: tier-S carve-out (Story 3) and judge placement as a BUILD-phase step, not inside land (Story 4). |
| adr | adr-2026-08-23-coverage-claims-grounded-by-verbatim-quote | story-1 | covered | Amended 2026-08-31: quote drawn from the cited task's Done when block (Story 1). |
| adr | adr-2026-08-23-criterion-layer-is-structural-at-land | story-3, story-7 | covered | Amended 2026-08-31: criterion layer engages at S over the plan carrier (Story 3); the judge step reads but never requires criterion rows (Story 7, zero-claim case). |
| adr | adr-2026-08-22-one-owner-per-review-question | story-6 | covered | Amended 2026-08-31: new map row owned by coverage_binding, which halts but never appends a task or routes to plan (Story 6). |
| criterion | Story 1 happy: Given a coherence `criterion` row citing `task-3` whose quote is a whitespace-normalized substring of one of task 3's `Done when` checks, when the spec lands, then the criterion layer reports no gap for that row | task-3 | covered | "The existing whitespace-normalization behavior is preserved" | diff-local |
| criterion | Story 1 happy: Given a criterion row citing `task-3, task-5` whose quote appears only in task 5's `Done when` checks, when the spec lands, then the row is grounded (any cited task's `Done when` suffices) | task-3 | covered | "A quote found in any one cited task's `Done when` (multi-task citation) reports no gap" | diff-local |
| criterion | Story 1 negative: Given a criterion row citing `task-14` whose quote appears in task 14's Steps prose but in none of its `Done when` checks, when the spec lands, then land rejects with gap id `criterion:quote-not-done-when:<n>` and the message names the criterion text, `task-14`, and every `Done when` check of task 14 verbatim | task-3 | covered | "reported as `criterion:quote-not-done-when:<n>` and the detail string includes the criterion, the cited task id(s), and each cited task's `Done when` checks verbatim" | diff-local |
| criterion | Story 1 negative: Given a criterion row whose quote appears nowhere in the cited task, when the spec lands, then the rejection is still `criterion:quote-ungrounded:<n>` (existing id unchanged, not the new id) | task-3 | covered | "A quote absent from the task entirely is still reported as `criterion:quote-ungrounded:<n>`" | diff-local |
| criterion | Story 1 negative: Given a rejected `criterion:quote-not-done-when:<n>` gap and a coherence waiver citing that exact id, when the spec lands, then the waiver is honored the same way as any other coverage gap | task-4 | covered | "A waiver naming `criterion:quote-not-done-when:<n>` clears that gap in the land aggregate" | diff-local |
| criterion | Story 1 negative: Given the `coherence-check` skill text, when its criterion-row rule is read, then it states the quote MUST be a check from the cited task's `Done when` block, not Steps or Files prose | task-18 | covered | "states the quote must be one of the cited task's `Done when` checks" | diff-local |
| criterion | Story 2 happy: Given a plan `## Coverage Check` table row whose four cells are the exact criterion text `Story 2 happy: Given …, when …, then …`, the task id `4`, a quote, and `diff-local`, when the table is parsed, then it yields one criterion claim with that criterion text, cited task `4`, that quote, and disposition `diff-local` | task-1 | covered | "returns `CriterionCoherenceRow` values with `rowClass: 'criterion'` for four-cell rows only" | diff-local |
| criterion | Story 2 happy: Given a table mixing legacy two-cell story→task rows and four-cell criterion rows, when the table is parsed, then each row is classified by cell count and the legacy rows produce exactly the `claim-<row>` reconciliation results they produce today | task-1, task-2 | covered | "asserts an identical `claim-<row>` result set" | diff-local |
| criterion | Story 2 happy: Given a criterion row citing `4 (landed)`, when the task id is resolved, then it resolves to `4` (the trailing parenthesized annotation is stripped before the membership check) | task-2 | covered | "`taskIdFromCitation('4 (landed)')` returns `'4'`" | diff-local |
| criterion | Story 2 negative: Given a criterion row citing task `9` when the plan's task tree has no task 9, when the spec lands, then land rejects with `criterion:task-missing:<n>:9` naming the criterion | task-2 | covered | "the existing `criterion:task-missing` and `criterion:invented` gap ids unchanged" | diff-local |
| criterion | Story 2 negative: Given a criterion row whose criterion cell matches no criterion extracted from the stories file, when the spec lands, then land rejects with `criterion:invented:<n>` | task-2 | covered | "the existing `criterion:task-missing` and `criterion:invented` gap ids unchanged" | diff-local |
| criterion | Story 2 negative: Given a four-cell criterion row whose disposition cell is empty or `outside-diff`, when the spec lands, then land rejects with the existing `criterion:disposition-missing:<n>` or `criterion:disposition-negative:<n>` gap naming the criterion | task-5 | covered | "The disposition checks (`criterion:disposition-missing`, `criterion:disposition-negative`) fire at S exactly as at M/L" | diff-local |
| criterion | Story 2 negative: Given every merged plan under `.docs/plans/` on the base branch, when each plan's `## Coverage Check` table is parsed with the new parser, then every plan yields the identical `claim-<row>` result set the retired parse produced (pinned corpus test) | task-2 | covered | "it fails if any merged plan's legacy rows change classification" | diff-local |
| criterion | Story 2 negative: Given the `plan` skill text, when its §7 Coverage Check is read, then it prescribes the four-cell criterion row form with the `Done when` quote rule and the diff-locality disposition | task-19 | covered | "shows the four-cell row form with the `Done when` quote and disposition cells and states the tier-S requirement" | diff-local |
| criterion | Story 3 happy: Given a tier-S spec whose plan table carries one grounded criterion row per extracted story criterion and no coherence artifact, when the spec lands, then the coherence gate passes | task-5 | covered | "A tier-S land with fully grounded four-cell rows and no coherence artifact passes the gate" | diff-local |
| criterion | Story 3 happy: Given a tier-S spec, when the coherence gate runs, then only the `criterion` layer is evaluated — no coherence artifact is required and no outcome, fr, story, orphan-task, or adr check runs | task-5 | covered | "returns `engaged: true` with exactly `{criterion}` and `carrier: 'plan'` at tier S" | diff-local |
| criterion | Story 3 negative: Given a tier-S spec whose plan has no criterion-level rows, when the spec lands, then land rejects with one `criterion:omitted:<n>` gap per extracted criterion, each naming the criterion text | task-6 | covered | "Tests (a), (b), (c) pass with the named gap ids" | diff-local |
| criterion | Story 3 negative: Given a tier-S spec whose criterion row quote is absent from the cited task's `Done when`, when the spec lands, then land rejects with `criterion:quote-not-done-when:<n>` exactly as at M/L | task-6 | covered | "Tests (a), (b), (c) pass with the named gap ids" | diff-local |
| criterion | Story 3 negative: Given a tier-S spec whose criterion row carries disposition `outside-diff` and no waiver names the gap, when the spec lands, then land rejects with `criterion:disposition-negative:<n>` exactly as at M/L | task-5 | covered | "The disposition checks (`criterion:disposition-missing`, `criterion:disposition-negative`) fire at S exactly as at M/L" | diff-local |
| criterion | Story 3 negative: Given a tier-S spec whose stories file has no extractable Given/When/Then criteria, when the spec lands, then the refusal is the non-waivable `criterion:stories-unparseable` | task-6 | covered | "(c) is refused even with a waiver naming it" | diff-local |
| criterion | Story 3 negative: Given a merged tier-S spec on the base branch with no criterion-level rows, when daemon discovery scans it, then it is eligible and dispatches exactly as before | task-6 | covered | "Test (d) passes with `src/conductor/src/engine/daemon-backlog.ts` untouched in this diff" | diff-local |
| criterion | Story 4 happy: Given `ALL_STEPS`, when its order is read, then `coverage_binding` follows `coherence_check` and precedes `acceptance_specs`, with `phase: 'BUILD'`, `enforcement: 'gating'`, prerequisites `['plan']`, and no `skippableForTiers` or `skippableForTracks` | task-7 | covered | "`ALL_STEPS` contains `coverage_binding` immediately after `coherence_check` and before `acceptance_specs`" | diff-local |
| criterion | Story 4 happy: Given the daemon's derived preseed set, when it is computed, then `coverage_binding` is not a member | task-7 | covered | "The derived daemon preseed set excludes `coverage_binding`" | diff-local |
| criterion | Story 4 happy: Given a config with no `coverage_binding` block, when the step runs, then it completes successfully with output `coverage_binding judge disabled` and writes `.pipeline/coverage-binding.json` recording `disabled` | task-9 | covered | "returns success with output `coverage_binding judge disabled`" | diff-local |
| criterion | Story 4 negative: Given `coverage_binding.judge.enabled: "yes"` (non-boolean), when the config is validated, then validation fails naming the key and the expected boolean type | task-8 | covered | "A non-boolean value fails `validate` naming the key and the expected type" | diff-local |
| criterion | Story 4 negative: Given the disabled default, when the step runs on a spec with a `does-not-assert`-shaped claim, then no provider dispatch occurs and no halt is written | task-9 | covered | "the provider mock records zero calls" | diff-local |
| criterion | Story 4 negative: Given a tier-S feature, when the step table's tier skips are evaluated, then `coverage_binding` is not skipped | task-7 | covered | "pinned tests in both files assert `coverage_binding` is not skipped" | diff-local |
| criterion | Story 5 happy: Given `coverage_binding.judge.enabled: true` and a spec with three grounded claims, when the step runs, then three fresh one-shot dispatches occur, each prompt containing exactly one criterion text and the cited task(s)' `Done when` checks and the judgement policy from `skills/coverage-binding/SKILL.md` | task-13 | covered | "performs exactly N provider invocations, each with `resume: false` and a prompt containing only that claim and the policy" | diff-local |
| criterion | Story 5 happy: Given a provider payload `{ verdict: 'asserts' }` for a claim, when the engine records it, then `.pipeline/coverage-binding.json` carries an engine-stamped entry with the feature slug, run identity, claim digest, criterion, task ids, the judged `Done when` checks, and the verdict | task-11, task-13 | covered | "The written envelope carries `slug`, `runId`, `status`, and per-claim `digest`, `criterion`, `taskIds`, `doneWhen`, and `verdict` fields" | diff-local |
| criterion | Story 5 happy: Given a persisted verdict whose claim digest equals the digest of the current (criterion, `Done when` checks) pair, when the step re-runs, then that claim is a cache hit, is not re-dispatched, and the envelope is still rewritten so the completion artifact is fresh for this run | task-13 | covered | "A digest cache hit is not re-dispatched and the envelope is still rewritten" | diff-local |
| criterion | Story 5 negative: Given a provider payload whose `verdict` is neither `asserts` nor `does-not-assert`, when the engine validates it, then the attempt is a typed infrastructure failure retried under the ordinary step retry ladder, no verdict is recorded for that claim, and no `needs-human` halt is written | task-14 | covered | "An out-of-vocabulary payload produces the typed error, no envelope verdict entry, and no halt marker" | diff-local |
| criterion | Story 5 negative: Given every model in the fallback ladder unavailable, when the step runs, then the step reports failed — never done — and the completion artifact is not written as a pass | task-14 | covered | "Ladder exhaustion returns `success: false` and the completion predicate remains unsatisfied" | diff-local |
| criterion | Story 5 negative: Given a judge prompt, when its content is inspected, then it contains no diff, no transcript, no stories prose beyond the criterion text, and instructs the judge not to read files | task-10, task-12 | covered | "Test (e) proves no diff, transcript, or Steps text is present on any claim" | diff-local |
| criterion | Story 5 negative: Given a `Done when` check edited after a verdict was persisted, when the step re-runs, then the claim's digest differs and it is re-dispatched | task-11, task-13 | covered | "changes on any `Done when` check edit" | diff-local |
| criterion | Story 6 happy: Given every claim judged `asserts`, when the step completes, then it is `done` and `build` becomes dispatchable | task-13 | covered | "All-`asserts` completes the step `done` with N envelope entries" | diff-local |
| criterion | Story 6 happy: Given one claim judged `does-not-assert` with `missingAssertion` "no check requires the coordinator to emit the five occurrences", when the step completes, then the step status is `refused` with kind `needs-human`, a `needs-human` halt record is written through `writeHaltMarker`, and the rendered halt lists the criterion, the bound task id, that task's actual `Done when` checks verbatim, and the `missingAssertion` text | task-15 | covered | "writes one `needs-human` halt record whose text includes the criterion, task id, the task's `Done when` checks verbatim, and the `missingAssertion`" | diff-local |
| criterion | Story 6 negative: Given a `does-not-assert` verdict, when the halt is written, then no plan task is appended and no decide-grant or route to `plan` is recorded | task-15 | covered | "The plan file is unchanged and no routing/grant record exists after the run" | diff-local |
| criterion | Story 6 negative: Given two failing claims, when the halt is written, then both are listed in one halt record, not two halts | task-15 | covered | "Two failing claims produce exactly one halt record listing both" | diff-local |
| criterion | Story 6 negative: Given the halt is cleared after the operator amends the plan and reseals, when the feature re-dispatches, then the resume clamp re-admits `coverage_binding` and the amended pair is re-judged rather than the stale verdict reused | task-16 | covered | "After halt clear and a `Done when` amendment, the step runs again, dispatches only the changed claim" | diff-local |
| criterion | Story 7 happy: Given the judge enabled and a claim citing a task whose plan block has no `Done when`, when the step runs, then no dispatch occurs for that claim, the envelope records it `not-applicable`, and the step completes `done` | task-10, task-13 | covered | "A `not-applicable` claim is never dispatched and does not block `done`" | diff-local |
| criterion | Story 7 negative: Given a spec mixing one legacy claim and one judgeable claim that returns `asserts`, when the step runs, then exactly one dispatch occurs and the step completes `done` | task-13 | covered | "A `not-applicable` claim is never dispatched and does not block `done`" | diff-local |
| criterion | Story 7 negative: Given a spec with zero criterion claims on any carrier, when the step runs, then it completes `done` with an envelope recording zero claims and no halt | task-10, task-13 | covered | "zero applicable claims completes `done` with an empty entries list" | diff-local |
| criterion | Story 8 happy: Given the judge enabled, when the step runs to completion, then one `coverage_binding_judged` per claim (carrying a verdict of `asserts`, `does-not-assert`, or `not-applicable`, a claim digest, and task ids) is emitted and appears in `.pipeline/events.jsonl`, and a failing verdict is reported through the existing `step_refused` and `loop_halt` events with `step: coverage_binding` | task-17 | covered | "A `coverage_binding_judged` event is observed in `.pipeline/events.jsonl` by the persister test" | diff-local |
| criterion | Story 8 negative: Given a sink-registry fixture omitting either of the two new event types, when the sink exhaustiveness test runs, then it fails naming the missing type | task-17 | covered | "the exhaustiveness test fails when either is omitted from a fixture" | diff-local |
| criterion | Story 8 negative: Given the disabled default, when the step runs, then exactly one `coverage_binding_disabled` event is emitted and no `coverage_binding_judged` event is emitted | task-9 | covered | "The disabled run emits exactly one `coverage_binding_disabled` event and no `coverage_binding_judged` event" | diff-local |
