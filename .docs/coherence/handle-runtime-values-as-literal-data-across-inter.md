# Coherence: Runtime values remain literal data

Date: 2026-09-06
Tier: M. Track: technical.

## Verdict

All four accepted stories, nine tasks and 19 exact criteria are covered and consistent. Two original intake outcomes require the narrowly recorded scope waiver: outcome-1 is broader than the approved surviving-site/input-grammar scope; outcome-3 names the removed bin/conduct assessment-state flow. The original staged outcome text is retained verbatim. These are not implementation gaps in the accepted stories, and the waiver does not waive their test proof.

## Mapping

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-2, story-3 | gap | outcome-1: surviving literal-data behaviors are covered, but the original universal verdict/step/status/multiline-trailer statement exceeds approved scope; retired bin/conduct paths are not restored and existing Git trailer grammar is preserved. |
| outcome | outcome-2 | story-1, story-2, story-3 | covered | Required lookup errors, configuration write errors, and summary processing errors surface through their caller-specific policies. |
| outcome | outcome-3 | | gap | outcome-3: /assess Step 7 save_state belonged to removed bin/conduct (7035ebbbc, #2052); operator explicitly approved its exclusion. No task falsely claims to restore or test it. |
| outcome | outcome-4 | story-1 | covered | Existing apostrophe-containing lookup values are compared literally; the rendered-hook proof distinguishes valid lookup from processing error. |
| outcome | outcome-5 | story-4 | covered | Direct Python source/heredoc and Node eval recurrence is rejected across the approved shipped/generated inventory, with failure tests and live integrity wiring. Arbitrary dynamic-shell dataflow is not claimed. |
| story | story-1 | task-1, task-2 | covered | Literal generated-hook lookup, compatibility, non-match and processing errors have separate scoped owners. |
| story | story-2 | task-3, task-4 | covered | Both permission and hook configuration helpers receive literal-path, merge/idempotency and truthful failure proof. |
| story | story-3 | task-5 | covered | Actual session-start script preserves counts/remaining context, warns on processing errors and stays quiet for absent state. |
| story | story-4 | task-6, task-7, task-8, task-9 | covered | Command-source and heredoc classification feeds real inventory scanning and repository integrity invocation. |
| task | task-1 | story-1 | covered | Owns generated-hook literal lookup and existing compatibility cases. |
| task | task-2 | story-1 | covered | Owns processing-error distinction through that generated hook. |
| task | task-3 | story-2 | covered | Owns permission helper integration and failure propagation. |
| task | task-4 | story-2 | covered | Owns hook helper integration and failure propagation. |
| task | task-5 | story-3 | covered | Owns actual session-start summary behavior. |
| task | task-10 | story-2 | covered | Records the installer-surface release waiver in the feature diff. |
| task | task-6 | story-4 | covered | Direct command-source lexical classification with safe and unsafe fixtures. |
| task | task-7 | story-4 | covered | Heredoc source classification with quoting, escaping and line handling. |
| task | task-8 | story-4 | covered | Automatic source/generated inventory and missing-input/loader/error coverage. |
| task | task-9 | story-4 | covered | Named repository integrity integration, not a terminal catch-all verification task. |
| criterion | Story 1 happy: S1.1: Given a local repository whose path contains spaces, single/double quotes, backslashes, or interpreter-looking text, and a status file containing a matching supplied single-line Task trailer value, when the generated commit-msg hook validates it, then lookup succeeds, without a syntax failure or unintended sentinel side effect. Exercise the trailer variants separately from repository-path variants; preserve Git's existing trailer parsing rather than defining multiline trailer values. | task-1 | covered | "The rendered commit-msg integration accepts matching literal trailer/path variants (spaces, single/double quotes, backslashes, interpreter-looking text), rejects absent literal IDs, and leaves all harmless execution sentinels absent." | diff-local |
| criterion | Story 1 happy: S1.2: Given a numeric ID or its string equivalent in a valid task-status file, when the matching trailer is validated, then the same match succeeds. Given an existing exemption or a missing trailer/status file, when the hook runs, then its existing pass-through behavior remains intact. | task-1 | covered | "The same rendered hook accepts numeric and string-equivalent IDs; existing merge/amend/rebase/engine exemptions, missing trailer/status-file pass-through, task-N/unknown-ID rejection, advisory scope-check behavior, and chained-hook behavior retain their observed results." | diff-local |
| criterion | Story 1 negative: S1.N1 (S1.1): Given an absent task ID containing quotes, backslashes, or interpreter-looking text, when the hook validates it, then it rejects as an ordinary non-match and no sentinel side effect occurs. The ID must not become a different existing ID through source interpretation. | task-1 | covered | "The rendered commit-msg integration accepts matching literal trailer/path variants (spaces, single/double quotes, backslashes, interpreter-looking text), rejects absent literal IDs, and leaves all harmless execution sentinels absent." | diff-local |
| criterion | Story 1 negative: S1.N2 (S1.1): Given a present status file that is malformed or unreadable, or an unavailable/failing Node interpreter, when lookup is required, then the hook rejects with a contextual processing-error diagnostic distinguishable from an ordinary non-match; it never prints a successful match or hides the error behind a not-found fallback. | task-2 | covered | "Rendered-hook integration rejects malformed JSON, required-read failure, missing Node, and failing Node with a contextual processing-error diagnostic rather than the ordinary ID-not-found message." | diff-local |
| criterion | Story 1 negative: S1.N3 (S1.2): Given a task-N trailer or a genuinely absent numeric/string ID, when validation is required, then existing rejection behavior remains. Missing evidence trailers do not become a new rejection condition, and advisory scope-check results do not become blockers. | task-1 | covered | "The same rendered hook accepts numeric and string-equivalent IDs; existing merge/amend/rebase/engine exemptions, missing trailer/status-file pass-through, task-N/unknown-ID rejection, advisory scope-check behavior, and chained-hook behavior retain their observed results." | diff-local |
| criterion | Story 2 happy: S2.1: Given settings, temporary-file, and harness-directory paths containing quotes, backslashes, spaces, newlines, or interpreter-looking text supported by the filesystem, when either configuration helper runs, then it reads/writes the exact intended paths and preserves literal hook-directory strings in settings. No unintended sentinel side effect occurs. This criterion covers configuration serialization, not new support for every downstream shell command-path grammar. | task-3, task-4 | covered | "The real configure_hooks integration writes exact settings and hook-directory strings for quotes, backslashes, spaces, newlines, and interpreter-looking text; repeated merges preserve unrelated/custom hooks without duplicate managed entries and without sentinel execution." | diff-local |
| criterion | Story 2 happy: S2.2: Given pre-existing unrelated settings and custom hook/permission entries, when the helpers configure settings and repeat the same operation, then unrelated values remain unchanged and managed entries are not duplicated. | task-3, task-4 | covered | "The real configure_hooks integration writes exact settings and hook-directory strings for quotes, backslashes, spaces, newlines, and interpreter-looking text; repeated merges preserve unrelated/custom hooks without duplicate managed entries and without sentinel execution." | diff-local |
| criterion | Story 2 negative: S2.N1 (S2.1): Given an unavailable/failing Python interpreter, unreadable input, malformed settings JSON, or unwritable target, when either helper runs, then that helper returns nonzero and reports which configuration operation failed, with no success message. The existing installation caller continues with its incomplete-configuration warning policy. | task-3, task-4 | covered | "Missing/failing Python and read/parse/write failure cases return nonzero with contextual failure output and no success report; malformed input remains byte-identical and the existing installation caller warns and continues." | diff-local |
| criterion | Story 2 negative: S2.N2 (S2.2): Given malformed settings that cannot be parsed, when configuration fails, then the original settings bytes remain unchanged rather than being replaced with a new default document. For write failures, no atomic rollback guarantee beyond current behavior is introduced; the failure must be surfaced. | task-3, task-4 | covered | "Missing/failing Python and read/parse/write failure cases return nonzero with contextual failure output and no success report; malformed input remains byte-identical and the existing installation caller warns and continues." | diff-local |
| criterion | Story 2 negative: S2.N3 (S2.1/S2.2): Given a valid path containing text that resembles Python or shell operations, when either helper runs, then only its intended configuration effects occur and existing unrelated settings survive. | task-3, task-4 | covered | "The real configure_hooks integration writes exact settings and hook-directory strings for quotes, backslashes, spaces, newlines, and interpreter-looking text; repeated merges preserve unrelated/custom hooks without duplicate managed entries and without sentinel execution." | diff-local |
| criterion | Story 3 happy: S3.1: Given valid existing pipeline state with string-valued steps plus unrelated non-string fields, when the actual session-start hook runs, then it reports the same done/skipped count over string-valued steps and continues the remaining context output. | task-5 | covered | "The actual session-start hook reports the expected done/skipped count over string-valued state, treats quoted/interpreter-looking JSON content literally without sentinel execution or additional state mutation, and completes its remaining context output." | diff-local |
| criterion | Story 3 happy: S3.2: Given no pipeline state file, when the hook runs, then it omits the pipeline summary quietly and continues normal context output. | task-5 | covered | "Absent state quietly omits the summary; malformed/unreadable state or missing/failing Python produces a contextual stderr warning, no fabricated success summary, and continued output with the existing non-blocking summary exit policy." | diff-local |
| criterion | Story 3 negative: S3.N1 (S3.1): Given malformed/unreadable state or an unavailable/failing Python interpreter while state is present, when the hook runs, then it emits a contextual stderr warning, emits no fabricated successful summary, continues its remaining output, and exits according to its existing non-blocking summary policy. | task-5 | covered | "Absent state quietly omits the summary; malformed/unreadable state or missing/failing Python produces a contextual stderr warning, no fabricated success summary, and continued output with the existing non-blocking summary exit policy." | diff-local |
| criterion | Story 3 negative: S3.N2 (S3.1/S3.2): Given interpreter-looking or quoted text in state keys/values, when the hook reads the JSON, then it only contributes according to the existing string-value/count rules and causes no unintended execution or additional state mutation. An absent file is not misreported as a parsing error. | task-5 | covered | "The actual session-start hook reports the expected done/skipped count over string-valued state, treats quoted/interpreter-looking JSON content literally without sentinel execution or additional state mutation, and completes its remaining context output." | diff-local |
| criterion | Story 4 happy: S4.1: Given shipped scripts and rendered hook assets using fixed Python or Node source with separately supplied data, when the repository interpreter-source validation entrypoint runs, then it succeeds. Quoted heredocs, literal dollar characters protected from shell expansion, multiline constant source, and argv/stdin/environment data transport are accepted. | task-6, task-7, task-9 | covered | "Static source with separately passed argv/stdin/environment data, protected literal dollar characters, comments and safe multiline words passes the classifier; malformed recognized source is reported, and no fixture code is executed." | diff-local |
| criterion | Story 4 happy: S4.2: Given a new shipped shell script or new rendered hook asset in the declared inventory, when validation runs, then that input is included automatically or an unclassified generated export makes the inventory check fail visibly; it cannot be silently omitted. Scope includes bin entrypoints and shell libraries, hooks, and rendered git/session hook assets; test fixtures and documentation examples are not production inputs. | task-8, task-9 | covered | "The inventory entrypoint discovers newly added shipped bin/hook scripts and shell libraries plus all rendered string exports from both hook-asset modules; unsafe additions fail with source/location while safe additions pass." | diff-local |
| criterion | Story 4 negative: S4.N1 (S4.1): Given a direct Python -c/heredoc or Node -e/--eval source containing shell parameter expansion, command substitution, or backticks, including multiline variants, when validation runs, then it exits nonzero and identifies the source asset and location. Unsafe fixture text is inspected without being executed. | task-6, task-7 | covered | "Classifier unit fixtures reject shell parameter, command-substitution, and backtick expansion inside direct Python -c and Node -e/--eval source, including multiline words, escaped newlines, and --eval=source, with source name and line." | diff-local |
| criterion | Story 4 negative: S4.N2 (S4.2): Given a newly added unsafe shipped script or unsafe rendered hook, when validation runs through its real entrypoint, then it rejects that addition. An empty inventory, required input read failure, render failure, or unclassified generated hook export is a validation failure, not success over partial inputs. | task-8, task-9 | covered | "Required file-read failure, empty shipped/generated inventory, generated-module load failure, and an unclassified generated export each produce nonzero validation status rather than success over a partial scan." | diff-local |
| criterion | Story 4 negative: S4.N3 (S4.1/S4.2): Given an unsafe specimen only in a documentation example or test fixture, when the production inventory scan runs, then that out-of-scope specimen does not fail the production scan; it still remains available as explicit checker test input. | task-8 | covered | "Documentation and test-only unsafe specimens do not enter the production inventory; explicit classifier fixtures still inspect them without execution." | diff-local |

Criterion rows use six cells: row class, exact criterion, cited task IDs, verdict, exact Done-when quote, and disposition. The legacy mapping rows above retain their five-cell schema.

## Semantic review

Every quoted completion check was checked against the actual plan's Done-when block. Paired helper/source-form criteria cite both applicable tasks; the quoted check is drawn from one real cited task and its complementary checks supply the remaining enumerated behavior. Task 9’s live gate wiring supplies enforcement rather than a source-only classifier with no caller.

In both directions, literal transport remains compatible with numeric/string IDs, custom settings preservation, and existing input grammars. Error visibility does not turn an advisory summary or scope result into a new blocker: required task lookup rejects processing errors, configuration helpers return failure to existing warning callers, and summary errors warn and continue. No task depends on #2266, external branch state, or a third-party service to satisfy a criterion. All criterion fixtures operate at changed local boundaries, so their disposition is diff-local.

No product FR rows apply to this technical track. No ADR row is required because this spec introduces no non-deleted ADR file; the architecture review reuses existing governing decisions without changing them. The approved component diagram still represents the planned argument flow and repository checker, so no architecture amendment is needed.

## Verify-Claims Ledger

Verified: four stories and nine tasks exist; 19 exact Given/When/Then criteria were extracted by the repository parser; criterion quote and Done-when validation passed. Outcome-1 and outcome-3 are explicitly marked gap, not given an invented affirmative verdict. The operator approved the bounded scope and removed-launcher exclusion before stories and plan acceptance. Confidence in semantic consistency: 95%, grounded in the cited criterion and task text. No unconfirmed load-bearing assumptions remain; the recorded waiver carries the approved scope boundary rather than inventing work for retired code.
