# Coherence: Rebase reopens completed repair tasks against stale boundaries

Date: 2026-09-18
Track: technical
Tier: M
Verdict: covered

## Outcome, story and task mapping

Outcome quotes are the staged intake projection verbatim. No product FR layer applies; the change-set ADR layer is the amended rebase-evidence-stamp-translation ADR.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote / Notes |
| --- | --- | --- | --- | --- |
| outcome | outcome-1 | story-1, story-2 | covered | After a supported rebase rewrites, drops, or absorbs a repair-boundary commit without losing the completed repair work, completed repair tasks remain complete and the daemon does not reopen them solely because the old SHA is absent from the new history. |
| outcome | outcome-2 | story-2, story-3 | covered | Post-repair task evidence remains bounded to commits after the equivalent rebased repair boundary. |
| outcome | outcome-3 | story-2, story-5 | covered | A missing, invalid, unmapped, or unreachable boundary still fails closed and cannot admit historical evidence from before the repair. |
| outcome | outcome-4 | story-1, story-2 | covered | Repeatedly resuming either reproduced feature after a successful rebase does not emit the same `repair boundary ... is not an ancestor of HEAD` retry. |
| story | story-1 | task-3, task-8 | covered | Direct-hit translation at the call site and post-rebase resolution proof. |
| story | story-2 | task-2, task-8 | covered | Pure successor rule with predecessor, unreachable, and merge-commit refusals; resolution proof. |
| story | story-3 | task-1, task-4 | covered | Store method through the engine-state seam; malformed and absent sections are no-ops. |
| story | story-4 | task-5, task-6, task-7 | covered | Event type, sink row, per-rewrite emission, residue obligation ids. |
| story | story-5 | task-9 | covered | Read-path fallback pinned with tests only; no successor search at read time. |
| task | task-1 | story-3 | covered | Add a baseline-rewrite method to the repair-obligation store. |
| task | task-2 | story-2 | covered | Pure successor selection for residue boundaries. |
| task | task-3 | story-1 | covered | Translate obligation baselines inside translateAfterRebase. |
| task | task-4 | story-3 | covered | Malformed or absent obligation section does not stop the rest of translation. |
| task | task-5 | story-4 | covered | repair_boundary_translated event type and sink row. |
| task | task-6 | story-4 | covered | Emit one translation event per rewritten obligation. |
| task | task-7 | story-4 | covered | Residue entries cite the obligations left unchanged. |
| task | task-8 | story-1, story-2 | covered | Task progress resolves through translated boundaries. |
| task | task-9 | story-5 | covered | Read-path fallback stays a fallback with no successor search. |
| adr | adr-2026-07-12-rebase-evidence-stamp-translation | story-1, story-2, story-4, story-5 | covered | Existing ADR amended by #2462 (additive D6–D9 note appended); stories carry the changed decisions. |

## Criterion mapping

Quotes are exact completion-check text from the cited task.

| Row class | Criterion | Task id(s) | Verdict | Done when quote | Disposition |
| --- | --- | --- | --- | --- | --- |
| criterion | Story 1 happy: Given an open obligation with `baseline.head` equal to a pre-rebase branch commit whose patch-id matches a post-rebase commit, when the engine rebase completes and translation runs, then the persisted obligation's `baseline.head` equals that post-rebase commit sha. | 3 | covered | "After `performRebase` with the default `translateAfterRebase`, the persisted obligation whose head had a patch-id match holds the post-image sha, asserted by reading `engine-state.json` in rebase-translate-acceptance.test.ts." | diff-local |
| criterion | Story 1 happy: Given the same translated obligation, when task progress evaluates its open tasks, then the evidence range is `newHead..HEAD` and a `Task:` trailer in that range resolves the task without any `repair boundary ... is not an ancestor of HEAD` reason. | 8 | covered | "task-progress.test.ts asserts a direct-translated obligation resolves from a post-boundary `Task:` trailer with no `unavailableReasons` entry after a real rebase." | diff-local |
| criterion | Story 1 negative: Given an obligation whose `baseline.head` is not a key in the rewrite map and is not in `onto..origHead`, when translation runs, then its `baseline.head` is byte-identical to the pre-translation value. | 3 | covered | "After `performRebase` with the default `translateAfterRebase`, the persisted obligation whose head had a patch-id match holds the post-image sha, asserted by reading `engine-state.json` in rebase-translate-acceptance.test.ts." | diff-local |
| criterion | Story 1 negative: Given a translated obligation, when task progress evaluates a task whose only `Task:` trailer is on a commit at or before the new boundary, then the task remains unresolved with the existing no-current-trailer outcome. | 8 | covered | "task-progress.test.ts asserts a direct-translated obligation resolves from a post-boundary `Task:` trailer with no `unavailableReasons` entry after a real rebase." | diff-local |
| criterion | Story 2 happy: Given an open obligation whose `baseline.head` is a residue commit (no patch-id match) and at least one later pre-image commit in `onto..origHead` is a rewrite-map key, when translation runs, then `baseline.head` equals the post-image of the earliest such later commit in first-parent order. | 3 | covered | "After `performRebase` with the default `translateAfterRebase`, an obligation whose head is a residue commit with a later first-parent pre-image commit in the rewrite map has its persisted `baseline.head` assigned the post-image of the earliest such commit, asserted by reading `engine-state.json` in rebase-translate-acceptance.test.ts." | diff-local |
| criterion | Story 2 happy: Given that successor-translated obligation, when task progress evaluates a task with a `Task:` trailer on a commit after the new boundary, then the task resolves. | 8 | covered | "task-progress.test.ts asserts a direct-translated obligation resolves from a post-boundary `Task:` trailer with no `unavailableReasons` entry after a real rebase." | diff-local |
| criterion | Story 2 negative: Given a residue boundary where the only surviving map keys are commits at or before the boundary in first-parent order, when translation runs, then `baseline.head` is unchanged and a later task-progress check returns the existing `repair boundary <sha> is not an ancestor of HEAD` reason. | 2 | covered | "`selectRepairBoundaryTranslation` returns `successor` with the post-image of the earliest first-parent commit strictly after the boundary, as asserted by the squashed-boundary fixture in rebase-translate.test.ts." | diff-local |
| criterion | Story 2 negative: Given a residue boundary whose successor candidate exists in the map but the mapped sha is not reachable from the post-rebase `HEAD`, when translation runs, then `baseline.head` is unchanged. | 2 | covered | "`selectRepairBoundaryTranslation` returns `successor` with the post-image of the earliest first-parent commit strictly after the boundary, as asserted by the squashed-boundary fixture in rebase-translate.test.ts." | diff-local |
| criterion | Story 2 negative: Given a pre-image branch containing a merge commit after the residue boundary, when translation runs, then the successor is chosen along the first-parent chain only and never a commit reachable solely through the merge's second parent. | 2 | covered | "`selectRepairBoundaryTranslation` returns `successor` with the post-image of the earliest first-parent commit strictly after the boundary, as asserted by the squashed-boundary fixture in rebase-translate.test.ts." | diff-local |
| criterion | Story 3 happy: Given an engine-state file with two obligations, `activePlanPath`, and appended-task bookkeeping, when translation rewrites one obligation, then every other field of the file is byte-identical and the other obligation is unchanged. | 1 | covered | "`RepairObligationStore.rewriteBaselines` changes exactly the `baseline.head` values named in its input map and the before/after deep-equal test on the whole engine-state JSON shows no other difference." | diff-local |
| criterion | Story 3 happy: Given an obligation with `baseline.tree`, `baseline.resolvedTaskIds`, `settlement`, and per-task statuses, when its `baseline.head` is translated, then those fields are unchanged. | 1 | covered | "`RepairObligationStore.rewriteBaselines` changes exactly the `baseline.head` values named in its input map and the before/after deep-equal test on the whole engine-state JSON shows no other difference." | diff-local |
| criterion | Story 3 negative: Given a malformed `repairObligations` section, when translation runs, then the engine-state file is not written, the rest of `translateAfterRebase` (task-evidence, task-status, seal rotation) still completes, and a later task-progress check reports the existing malformed-state reason. | 4 | covered | "With a malformed `repairObligations` section, `translateAfterRebase` leaves `engine-state.json` byte-identical and still rewrites task-evidence.json and task-status.json, asserted in one acceptance test." | diff-local |
| criterion | Story 3 negative: Given the engine-state store's `update` mutator is invoked, when the mutator returns, then no code path in translation has opened `engine-state.json` for direct write outside that store. | 1 | covered | "`RepairObligationStore.rewriteBaselines` changes exactly the `baseline.head` values named in its input map and the before/after deep-equal test on the whole engine-state JSON shows no other difference." | diff-local |
| criterion | Story 3 negative: Given an engine-state file that does not exist, when translation runs, then no file is created and translation completes without error. | 1 | covered | "`RepairObligationStore.rewriteBaselines` changes exactly the `baseline.head` values named in its input map and the before/after deep-equal test on the whole engine-state JSON shows no other difference." | diff-local |
| criterion | Story 4 happy: Given one obligation translated directly and one by successor, when translation runs, then two `repair_boundary_translated` events are emitted, each carrying the obligation id, old sha, new sha, and `rule` equal to `direct` or `successor` respectively. | 6 | covered | "The capturing emitter test sees exactly one `repair_boundary_translated` event per rewritten obligation, with `rule` equal to `direct` for a map-key head and `successor` for a residue head, and correct `from`/`to` shas." | diff-local |
| criterion | Story 4 happy: Given a residue boundary left unchanged, when translation runs, then the `rebase_citation_residue` event's entry for that sha lists the obligation id under `citingObligationIds`. | 7 | covered | "`ResidueEntry` carries `citingObligationIds`, and the unchanged-boundary fixture asserts the residue event entry and `rebase-residue.json` both list the obligation id for that sha." | diff-local |
| criterion | Story 4 negative: Given no obligation needed translation, when translation runs, then zero `repair_boundary_translated` events are emitted. | 6 | covered | "The capturing emitter test sees exactly one `repair_boundary_translated` event per rewritten obligation, with `rule` equal to `direct` for a map-key head and `successor` for a residue head, and correct `from`/`to` shas." | diff-local |
| criterion | Story 4 negative: Given translation ran without an event emitter injected, when it completes, then the store is still rewritten and no error is thrown. | 6 | covered | "The capturing emitter test sees exactly one `repair_boundary_translated` event per rewritten obligation, with `rule` equal to `direct` for a map-key head and `successor` for a residue head, and correct `from`/`to` shas." | diff-local |
| criterion | Story 4 negative: Given the `EVENT_SINKS` table, when `repair_boundary_translated` is looked up, then a row exists with `persist: true`, and the `ConductorEvent` union type-checks the payload fields. | 5 | covered | "`EVENT_SINKS.repair_boundary_translated` is present with `persist: true`, asserted by event-sinks.test.ts, and the exhaustiveness check in that file still passes." | diff-local |
| criterion | Story 5 happy: Given an obligation admitted before this change whose `baseline.head` is a direct rewrite-map key but the store was never rewritten, when task progress evaluates it, then the read path follows `.pipeline/rebase-rewrites.json` and resolves the task as today. | 9 | covered | "autoheal.test.ts asserts the residue-boundary case returns `unavailable` with the existing non-ancestor reason and the git-runner spy recorded zero `rev-list` calls." | diff-local |
| criterion | Story 5 negative: Given an obligation whose `baseline.head` is residue and the store was not rewritten, when task progress evaluates it, then the read path returns `repair boundary <sha> is not an ancestor of HEAD` and does not attempt any successor search. | 9 | covered | "autoheal.test.ts asserts the residue-boundary case returns `unavailable` with the existing non-ancestor reason and the git-runner spy recorded zero `rev-list` calls." | diff-local |
| criterion | Story 5 negative: Given a branch rebased by hand with no `.pipeline/rebase-rewrites.json`, when task progress evaluates an open obligation with a non-ancestor boundary, then the result is `unavailable` with the existing reason and no commits are admitted. | 9 | covered | "autoheal.test.ts asserts the residue-boundary case returns `unavailable` with the existing non-ancestor reason and the git-runner spy recorded zero `rev-list` calls." | diff-local |
| criterion | Story 5 negative: Given a `rebase-rewrites.json` that maps the boundary to a sha not reachable from `HEAD`, when task progress evaluates it, then the result is `unavailable`. | 9 | covered | "autoheal.test.ts asserts the residue-boundary case returns `unavailable` with the existing non-ancestor reason and the git-runner spy recorded zero `rev-list` calls." | diff-local |
