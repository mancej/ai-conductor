# Coherence Mapping: enable-single-repo-daemon-concurrency-un-clamp-the

**Date:** 2026-08-27
**Track:** technical (no PRD — the fr row class is omitted as not applicable)
**Sources:** staged intake outcomes (Source-Ref jstoup111/ai-conductor#568), the accepted stories, the 25-task plan, and the nine non-deleted change-set ADR files.

| Row class | Cited id / criterion | Counterpart / task id(s) | Verdict | Notes / verbatim quote | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-1 | covered | Config/flag-gated N with default 1; both-features-progress proof cited by Story 1's criteria. |
| outcome | outcome-2 | story-3, story-4, story-5 | covered | Isolation of run-state, park markers, and main checkout across the seam, claims, and N=2 integration stories. |
| outcome | outcome-3 | story-6, story-7 | covered | Non-starving refresh/ff/sweeps and drain-then-act restarts. |
| outcome | outcome-4 | story-9 | covered | Slug attribution incl. the residual tee/global-subscriber sinks. |
| outcome | outcome-5 | story-2 | covered | Byte-for-byte serial equivalence at the default. |
| story | story-1 | task-9, task-10 | covered | Config key + precedence/un-clamp tasks cite Story 1. |
| story | story-2 | task-11, task-25 | covered | Scheduler trace equivalence and full-suite serial pin. |
| story | story-3 | task-1, task-2, task-6, task-8 | covered | WorkOrder contract, validation, pinned materialization, executor extraction. |
| story | story-4 | task-3, task-4, task-5 | covered | Claims interface, single-authority wiring, park-guarded claim path. |
| story | story-5 | task-20, task-21, task-22 | covered | Lifecycle serialization, claim-liveness refusals, N=2 isolation proof. |
| story | story-6 | task-7, task-12, task-13 | covered | Pin-aware setup marker, busy refresh/sweeps/rekick, leak-refusal logging. |
| story | story-7 | task-14, task-15, task-16, task-17 | covered | Drain happy path, drain negatives, pause/episode gates, SIGTERM scaling. |
| story | story-8 | task-18, task-19 | covered | Live-boundary coordinator and fail-closed deferral. |
| story | story-9 | task-23 | covered | Central slug stamping + sink declarations. |
| story | story-10 | task-24 | covered | Legacy env-branch refusal guard. |
| task | task-1 | story-3 | covered | WorkOrder type with document manifest. |
| task | task-2 | story-3 | covered | Fail-closed order validation. |
| task | task-3 | story-4 | covered | WorkClaims interface, in-memory impl. |
| task | task-4 | story-4 | covered | Pool consumes claims; single dispatch authority. |
| task | task-5 | story-4 | covered | Park guard + enumeration test on the claim path. |
| task | task-6 | story-3 | covered | Pinned-base worktree materialization. |
| task | task-7 | story-6 | covered | Pin-aware setup-once marker. |
| task | task-8 | story-3 | covered | FeatureExecutor extraction; root-free executor path. |
| task | task-9 | story-1 | covered | daemon_concurrency key, fail-loud validation, registry row. |
| task | task-10 | story-1 | covered | Flag-over-config resolution replaces the clamp. |
| task | task-11 | story-2 | covered | Maintenance scheduler with N=1 trace equivalence. |
| task | task-12 | story-6 | covered | Busy refresh/ff, busy sweeps, rekick scoping. |
| task | task-13 | story-6 | covered | Multi-branch leak refusal logging. |
| task | task-14 | story-7 | covered | Drain mode + drained-boundary restart. |
| task | task-15 | story-7 | covered | Drain negative paths. |
| task | task-16 | story-7 | covered | Pause/episode gates on claims and restarts. |
| task | task-17 | story-7 | covered | SIGTERM force-release scaling. |
| task | task-18 | story-8 | covered | Live-boundary coordinator orderings. |
| task | task-19 | story-8 | covered | Unproven-containment deferral; fail-closed halts kept. |
| task | task-20 | story-5 | covered | Single-flight worktree lifecycle. |
| task | task-21 | story-5 | covered | Claim-liveness removal refusals. |
| task | task-22 | story-5 | covered | N=2 isolation integration proof. |
| task | task-23 | story-9 | covered | Central slug attribution for residual sinks. |
| task | task-24 | story-10 | covered | Legacy env-branch concurrency refusal. |
| task | task-25 | story-2 | covered | Serial-equivalence pin + OTel N=2 parity case. |
| adr | adr-2026-08-27-daemon-dispatcher-executor-seam | story-1, story-3, story-4, story-6, story-7, story-8, story-9, story-10 | covered | D1–D8 implemented across the seam/claims/policy/boundary/attribution stories. |
| adr | adr-2026-06-29-daemon-supervisor-port-and-attachable-hosting | story-1, story-2 | covered | D4 amendment honored: default stays serial; opt-in un-clamp. |
| adr | adr-2026-07-03-daemon-auto-restart-stale-engine | story-7 | covered | Drained-boundary generalization preserves the mid-build invariant. |
| adr | adr-2026-07-03-harness-daemon-profile | story-7 | covered | Go-live boundary remains install-or-drained-restart. |
| adr | adr-2026-07-04-pending-restart-queue | story-7 | covered | Queued restart fires at the drained pool. |
| adr | adr-2026-07-04-respawn-in-place-restart | story-7 | covered | Respawn gating at drained/paused preserved. |
| adr | adr-2026-07-07-single-generation-stale-respawn | story-7 | covered | One-shot-per-firing bound preserved under drain. |
| adr | adr-2026-07-22-origin-refresh-before-engine-rebuild | story-6 | covered | Busy fetch under pinning; rebuild half stays drained-only; rate limit kept. |
| adr | adr-2026-08-26-setup-once-per-worktree-marker | story-6 | covered | Base-equality predicate keyed on the order's pinned SHA. |
| criterion | Story 1 happy: Given `daemon_concurrency: 2` in the repo's config and no `--concurrency` flag, when the daemon starts, then the pool's effective concurrency is 2 and the startup log line reports the resolved value and its source. | task-10 | covered | "the startup log names the resolved value and its source" | diff-local |
| criterion | Story 1 happy: Given `daemon_concurrency: 2` in config and `--concurrency 3` passed explicitly, when the daemon starts, then the effective concurrency is 3 (explicit flag wins over config). | task-10 | covered | "explicit `--concurrency 3` + config 2 → 3" | diff-local |
| criterion | Story 1 happy: Given no config key and no flag, when the daemon starts, then the effective concurrency is 1. | task-10 | covered | "neither → 1" | diff-local |
| criterion | Story 1 happy: Given two independent eligible features and effective concurrency 2, when the daemon dispatches, then both features are claimed and progress concurrently within one daemon process. | task-22 | covered | "two features build concurrently to completion" | diff-local |
| criterion | Story 1 negative: Given `daemon_concurrency: 0` (or a negative or non-integer value), when config is validated, then validation rejects it with a message naming the key and the accepted range, and the daemon does not start with a silently clamped value. | task-9 | covered | "surfaces the validation error and exits non-zero rather than clamping" | diff-local |
| criterion | Story 1 negative: Given `daemon_concurrency` present but unregistered in the config-key consumer registry, when the registry exhaustiveness test runs, then the test fails naming the key (adr-2026-08-26 D4). | task-9 | covered | "consumer-registry exhaustiveness test passes with the new row" | diff-local |
| criterion | Story 1 negative: Given a supervised daemon launched via tmux with no flag, when it reads config, then the config key alone raises concurrency (the tmux launch path passes no flag today, so config is the only operator lever there). | task-10 | covered | "no flag + config 2 → 2" | diff-local |
| criterion | Story 2 happy: Given effective concurrency 1, when the daemon runs a backlog of features, then dispatch order, gate evaluation points (refresh, sweeps, stale-engine, restart), and log output shape match the pre-change daemon. | task-25 | covered | "match the recorded pre-change trace" | diff-local |
| criterion | Story 2 happy: Given the existing daemon test corpus, when it runs against the refactored daemon at concurrency 1, then it passes without semantic edits (mechanical renames allowed). | task-25 | covered | "the full daemon suite is green" | diff-local |
| criterion | Story 2 negative: Given effective concurrency 1 and one feature in flight, when a stale engine is detected or a restart is queued, then it fires only after that feature finishes — exactly the pre-change idle-boundary timing. | task-14 | covered | "rebuild+restart fires exactly once at the drained boundary" | diff-local |
| criterion | Story 2 negative: Given effective concurrency 1, when discovery would refresh mid-build, then no fetch or root fast-forward occurs while the single feature is in flight (pre-change quiescent behavior preserved at the default). | task-11 | covered | "at N=1 every policy degenerates to the old `inFlight.size === 0` condition" | diff-local |
| criterion | Story 3 happy: Given an eligible feature, when the dispatcher builds its work order, then the order contains repo identity, feature slug, pinned base SHA, and a manifest of the spec's governing documents as refs plus content hashes, and the whole order survives JSON round-tripping without loss. | task-1 | covered | "JSON round-trip equality for a fully populated order" | diff-local |
| criterion | Story 3 happy: Given a work order, when the executor materializes its workspace, then the worktree is cut from the order's pinned base SHA and the manifest's documents resolve to content matching their recorded hashes. | task-6 | covered | "a worktree whose merge-base/HEAD start point is S1, not the tip" | diff-local |
| criterion | Story 3 negative: Given a manifest entry whose resolved content no longer matches its recorded hash, when the executor validates the order, then the dispatch fails closed for that feature with an error naming the mismatched document, and no build starts. | task-2 | covered | "the error names the offending ref or SHA" | diff-local |
| criterion | Story 3 negative: Given a work order referencing a base SHA absent from the local repository, when the executor materializes the workspace, then the dispatch fails closed with an error naming the missing SHA rather than silently falling back to the branch tip. | task-2 | covered | "makes workspace materialization reject, naming the SHA" | diff-local |
| criterion | Story 3 negative: Given the executor runs a build, when any code on the executor path resolves feature run-state or artifacts, then it does so from the workspace and the work order, never from a persisted manifest file acting as artifact-resolution authority (adr-2026-07-28 D3 preserved). | task-8 | covered | "accepts exactly a WorkOrder (plus injected deps)" | diff-local |
| criterion | Story 4 happy: Given two open slots and one eligible feature, when the fill loop runs twice, then the feature is claimed once, the second pass observes the claim, and exactly one executor runs it. | task-4 | covered | "two fill passes over one eligible feature dispatch it exactly once" | diff-local |
| criterion | Story 4 happy: Given a finished executor, when its result is collected, then the claim is released and the feature's terminal handling (processed marker, park, halt) proceeds exactly as today. | task-4 | covered | "terminal handling (processed/park/halt paths) is byte-identical" | diff-local |
| criterion | Story 4 negative: Given a feature with an operator park marker, when the dispatcher's claim path evaluates it, then the same `isOperatorParked` predicate consulted by every existing dispatch path refuses the claim before any build-start side effect (adr-2026-07-13 D2), and the entry-point enumeration test covers the claim path. | task-5 | covered | "a parked slug produces no claim, no worktree, and no dispatch" | diff-local |
| criterion | Story 4 negative: Given a feature already claimed by a running executor, when a halt-clear wake or re-kick sweep makes it look eligible, then `pickEligible`'s existing gates plus the claim registry refuse a second dispatch (adr-2026-07-04-event-driven-halt-clear-wake D3 — no second authority). | task-4 | covered | "exactly-once dispatch across repeated fill/collect cycles" | diff-local |
| criterion | Story 4 negative: Given the daemon restarts mid-build, when it comes back up, then in-memory claims are gone by design and the backlog re-derives work from durable state without duplicate or lost features (in-flight worktrees resume via the existing re-dispatch path). | task-4 | covered | "a fresh claims registry plus backlog re-derivation dispatches every unfinished feature exactly once" | diff-local |
| criterion | Story 5 happy: Given two features building concurrently, when both complete, then each worktree's `.pipeline/` (task-status, evidence sidecar, events.jsonl) contains only its own feature's records and the root checkout's tracked and untracked state is unchanged by either build. | task-22 | covered | "contains only its own feature's records" | diff-local |
| criterion | Story 5 happy: Given two features building concurrently, when one auto-parks, then its park marker lands in the main checkout's `.daemon/parked/<its-slug>` and the other feature's dispatch state is unaffected. | task-22 | covered | "writes its marker under the main checkout's park directory" | diff-local |
| criterion | Story 5 negative: Given two executors running, when one halts with a HALT marker, then the marker is written only under its own worktree's `.pipeline/` and the sibling build continues to completion. | task-22 | covered | "lands only in its own worktree while the sibling completes" | diff-local |
| criterion | Story 5 negative: Given worktree lifecycle operations for two slugs racing (add/remove), when they execute, then the dispatcher serializes them against the shared `.git` so neither fails with a lock error nor prunes the other's half-registered worktree. | task-20 | covered | "execute serially through one dispatcher-owned queue" | diff-local |
| criterion | Story 5 negative: Given a provider scratch sweep at a dispatch boundary, when other executors are live, then their leased scratch directories are not reclaimed (owner lease respected at N>1). | task-22 | covered | "reclaims neither live executor's leased scratch directory" | diff-local |
| criterion | Story 5 negative: Given any worktree-removal predicate (shipped-record reap, resolution-worktree lifecycle), when it evaluates a slug with an active work claim, then the claim registry answers "genuinely active" and the removal is refused with a logged reason. | task-21 | covered | "refuse to remove a worktree whose slug holds an active work claim" | diff-local |
| criterion | Story 6 happy: Given one executor busy and one slot free, when a spec merges to origin, then a refresh (fetch + root fast-forward) occurs within the poll interval, discovery finds the new spec, and the free slot dispatches it while the first build continues undisturbed. | task-12 | covered | "dispatched into the free slot while the first build's pinned worktree is untouched" | diff-local |
| criterion | Story 6 happy: Given all slots busy, when the sweep timer elapses, then `sweepBestEffort` runs, skipping in-flight slugs via its existing `isFeatureInFlight` predicate. | task-12 | covered | "the periodic sweep runs on its timer with all slots occupied, skipping in-flight slugs" | diff-local |
| criterion | Story 6 happy: Given a root fast-forward while executors are busy, when in-flight builds continue, then their worktrees and pinned bases are untouched, and the setup-once marker's base check for those features still compares against their orders' pinned SHAs (no spurious full setup re-runs). | task-7 | covered | "no re-setup on root advance with unchanged pin" | diff-local |
| criterion | Story 6 happy: Given a self-host executor whose containment is unproven has an open fingerprint window, when a root mutation (fetch/fast-forward, rebuild, relink) becomes due, then the mutation is deferred until no unproven-containment window is open (windows close between provider dispatches, so deferral does not require a full drain), and the deferral is logged with its reason. | task-19 | covered | "the mutation ran after the window closed without a full pool drain" | diff-local |
| criterion | Story 6 negative: Given the root checkout is not on the default branch, when fast-forward is attempted, then it refuses exactly as today and logs the reason. | task-12 | covered | "The off-default-branch refusal and the minimum-interval limit each keep a passing test" | diff-local |
| criterion | Story 6 negative: Given the main checkout is dirty with edits attributable to multiple in-flight feature branches, when the leak triage's all-or-nothing heal cannot pick a single candidate, then fast-forward refuses and logs the full dirty-entry list loudly (no silent stall). | task-13 | covered | "listing every dirty entry and the candidate branches" | diff-local |
| criterion | Story 6 negative: Given a rate-limit episode is active, when slots are free, then the dispatcher claims no new work while in-flight executors continue (adr-2026-07-05 D2), and resume is jittered. | task-16 | covered | "episode active → no new claims, in-flight untouched" | diff-local |
| criterion | Story 6 negative: Given the base advanced while a halted (not in-flight) feature waits, when `rekickSweep` runs, then the halted feature re-kicks per the existing per-SHA bound, and no in-flight feature is rebased or re-kicked by the advance. | task-12 | covered | "re-kicks only non-in-flight halted features under the existing per-SHA bound" | diff-local |
| criterion | Story 7 happy: Given a stale engine detected with two executors running, when the dispatcher enters drain (claims stop), then both builds finish normally, and the rebuild + restart fires exactly once at the drained boundary. | task-14 | covered | "both builds finish, rebuild+restart fires exactly once at the drained boundary" | diff-local |
| criterion | Story 7 happy: Given a durable RESTART-PENDING marker and a busy pool, when executors finish, then the queued restart fires at the drained boundary with the existing marker-consume → lock-release → exit ordering. | task-14 | covered | "marker-consume → lock-release → exit ordering" | diff-local |
| criterion | Story 7 happy: Given a restart is queued while draining, when the operator checks daemon status, then the pending restart and the full set of features it is waiting on (the drain set) are visible. | task-14 | covered | "reports the pending restart and the full drain set of feature slugs" | diff-local |
| criterion | Story 7 negative: Given work in flight, when staleness is probed, then no rebuild, relink, or restart touches the root checkout before the pool is drained (work-in-flight suppression). | task-15 | covered | "no rebuild/relink/restart touches the root before drained" | diff-local |
| criterion | Story 7 negative: Given a stale-engine trigger that fails to fire, when the drained boundary is reached again later, then the retry follows the existing log-and-stay-alive path with the one-shot-per-firing bound intact (no restart loop). | task-15 | covered | "the one-shot-per-firing bound intact" | diff-local |
| criterion | Story 7 negative: Given a drain in progress, when a new high-priority feature merges, then it is not claimed until after the restart completes, and the post-restart daemon picks it up (drain is strict, not best-effort). | task-15 | covered | "a new eligible feature during drain is not claimed and the post-restart daemon picks it up" | diff-local |
| criterion | Story 7 negative: Given a rate-limit episode is active at the drained boundary, when a queued or stale-engine restart would fire, then it is deferred until the episode ends (the episode gate composes with drain — a restart mid-episode would respawn a daemon with no episode memory). | task-16 | covered | "queued/stale-engine restart deferred until the episode ends" | diff-local |
| criterion | Story 7 negative: Given SIGTERM arrives while N executors are draining, when the bounded force-release timeout is evaluated, then the bound scales with the number of in-flight executors (per-executor allowance), so a routine N-worker drain does not force-release the lock while builds still run. | task-17 | covered | "the per-executor allowance times the number still running" | diff-local |
| criterion | Story 7 negative: Given the daemon is paused, when slots are free, then the pause predicate gates the claim loop (no new claims) while in-flight executors run to completion. | task-16 | covered | "pause marker present → no new claims while in-flight executors run to completion" | diff-local |
| criterion | Story 8 happy: Given two self-host executors with overlapping provider-dispatch windows, when each verifies its boundary, then neither halts on the other's excluded-path writes and both runs complete. | task-18 | covered | "B's window never blames A's excluded-path writes" | diff-local |
| criterion | Story 8 happy: Given a dispatcher-attributed root mutation (fast-forward, rebuild, relink) while a proven-contained fingerprint window is open, when the window verifies, then the mutation is recognized as dispatcher-attributed (serialized or re-baselined) and does not halt the executor's run. | task-18 | covered | "is serialized or recorded as an attributed re-baseline" | diff-local |
| criterion | Story 8 happy: Given an executor whose containment is unproven, when the dispatcher schedules root mutations, then it never mutates the root while that executor's fingerprint window is open — the fail-closed semantics of unproven containment are preserved by deferral, not by attribution. | task-19 | covered | "a due root mutation is deferred until no such window is open" | diff-local |
| criterion | Story 8 negative: Given an unattributed write to the live root checkout (e.g. an operator edit outside excluded paths with containment unproven), when any open window verifies, then that run still halts fail-closed naming the drifted path — attribution machinery never converts real drift into a pass. | task-19 | covered | "an unattributed root write during any window still halts naming the drifted path" | diff-local |
| criterion | Story 8 negative: Given provider-state drift (live provider home changed), when a window verifies, then the halt remains unconditional for the affected run exactly as today (no containment escape for provider state). | task-19 | covered | "provider-state drift still halts unconditionally" | diff-local |
| criterion | Story 8 negative: Given the fingerprint surface, when this feature's diff is inspected, then no path is added to the exclusion list (adr-2026-08-17 D4). | task-18 | covered | "adds no entry to the fingerprint exclusion list" | diff-local |
| criterion | Story 9 happy: Given two executors running, when they emit build progress, then every feature-owned line in `.daemon/daemon.log` carries its `[slug]` tag and every persisted event lands in its own worktree's `events.jsonl`. | task-23 | covered | "with two interleaved fake builds" | diff-local |
| criterion | Story 9 happy: Given an engine warning surfaces via the process-level `console.warn`/`console.error` tee during a build, when it is written to the daemon log, then it carries the owning feature's slug tag stamped centrally (not threaded through call sites). | task-23 | covered | "carry the owning feature's slug tag, stamped by central machinery" | diff-local |
| criterion | Story 9 negative: Given a genuinely daemon-global message (sweep error, drain announcement), when it is logged, then it carries the bare `[daemon]` prefix and no feature tag (no false attribution). | task-23 | covered | "a genuinely daemon-global line keeps the bare daemon prefix and gains no tag" | diff-local |
| criterion | Story 9 negative: Given interleaved lifecycle transitions from two features, when the transition-suppression logic runs, then suppression stays per-slug (one feature's repeated status does not suppress the other's). | task-23 | covered | "lifecycle-transition suppression stays per-slug under interleaving" | diff-local |
| criterion | Story 10 happy: Given the modern `providerExecution` path, when N=2 builds run, then no code mutates process-global provider env vars (per-invocation env only). | task-24 | covered | "the modern path performs no process-global provider env mutations during dispatch" | diff-local |
| criterion | Story 10 negative: Given a dispatch that would take the legacy no-`providerExecution` branch (which mutates `CLAUDE_CONFIG_DIR` process-globally), when effective concurrency is greater than 1, then that dispatch is refused fail-closed with a logged reason naming the branch and the concurrency conflict, and the feature is not silently built serially. | task-24 | covered | "is refused with a distinct, greppable error naming the branch and the concurrency conflict" | diff-local |
| criterion | Story 10 negative: Given effective concurrency 1, when the legacy branch is taken, then behavior is unchanged (restore-in-finally preserved). | task-24 | covered | "at concurrency 1 the legacy branch behaves exactly as before (restore-in-finally asserted)" | diff-local |

Consistency pass (§4d): the closest cross-layer pair — Story 2's "no fetch or root fast-forward
occurs while the single feature is in flight" versus Story 6's busy-refresh — is precondition-
disjoint (effective concurrency 1 vs a free slot at N>1) and both bind to task-11's N=1 policy
degeneration, so neither direction breaks the other. No contradictions or oscillations found;
zero gap and zero fail rows.
