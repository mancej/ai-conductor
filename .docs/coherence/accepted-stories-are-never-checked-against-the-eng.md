# Coherence: One owner for accepted-story readability

Date: 2026-09-23
Track: technical
Tier: M
Verdict: covered

## Outcome, story and task mapping

Four staged intake outcomes, four stories, ten tasks, one changed-ADR row, and 21 criterion rows. Outcome quotes are the staged projection verbatim. No product FR layer applies: this is a technical-track spec with no PRD. Criterion quotes are exact completion-check text from the cited task's Done when block.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote / Notes |
| --- | --- | --- | --- | --- |
| outcome | outcome-1 | story-1 | covered | A story whose criteria the engine's own extractor cannot read is rejected **when it is authored |
| outcome | outcome-2 | story-1 | covered | The rejection names the shape that is required (headed Happy Path / Negative Paths sections, |
| outcome | outcome-3 | story-2 | covered | `engineer land` and the daemon `stories` gate agree on what a valid accepted story is; a file |
| outcome | outcome-4 | story-1 | covered | The `stories` skill's authored output satisfies the extractor by construction, so the format is |
| story | story-1 | task-4, task-5 | covered | The land rung refuses an unreadable artifact and names both the failing story and the required shape; readable specs still reach the existing rungs. |
| story | story-2 | task-3, task-6, task-7 | covered | One exported predicate, both consumers routed through it, and their agreement pinned across a shape corpus. |
| story | story-3 | task-1, task-2 | covered | One bullet primitive beneath both derivations, and the positional section lookup that depends on their alignment. |
| story | story-4 | task-8, task-9, task-10 | covered | Strictness confined to land and the DECIDE gate, the evidence derivation unchanged in kind, and the corpus left untouched. |
| task | task-1 | story-3 | covered | Shared bullet primitive beneath the authoritative extractor; wrapped bullets join instead of dropping. |
| task | task-2 | story-3 | covered | Positional section resolution proven against the aligned derivation. |
| task | task-3 | story-2 | covered | The single exported owner of the readability question, built from the module's own primitives. |
| task | task-4 | story-1 | covered | Land ladder rung with its own refusal code, thrown before any commit exists. |
| task | task-5 | story-1 | covered | Refusal wording carries the required shape; the readable path still reaches the existing rungs. |
| task | task-6 | story-2 | covered | The DECIDE gate drops its private checks for the shared predicate, keeping DRAFT and scoping rungs. |
| task | task-7 | story-2 | covered | Corpus-level agreement between the two consumers, failing if either regains a private check. |
| task | task-8 | story-4 | covered | Consumer boundary and discovery admission pinned, so merged specs keep building. |
| task | task-9 | story-4 | covered | The acceptance_specs required-evidence derivation proven free of the predicate. |
| task | task-10 | story-4 | covered | Scope boundary verified on the diff: no other feature's stories artifact is touched. |
| adr | adr-2026-09-23-one-owner-for-accepted-story-readability | story-1, story-2, story-3, story-4 | covered | D1 is delivered by the single exported predicate and both consumers calling it (tasks 3, 4, 6). D2 is delivered by routing both derivations through one bullet primitive (tasks 1, 2). D3 is delivered by pinning that no BUILD or SHIP consumer reaches the predicate and that the acceptance_specs derivation is unchanged in kind (tasks 8, 9). |
| criterion | Story 1 happy: Given a spec worktree whose stories file states every story's criteria as single-line Given/When/Then bullets under headed Happy Path and Negative Paths sections, when land runs, then the spec commits and no readability refusal is raised | 5 | covered | "a test asserts a worktree whose stories file states every story's criteria as single-line Given/When/Then bullets under headed sections commits, and that no readability refusal is raised" | diff-local |
| criterion | Story 1 happy: Given a spec worktree whose stories file has five stories all stating readable criteria, when land runs, then land reaches its existing approval and plan-reference checks unchanged | 5 | covered | "a test asserts that for a readable five-story worktree the existing approval and plan-reference rungs still run and still produce their own refusals when violated" | diff-local |
| criterion | Story 1 negative: Given a spec worktree whose Story 3 states its criteria as bold lines followed by one clause per bullet, when land runs, then land refuses and the refusal names Story 3 | 4 | covered | "a test asserts land refuses a worktree whose Story 3 states its criteria as bold lines with one clause per bullet, and that the refusal message contains `Story 3`" | diff-local |
| criterion | Story 1 negative: Given the refusal raised for Story 3, when the operator reads the message, then it states that each criterion must be one single-line Given/When/Then bullet under a headed Happy Path or Negative Paths section | 5 | covered | "the refusal message states that each criterion must be one single-line Given/When/Then bullet under a headed Happy Path or Negative Paths section, and a test asserts that sentence is present" | diff-local |
| criterion | Story 1 negative: Given a Small-tier spec worktree that authors no coherence artifact and whose only story yields zero readable criteria, when land runs, then land refuses rather than committing the spec | 4 | covered | "a test asserts land refuses a Small-tier worktree that authors no coherence artifact and whose only story yields zero readable criteria, and that no commit is created" | diff-local |
| criterion | Story 1 negative: Given a spec worktree whose Story 2 states four readable criteria and one bullet carrying only a Given clause, when land runs, then land refuses and the refusal names Story 2 | 4 | covered | "a test asserts land refuses a worktree whose Story 2 states four readable criteria plus one bullet carrying only a Given clause, and that the refusal message contains `Story 2`" | diff-local |
| criterion | Story 2 happy: Given any stories file, when land and the `stories` step gate each evaluate it, then both obtain their verdict from the single predicate exported by `story-criteria.ts` | 6 | covered | "a test asserts land and the gate reach their verdict through the same exported predicate — stubbing it changes both — and that they agree on that file" | diff-local |
| criterion | Story 2 happy: Given a stories file the `stories` step gate accepts, when land evaluates that same file, then land accepts it | 7 | covered | "the test asserts a fixture the gate accepts is also accepted by land and a fixture the gate refuses is also refused by land, and fails if either consumer is rewired to its own check" | diff-local |
| criterion | Story 2 negative: Given a stories file in which Story 2 carries a Happy Path section and no Negative Paths section, when land evaluates it, then land refuses naming Story 2, matching the `stories` step gate's refusal on the same file | 6 | covered | "a test asserts both land and the gate refuse a stories file in which Story 2 carries a Happy Path section and no Negative Paths section, each naming `Story 2` in its refusal" | diff-local |
| criterion | Story 2 negative: Given a stories file the `stories` step gate refuses, when land evaluates that same file, then land refuses it as well | 7 | covered | "the test asserts a fixture the gate accepts is also accepted by land and a fixture the gate refuses is also refused by land, and fails if either consumer is rewired to its own check" | diff-local |
| criterion | Story 2 negative: Given a corpus of stories files spanning readable, zero-criteria, and missing-section shapes, when both consumers evaluate every file, then no file is accepted by one consumer and refused by the other | 7 | covered | "the test asserts that for every fixture the two consumers agree — no fixture is accepted by one consumer and refused by the other" | diff-local |
| criterion | Story 3 happy: Given a story whose Given/When/Then bullet is hard-wrapped across two lines with the continuation indented, when the authoritative criterion list and the criterion id list are derived from it, then both contain the same number of entries | 1 | covered | "a test asserts `extractAuthoritativeStoryCriteria` and `extractStoryCriterionIds` return equal entry counts for a story whose first Given/When/Then bullet is hard-wrapped across two lines with the continuation indented" | diff-local |
| criterion | Story 3 happy: Given a story stating three criteria of which the first is hard-wrapped, when the id for the third criterion is resolved to its happy or negative section, then the section returned is the one that bullet was authored under | 2 | covered | "a test asserts `criterionStorySection` returns the section the bullet was authored under for the third criterion of a story whose first bullet is hard-wrapped" | diff-local |
| criterion | Story 3 negative: Given a bullet carrying its Given clause on the first line and its Then clause on an indented continuation line, when the authoritative criterion list is derived, then that bullet appears as one criterion rather than being dropped | 1 | covered | "a test asserts a bullet carrying `given` on its first line and `then` on an indented continuation line appears as exactly one criterion in the authoritative list rather than being dropped" | diff-local |
| criterion | Story 3 negative: Given a story containing one hard-wrapped bullet, when the id for its last criterion is resolved, then a happy or negative section is returned rather than no section | 2 | covered | "a test asserts `criterionStorySection` returns a happy or negative section rather than no section for the last criterion of a story containing one hard-wrapped bullet" | diff-local |
| criterion | Story 3 negative: Given a stories file in which every story contains at least one hard-wrapped bullet, when the authoritative criterion list and the criterion id list are derived, then their entry counts are equal for every story in the file | 1 | covered | "a test asserts the two derivations return equal counts for every story in a fixture file in which every story contains at least one hard-wrapped bullet" | diff-local |
| criterion | Story 4 happy: Given a merged spec whose stories file the readability predicate would refuse, when the daemon discovers and dispatches it, then discovery admits it and the build proceeds | 8 | covered | "a test asserts daemon discovery admits a merged spec whose stories artifact the predicate refuses, so the spec is dispatched rather than skipped and the build proceeds" | diff-local |
| criterion | Story 4 happy: Given that merged spec reaching `acceptance_specs`, when the step derives its required evidence, then the readability predicate adds no requirement the step did not already impose | 9 | covered | "a test asserts the `acceptance_specs` required-evidence derivation calls no readability predicate and its required criterion set is produced solely by `extractAuthoritativeStoryCriteria`" | diff-local |
| criterion | Story 4 negative: Given a merged spec whose stories file yields zero readable criteria, when the feature runs through BUILD and SHIP, then no BUILD or SHIP step refuses it on readability grounds | 8 | covered | "a test asserts a merged spec whose stories artifact yields zero readable criteria runs through the BUILD and SHIP step gates without any step refusing it on readability grounds" | diff-local |
| criterion | Story 4 negative: Given a merged spec whose stories file is missing a Negative Paths section, when the daemon evaluates it for dispatch, then it is dispatched rather than skipped | 8 | covered | "a test asserts daemon discovery admits a merged spec whose stories artifact the predicate refuses, so the spec is dispatched rather than skipped and the build proceeds" | diff-local |
| criterion | Story 4 negative: Given this feature's own change set, when the diff is inspected, then no other feature's stories artifact is added, modified, or deleted | 10 | covered | "the feature's `base...HEAD` diff lists exactly one path in the repository's stories artifact directory, this feature's own artifact, and no other stories file appears as added, modified, or deleted" | diff-local |

## Judgement notes

These are the three rows whose verdict rests on a judgement a reader should be able to check rather than re-derive.

**outcome-4.** The staged bullet asks that the stories skill's authored output satisfy the extractor by construction. That is already true at this HEAD: `skills/stories/SKILL.md` shows headed `#### Happy Path` and `#### Negative Paths` sections with single-line Given/When/Then bullets, which is exactly the shape the extractor reads, and the skill additionally forbids authored criterion ids. The outcome therefore needs no new authoring work; what this feature adds is that any drift away from that shape now fails at land instead of at `acceptance_specs`. It is mapped to story-1 because story-1 is where that enforcement and its required-shape message live. Verified by reading the skill template, 95%.

**criterion row for Story 4 happy 2 (task-9).** The cited check asserts that the `acceptance_specs` required-evidence derivation calls no readability predicate. Task 1 does change what `extractAuthoritativeStoryCriteria` returns for a hard-wrapped bullet, and that function feeds this derivation, so a reader may ask whether the criterion still holds. It does: the criterion promises only that *the readability predicate* adds no requirement. The derivation change is a separate obligation that `adr-2026-09-23-one-owner-for-accepted-story-readability` decision 3 exempts explicitly, on the grounds that it changes derivation rather than adding a requirement and corrects BUILD-side routing that is already wrong.

**criterion row for Story 1 negative 3 (task-4), the Small-tier case.** `adr-2026-07-21-s-tier-pipeline-knobs` D1 states that smallness is expressed entirely through existing resolution knobs with "no new land primitive", and this criterion engages a new land rung at tier S. That ADR is not in this spec's change set, so it is outside the achievability constraint sweep, and the rung is tier-agnostic rather than S-specific — it applies identically at S, M, and L, as the existing approval and plan-reference rungs already do. The reading is recorded in `.docs/conflicts/` rather than settled here; if the operator reads the clause as forbidding any new land rung at S, this criterion is the one to amend.
