# Coherence Mapping: Daemon-level metrics — queue depth, halts, gate outcomes, and monotonic counters

Feature: no-daemon-level-metrics-queue-depth-halts-and-gate (#1937). Tier L, technical track
(no PRD — the fr row class is omitted). Outcome ids are 1-based over the six staged intake bullets
in `.pipeline/intake-outcomes.md`. The adr row covers the one non-deleted `adr-*.md` in this
change set (adr-014, amended additively with decisions 7-9). Criterion rows carry the
extractor-exact criterion text, cited task id(s), verdict, a verbatim quote from one cited task's
`Done when` block, and the diff-locality disposition. Revised 2026-09-06 pre-land: metric recording moved into an event-fed listener (adr-014 D7/D9
rewritten clean). Consistency pass (§4d): every cross-layer
pair sharing a subject (outcome-2 ↔ task-8 counters, outcome-5 ↔ task-4 identity, adr D8 ↔
story-2, adr D9 ↔ story-4 halt vocabulary, story-3 zero-vs-absent ↔ task-11) was tested in both
directions; none yields two "no" answers. The one ambiguity surfaced during stories — an empty
backlog state reporting 0 versus absent — was decided by the operator (0) and story-3/task-11
agree.

| Row class | Cited id / criterion | Counterpart / cited task id(s) | Verdict | Notes / quote | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-3 | covered | Backlog counts per state over time via daemon.backlog gauges sampled each tick, no CLI or local file read |
| outcome | outcome-2 | story-4, story-1 | covered | Dispatches, halts by class and step, ships as counters; run.outcomes made monotonic (event-fed listener) so halt rate is chartable |
| outcome | outcome-3 | story-5 | covered | gate.verdicts pass/fail and gate.kickbacks per gate and feature; rates are one query for #1835 |
| outcome | outcome-4 | story-6 | covered | feature.duration.wall and .active histograms recorded at ship |
| outcome | outcome-5 | story-2 | covered | project and worker on every data point; service.instance.id=project/worker; feature on per-feature points |
| outcome | outcome-6 | story-3 | covered | Idle-daemon acceptance: up, backlog, slots exported with no dispatch |
| adr | adr-014-otel-observability-exporter | story-1, story-2, story-7, story-8 | covered | Amended additively (D7-D9, approved 2026-09-06; D7/D9 rewritten clean pre-land for the event-fed listener). Obligation rows judged: D1/D2 no-change (listener via otelEventTypes, no new plugin kind); D3/D6 existing (wire.ts factory seam, transport.ts reuse); D4 task-9/task-11 (hook does no I/O, snapshot precomputed); D5 task-2 (unwritable ledger never throws); D7 task-6/7/8/19/20 (one MeterProvider and one listener per daemon, spans-only visualizers, parity, child-process variant); D8 task-4/5 (worker-stable metric Resource, feature on data points only); D9 task-1/2/11/14/16/17/21 (four typed events, sibling ledger, slug-tagged forwarding counted once, eight halt values, absence-not-zero, run outcomes from terminal events). No decision contradicted by a story or task |
| story | story-1 | task-6, task-7, task-8, task-21 | covered | Every criterion of story 1 has a criterion row below citing one of these tasks |
| story | story-2 | task-3, task-4, task-5 | covered | Every criterion of story 2 has a criterion row below citing one of these tasks |
| story | story-3 | task-9, task-10, task-11, task-12 | covered | Every criterion of story 3 has a criterion row below citing one of these tasks |
| story | story-4 | task-13, task-14, task-15 | covered | Every criterion of story 4 has a criterion row below citing one of these tasks |
| story | story-5 | task-17, task-18 | covered | Every criterion of story 5 has a criterion row below citing one of these tasks |
| story | story-6 | task-16 | covered | Every criterion of story 6 has a criterion row below citing one of these tasks |
| story | story-7 | task-1, task-2, task-17, task-19, task-20, task-21 | covered | Every criterion of story 7 has a criterion row below citing one of these tasks |
| story | story-8 | task-6, task-19 | covered | Every criterion of story 8 has a criterion row below citing one of these tasks |
| task | task-1 | story-7 | covered | infrastructure: Add the four daemon-level event variants and their sink rows |
| task | task-2 | story-7 | covered | infrastructure: Persist daemon-bus events to the daemon ledger, skipping forwarded copies |
| task | task-3 | story-2 | covered | happy-path: Resolve otel.worker_name with hostname fallback and register the config key |
| task | task-4 | story-2 | covered | happy-path: Re-key the metric Resource to project/worker and keep the trace Resource feature-scoped |
| task | task-5 | story-2 | covered | happy-path: MetricsRecorder carries project/worker on every point and feature only on per-feature instruments |
| task | task-6 | story-1, story-8 | covered | infrastructure: OtelVisualizer runs spans-only when told to, and owns its meter otherwise |
| task | task-7 | story-1 | covered | infrastructure: Wire the daemon-owned meter and listener at daemon start; dispatches are spans-only |
| task | task-8 | story-1 | covered | happy-path: Counters accumulate across re-dispatches under one daemon |
| task | task-9 | story-3 | covered | infrastructure: Daemon loop reports a per-tick snapshot through a DaemonDeps hook |
| task | task-10 | story-3 | covered | happy-path: Track first-seen time per spec for backlog age |
| task | task-11 | story-3 | covered | happy-path: Emit daemon_backlog_snapshot and record the daemon-level gauges |
| task | task-12 | story-3 | covered | happy-path: An idle daemon exports liveness, backlog, and slot series |
| task | task-13 | story-4 | covered | happy-path: Classify dispatch kind and count feature dispatches |
| task | task-14 | story-4 | covered | happy-path: Count halts by sidecar classification and halting step |
| task | task-15 | story-4 | covered | happy-path: Emit feature_shipped and count ships; parked features count as neither |
| task | task-16 | story-6 | covered | happy-path: Record wall and active feature durations with absence-not-zero |
| task | task-17 | story-5, story-7 | covered | infrastructure: Forwarded per-feature events carry their slug and are counted once |
| task | task-18 | story-5 | covered | happy-path: Count gate verdicts, kickbacks, and stalls |
| task | task-19 | story-7, story-8 | covered | infrastructure: Interactive path uses the listener with its own meter; listener coverage is enforced |
| task | task-20 | story-7 | covered | infrastructure: Listener derives the existing step and cost instruments from forwarded events |
| task | task-21 | story-1, story-7 | covered | happy-path: Listener records run outcomes from terminal events, including dispatch end |
| criterion | Story 1 happy: Given OTel is enabled and one daemon process dispatches feature S, which halts, is re-kicked, and halts again, when metrics are exported after the second dispatch, then conductor.run.outcomes{feature=S, outcome=halted} reads 2 and never reads 1 in between | task-8 | covered | "reads 2 after two halting dispatches under one daemon" | diff-local |
| criterion | Story 1 happy: Given one daemon process dispatches feature S twice and each dispatch retries the build step once, when metrics are exported after the second dispatch, then conductor.step.retries{feature=S, step=build} reads 2, monotonic across both dispatches | task-8 | covered | "`conductor.step.retries` for the feature is 2 after two single-retry dispatches" | diff-local |
| criterion | Story 1 happy: Given OTel is enabled, when the daemon starts, then exactly one MeterProvider exists for the daemon's lifetime and both dispatches of Story 1's feature are recorded onto it by the daemon's metrics listener from their forwarded events | task-7 | covered | "one `MeterProvider` and one `MetricsListener` are constructed per daemon start and two dispatches each wire a `metrics: false` visualizer" | diff-local |
| criterion | Story 1 happy: Given a dispatch runs in a process that exits at dispatch end, when its forwarded step and terminal events reach the daemon bus, then the same per-feature counters continue from their prior values, because no metric state lived in the exited process | task-8 | covered | "child-process-shaped fake that emits its events and exits, and asserts the feature's counters continue from their prior values" | diff-local |
| criterion | Story 1 negative: Given feature A's dispatch stops while feature B is still running under the same daemon, when A's per-dispatch visualizer stops, then the daemon meter is force-flushed (so A's final data points are exported before the daemon could die) but not shut down, the visualizer holds no meter of its own to shut down, and B's next step still exports conductor.step.duration{feature=B} | task-6 | covered | "a `metrics: false` visualizer constructs no `MeterProvider` and `stop()` calls no meter method while spans still flush" | diff-local |
| criterion | Story 1 negative: Given the daemon process itself restarts, when metrics resume, then counters restart from zero exactly once (an ordinary process restart) and the exported series carries the same identity so backend rate functions treat it as a counter reset, not a new series | task-8 | covered | "identical series identity attribute set with counters restarting from zero exactly once" | diff-local |
| criterion | Story 1 negative: Given OTel is disabled or the otel config block is absent, when the daemon starts and dispatches a feature, then no MeterProvider is constructed, no recorder is passed to the dispatch, and daemon behavior is byte-for-byte unchanged from today | task-7 | covered | "disabled or absent OTel config constructs no `MeterProvider` and leaves per-dispatch wiring unchanged" | diff-local |
| criterion | Story 2 happy: Given project P and a worker whose resolved name is W, when the daemon exports metrics, then the metric Resource carries service.name=ai-conductor, service.instance.id=P/W, conductor.project, conductor.worker=W, and host.name equal to the OS hostname | task-4 | covered | "`service.instance.id === 'P/W'` and `host.name` present" | diff-local |
| criterion | Story 2 happy: Given a per-feature instrument such as conductor.step.duration for feature S, when it is exported, then its data point carries project=P, worker=W, and feature=S as attributes | task-5 | covered | "a per-feature data point carries exactly `project`, `worker`, `feature` plus the instrument's own attributes" | diff-local |
| criterion | Story 2 happy: Given a daemon-level instrument such as conductor.daemon.backlog, when it is exported, then its data point carries project=P and worker=W and no feature attribute | task-5 | covered | "a daemon-level data point carries `project` and `worker` and has no `feature` key" | diff-local |
| criterion | Story 2 happy: Given otel.worker_name is set to a non-blank value in .ai-conductor/config.yml, when the daemon exports, then W is that trimmed value; given it is absent or blank, then W is the OS hostname | task-3 | covered | "the resolved worker is the trimmed value, the hostname, the hostname, and `unknown` respectively" | diff-local |
| criterion | Story 2 negative: Given the shared meter serves many features, when the metric Resource is inspected, then it carries no conductor.feature and no conductor.branch attribute (asserted on the exported Resource, not on data-point attributes) | task-4 | covered | "`conductor.feature` and `conductor.branch` are absent from the metric Resource and present on the trace Resource" | diff-local |
| criterion | Story 2 negative: Given a per-dispatch visualizer exports spans, when the trace Resource is inspected, then it still carries conductor.feature, conductor.branch, conductor.run.id, and conductor.engine.version, unchanged from today | task-4 | covered | "`conductor.feature` and `conductor.branch` are absent from the metric Resource and present on the trace Resource" | diff-local |
| criterion | Story 2 negative: Given os.hostname() throws or returns an empty string, when identity is resolved, then W falls back to the literal unknown and the daemon proceeds without failing | task-3 | covered | "an `os.hostname()` that throws" | diff-local |
| criterion | Story 3 happy: Given a running daemon with an empty backlog and no dispatch in flight, when a metrics export occurs, then conductor.daemon.up reads 1 and conductor.daemon.backlog{state} has a data point for each of eligible, waiting, blocked, gated, and parked (each 0) | task-12 | covered | "asserts `conductor.daemon.up` = 1, five backlog points reading 0, and both slot states are exported after one tick" | diff-local |
| criterion | Story 3 happy: Given discovery finds 3 eligible, 2 waiting, 1 blocked, 4 gated specs and 2 operator-parked features, when the tick's snapshot is exported, then conductor.daemon.backlog reads 3, 2, 1, 4, 2 for those states respectively | task-11 | covered | "all five `conductor.daemon.backlog` states are present with the exact counts" | diff-local |
| criterion | Story 3 happy: Given the oldest eligible spec became eligible 36 hours ago, when the snapshot is exported, then conductor.daemon.backlog.oldest_age{state=eligible} reads approximately 129600 seconds | task-11 | covered | "`oldest_age`, `slots`, `inflight`, `poll.duration`, and `up` data points match the snapshot payload" | diff-local |
| criterion | Story 3 happy: Given daemon concurrency is 3 and 2 features are in flight, when the snapshot is exported, then conductor.daemon.slots{state=busy} reads 2, conductor.daemon.slots{state=free} reads 1, and conductor.daemon.inflight{feature} reads 1 for each of the two in-flight slugs | task-11 | covered | "`oldest_age`, `slots`, `inflight`, `poll.duration`, and `up` data points match the snapshot payload" | diff-local |
| criterion | Story 3 happy: Given a discovery pass took 840 ms, when the snapshot is exported, then conductor.daemon.poll.duration has one observation of 840 ms | task-9 | covered | "`pollDurationMs` equals the measured discovery duration within 50 ms of a fake clock" | diff-local |
| criterion | Story 3 negative: Given the daemon is paused, when the snapshot is exported, then conductor.daemon.blocked_reason{reason=paused} reads 1 and the other three reasons read 0, so an idle-because-paused daemon is distinguishable from an idle-because-drained one | task-11 | covered | "`blocked_reason` reads 1 for `paused` and 0 for `build_auth_missing`, `gh_version`, `episode_active`" | diff-local |
| criterion | Story 3 negative: Given build auth is missing, when the snapshot is exported, then conductor.daemon.blocked_reason{reason=build_auth_missing} reads 1 | task-11 | covered | "a second snapshot with `build_auth_missing` true flips those values" | diff-local |
| criterion | Story 3 negative: Given the daemon is hard-killed, when the backend's next scrape interval passes, then conductor.daemon.up stops being reported (the series goes stale) rather than continuing to read 1 | task-12 | covered | "no `up` observation is recorded after the loop stops, so the series goes stale when the daemon dies" | diff-local |
| criterion | Story 3 negative: Given a backlog state has no members, when the snapshot is exported, then that state's data point reads 0 rather than being absent, so dashboards never show a gap for an empty state | task-11 | covered | "a zero-member state reads 0 rather than being absent" | diff-local |
| criterion | Story 3 negative: Given the backlog contains an eligible spec whose eligibility timestamp cannot be determined, when oldest_age is computed, then that spec is excluded from the age and the count still includes it | task-10 | covered | "a member with an unreadable marker is excluded from the age while `counts.eligible` still includes it" | diff-local |
| criterion | Story 4 happy: Given the daemon dispatches feature S for the first time, when the dispatch begins, then conductor.feature.dispatches{feature=S, kind=initial} increments by 1 | task-13 | covered | "`kind` is `initial`, `rekick`, and `resume` for the three fixtures above" | diff-local |
| criterion | Story 4 happy: Given feature S halts with class needs-human at step build_review and a halt record is written, when metrics are exported, then conductor.feature.halts{feature=S, haltClass=needs-human, step=build_review} reads 1 | task-14 | covered | "A test covers all eight sidecar values on `conductor.feature.halts`" | diff-local |
| criterion | Story 4 happy: Given the operator clears S's HALT and the daemon re-dispatches it, when the dispatch begins, then conductor.feature.dispatches{feature=S, kind=rekick} increments by 1 | task-13 | covered | "`kind` is `initial`, `rekick`, and `resume` for the three fixtures above" | diff-local |
| criterion | Story 4 happy: Given feature S ships, when the shipped record is landed, then conductor.feature.shipped{feature=S} increments by 1 | task-15 | covered | "`conductor.feature.shipped{feature=S}` reads 1 after the ship branch" | diff-local |
| criterion | Story 4 negative: Given a halt whose HALT.class sidecar is missing, unreadable, or holds an unrecognized value, when it is recorded, then haltClass carries the existing disposition value unclassified, never an invented label and never an empty string | task-14 | covered | "plus `legacy` and `unclassified` derivations" | diff-local |
| criterion | Story 4 negative: Given a halt from a build older than the class sidecar, when it is recorded, then haltClass carries the existing disposition value legacy | task-14 | covered | "plus `legacy` and `unclassified` derivations" | diff-local |
| criterion | Story 4 negative: Given a halt whose sidecar holds kickback-cap or over-scope (the two operator-owned classes the conductor writes beyond the base HaltClass union), when it is recorded, then haltClass carries that value verbatim rather than folding it to unclassified, so operator-attention halts are never miscounted as unknown | task-14 | covered | "`kickback-cap` and `over-scope` are recorded verbatim, not folded to `unclassified`" | diff-local |
| criterion | Story 4 negative: Given a feature is operator-parked, when metrics are exported, then it appears in conductor.daemon.backlog{state=parked} and increments neither conductor.feature.halts nor conductor.feature.shipped | task-15 | covered | "a parked feature is present in `conductor.daemon.backlog{state=parked}` and absent from `feature.halts` and `feature.shipped`" | diff-local |
| criterion | Story 4 negative: Given the daemon resumes a feature whose worktree already exists and which was not halted, when the dispatch begins, then kind is resume, not initial and not rekick | task-13 | covered | "`kind` is `initial`, `rekick`, and `resume` for the three fixtures above" | diff-local |
| criterion | Story 4 negative: Given the halt record write fails after the HALT marker was written, when metrics are exported, then conductor.feature.halts still increments from the loop_halt event and the metrics handler does not throw | task-14 | covered | "a halt still increments the counter when the halt-record write fails" | diff-local |
| criterion | Story 5 happy: Given gate build_review passes for feature S, when the verdict event is emitted, then conductor.gate.verdicts{feature=S, step=build_review, outcome=pass} increments by 1 | task-18 | covered | "the exact attribute sets and values for pass, fail-plus-kickback, and stall above" | diff-local |
| criterion | Story 5 happy: Given gate build_review fails for feature S and routes work back to build, when the events are emitted, then conductor.gate.verdicts{feature=S, step=build_review, outcome=fail} increments by 1 and conductor.gate.kickbacks{feature=S, from=build_review, to=build} increments by 1 | task-18 | covered | "the exact attribute sets and values for pass, fail-plus-kickback, and stall above" | diff-local |
| criterion | Story 5 happy: Given a build stalls with reason no_task_progress, when the stall event is emitted, then conductor.daemon.stalls{reason=no_task_progress} increments by 1 and carries no feature attribute | task-18 | covered | "the exact attribute sets and values for pass, fail-plus-kickback, and feature-free stall above" | diff-local |
| criterion | Story 5 negative: Given a gate verdict is emitted on the feature bus during a daemon dispatch, when it reaches the daemon-level listener, then it is counted exactly once (the forwarded copy is counted, the original is not double-counted) | task-17 | covered | "one feature-bus `gate_verdict` yields exactly one `conductor.gate.verdicts` point with value 1 and `feature=S`" | diff-local |
| criterion | Story 5 negative: Given a gate verdict for a step name outside the known gate set, when it is recorded, then the step attribute carries the step name verbatim and no error is raised | task-18 | covered | "an unknown step name is carried verbatim on the `step` attribute with no thrown error" | diff-local |
| criterion | Story 5 negative: Given the same gate fails three times in one dispatch, when metrics are exported, then verdicts{outcome=fail} reads 3, not 1 | task-18 | covered | "three fails for one gate in one dispatch read `outcome=fail` = 3" | diff-local |
| criterion | Story 6 happy: Given feature S was first dispatched at T0 and ships at T1, when the ship is recorded, then conductor.feature.duration.wall{feature=S} has one observation of T1 minus T0 in milliseconds | task-16 | covered | "one wall observation equal to ship time minus `runStartedAt` and one active observation equal to `activeMs` for an exact rollup" | diff-local |
| criterion | Story 6 happy: Given feature S's timing rollup reports an exact active total of A ms, when the ship is recorded, then conductor.feature.duration.active{feature=S} has one observation of A | task-16 | covered | "one wall observation equal to ship time minus `runStartedAt` and one active observation equal to `activeMs` for an exact rollup" | diff-local |
| criterion | Story 6 happy: Given S halted for two days between two dispatches, when both durations are recorded, then wall exceeds active by at least those two days | task-16 | covered | "wall minus active is at least 172800000 ms" | diff-local |
| criterion | Story 6 negative: Given the timing rollup reports state partial or unavailable, when the ship is recorded, then conductor.feature.duration.active has no data point for S (absent, never zero or a fabricated total) and conductor.feature.duration.wall is still recorded | task-16 | covered | "a partial rollup yields zero active observations (count 0, not a 0-valued point) while wall is recorded" | diff-local |
| criterion | Story 6 negative: Given the worktree's conduct-state has no run_started_at, when the ship is recorded, then conductor.feature.duration.wall has no data point for S and the ship counter still increments | task-16 | covered | "a missing `runStartedAt` yields zero wall observations while `feature.shipped` increments" | diff-local |
| criterion | Story 6 negative: Given a feature terminates by halt rather than ship, when the terminal is recorded, then neither duration histogram gains an observation | task-16 | covered | "a halted terminal leaves both histograms with zero observations" | diff-local |
| criterion | Story 7 happy: Given the daemon completes a discovery tick, when the snapshot is emitted, then a daemon_backlog_snapshot event is appended to the daemon ledger at .daemon/events.jsonl in the same schema as .pipeline/events.jsonl | task-2 | covered | "a root-bus snapshot event appears exactly once in `.daemon/events.jsonl` in the same JSON schema as `.pipeline/events.jsonl`" | diff-local |
| criterion | Story 7 happy: Given the daemon dispatches or ships a feature, when the lifecycle point is reached, then feature_dispatch_started, feature_dispatch_ended, and feature_shipped events are emitted on the daemon bus and appended to the daemon ledger | task-21 | covered | "`feature_dispatch_ended` is emitted on the root bus with the observed outcome when a dispatch returns" | diff-local |
| criterion | Story 7 happy: Given a step_completed is emitted on a feature bus and forwarded to the daemon bus, when the daemon's metrics listener records it, then the resulting conductor.step.duration data point carries that feature's slug as its feature attribute and the same attribute keys, unit, and name as the pre-change instrument | task-20 | covered | "recorded from a forwarded event stream with the pre-change names, units, and attribute keys" | diff-local |
| criterion | Story 7 happy: Given the three new event types exist, when the event-sink registry is compiled, then each has a sink declaration row with otel true and the OTel subscription list derived from the registry includes them | task-1 | covered | "`otelEventTypes()` and `persistedEventTypes()` each contain the four new type names" | diff-local |
| criterion | Story 7 negative: Given a gate_verdict is emitted on a feature bus and forwarded to the daemon bus, when both ledgers are read, then the event appears once in that feature's .pipeline/events.jsonl and zero times in .daemon/events.jsonl | task-2 | covered | "a forwarded gate_verdict appears once in the feature ledger and zero times in the daemon ledger" | diff-local |
| criterion | Story 7 negative: Given a new event type is added to the union without a sink row, when the project compiles, then compilation fails naming the missing row | task-1 | covered | "a union member without an EVENT_SINKS row fails compilation" | diff-local |
| criterion | Story 7 negative: Given a new event type has a sink row with otel true but no metrics-listener handler case, when the listener coverage test runs, then it fails naming the unhandled type | task-19 | covered | "names any `otel: true` event type lacking a `MetricsListener` handler and passes with every case present" | diff-local |
| criterion | Story 7 negative: Given the daemon ledger's directory is unwritable, when a snapshot is emitted, then the daemon logs the write failure once, the metrics are still recorded, and the loop continues | task-2 | covered | "one logged failure line, no throw, and the emitter still delivers the event to other subscribers" | diff-local |
| criterion | Story 8 happy: Given OTel is enabled and conduct runs interactively, when the run completes, then the visualizer owns the TracerProvider, a metrics listener on the same run bus owns an interactive MeterProvider, the existing instruments are exported with unchanged names and attributes, and both providers are shut down on stop | task-19 | covered | "the interactive exported instrument set (names, units, attribute keys) equals the pre-change set" | diff-local |
| criterion | Story 8 happy: Given the interactive path, when the metric Resource is inspected, then service.instance.id is P/W with W resolved exactly as in Story 2 and per-feature data points still carry feature | task-19 | covered | "the metric Resource is `P/W` with `feature` on per-feature points" | diff-local |
| criterion | Story 8 negative: Given the interactive path passes no spans-only flag, when the visualizer and listener initialize, then the listener constructs its own meter and the visualizer initializes without throwing on the absent flag | task-19 | covered | "a visualizer without the `metrics` flag initializes without throwing" | diff-local |
| criterion | Story 8 negative: Given the interactive listener owns its meter, when the run stops, then meterProvider.shutdown() is called exactly once | task-19 | covered | "the interactive listener's meter receives `shutdown()` exactly once on stop" | diff-local |

> **Amended 2026-09-08 by #1937:** The two Story 4 halt-classification mappings above are superseded by the accepted story wording: unreadable or unrecognized sidecars derive `unclassified`; an absent `HALT.class` with `HALT` present derives `legacy`. Task 14 covers both derivations.

> **Amended 2026-09-08 by operator resolution of AB-4:** The preceding amendment is superseded by the approved ADR's fail-closed boundary. The first Story 4 mapping includes absent sidecars and derives `unclassified`; the second covers a pre-boundary halt explicitly stamped `legacy` by the one-time migration. Task 14 owns the implementation and proof for both mappings.
