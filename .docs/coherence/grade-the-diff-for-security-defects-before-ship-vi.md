# Coherence: Grade the diff for security defects before ship via a build_review security rubric

**Date:** 2026-09-14
**Status:** PASS — zero coverage, consistency, or achievability findings
**Operator review:** Approved by James Stoup, 2026-09-14
**Source:** jstoup111/ai-conductor#2034
**Track / tier:** Technical / M
**Plan:** .docs/plans/grade-the-diff-for-security-defects-before-ship-vi.md
**Stories:** .docs/stories/grade-the-diff-for-security-defects-before-ship-vi.md (6 accepted stories, 45 criteria)
**Intake outcomes:** three staged desired-outcome bullets from the issue

This is a technical-track specification with no PRD, so FR rows are omitted. The five amended approved ADRs in this change set engage the ADR layer; their 22 citable decisions are judged against the plan's Architecture Obligation Coverage table under the `adr` rows.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
|---|---|---|---|---|
| outcome | outcome-1 | story-4, story-6 | covered | A build whose diff introduces a security defect (e.g. committed credential, injection path, missing authz check) fails build_review with a finding naming the defect and location, before the SHIP tail runs. |
| outcome | outcome-2 | story-3, story-6 | covered | A clean diff passes without security-related kickback noise (false-positive rate low enough that operators do not routinely override). |
| outcome | outcome-3 | story-4 | covered | Security findings flow through the adjudicator (#2033) like any other rubric's. |
| adr | adr-2026-08-22-build-review-opt-in-rubric-container | story-1, story-3, story-5 | covered | D1 membership and default-off are delivered by Tasks 1–2 (registry descriptor, `enabled: false` default); D4 preserved contracts by Task 6 (content-region locus, closed vocabulary); D2, D3, D5 impose no change (retired keys, test-quality specifics, tier-independence untouched). Plan obligation rows judged decision by decision: all five dispositions are semantically right. |
| adr | adr-2026-08-22-one-owner-per-review-question | story-4, story-5 | covered | D1's amended map assigns the security question to the rubric; Task 14 removes the competing code-review instruction and Task 10 proves the rubric never appends a task or routes to plan. Disposition `task` with two owners is right. |
| adr | adr-2026-08-21-review-bound-by-plan-done-when-criteria | story-4, story-6 | covered | D3 and D6 as amended exempt `security` from plan-binding: Task 10 proves no `beyond` path exists for it and Task 12 fixes a contract with no `boundTo`. D1, D4, D5 are no-change because the land-gate rung, the retained record kind, and daemon filing are unreachable for a rubric that never produces `beyond`; D2's optional field is absent from the security contract (Task 12). |
| adr | adr-2026-08-16-closed-build-review-finding-vocabularies | story-3, story-5, story-6 | covered | D1 (ten-member closed set, Task 6), D3 (out-of-vocabulary rejection and repair, Task 6), D5 (integrity guard binding both directions, Task 13) are task-delivered; D2 and D6 cite `normalizeBuildReviewFindingVocabularyMember` and `deriveEffectiveBuildReviewVerdictWithDispositions`, both present in `build-review-domain.ts` and `build-review-aggregate.ts`; D4 and D7 are no-change because no contract bump occurs. |
| adr | adr-2026-08-18-content-anchored-finding-reference-schema | story-3, story-6 | covered | D3 content-region binding for `security.locus` is delivered by Task 6's anchor grammar and identity hash; D1 `path` and D2 `plan-task` kinds are unused by this rubric and unchanged. No fourth kind is introduced. |
| story | story-1 | task-1, task-2, task-3, task-11 | covered | Register the security rubric as an opt-in built-in member; every happy and negative criterion is grounded in a cited task's Done-when check below. |
| story | story-2 | task-4, task-5 | covered | Freeze a whole-diff projection and cache the security judgement by content; every happy and negative criterion is grounded in a cited task's Done-when check below. |
| story | story-3 | task-6, task-7, task-8 | covered | Validate security findings against a closed vocabulary and content-region anchors; every happy and negative criterion is grounded in a cited task's Done-when check below. |
| story | story-4 | task-9, task-10 | covered | Fail the lap on a security finding and route it through the adjudicator; every happy and negative criterion is grounded in a cited task's Done-when check below. |
| story | story-5 | task-12, task-13, task-14 | covered | Bind the security skill contract to the engine and give security one owner; every happy and negative criterion is grounded in a cited task's Done-when check below. |
| story | story-6 | task-12 | covered | Judge the diff for concrete, evidenced security defects only; every happy and negative criterion is grounded in a cited task's Done-when check below. |
| task | task-1 | story-1 | covered | Register the security descriptor in the closed catalog; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-2 | story-1 | covered | Validate and default the security rubric policy; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-3 | story-1 | covered | Classify the security branch as dispatchable or skipped; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-4 | story-2 | covered | Derive the whole-diff security projection; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-5 | story-2 | covered | Prove content-addressed cache identity for security judgements; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-6 | story-3 | covered | Add the security vocabulary and content-region anchor grammar; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-7 | story-3 | covered | Stamp the security envelope and refuse reviewer-supplied identity; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-8 | story-3 | covered | Apply the security confidence floor; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-9 | story-4 | covered | Join security outcomes into the aggregate verdict; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-10 | story-4 | covered | Route security findings through the adjudicator with no plan-binding exemption; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-11 | story-1 | covered | Render the security dispatch prompt from its projection and vocabulary; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-12 | story-5, story-6 | covered | Author the build-review-security skill and its fixture judgements; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-13 | story-5 | covered | Bind the security skill to the integrity guard and the generated model table; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| task | task-14 | story-5 | covered | Remove security grading from the per-batch code-review evaluator; the Story line cites the story whose criteria the Done-when checks assert, with no terminal catch-all and no foreign protected-artifact mutation. |
| criterion | Story 1 happy: Given a project config with `build_review.rubrics.security.enabled: true`, when the build_review step classifies its branches, then `security` is a dispatchable branch alongside any other enabled member and carries its resolved `llm_provider`, `model`, `effort`, `model_fallback_ladder`, `max_retries`, `escalate`, and `min_confidence`. | 1, 3 | covered | "returns a dispatchable `security` branch" | diff-local |
| criterion | Story 1 happy: Given a project config that never mentions `security`, when configuration is resolved, then `security` resolves to `enabled: false` with default effort `high` and the branch settles as skipped with reason `disabled` without any provider, cache, or preflight work. | 2 | covered | "yields `rubrics.security.enabled === false`" | diff-local |
| criterion | Story 1 happy: Given `build_review.rubrics.security.enabled: true` and every other member disabled, when the lap runs, then only `security` is dispatched and the outer verdict is derived from its judged result alone. | 3 | covered | "invokes `dispatchModel` exactly once with `rubric: 'security'`" | diff-local |
| criterion | Story 1 negative: Given a project config with `build_review.rubrics.security.enabled: "yes"`, when configuration is validated, then validation fails naming `build_review.rubrics.security.enabled` and its expected boolean type, and no lap is dispatched. | 2 | covered | "rejects `build_review.rubrics.security.enabled: "yes"` and `effort: extreme`" | diff-local |
| criterion | Story 1 negative: Given a project config with `build_review.rubrics.security.effort: extreme`, when configuration is validated, then validation fails naming the key and the allowed effort values. | 2 | covered | "rejects `build_review.rubrics.security.enabled: "yes"` and `effort: extreme`" | diff-local |
| criterion | Story 1 negative: Given an enabled gate with `security` and `testQuality` both disabled, when the lap runs, then the verdict is PASS with reason `build_review_no_rubrics` and no grader is dispatched. | 3 | covered | "settles PASS with reason `build_review_no_rubrics`" | diff-local |
| criterion | Story 2 happy: Given a frozen snapshot whose diff touches three files across two prior batches, when the security projection is derived, then it lists all three files with their change kinds and hunk ranges, carries `mergeBase`, `headSha`, `lapId`, `snapshotDigest`, and `contentDigest`, and contains no test-scope, preflight, or counterfactual field. | 4 | covered | "`changedFiles` equals the by-reference parse of the frozen diff" | diff-local |
| criterion | Story 2 happy: Given a prior judged security result cached for a projection digest, when a later lap derives an identical projection under the same policy fingerprint and skill digest, then the branch is served from cache with no dispatch and a `build_review_cache_hit` occurrence for `security`. | 5 | covered | "served from cache with zero `dispatchModel` calls" | diff-local |
| criterion | Story 2 happy: Given a rebase that changes commit identities but leaves every hunk's content unchanged, when the projection is derived, then its digest equals the pre-rebase digest and the cached judgement is reused. | 5 | covered | "produce equal `security` projection digests" | diff-local |
| criterion | Story 2 negative: Given a cached security result, when `skills/build-review-security/SKILL.md` changes by one byte, then the cache lookup misses and the rubric is re-dispatched. | 5 | covered | "produces a cache miss and one dispatch for `security`" | diff-local |
| criterion | Story 2 negative: Given a cached security result, when the resolved `security` policy changes its model, then the cache lookup misses and the rubric is re-dispatched. | 5 | covered | "produces a cache miss and one dispatch for `security`" | diff-local |
| criterion | Story 2 negative: Given a snapshot whose diff is empty after machinery-authored path exclusion, when the security projection is derived, then `changedFiles` is empty, the rubric judges no findings, and the branch settles PASS without inventing a scope. | 4 | covered | "yields a `security` projection with `changedFiles: []`" | diff-local |
| criterion | Story 2 negative: Given `skills/build-review-security/SKILL.md` cannot be read at dispatch, when the branch runs, then it settles as an infrastructure failure with reason `cache-read-failed` and is never recorded as a cache hit or a PASS. | 5 | covered | "infrastructure failure `cache-read-failed`" | diff-local |
| criterion | Story 3 happy: Given a provider payload with one finding whose `concernKind` is `injection` and whose `anchor` is `{rubric: "security", locus: {path, contentHash: "sha256:…", display}}` over a projected hunk, when the result is validated, then the engine stamps a judged envelope for `security` with `kind`, `rubric`, `contractVersion`, `lapId`, and `snapshotDigest` taken from the projection and a `FAIL` verdict. | 7 | covered | "envelope identity fields come from the projection" | diff-local |
| criterion | Story 3 happy: Given two findings that differ only in `summary` and `evidenceLocations`, when finding identities are canonicalized, then they yield the same finding id. | 6 | covered | "share one id" | diff-local |
| criterion | Story 3 happy: Given a finding with integer `confidence` 40 and a resolved `security.min_confidence` of 60, when the effective verdict is derived, then the finding is suppressed as below the floor and does not fail the lap on its own. | 8 | covered | "suppresses a `security` finding whose confidence is below" | diff-local |
| criterion | Story 3 negative: Given a provider payload whose finding has `concernKind: "other"`, when the result is validated, then the payload is rejected naming the closed vocabulary, one repair turn is offered, and a byte-identical repair settles the branch as a dispatch failure rather than a PASS. | 6 | covered | "An out-of-vocabulary `concernKind`" | diff-local |
| criterion | Story 3 negative: Given a provider payload whose anchor carries `line: 42` instead of a `contentHash`, when the result is validated, then the payload is rejected naming the content-region grammar and no finding identity is minted. | 6 | covered | "rejects a coordinate-bearing locus" | diff-local |
| criterion | Story 3 negative: Given a provider payload that includes `verdict: "PASS"` or `rubric: "testQuality"` at the top level, when the result is validated, then the reviewer-supplied envelope fields are rejected and the engine stamps its own. | 7 | covered | "reviewer-supplied envelope field is rejected" | diff-local |
| criterion | Story 3 negative: Given a provider payload whose anchor path is not in the projection's `changedFiles`, when the result is validated, then the finding is rejected as outside the frozen input. | 6 | covered | "anchor path outside the projection's `changedFiles`" | diff-local |
| criterion | Story 3 negative: Given a provider reply stating it cannot perform a security review, when the result is validated, then the branch settles as an infrastructure failure, never as an empty-findings PASS. | 7 | covered | "A provider refusal settles the `security` branch as an infrastructure failure" | diff-local |
| criterion | Story 4 happy: Given `security` enabled and a judged result with one `committed-secret` finding anchored to the hunk that adds the credential, when the lap is joined, then the aggregate verdict is `FAIL`, the finding is a raw source for the adjudicator with `sourceId` `security:<findingId>`, and the outer verdict event reports the failure before any SHIP step runs. | 9 | covered | "yields `verdict: FAIL` with `security` among the failed rubrics" | diff-local |
| criterion | Story 4 happy: Given `security` enabled and a judged result with zero findings, when the lap is joined, then the aggregate verdict is `PASS` and the outer verdict event carries no security reason. | 9 | covered | "yields `verdict: PASS` with no security reason" | diff-local |
| criterion | Story 4 happy: Given a security finding whose location no plan task's `Done when:` names, when the lap is joined, then the finding is treated as bound and blocking, no `beyond` bucket or intake filing is produced, and the adjudicator receives it as an ordinary source. | 10 | covered | "includes an off-plan `security` finding in `currentFindings`" | diff-local |
| criterion | Story 4 happy: Given the adjudicator returns an `act` case for a security finding, when the route is reduced, then BUILD receives a bounded retry work order under the existing cumulative bound and no plan task is appended. | 10 | covered | "reduces to route `build` with a work order and appends no plan task" | diff-local |
| criterion | Story 4 negative: Given `security` and `testQuality` both enabled and `testQuality` settles as an infrastructure failure while `security` returns a valid judged result, when the lap is joined, then the security judged result and its findings are preserved and the lap's coverage records the test-quality failure separately. | 9 | covered | "preserves the security findings in the aggregate" | diff-local |
| criterion | Story 4 negative: Given the adjudicator returns `defer` for a security finding, when the route is reduced, then the finding is filed as an intake issue with the deferral justification and the lap does not silently PASS on that finding. | 10 | covered | "A `defer` case files intake with its justification" | diff-local |
| criterion | Story 4 negative: Given the adjudicator returns `refute` with evidence for a security finding, when the route is reduced, then the finding settles as refuted without charging the kickback ledger. | 10 | covered | "a `refute` case settles the source without charging the kickback ledger" | diff-local |
| criterion | Story 4 negative: Given the security branch ends in an infrastructure failure, when the lap is joined, then the aggregate verdict is not `PASS`, the mechanical-fault lane is charged rather than the kickback budget, and the outer verdict names the failed rubric. | 9 | covered | "charged to the mechanical-fault lane" | diff-local |
| criterion | Story 4 negative: Given a security finding that the disposition store already carries as an operator-accepted risk under the current contract version, when the effective verdict is derived, then that finding is excluded and an otherwise clean lap passes. | 9 | covered | "operator-accepted `security` finding is excluded" | diff-local |
| criterion | Story 5 happy: Given `skills/build-review-security/SKILL.md` declares the ten-member `**Closed vocabulary:**` line and the `**Reference grammar:**` line for `anchor.locus`, when the rubric vocabulary integrity check runs, then it iterates `security` alongside `testQuality`, executes the engine parser against each declared member, and passes. | 13 | covered | "iterates `security` in its rubric table and both per-rubric loops" | diff-local |
| criterion | Story 5 happy: Given `skills/build-review-security/SKILL.md` exists with `name`, `description`, `enforcement: gating`, `phase: build`, and `disable-model-invocation: true`, when the harness integrity suite runs, then the frontmatter, invocation-policy, and model-table checks pass with a generated `build-review-security` row. | 12 | covered | "carries the four required frontmatter fields" | diff-local |
| criterion | Story 5 happy: Given the per-batch code-review evaluator prompt, when it is rendered, then it contains no instruction to grade security, and its calibration text no longer names security vulnerabilities as an evaluator target. | 14 | covered | "contains no security-grading instruction" | diff-local |
| criterion | Story 5 negative: Given the skill text lists an eleventh vocabulary member the engine does not know, when the rubric vocabulary integrity check runs, then it fails naming `security` and the unknown member. | 13 | covered | "exits non-zero naming `security` and the drifted member" | diff-local |
| criterion | Story 5 negative: Given the engine vocabulary gains a member the skill text does not list, when the rubric vocabulary integrity check runs, then it fails naming `security` and the missing member. | 13 | covered | "exits non-zero naming `security` and the drifted member" | diff-local |
| criterion | Story 5 negative: Given `ARCHITECTURE.md`'s model table is hand-edited to add the security row without the metadata entry, when the model-table drift check runs, then it fails. | 13 | covered | "A hand-edited `ARCHITECTURE.md` security row without the metadata entry fails integrity check 5a" | diff-local |
| criterion | Story 6 happy: Given a diff that adds a string literal matching a live-credential shape to a committed source file, when the security rubric judges it, then it returns one `committed-secret` finding anchored to that hunk with the file path in `evidenceLocations` and a calibrated integer `confidence`. | 12 | covered | "validate as judged `FAIL` results with the named concern kinds" | diff-local |
| criterion | Story 6 happy: Given a diff that builds a shell or SQL command by concatenating request-derived input, when the security rubric judges it, then it returns one `injection` finding per independent sink anchored to the hunk that introduces the concatenation. | 12 | covered | "validate as judged `FAIL` results with the named concern kinds" | diff-local |
| criterion | Story 6 happy: Given a diff that removes an authorization check from a request handler, when the security rubric judges it, then it returns one `broken-access-control` finding anchored to the hunk that removes the check and cites the now-unprotected handler in `evidenceLocations`. | 12 | covered | "validate as judged `FAIL` results with the named concern kinds" | diff-local |
| criterion | Story 6 happy: Given a diff that makes an outbound request to a URL taken from request input without an allow-list, when the security rubric judges it, then it returns one `ssrf` finding anchored to the introducing hunk. | 12 | covered | "validate as judged `FAIL` results with the named concern kinds" | diff-local |
| criterion | Story 6 negative: Given a diff that only renames variables and reorders imports in a request handler, when the security rubric judges it, then it returns zero findings. | 12 | covered | "zero blocking findings" | diff-local |
| criterion | Story 6 negative: Given a diff that adds a test fixture containing an obviously fake credential under a test directory, when the security rubric judges it, then it returns zero `committed-secret` findings or a finding whose `confidence` is below 50. | 12 | covered | "zero blocking findings" | diff-local |
| criterion | Story 6 negative: Given a diff that touches a dependency manifest version without changing any code path, when the security rubric judges it, then it returns zero findings, because vulnerable-component judgement is out of this rubric's vocabulary. | 12 | covered | "zero blocking findings" | diff-local |
| criterion | Story 6 negative: Given a diff whose defect is an architectural design choice with no single introducing hunk, when the security rubric judges it, then it returns zero findings rather than anchoring an `insecure-design` concern the vocabulary does not admit. | 12 | covered | "zero blocking findings" | diff-local |
| criterion | Story 6 negative: Given a diff that introduces an exposure whose exploitable sink is on an unchanged line, when the security rubric judges it, then the finding anchors to the changed hunk that creates the exposure and names the unchanged line only in `evidenceLocations`. | 12 | covered | "zero blocking findings" | diff-local |

## Consistency and achievability

- **Cross-layer pairs checked both directions.** Outcome 1 (fail before SHIP) against Task 10 (adjudicator routes `act`/`defer`/`refute`): satisfying either leaves the other intact because `defer` files intake without publishing PASS on that source and the aggregate FAIL precedes the SHIP tail. Outcome 2 (clean diff passes without noise) against Task 12's non-finding fixtures and Task 8's floor: no oscillation — suppression never re-raises. ADR adr-2026-08-21 D3 (as amended) against Story 4's "never `beyond`": consistent; verified no engine code implements `beyond`.
- **Preserved-behaviour sweep.** Story 1's "never mentions `security`" and "both disabled → `build_review_no_rubrics`" criteria are protected observables; Tasks 3, 5, and 11 add dispatch, cache, and prompt effects only on the enabled branch, and Task 3's Done-when bounds the disabled branch to zero callbacks.
- **Representable outcomes.** Every scenario maps to an existing value: judged PASS/FAIL, skipped `disabled`, infrastructure `cache-read-failed`/provider refusal, suppressed, accepted-risk, and adjudicator `act`/`defer`/`refute`. No new state is invented.
- **Input boundary.** Criterion S3.7 (anchor outside `changedFiles`) is owned by Task 6, whose check names the projection's `changedFiles` as the boundary that Task 4 derives from the frozen diff.
- **Mechanism check.** Each criterion quote names a function, file, gate, or test assertion; no quote is a bare restatement of the Then-clause.

## Verification and limitations

- **Verified:** every cited task id, story id, and ADR stem exists in its artifact; every criterion quote is an exact substring of the cited task's Done-when block (checked programmatically while authoring); the obligation table's `existing` evidence names symbols present in `src/conductor/src/engine`.
- **Semantic verdict, 93% confidence:** the cited checks together deliver each accepted criterion without contradicting an amended decision. This is a specification judgement, not runtime coverage. No unresolved load-bearing assumption is required for the PASS.
- **Not claimed:** live provider grading quality for Story 6 — the fixture payloads prove the contract accepts the intended judgements, not that a model produces them; the false-positive risk is carried by the architecture review's register and the `min_confidence` floor.
- **Advisory overlap:** `origin/spec/daemon-self-host-guardrails` shares the three config files with Tasks 1–2; a rebase watchpoint, not a conflict.

**Verdict:** PASS. Ready for composer land/handoff pending operator review.

> **Amended 2026-09-16 by operator approval (James Stoup, interactive halt recovery for #2568):** Task 7 and the Story 3 envelope criterion retain their mapping, but their rejection wording above is superseded by adr-2026-08-19 D4 and the corrected story/plan: reviewer envelope fields are ignored; the engine stamps authoritative metadata and derives the verdict from validated findings. The coordinator tests prove this behavior.
