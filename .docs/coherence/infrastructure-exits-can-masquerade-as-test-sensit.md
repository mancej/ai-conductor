# Coherence Mapping: infrastructure-exits-can-masquerade-as-test-sensit (#2051)

Technical track — no `fr` rows. Outcome rows map the staged intake Desired-outcome bullets.

| Row class | Cited id | Counterpart id(s) | Verdict | Notes / quote | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-3, story-1 | covered | Story 3 makes an indeterminate counterfactual contribute neither support nor a finding; Story 1 removes the exit-code fiat |
| outcome | outcome-2 | story-1 | covered | Story 1: the counterfactual result no longer by itself implies sensitivity; unrevertable-state neutral treatment beyond this is scope-excluded per the track marker |
| outcome | outcome-3 | story-4 | covered | Story 4 keeps genuine reverted-production failures creditable across frameworks via reviewer judgement |
| outcome | outcome-4 | story-3 | covered | Story 3: a concrete stub-passable assertion still grounds a finding independently of the counterfactual |
| story | story-1 | task-1, task-2 | covered | tasks 1-2 cite Story 1 in their Story lines |
| story | story-2 | task-3, task-4, task-5, task-6 | covered | tasks 3-6 cite Story 2 |
| story | story-3 | task-7, task-9 | covered | tasks 7 and 9 cite Story 3 |
| story | story-4 | task-8 | covered | task 8 cites Story 4 |
| task | task-1 | story-1 | covered | Story line cites Story 1 happy criteria |
| task | task-2 | story-1 | covered | Story line cites Story 1 negatives |
| task | task-3 | story-2 | covered | Story line cites Story 2 |
| task | task-4 | story-2 | covered | Story line cites Story 2 |
| task | task-5 | story-2 | covered | Story line cites Story 2 negative |
| task | task-6 | story-2 | covered | Story line cites Story 2 negatives |
| task | task-7 | story-3 | covered | Story line cites Story 3 |
| task | task-8 | story-4 | covered | Story line cites Story 4; its rule text also serves story-3 |
| task | task-9 | story-3 | covered | infrastructure task; Story line cites Story 3's drift-binding Done-when |
| adr | adr-2026-08-30-counterfactual-sensitivity-judged-not-exit-coded | story-1, story-2, story-3, story-4 | covered | all four stories implement its decisions 1-5 |
| adr | adr-2026-08-17-framework-agnostic-tautology-scoped-run | story-1 | covered | amended in change set; Story 1 preserves its no-output-parsing and mechanical-fault-lane rules |
| adr | adr-2026-08-13-engine-managed-build-review-rubric-branches | story-1, story-2 | covered | amended in change set; Stories 1-2 honor its projection and cache principles |
| criterion | Story 1 happy: Given a counterfactual scoped run that completes with exit code 0, when the preflight materializes its result, then the classification is `stayed-green` and the scoped-run evidence carries `exitCode: 0` with an empty failure excerpt | task-1 | covered | exit 0 still materializes `stayed-green` with empty excerpt | diff-local |
| criterion | Story 1 happy: Given a counterfactual scoped run that completes with a nonzero exit code, when the preflight materializes its result, then the completed result's classification value asserts only that the run exited nonzero and the scoped-run evidence carries the exit code and the bounded head/tail excerpt | task-1 | covered | a completed nonzero scoped run materializes `classification: 'nonzero-exit'` carrying exit code and bounded excerpt | diff-local |
| criterion | Story 1 negative: Given a counterfactual run that fails to launch, times out, or dies on a signal, when the preflight materializes its result, then the classification is `infrastructure-failure` with its existing closed reason, exactly as before this change | task-2 | covered | each infrastructure reason (launch/timeout/signal) still maps to `infrastructure-failure` with its existing closed reason | diff-local |
| criterion | Story 1 negative: Given a completed nonzero exit, when the projection for the testQuality reviewer is built, then no projection field states or implies a sensitivity verdict — the excerpt and exit facts are present and unaltered | task-2 | covered | adds no field implying a verdict | diff-local |
| criterion | Story 1 negative: Given a preflight result cached before this change under the old evidentiary `red` value, when the next lap runs against the changed skill digest, then the cache misses and the preflight re-materializes rather than serving the stale classification meaning | task-1 | covered | cache-eligibility checks | diff-local |
| criterion | Story 2 happy: Given a testQuality reviewer result whose `counterfactualSensitivity` is `supports`, `indeterminate`, or `not-applicable`, when the engine validates the stamped envelope, then the result is accepted and the field is persisted with the judged envelope under `contractVersion: v3` | task-3 | covered | a v3 testQuality result with `counterfactualSensitivity` of each of `supports`, `indeterminate`, `not-applicable` validates | diff-local |
| criterion | Story 2 happy: Given an accepted result with the field present, when the branch verdict is settled and cached, then the persisted artifact and cache entry carry the field unchanged | task-4 | covered | finds the field intact | diff-local |
| criterion | Story 2 negative: Given a reviewer result whose `counterfactualSensitivity` is outside the closed vocabulary, when the engine validates it, then the result is rejected as malformed and the branch reruns as `absent` — no kickback is routed and the convergence cap does not tick | task-5 | covered | the branch settles as `absent` (rerun), no kickback route is produced, and the cumulative convergence counter does not increment | diff-local |
| criterion | Story 2 negative: Given a reviewer result omitting the field entirely, when the engine validates it, then validation accepts the result (the field is optional) and downstream weighing treats the counterfactual as it does today | task-3 | covered | a result omitting the field validates | diff-local |
| criterion | Story 2 negative: Given a stored accepted-risk disposition recorded before this change, when a later lap re-judges under the updated contract, then the disposition still matches its finding — the field is excluded from finding identity and the contract version has not bumped | task-6 | covered | a stored disposition keyed on the old id still matches a finding from a field-bearing result | diff-local |
| criterion | Story 2 negative: Given any reviewer result, when the engine canonicalizes finding identities, then `counterfactualSensitivity` contributes nothing to any finding id | task-6 | covered | canonical finding ids computed from two otherwise-identical results | diff-local |
| criterion | Story 3 happy: Given a reviewer result with `counterfactualSensitivity: indeterminate` and no findings, when the branch verdict is settled, then the branch passes exactly as an empty-findings result passes today | task-7 | covered | settles PASS exactly as an empty-findings result does today | diff-local |
| criterion | Story 3 happy: Given a reviewer result with `counterfactualSensitivity: indeterminate` and a `test-insensitive` finding citing a concrete stub-passable assertion, when the branch verdict is settled, then the finding stands — indeterminacy never suppresses an independently evidenced finding | task-7 | covered | settles FAIL with that finding intact | diff-local |
| criterion | Story 3 negative: Given an indeterminate counterfactual, when the reviewer weighs evidence per the skill contract, then the nonzero exit is not usable as sensitivity support for any in-scope test | task-8 | covered | `indeterminate` contributes neither sensitivity support nor a finding | diff-local |
| criterion | Story 3 negative: Given an indeterminate counterfactual, when the branch settles, then no plan task is appended and no route other than the existing pass/fail outcomes is taken | task-7 | covered | the engine never branches on the field's value | diff-local |
| criterion | Story 3 negative: Given repeated laps each ending indeterminate with findings unresolved, when the cumulative convergence bound is evaluated, then it increments exactly as today and terminates the loop at the existing cap | task-7 | covered | increment the cumulative bound exactly as today | diff-local |
| criterion | Story 4 happy: Given a counterfactual excerpt showing assertion or example failures after the intended tests executed, when the reviewer judges it, then `counterfactualSensitivity: supports` is the contract-conformant judgement and the engine accepts and persists it | task-8 | covered | (a) executed-example failures on the reverted tree | diff-local |
| criterion | Story 4 happy: Given a counterfactual excerpt showing a collection or load failure caused by reverted production (a removed module or symbol the changed tests require), when the reviewer judges it, then `supports` is the contract-conformant judgement per the skill text's named collection-failure case | task-8 | covered | (b) collection/load failures caused by reverted production | diff-local |
| criterion | Story 4 negative: Given a counterfactual excerpt showing only environment failure (database authentication, service boot, network) before any intended test could bear on behavior, when the reviewer judges it per the skill text, then `supports` is not a conformant judgement — the contract names this shape `indeterminate` | task-8 | covered | `indeterminate` covers environment failure before intended tests bear on behavior | diff-local |
| criterion | Story 4 negative: Given any judged `supports` value, when the outer verdict is settled, then `supports` alone raises no finding and blocks nothing — it remains evidence, never a verdict | task-7 | covered | flip the finding presence, verdict flips | diff-local |
