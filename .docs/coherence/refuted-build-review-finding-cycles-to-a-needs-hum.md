# Coherence Mapping: Refuted build_review finding cycles to a needs-human halt instead of settling

**Date:** 2026-09-09  
**Tier:** M  
**Track:** technical — the `fr` row class is omitted because this track has no PRD.  
**Outcome source:** `.pipeline/intake-outcomes.md` staged from `jstoup111/ai-conductor#2409` with three `## Desired outcome` bullets.  
**ADR row pool:** four non-deleted ADR files in this spec change set, each amended in this DECIDE pass: the predecessor adjudication ADR (D7.1–D7.6; citable ids D1–D10), the mixed-laps successor (D3.4, D5.3; citable D1–D5), the kickback no-op escalation ADR (D2.1; citable D1–D3), and the stable-finding-dispositions ADR (D4.1; citable D4). Sub-items are not separately citable; the plan's Architecture Obligation Coverage carries one row per citable id (19 rows).  
**Consistency pass (§4d):** every covered row was re-read in both directions across layers. The one cross-layer tension — outcome-3 ("a genuinely repeated, unrefuted finding still halts") against task-6 ("routes PASS") — is not an oscillation: task-6's PASS is conditioned on an admitted `refute` row while task-9 pins the `act` re-proposal halt unchanged; satisfying either leaves the other intact. Preserved-behavior sweep: the unrefuted-repeat path gains no new side effect — task-9's Done-when asserts no `remediation_case_refuted` occurrence on either halt.  
**Achievability pass (§4g):** every criterion was walked through its cited Done-when checks against the amended decisions. Closed sets were checked for representability: rejection reasons (`refute-without-binding`, `refutation-without-refuted-assertion`, `refutation-confidence-not-high`, `invalid-refute-effect`, `malformed-refutation-evidence`, `unresolvable-refutation-evidence`, `illegal-disposition-transition`, `refutation-repeat`), routes (`pass`/`halt`), effect states (`none`/`reserved`/`applied`/`failed`), and halt class (`needs-human`) each represent every required happy and negative scenario. Input boundaries: task-1 owns the parse boundary for evidence keys, task-3 owns path/excerpt resolution against the tree, task-5 owns the attempted/applied precondition read from the work order's attempted set. No `CANNOT-DELIVER` finding.  
**Architecture-decision tie-out (§4f):** each of the 19 obligation rows was judged: `task` rows cite Done-when checks that are the decision's own assertion (e.g. D7 ↔ "routes PASS with unchanged ledger counts", mixed D5 ↔ "exact refuted source id is absent from the live source set", dispositions D4 ↔ "prints the refuted case fields under an autonomous-outcomes section"); `existing` rows cite the shipped coordinator and join (PR #2087); `no-change` rows name decisions whose subject (reduced coverage, frozen snapshot, config switch, mechanical classifier, grader confidence, progress classifier, ledger baseline schema) no task reads or writes.

| Row class | Cited id / criterion | Counterpart / task id(s) | Verdict | Notes / verbatim quote | Disposition |
| --- | --- | --- | --- | --- | --- |
| outcome | outcome-1 | story-1, story-4, story-6 | covered | Refuted re-raise settles without a needs-human halt (Story 1), stays settled (Story 4), and the rationale is visible in build-review findings (Story 6). |
| outcome | outcome-2 | story-5 | covered | The narrow true remainder files an intake issue through the deferral effect and never enters a remediation lap; the operator confirmed on 2026-09-09 that the deferral lane replaces a build_review-to-simplify seam, which does not exist. |
| outcome | outcome-3 | story-3 | covered | An `act` re-proposal on an attempted case halts with the unchanged semantic-repeat reason and occurrence. |
| story | story-1 | task-1, task-4, task-6, task-7 | covered | Every cited task's **Story:** line names Story 1; criteria mapped row by row below. |
| story | story-2 | task-1, task-2, task-3, task-4, task-5, task-6 | covered | Every cited task's **Story:** line names Story 2; criteria mapped row by row below. |
| story | story-3 | task-5, task-9 | covered | Every cited task's **Story:** line names Story 3; criteria mapped row by row below. |
| story | story-4 | task-7, task-10 | covered | Every cited task's **Story:** line names Story 4; criteria mapped row by row below. |
| story | story-5 | task-2, task-11 | covered | Every cited task's **Story:** line names Story 5; criteria mapped row by row below. |
| story | story-6 | task-12 | covered | Every cited task's **Story:** line names Story 6; criteria mapped row by row below. |
| story | story-7 | task-6, task-8, task-13 | covered | Every cited task's **Story:** line names Story 7; criteria mapped row by row below. |
| task | task-1 | story-2, story-1 | covered | **Story:** line cites story-2, story-1. |
| task | task-2 | story-2, story-5 | covered | **Story:** line cites story-2, story-5. |
| task | task-3 | story-2 | covered | **Story:** line cites story-2. |
| task | task-4 | story-1, story-2 | covered | **Story:** line cites story-1, story-2. |
| task | task-5 | story-2, story-3 | covered | **Story:** line cites story-2, story-3. |
| task | task-6 | story-1, story-2, story-7 | covered | **Story:** line cites story-1, story-2, story-7. |
| task | task-7 | story-4, story-1 | covered | **Story:** line cites story-4, story-1. |
| task | task-8 | story-7 | covered | **Story:** line cites story-7. |
| task | task-9 | story-3 | covered | **Story:** line cites story-3. |
| task | task-10 | story-4 | covered | **Story:** line cites story-4. |
| task | task-11 | story-5 | covered | **Story:** line cites story-5. |
| task | task-12 | story-6 | covered | **Story:** line cites story-6. |
| task | task-13 | story-7 | covered | **Story:** line cites story-7. |
| adr | adr-2026-08-29-build-review-remediate-case-adjudication | story-1, story-2, story-3, story-4, story-5 | covered | D7.1–D7.6 appended this pass and implemented by Stories 1–5. Plan obligation rows D1–D10 judged: D2/D4/D5/D6/D7/D9 task-dispositioned to Done-when checks that are the decision text itself; D3 existing (raw join and coordinator shipped in PR #2087); D1/D8/D10 no-change (reduced coverage, frozen snapshot, and the adjudication switch are untouched). |
| adr | adr-2026-08-29-mixed-build-review-laps-preserve-content-adjudication | story-1, story-3, story-4 | covered | D3.4 and D5.3 appended this pass. Obligation rows D1–D5 judged: D3 task (task-6 PASS plus task-9 unchanged halt), D5 task (task-10 exact-id exclusion), D2 existing (one fresh remediate dispatch, shipped), D1/D4 no-change (branch classifier and grader confidence untouched). |
| adr | adr-2026-07-13-kickback-build-no-op-escalation | story-1 | covered | D2.1 appended this pass. Obligation rows D1–D3 judged: D2 task (task-6 effective PASS means the guard is not consulted, matching the #1831 exit), D1/D3 no-change (progress classifier and ledger baseline untouched). |
| adr | adr-2026-08-13-stable-build-review-finding-dispositions | story-6 | covered | D4.1 appended this pass and is the only citable decision the parser extracts from this ADR. Obligation row D4 task (task-12 autonomous-outcomes section, fail-closed on unreadable store). |
| criterion | Story 1 happy: Given an attempted case and a new lap whose rubric re-raises the same source id, when the judge binds that case with a `refute` row carrying one `refuted` assertion, resolvable evidence references, `high` confidence, and an effect of `none`, then the lap routes PASS with the trace reporting a finalized non-action outcome for that source | task-6 | covered | the coordinator routes PASS with unchanged ledger counts and no published work order | diff-local |
| criterion | Story 1 happy: Given the same refutation, when the lap completes, then the kickback ledger's build_review count and cumulative count are unchanged from before the lap and no BUILD work order is published | task-6 | covered | the coordinator routes PASS with unchanged ledger counts and no published work order | diff-local |
| criterion | Story 1 happy: Given the same refutation, when the lap completes, then the case record is resolved with a `refuted` terminal and carries the refuted claim, every assertion verdict, and the judge rationale | task-6 | covered | the coordinator routes PASS with unchanged ledger counts and no published work order | diff-local |
| criterion | Story 1 negative: Given an attempted case and a re-raise, when the judge binds it with a `refute` row whose every assertion is `upheld`, then the judgement is rejected fail-closed before any durable write and the lap does not route PASS | task-6 | covered | the coordinator routes PASS with unchanged ledger counts and no published work order | diff-local |
| criterion | Story 1 negative: Given an attempted case and a re-raise, when the judge binds it with a `refute` row whose confidence is `medium` or `low`, then the judgement is rejected fail-closed and the case remains open with disposition `act` | task-6 | covered | the coordinator routes PASS with unchanged ledger counts and no published work order | diff-local |
| criterion | Story 1 negative: Given an attempted case and a re-raise, when the judge binds it with a `refute` row whose effect is a BUILD action, then the judgement is rejected fail-closed and no work order is published | task-6 | covered | the coordinator routes PASS with unchanged ledger counts and no published work order | diff-local |
| criterion | Story 2 happy: Given a `refute` row bound to an attempted case with no prior refutation, when every evidence reference names an existing path whose file contains the normalized excerpt, then the row is admitted and the case transitions from `act` to `refute` | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 happy: Given an admitted refutation, when the case store is read back, then the refutation is present on the case record and no operator disposition record was created or changed | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row with no existing-case binding, when the judgement is validated, then it is rejected with a reason naming the missing binding and nothing is persisted | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row bound to an open `act` case that BUILD never attempted, when the judgement is reconciled, then it is rejected as an illegal disposition transition and the case remains open with disposition `act` | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row bound to an attempted case whose action effect is still reserved or failed, when the judgement is reconciled, then it is rejected as an illegal disposition transition | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row bound to a case with disposition `defer` or `reject`, when the judgement is reconciled, then it is rejected as an illegal disposition transition | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row whose evidence reference names a path that does not exist at the current tree, when the judgement is validated, then the whole judgement is rejected fail-closed and no waiver path accepts it | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row whose evidence reference names an existing path but an excerpt that does not occur in that file after whitespace normalization, when the judgement is validated, then the whole judgement is rejected fail-closed | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a `refute` row whose evidence reference carries a line number, hunk offset, or commit SHA field, when the judgement is parsed, then it is rejected as malformed | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 2 negative: Given a valid refutation, when the operator disposition store is inspected afterwards, then it contains no record for the refuted finding and the finding is not reported as operator-accepted | task-2, task-3, task-5 | covered | every rejection above leaves the case store byte-identical | diff-local |
| criterion | Story 3 happy: Given an attempted case and a re-raise, when the judge again proposes `act` on that case, then the lap halts needs-human with the existing semantic remediation case repeat reason naming the case id and the existing repeat-halt occurrence is emitted | task-9 | covered | the halt reason names the case id and the class is needs-human | diff-local |
| criterion | Story 3 happy: Given a case already resolved by refutation, when a later judgement binds a live source (one whose id is not the refuted source id) to that case with another `refute` row, then the lap halts needs-human with a reason naming the case id and the refutation repeat | task-9 | covered | the halt reason names the case id and the class is needs-human | diff-local |
| criterion | Story 3 negative: Given the second-refutation halt, when the case store is read back, then the original refutation is unchanged and no second refutation was persisted | task-9 | covered | the halt reason names the case id and the class is needs-human | diff-local |
| criterion | Story 3 negative: Given the second-refutation halt, when the kickback ledger is read back, then no charge was recorded | task-9 | covered | the halt reason names the case id and the class is needs-human | diff-local |
| criterion | Story 3 negative: Given an attempted case and a re-raise where the judge proposes `act`, when the halt is written, then its class is needs-human and the halt survives daemon sweeps until an operator clears it | task-9 | covered | the halt reason names the case id and the class is needs-human | diff-local |
| criterion | Story 4 happy: Given a case resolved by refutation with effect `none`, when a later lap re-raises the exact same source id, then that source is removed from the live source set, the judge is not dispatched for it, and the lap routes PASS when no other source is live | task-10 | covered | the exact refuted source id is absent from the live source set | diff-local |
| criterion | Story 4 happy: Given a case resolved by refutation whose residual deferral is applied, when a later lap re-raises the same source id, then the source is settled the same way | task-10 | covered | the exact refuted source id is absent from the live source set | diff-local |
| criterion | Story 4 negative: Given a case resolved by refutation whose residual deferral effect is reserved or failed, when a later lap re-raises the same source id, then the source is not settled, the lap does not route PASS, and the unfinished effect is reported as the blocker | task-10 | covered | the exact refuted source id is absent from the live source set | diff-local |
| criterion | Story 4 negative: Given a case resolved by refutation, when a later lap raises a finding whose id has drifted from the refuted source id, then the drifted finding is a live source and is adjudicated normally | task-10 | covered | the exact refuted source id is absent from the live source set | diff-local |
| criterion | Story 4 negative: Given a case resolved by refutation, when a later lap re-raises the same source id, then no regression halt is written for that case | task-10 | covered | the exact refuted source id is absent from the live source set | diff-local |
| criterion | Story 5 happy: Given a `refute` row whose effect is a complete deferral with a title, body, and exclusion rationale, when the refutation is admitted, then one intake issue is filed through the existing tracker seam with the sanitized body and the deferral effect is recorded as applied with the issue reference | task-11 | covered | residual is filed through the injected tracker client with the sanitized body | diff-local |
| criterion | Story 5 happy: Given the same lap, when it completes, then no BUILD action task exists for the remainder and no plan task was appended | task-11 | covered | residual is filed through the injected tracker client with the sanitized body | diff-local |
| criterion | Story 5 negative: Given a `refute` row whose deferral omits the exclusion rationale, when the judgement is validated, then it is rejected with the existing invalid-deferral reason | task-11 | covered | residual is filed through the injected tracker client with the sanitized body | diff-local |
| criterion | Story 5 negative: Given a `refute` row with a deferral, when the tracker is unavailable at filing time, then the deferral effect is recorded as failed, the lap does not route PASS, and the failure occurrence is emitted | task-11 | covered | residual is filed through the injected tracker client with the sanitized body | diff-local |
| criterion | Story 5 negative: Given a `refute` row with a deferral whose marker already matches an existing issue, when the effect is applied, then the existing issue is reused and no duplicate is filed | task-11 | covered | residual is filed through the injected tracker client with the sanitized body | diff-local |
| criterion | Story 5 negative: Given a `refute` row with a deferral whose body contains tracker-directed text, when the issue is filed, then the body passed to the tracker is the sanitized form | task-11 | covered | residual is filed through the injected tracker client with the sanitized body | diff-local |
| criterion | Story 6 happy: Given a feature whose case store holds a refuted case, when the operator runs `build-review findings` for that feature, then the output lists the case id, disposition, resolution, source ids, effect state, the refuted claim, each assertion verdict, and the judge rationale, labeled as an autonomous outcome | task-12 | covered | prints the refuted case fields under an autonomous-outcomes section | diff-local |
| criterion | Story 6 happy: Given a feature with both an operator disposition and a refuted case, when findings is rendered, then the two are printed in distinct sections and neither is described as the other | task-12 | covered | prints the refuted case fields under an autonomous-outcomes section | diff-local |
| criterion | Story 6 negative: Given a feature whose case store file is malformed, when findings runs, then the command reports the unreadable store and exits non-zero rather than omitting the cases | task-12 | covered | prints the refuted case fields under an autonomous-outcomes section | diff-local |
| criterion | Story 6 negative: Given a feature whose case store carries an unknown store version, when findings runs, then the command reports the unknown version and exits non-zero | task-12 | covered | prints the refuted case fields under an autonomous-outcomes section | diff-local |
| criterion | Story 6 negative: Given a feature with no case store file, when findings runs, then the listing succeeds and reports no autonomous cases | task-12 | covered | prints the refuted case fields under an autonomous-outcomes section | diff-local |
| criterion | Story 6 negative: Given the JSON output mode, when a refuted case is present, then the JSON carries the same case fields as the human rendering | task-12 | covered | prints the refuted case fields under an autonomous-outcomes section | diff-local |
| criterion | Story 7 happy: Given an admitted refutation, when the lap completes, then exactly one refutation occurrence for that case id is persisted to the events file with the lap id and, when a residual was filed, the residual effect id | task-8, task-13 | covered | one refutation occurrence per case id is persisted | diff-local |
| criterion | Story 7 happy: Given the remediate skill text, when the contract test runs, then the case-v1 section enumerates `refute` and `refuted`, the refutation record fields, and the binding, confidence, and evidence rules | task-8, task-13 | covered | one refutation occurrence per case id is persisted | diff-local |
| criterion | Story 7 negative: Given a rejected refutation, when the lap completes, then no refutation occurrence is emitted and the existing adjudication-failed occurrence carries the rejection reason | task-8, task-13 | covered | one refutation occurrence per case id is persisted | diff-local |
| criterion | Story 7 negative: Given the event sink registry, when the refutation member is missing a sink declaration, then the registry's exhaustiveness check fails to compile | task-8, task-13 | covered | one refutation occurrence per case id is persisted | diff-local |
| criterion | Story 7 negative: Given the remediate skill text with the refutation rules removed, when the contract test runs, then it fails naming the missing rule | task-8, task-13 | covered | one refutation occurrence per case id is persisted | diff-local |
