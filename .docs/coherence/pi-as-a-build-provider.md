# Coherence: Pi as a build provider

Date: 2026-09-24
Track: technical
Tier: L
Verdict: covered

## Outcome, story and task mapping

Six staged intake outcomes, seven stories, 21 tasks, three changed-ADR rows, and 44 criterion rows. Outcome quotes are the staged projection verbatim. No product FR layer applies: this is a technical-track spec with no PRD. Criterion quotes are exact completion-check text from the cited task's Done when block.

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Quote / Notes |
| --- | --- | --- | --- | --- |
| outcome | outcome-1 | story-5 | covered | A config selecting `llm_provider: pi` (run-level or `steps.<step>.llm_provider`) dispatches that work through the Pi CLI headlessly and the step completes with a normal verdict. |
| outcome | outcome-2 | story-6 | covered | Pi participates in the existing candidate-fallback ladder: when Pi is unavailable or exhausted, the run advances to the next configured provider exactly as claude/codex do today. |
| outcome | outcome-3 | story-5 | covered | Every Pi invocation is a fresh session (no resume), consistent with the harness's no-provider-session-resume policy. |
| outcome | outcome-4 | story-6 | covered | Pi CLI failures (auth, rate limit, model unavailable, missing binary) surface as the harness's classified provider failure signals, not as opaque step errors. |
| outcome | outcome-5 | story-4 | covered | Config validation rejects a `pi` selection with the same quality of error messaging as an unknown provider today, until/unless Pi is installed and registered. |
| outcome | outcome-6 | story-5, story-7 | covered | The default test suite exercises the Pi integration against a faithful fake; no real Pi/LLM calls outside opt-in smoke tests. |
| story | story-1 | task-1, task-3, task-4, task-8 | covered | Catalog, registration, executables and env from descriptors, and the parser-based literal ban. |
| story | story-2 | task-2, task-5, task-6, task-7 | covered | Named capability refusal and the three capability-narrowed consumer slices. |
| story | story-3 | task-9, task-10, task-11 | covered | Installation discovery with a closed reason set, exec guard, and one spine event. |
| story | story-4 | task-12, task-13, task-14, task-21 | covered | Not-installed validation ahead of unknown-provider, boot fail-fast in both entry points, and non-dispatching subcommands left alone. |
| story | story-5 | task-15, task-16, task-17 | covered | Pi adapter, fresh-session argv, faithful stream parsing, and abort. |
| story | story-6 | task-18, task-19 | covered | Classified Pi failures anchored to observed output and fallback-ladder parity. |
| story | story-7 | task-20 | covered | Live coverage over catalog plus plugins with a credential-gated Pi leg. |
| task | task-1 | story-1 | covered | Single descriptor table, derived id type, executable resolution including the new claude override. |
| task | task-2 | story-2 | covered | Named refusal for undeclared capabilities. |
| task | task-3 | story-1 | covered | Registration, policy tables, and defaults read the catalog. |
| task | task-4 | story-1 | covered | Executables, env namespaces, and invocation prefix resolved from descriptors. |
| task | task-5 | story-2 | covered | Build-review paths refuse by capability instead of branching on ids. |
| task | task-6 | story-2 | covered | Self-host paths refuse by capability instead of branching on ids. |
| task | task-7 | story-2 | covered | Runtime readiness, event and contract types, and residual literals derived from the catalog. |
| task | task-8 | story-1 | covered | Parser-based structural ban on id literals, closing D1 mechanically. |
| task | task-9 | story-3 | covered | Installation discovery with injected runner. |
| task | task-10 | story-3 | covered | Closed reason set and exec guard. |
| task | task-11 | story-3 | covered | Discovery result on the existing event spine. |
| task | task-12 | story-4 | covered | Distinct not-installed error, ordered ahead of unknown-provider. |
| task | task-13 | story-4 | covered | Integration owner for boot discovery and fail-fast in both entry points. |
| task | task-14 | story-4 | covered | CI-safe: only dispatching entry points probe. |
| task | task-15 | story-5 | covered | Pi adapter and descriptor. |
| task | task-16 | story-5 | covered | Stream parsing faithful to recorded Pi output. |
| task | task-17 | story-5 | covered | Lifecycle abort. |
| task | task-18 | story-6 | covered | Classified signals, anchored only to observed output. |
| task | task-19 | story-6 | covered | Fallback parity. |
| task | task-20 | story-7 | covered | Coverage over catalog plus plugins; credential-gated Pi leg. |
| task | task-21 | story-4 | covered | Both-path ordering, step and ladder exits through boot, and existing boot tests kept green. |
| adr | adr-2026-09-24-built-in-provider-catalog-and-boot-discovery | story-1, story-2, story-3, story-4, story-5, story-6, story-7 | covered | D1 and D2 by the catalog, capability refusal, and literal ban (tasks 1, 2, 5 to 8). D3 and D4 by discovery, startup validation, and boot wiring in both entry points (tasks 9 to 13, 21). D5 and D6 by the Pi adapter (tasks 15 to 18). D7 by live coverage (task 20). D8 by validation ordering and subcommand scoping (tasks 12, 14). |
| adr | adr-2026-08-12-live-provider-coverage-from-plugin-registry | story-7 | covered | D1 amendment delivered by the live-coverage test enumerating the catalog plus registered plugins (task 20). |
| adr | adr-2026-07-20-ci-fix-startup-preflight-and-error-classification | story-4 | covered | D3 amendment delivered by boot fail-fast on uninstalled providers (tasks 12, 13); D1 and D2 need no change here. |
| criterion | Story 1 happy: Given the catalog declares claude, codex, and pi, when the engine boots with all three installed, then the registered llm_provider ids are exactly the catalog ids. | 3 | covered | "a test asserts `registerBuiltins` given every catalog id as installed registers exactly the `BUILT_IN_PROVIDERS` ids as `llm_provider` plugins and no other built-in" | diff-local |
| criterion | Story 1 happy: Given an existing claude or codex configuration, when a step dispatches after the refactor, then the argv, environment prefixes, provider home, model ladder, and fallback behavior equal the pre-refactor behavior. | 4, 3, 19 | covered | "the existing claude and codex argv, environment-prefix, and provider-home tests pass with unchanged assertions" | diff-local |
| criterion | Story 1 happy: Given `CLAUDE_EXECUTABLE` is set to an absolute path, when a claude step dispatches, then that path is the spawned executable. | 4 | covered | "a test asserts a claude invoke with `CLAUDE_EXECUTABLE` set to an absolute path spawns that path as the subprocess executable" | diff-local |
| criterion | Story 1 negative: Given a production source file outside the catalog and the provider's own adapter module, when it contains a built-in provider id literal, then the structural test fails naming the file and line. | 8 | covered | "`provider-id-literals.test.ts` parses production source with the TypeScript compiler API and fails listing file and line for every string-literal node equal to a catalog id outside the catalog and adapter modules" | diff-local |
| criterion | Story 1 negative: Given a user-facing display string that names a provider, when the structural test runs, then it passes only if the string is supplied by the catalog descriptor rather than written literally. | 8 | covered | "a test asserts a provider display string written literally in a fixture is reported, and production display strings come from the descriptor `displayName` field" | diff-local |
| criterion | Story 1 negative: Given `CLAUDE_EXECUTABLE` names a path that does not exist, when the engine boots with claude configured, then startup fails with the not-installed error for claude naming reason not-found. | 9 | covered | "a test asserts that when `CLAUDE_EXECUTABLE` names a path that does not exist, claude is reported missing with reason `not-found` and `validateProviderInstallation` then raises `ProviderNotInstalledError` for claude" | diff-local |
| criterion | Story 2 happy: Given claude and codex declare their current capabilities, when self-host, build-review read-only review, and review-policy catalog paths run for them, then behavior is unchanged. | 5, 6 | covered | "build-review read-only review and policy modules accept `ProviderWith` capability types, and the existing claude and codex build-review tests pass with unchanged assertions" | diff-local |
| criterion | Story 2 happy: Given pi is selected for an ordinary build step, when the step dispatches, then no capability refusal occurs. | 15 | covered | "the pi descriptor declares no selfHost, readOnlyReview, reviewPolicyCatalog, writeFence, nativeSchema, costSelfReporting, or readiness capability, and a test asserts an ordinary build step with pi raises no capability refusal" | diff-local |
| criterion | Story 2 negative: Given pi is selected for a path that requires the selfHost capability, when that path is reached, then it fails before spawning with an error naming provider pi, capability selfHost, and the owning intake. | 6 | covered | "a test asserts preparing a self-host provider home for pi throws `ProviderCapabilityUnsupportedError` naming `selfHost` and `#1887`, and that the subprocess spawn stub was never called" | diff-local |
| criterion | Story 2 negative: Given pi is selected for build-review with a custom review policy, when the review-policy catalog path is reached, then it fails naming capability reviewPolicyCatalog instead of falling into the codex or claude branch. | 5 | covered | "a test asserts the review-policy catalog factory given provider pi throws `ProviderCapabilityUnsupportedError` naming `reviewPolicyCatalog`, and that neither the claude nor the codex policy discovery stub was called" | diff-local |
| criterion | Story 2 negative: Given a descriptor omits a capability flag, when any consumer queries it, then the capability is treated as unsupported. | 1 | covered | "a test asserts, for each of readiness, selfHost, readOnlyReview, reviewPolicyCatalog, supportsSessionResume, costSelfReporting, writeFence, and nativeSchema, a descriptor omitting that flag is reported unsupported by the catalog capability query" | diff-local |
| criterion | Story 3 happy: Given claude and codex executables resolve and their version probes exit 0 and pi is absent, when the daemon boots, then claude and codex are registered and pi is not. | 13, 9 | covered | "a test asserts `runDaemonMode` with only claude and codex discovered registers claude and codex as `llm_provider` plugins and does not register pi" | diff-local |
| criterion | Story 3 happy: Given a provider executable override env var is set, when discovery runs, then the override path is probed instead of the PATH lookup. | 9 | covered | "a test asserts that when a descriptor override env var is set, the injected runner receives the override path instead of the PATH lookup result" | diff-local |
| criterion | Story 3 happy: Given discovery completes, when the event log is read, then one provider-discovery event lists the installed ids and each missing id with its reason. | 11 | covered | "a test asserts exactly one `provider_discovery` event carrying the installed ids and each missing id with its reason is persisted to `.pipeline/events.jsonl` per boot" | diff-local |
| criterion | Story 3 happy: Given the CLI entry point boots, when discovery runs, then it produces the same installed set as the daemon for the same environment. | 13 | covered | "a test asserts the CLI and daemon entry points given the same injected environment produce the same installed set" | diff-local |
| criterion | Story 3 negative: Given an executable resolves but its version probe exits non-zero, when discovery runs, then that provider is missing with reason version-failed. | 10 | covered | "a test asserts a version probe exiting 1 yields reason `version-failed` and an EACCES spawn error yields reason `not-executable`" | diff-local |
| criterion | Story 3 negative: Given an executable resolves but is not executable, when discovery runs, then that provider is missing with reason not-executable. | 10 | covered | "a test asserts a version probe exiting 1 yields reason `version-failed` and an EACCES spawn error yields reason `not-executable`" | diff-local |
| criterion | Story 3 negative: Given a version probe does not exit within the probe timeout, when discovery runs, then that provider is missing with reason timeout and boot is not delayed beyond the timeout. | 10 | covered | "a test asserts a probe that never exits yields reason `timeout` and `discoverInstalledProviders` resolves within the timeout plus 100ms" | diff-local |
| criterion | Story 3 negative: Given the test environment forbids real exec, when discovery runs without an injected runner, then it throws the real-exec guard error rather than spawning. | 10 | covered | "a test asserts calling `discoverInstalledProviders` without an injected runner while real exec is forbidden throws the `assertRealExecAllowed` error and spawns nothing" | diff-local |
| criterion | Story 4 happy: Given every provider named in config is installed, when the daemon boots, then startup continues and no provider error is raised. | 12, 13 | covered | "a test asserts `validateProviderInstallation` returns without error when every configured provider is installed" | diff-local |
| criterion | Story 4 negative: Given run-level `llm_provider: pi` and pi is not installed, when the daemon boots, then it exits non-zero with an error naming provider pi, the config path, and the discovery reason. | 13, 12 | covered | "a test asserts `runDaemonMode` with run-level pi configured and discovery reporting pi missing exits non-zero with the not-installed message naming pi, its config path, and the discovery reason, and never calls the claim, worktree-create, or dispatch stubs" | diff-local |
| criterion | Story 4 negative: Given `steps.build.llm_provider` names codex and codex is not installed, when the daemon boots, then the error names the step config path. | 21, 12 | covered | "a test asserts `runDaemonMode` exits non-zero with the not-installed error naming the config path for a missing `steps.build.llm_provider` codex and for a pi-then-claude ladder whose pi entry is missing" | diff-local |
| criterion | Story 4 negative: Given a fallback ladder of pi then claude and only claude is installed, when the daemon boots, then startup fails naming pi rather than silently dropping it from the ladder. | 21, 12 | covered | "a test asserts `runDaemonMode` exits non-zero with the not-installed error naming the config path for a missing `steps.build.llm_provider` codex and for a pi-then-claude ladder whose pi entry is missing" | diff-local |
| criterion | Story 4 negative: Given config names an id that is neither a catalog id nor a registered plugin, when the daemon boots, then the existing unknown-provider error is raised, worded differently from the not-installed error. | 12 | covered | "a test asserts a name that is neither a catalog id nor a registered plugin raises the existing unknown-provider error whose wording differs from `ProviderNotInstalledError`" | diff-local |
| criterion | Story 4 negative: Given config names catalog id pi and pi is not installed, when startup validation runs, then the not-installed error is raised and the unknown-provider error is not, and any available-names list shows installed providers only. | 12 | covered | "a test asserts a catalog id that is not installed raises `ProviderNotInstalledError` and not the unknown-provider error, and the unknown-provider available-names list contains installed ids only" | diff-local |
| criterion | Story 4 negative: Given no provider CLI is installed on the machine, when a subcommand that never dispatches a provider runs (for example rate-card refresh or overlap-scan), then it neither probes provider executables nor fails for a missing provider. | 14 | covered | "a test asserts the `rate-card refresh`, `overlap-scan`, and `render-diagrams` subcommands complete with no provider executable resolvable on PATH and the discovery stub never called" | diff-local |
| criterion | Story 4 negative: Given the CLI entry point with the same missing provider, when a conduct run starts, then it fails with the same not-installed error before any step dispatches. | 13 | covered | "a test asserts the CLI `conduct run` path with the same missing provider fails with the same not-installed error before any step dispatch stub is called" | diff-local |
| criterion | Story 5 happy: Given pi is installed and selected for a step, when the step dispatches, then Pi is spawned with print mode, no session, and JSON mode, with the prompt on stdin. | 15 | covered | "a test asserts `PiProvider.invoke` spawns the resolved pi executable with `-p --no-session --mode json`, writes the prompt to stdin, and passes no `--model` argument" | diff-local |
| criterion | Story 5 happy: Given Pi emits a JSONL stream ending in a terminal assistant message and exits 0, when the adapter parses it, then the invoke result carries that message as output and the step reaches its normal verdict. | 16, 19 | covered | "a test asserts a fixture stream ending in a terminal assistant message with exit 0 yields an invoke result whose output equals that message and whose step reaches a normal verdict" | diff-local |
| criterion | Story 5 happy: Given Pi reports cumulative usage in the stream, when the invoke result is built, then the usage is attached and the cost is recorded as cost-unmetered. | 16 | covered | "a test asserts cumulative usage from the stream is attached to the invoke result and its cost state is `cost-unmetered`" | diff-local |
| criterion | Story 5 happy: Given a step retries after a failure, when Pi is invoked again, then the retry also runs with no session and never resumes a prior one. | 15 | covered | "a test asserts a second invoke for a retried step also carries `--no-session`, and `PiProvider.supportsSessionResume` is false" | diff-local |
| criterion | Story 5 negative: Given Pi exits 0 but the stream contains no terminal assistant message, when the adapter parses it, then the invoke result is a step failure naming the missing terminal message. | 16 | covered | "a test asserts a stream with no terminal assistant message and exit 0 yields a step failure whose reason names the missing terminal message" | diff-local |
| criterion | Story 5 negative: Given a JSONL line is malformed, when the adapter parses the stream, then that line is ignored and the result is still derived from the valid terminal message if present. | 16 | covered | "a test asserts a stream containing a malformed line still yields the terminal message as output, the malformed line being ignored" | diff-local |
| criterion | Story 5 negative: Given the step is aborted by the lifecycle supervisor, when Pi is running, then the Pi subprocess is terminated and the result is an aborted step, not a success. | 17 | covered | "a test asserts aborting the invoke signal while Pi runs kills the fake subprocess and the invoke result is an aborted step rather than a success" | diff-local |
| criterion | Story 6 happy: Given a ladder of pi then claude, when Pi fails with a run-scope unavailable signal, then the run advances to claude exactly as it would from codex. | 19 | covered | "a test parameterized over pi and codex as the first rung asserts a run-scope `providerUnavailable` result causes identical advance to the claude fake and the step completes on claude" | diff-local |
| criterion | Story 6 happy: Given Pi reports its unknown-model error on stderr and exits 1, when the adapter classifies it, then the result carries the model-unavailable signal. | 18 | covered | "a test asserts stderr `Error: Model "x" not found. Use --list-models to see available models.` with exit 1 yields `modelUnavailable`" | diff-local |
| criterion | Story 6 negative: Given the Pi executable disappears after boot, when a step spawns Pi, then ENOENT or exit 127 maps to provider-unavailable with run scope. | 18 | covered | "a test asserts ENOENT and exit 127 each yield `providerUnavailable` with `providerUnavailableScope` run" | diff-local |
| criterion | Story 6 negative: Given Pi exits non-zero with stderr matching no confirmed signature, when the adapter classifies it, then the result is an ordinary step failure with no provider signal set. | 18 | covered | "a test asserts a non-zero exit whose stderr matches no anchored signature yields a step failure with no provider signal set" | diff-local |
| criterion | Story 6 negative: Given Pi exits non-zero with auth-failure or rate-limit output, when the adapter classifies it, then the result is an ordinary step failure with no auth-failure or rate-limited signal set, because no Pi auth or rate-limit signature is anchored in this feature. | 18 | covered | "`pi-provider.ts` anchors no auth-failure or rate-limit regex, and a test asserts Pi auth-failure and rate-limit style stderr with a non-zero exit yields a step failure with neither `authFailure` nor `rateLimited` set" | diff-local |
| criterion | Story 7 happy: Given the catalog includes pi, when the live-coverage structural test runs, then it finds a Pi descriptor entry and a Pi live smoke leg. | 20 | covered | "`live-e2e-providers.ts` is keyed by catalog ids, contains a pi entry, and `daemon-e2e-live-pi.smoke.test.ts` runs a trivial Pi step when credentials are present and live tests are opted in" | diff-local |
| criterion | Story 7 happy: Given an external `llm_provider` plugin is registered, when the live-coverage structural test runs, then that plugin is enumerated alongside the catalog ids. | 20 | covered | "the live-coverage structural test iterates `BUILT_IN_PROVIDERS` together with registered external plugin ids and a test asserts a registered fixture plugin is enumerated" | diff-local |
| criterion | Story 7 happy: Given Pi credentials are present and live tests are opted in, when the Pi smoke leg runs, then a trivial Pi step completes through the real CLI. | 20 | covered | "`live-e2e-providers.ts` is keyed by catalog ids, contains a pi entry, and `daemon-e2e-live-pi.smoke.test.ts` runs a trivial Pi step when credentials are present and live tests are opted in" | diff-local |
| criterion | Story 7 negative: Given a test machine without Pi installed, when the live-coverage structural test runs, then it still requires the Pi entry because it enumerates the catalog plus registered plugins, not discovered providers. | 20 | covered | "a test asserts the live-coverage structural test requires the pi entry and smoke leg while discovery reports no provider installed" | diff-local |
| criterion | Story 7 negative: Given Pi credentials are absent, when the live smoke suite runs, then the Pi leg is skipped with a named reason and the default suite makes no real Pi call. | 20 | covered | "a test asserts the Pi smoke leg skips with a named reason when Pi credentials are absent and makes no real Pi call" | diff-local |

## Outcome interpretation notes

- **outcome-2.** "Unavailable" is read as unavailable at runtime, after boot. A provider that is not installed now fails startup under ADR D4 and the adr-2026-07-20 decision 3 amendment; the operator chose this as a bug fix. An installed provider that is unavailable at runtime advances the ladder exactly as claude and codex do (task 19). Pi exhaustion (rate-limit or usage) is not classified in this feature. The operator deferred Pi auth and rate-limit signatures to a follow-up intake because BUILD never invokes a real Pi, so until then such output is an ordinary step failure (task 18).
- **outcome-5.** Configuration that selects an uninstalled pi is rejected at every entry point that dispatches provider work (ADR D8). The error is intentionally distinct from the unknown-provider error and names the provider, config path, and discovery reason (ADR D4), which meets the outcome's quality bar.
