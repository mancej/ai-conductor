# Coherence: Portable, non-competing build review policy

**Date:** 2026-09-11
**Status:** PASS — zero remaining coverage, consistency, or achievability findings
**Operator review:** Approved by James Stoup, 2026-09-11
**Source:** jstoup111/ai-conductor#1986
**Track / tier:** Product / L
**Plan:** .docs/plans/projects-cannot-add-portable-non-competing-build-r.md (operator-approved 2026-09-11; additive coherence clarifications preserve its scope)
**Stories:** .docs/stories/projects-cannot-add-portable-non-competing-build-r.md (18 accepted stories, 93 criteria)
**PRD:** .docs/specs/2026-09-10-projects-cannot-add-portable-non-competing-build-r.md
**Model basis:** Native Codex session, following HARNESS Model Selection; the Claude opus alias is not substituted across providers.

## Review basis

This is an artifact consistency and achievability verdict, not a claim that the proposed runtime already passes tests. Every counterpart was checked against the accepted story and actual plan completion text. The repository parsers verify identities, exact quotes, required rows, and the architecture table; the semantic review below judges whether those checks together deliver their requirement under the approved design.

The staged intake marker contains the correct Source-Ref but zero Desired-outcome bullets. The verified readStagedIntakeOutcomes contract therefore makes the outcome row class not required; no outcome rows are fabricated. The approved PRD retains the full issue scope. Sixteen functional FR rows, eighteen story rows, forty task rows, seven changed-ADR rows, and ninety-three criterion rows are required: 174 rows total.

The PRD's historical FR-17 adoption-guidance text is retained unchanged under Delivery Requirements with its approved classification note. The stories/plan documentation boundaries exclude an ordinary documentation story or functional task. HARNESS Conventions requires README and affected guides in the same implementation change and finish verifies them; this obligation remains required rather than being credited as a runtime test or silently removed. D12's functional publication/events obligation is assigned to Task 40.

## Requirements, stories, tasks, and changed decisions

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes |
| --- | --- | --- | --- | --- |
| fr | fr-1 | story-1 | covered | Source selection and real-provider delivery cover project, global, and locally installed plugin sources without an authored copy; unavailable and ambiguous origins cannot substitute. |
| fr | fr-2 | story-2 | covered | Loaded configuration reaches the effective catalog; disabled members have zero discovery/cache/judge participation and invalid declarations stop before dispatch. |
| fr | fr-3 | story-3, story-4 | covered | Canonical catalog/preflight boundaries distinguish missing, ambiguous, partial, unreadable, and declared-incompatible policy from judged coverage; runtime unsupported follows the approved FR-3 amendment. |
| fr | fr-4 | story-5, story-9 | covered | Both real invoke adapters and candidate-loop integration deliver the selected obligations with actual producing identity, independent fallback resolution, and existing cause-specific failure handling. |
| fr | fr-5 | story-4, story-6 | covered | Complete package capture and read-only review adaptation preserve supporting criteria; missing, escaping, changing, oversized, or incompatible material cannot become an eligible informed judgment. |
| fr | fr-6 | story-7 | covered | Engine-stamped self-describing evidence and the existing publication consumers preserve original content/source/provider attribution and distinguish reuse or failure from new judging. |
| fr | fr-7 | story-8, story-9 | covered | Actual-candidate complete content and semantic cache identity control hit eligibility; candidate partitioning, strict legacy parsing, and atomic writes prevent incompatible or partial reuse. |
| fr | fr-8 | story-10 | covered | One captured source view reaches every member in a custom lap, including built-in peers; the two real invoke adapters consume the probed Linux read-only profile and private scratch. |
| fr | fr-9 | story-11 | covered | The shared settled-lap operation is wired into both mode entry points; one adjudicator/route owns eligible content and no-content paths remain no-judgment routes. |
| fr | fr-10 | story-12 | covered | Complete adjudication input plus explicit consistency and exhaustive case validation yield one justified repair set or zero-effect stop, preserving all merged/conflicting sources. |
| fr | fr-11 | story-13 | covered | Source-graph completeness, current membership, floors, exact settled recurrence, and action-only work publication account for every live finding without autonomous risk authority. |
| fr | fr-12 | story-14 | covered | Actual admitting task references and owner escalation prohibit off-plan repair and current-gap deferral; explicit decision resolution re-evaluates rather than rewriting historic verdicts. |
| fr | fr-13 | story-15 | covered | The existing effect/ledger/progression boundaries charge one admitted route, retain semantic-repeat limits and genuine refund rules, and require current invalidated test/review proof after repair. |
| fr | fr-14 | story-16 | covered | Leased recovery replays only missing authorized effects while preserving custom provenance, unresolved history, decision stops, and current operator state through crashes and competing resumes. |
| fr | fr-15 | story-17 | covered | Accepted risk remains finding/content-bound; reduced coverage uses validated declaration plus closed reason with no digest requirement and cannot suppress findings or zero-judgment safeguards. |
| fr | fr-16 | story-18 | covered | Built-in-only compatibility keeps defaults, empty scope/container, specialized schema, operator binding, and execution-mode behavior while applying the actual-candidate cache correction. |
| story | story-1 | task-3, task-4, task-5, task-16 | covered | Select an installed policy from each supported source; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-2 | task-1, task-2, task-23 | covered | Control participation through project configuration; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-3 | task-3, task-4, task-5, task-6, task-11 | covered | Stop clearly when policy selection cannot be established; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-4 | task-10, task-11, task-16 | covered | Adopt criteria under the supported review role; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-5 | task-6, task-14, task-15, task-16, task-19 | covered | Preserve policy obligations across providers and fallback; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-6 | task-7, task-8, task-9 | covered | Review with complete, stable supporting material; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-7 | task-21, task-22, task-23, task-40 | covered | Attribute judgments and reuse to their real source; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-8 | task-17, task-18, task-19 | covered | Reuse only compatible effective review input; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-9 | task-15, task-18, task-19 | covered | Keep candidate-specific caches correct during fallback; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-10 | task-12, task-13, task-14 | covered | Protect a shared immutable input for custom-policy laps; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-11 | task-32, task-33, task-34 | covered | Use one aggregate authority in attended and daemon review; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-12 | task-28, task-29, task-30, task-31, task-32 | covered | Resolve overlapping and contradictory findings before repair; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-13 | task-21, task-22, task-24, task-29, task-30, task-34, task-35 | covered | Account for every blocking custom finding; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-14 | task-28, task-29, task-30, task-31, task-35 | covered | Return off-plan findings to their decision owner; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-15 | task-24, task-35, task-36, task-37 | covered | Repair within the existing convergence limits and verify again; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-16 | task-23, task-31, task-37, task-38, task-39 | covered | Recover custom review without duplicated effects or lost blockers; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-17 | task-25, task-26, task-27, task-34, task-39, task-40 | covered | Preserve exact operator risk and coverage authority; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| story | story-18 | task-2, task-20, task-21 | covered | Preserve existing projects while correcting policy identity; all happy and negative criteria are grounded separately below, and the cited FRs are delivered by these scenarios. |
| task | task-1 | story-2 | covered | Load valid custom declarations into the effective rubric catalog; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-2 | story-2, story-18 | covered | Reject unsafe declarations before dispatch while preserving legacy keys; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-3 | story-1, story-3 | covered | Select one canonical installed policy from host-neutral descriptors; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-4 | story-1, story-3 | covered | Read Codex installed skills through the prepared app-server catalog; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-5 | story-1, story-3 | covered | Read Claude installed skills and enabled plugin manifests; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-6 | story-3, story-5 | covered | Terminate failed metadata discovery without candidate fallback; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-7 | story-6 | covered | Capture and hash the complete selected policy package; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-8 | story-6 | covered | Refuse incomplete, escaping, and oversized policy resources; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-9 | story-6 | covered | Reject changing or partially stored policy captures; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-10 | story-4 | covered | Adapt ordinary installed skill text into the review contract; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-11 | story-3, story-4 | covered | Represent declared and runtime policy incompatibility explicitly; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-12 | story-10 | covered | Materialize one source view for every member of a custom lap; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-13 | story-10 | covered | Enforce the Linux read-only review boundary and its probes; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-14 | story-5, story-10 | covered | Carry the read-only profile through both provider invoke adapters; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-15 | story-5, story-9 | covered | Expose a prepared-candidate operation inside the auxiliary lifecycle; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-16 | story-1, story-4, story-5 | covered | Wire installed policy judging through the actual candidate boundary; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-17 | story-8 | covered | Define complete semantic cache identity and stable incidental fields; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-18 | story-8, story-9 | covered | Partition candidate entries and refuse incomplete cache writes; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-19 | story-5, story-8, story-9 | covered | Run cache lookup and write after each actual candidate prepares; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-20 | story-18 | covered | Bind built-in review to its effective candidate without custom prerequisites; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-21 | story-7, story-13, story-18 | covered | Parse a bounded custom finding contract independently of built-in vocabulary; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-22 | story-7, story-13 | covered | Validate content references and stamp custom result identity; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-23 | story-2, story-7, story-16 | covered | Persist and read self-describing custom evidence and reuse provenance; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-24 | story-13, story-15 | covered | Extend suppression and exact settled recurrence to custom identities; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-25 | story-17 | covered | Match custom accepted risk to the exact judged content; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-26 | story-17 | covered | Match digestless custom coverage to declaration and closed reason; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-27 | story-17 | covered | Expose custom operator decisions through existing CLI and late-state checks; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-28 | story-12, story-14 | covered | Deliver complete policy, ownership, task, and prior-case context; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-29 | story-12, story-13, story-14 | covered | Represent consistency, admission, and escalation in case-v2; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-30 | story-12, story-13, story-14 | covered | Reject incomplete source graphs and illegal partial action sets; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-31 | story-12, story-14, story-16 | covered | Persist decision-owner stops without turning current gaps into deferrals; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-32 | story-11, story-12 | covered | Create one shared custom-review outcome operation; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-33 | story-11 | covered | Route attended and daemon custom review through the shared operation; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-34 | story-11, story-13, story-17 | covered | Compose mixed coverage and content outcomes without erasing either; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-35 | story-13, story-14, story-15 | covered | Publish only admitted consistent repairs through the existing work order; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-36 | story-15 | covered | Reopen invalidated verification when a custom repair changes code; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-37 | story-15, story-16 | covered | Preserve cumulative charges and semantic repeat stops for custom cases; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-38 | story-16 | covered | Resume persisted custom outcomes and apply only missing effects; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-39 | story-16, story-17 | covered | Refuse corrupt state and competing recovery effects; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| task | task-40 | story-7, story-17 | covered | Publish policy and decision provenance through existing event consumers; named completion checks own the listed behavior and its scoped proof, with no terminal catch-all or foreign protected-artifact mutation. |
| adr | adr-2026-09-10-portable-build-review-policy | story-1, story-2, story-3, story-4, story-5, story-6, story-7, story-8, story-9, story-10, story-11, story-12, story-13, story-14, story-15, story-16, story-17, story-18 | covered | D1–D12 map to the catalog/candidate/material/profile/cache/result/context/case/application/recovery/publication owners. The complete-package check, total closed failure mapping, no-content route qualification, and existing PR/shipped publication checks now make the prior intended obligations explicit. D12 ordinary adoption guidance remains required delivery work under HARNESS and the PRD classification; it is not a functional task. |
| adr | adr-2026-09-10-separate-custom-review-coverage-identity | story-7, story-13, story-16, story-17, story-18 | covered | D1–D4 are honored by separate typed record kinds and matchers, feature/declaration/reason coverage with unavailable content, existing explicit operator/exhaustion/current-state gates, minimum judged coverage, and existing store/publication paths. Content-bound risk cannot be transferred through a coverage record. |
| adr | adr-2026-08-18-mechanical-rubric-faults-are-their-own-lane | story-3, story-4, story-7, story-11, story-15, story-16, story-17 | covered | D1/D2 preserve kind-based closed causes; D3 is read under the later mixed-lap amendment, so infrastructure-only keeps no semantic dispatch/charge while eligible mixed content can act. D4/D5 retain bounded mechanical allowance and needs-human exhaustion. D6–D10 use the exact existing CLI, separate records, declaration/reason custom extension, healed/zero-judgment safeguards, and current-failure publication on existing consumers. |
| adr | adr-2026-08-21-engine-identity-in-build-review-cache-key | story-5, story-6, story-7, story-8, story-9, story-17, story-18 | covered | D1/D2 preserve the pure cache boundary and engine content-only stamp semantics. Amended D3/D6 resolve complete delivered policy inside each actual candidate. D4 staged legacy causes and D5 existing discard-event behavior are retained. Amended D7 preserves built-in dispositions and applies distinct custom risk/coverage identities, without cache-clear commands or a new expiry mechanism. |
| adr | adr-2026-08-22-build-review-opt-in-rubric-container | story-2, story-7, story-11, story-18 | covered | Amended D1 admits default-off custom declarations without changing built-in defaults or empty-container PASS. D2 retired/unknown key behavior remains. D3 honors specialized testQuality scope and later engine-owned scope refinement. D4 keeps built-in vocabularies/anchors distinct from custom parsing and evidence. D5 preserves no-custom mode/tier/empty behavior. |
| adr | adr-2026-08-22-one-owner-per-review-question | story-11, story-12, story-13, story-14, story-15 | covered | This changed ADR has no numbered parser-enumerated decisions, but its ownership map is load-bearing. Tasks 28–31 deliver reserved-owner and admitted-plan context, reject illegal effects, and record product/plan/architecture stops. Tasks 32–37 retain one bounded repair authority without custom plan append or a second completion/architecture judge. |
| adr | adr-2026-08-29-mixed-build-review-laps-preserve-content-adjudication | story-7, story-11, story-12, story-13, story-14, story-15, story-16, story-17 | covered | D1–D3 retain mixed-content admission with independent infrastructure blocking and one shared case authority. Amended D4 uses integer confidence 0–100, absent-confidence blocking, identity independence, durable non-blocking suppression history and existing event visibility. D5 skips only exact finalized non-actions, retaining attempted/pending cases and full history. Shared-operation no-content behavior is explicitly qualified. |

## Criterion-level delivery

The quotation is from a cited task's completion block; the cited tasks are judged together, including their named source/entry boundaries. Every criterion is diff-local: test fakes exercise the implemented interface and control flow, not changing live provider availability or an inaccessible private package. Acceptance flow A ends at aggregate routing; flow B ends at post-repair progression. Lower-layer tests retain the other explicit failure permutations.

| Row class | Criterion | Cited task id(s) | Verdict | Done when quote | Disposition |
| --- | --- | --- | --- | --- | --- |
| criterion | Story 1 happy: Given one enabled project-local installation of a named policy and a valid enabled custom declaration, when review resolves that selection, then it selects that installation, delivers its criteria, and leaves its installed files unchanged without creating an authored project copy. | task-3, task-4, task-5, task-16 | covered | "The public build-review runner delivers unchanged installed project/global/plugin criteria through both real provider adapters and publishes engine-stamped judged results from the actual candidate." | diff-local |
| criterion | Story 1 happy: Given one enabled global installation of a named policy and no competing match, when the same kind of declaration is reviewed, then the global installation supplies the criteria without requiring a local installation or changing another project's selection. | task-3, task-4, task-5, task-16 | covered | "The public build-review runner delivers unchanged installed project/global/plugin criteria through both real provider adapters and publishes engine-stamped judged results from the actual candidate." | diff-local |
| criterion | Story 1 happy: Given an enabled installed plugin containing a named skill, when the project selects that plugin-qualified skill, then review uses that plugin's skill and preserves its plugin identity and installation source. | task-3, task-4, task-5, task-16 | covered | "The public build-review runner delivers unchanged installed project/global/plugin criteria through both real provider adapters and publishes engine-stamped judged results from the actual candidate." | diff-local |
| criterion | Story 1 negative: Given distinct project and global installations match an unqualified selection, when review resolves it, then it names the conflicting sources and requests disambiguation without invoking either policy; an explicit matching source selection resolves the ambiguity. | task-3, task-16 | covered | "The resolver refuses distinct-origin ambiguity even for byte-equal copies, selects the explicitly qualified source, and names absent, unreadable, disabled, marketplace-only, or incomplete installations without substituting another copy." | diff-local |
| criterion | Story 1 negative: Given the selected global installation is absent or unreadable, when review resolves it, then it names the policy and failed source without substituting a project or harness copy. | task-3, task-16 | covered | "The resolver refuses distinct-origin ambiguity even for byte-equal copies, selects the explicitly qualified source, and names absent, unreadable, disabled, marketplace-only, or incomplete installations without substituting another copy." | diff-local |
| criterion | Story 1 negative: Given a plugin is disabled, only present in a marketplace listing, or lacks locally available required files, when review resolves its skill, then it reports unavailable installed policy without downloading, enabling, or choosing a cached alternative. | task-3, task-16 | covered | "The resolver refuses distinct-origin ambiguity even for byte-equal copies, selects the explicitly qualified source, and names absent, unreadable, disabled, marketplace-only, or incomplete installations without substituting another copy." | diff-local |
| criterion | Story 2 happy: Given a valid custom declaration, when the maintainer enables it, then the existing build-review gate includes that member alongside enabled built-in members without a harness implementation edit. | task-1 | covered | "Configuration-to-catalog integration loads a valid custom declaration alongside testQuality and resolves its existing execution-policy fields without editing the registry implementation per policy." | diff-local |
| criterion | Story 2 happy: Given a declaration is disabled or omits enablement, when review runs, then it performs no discovery, cache access, or judgment for that member; disabling the public gate disables all its members. | task-1 | covered | "The effective catalog excludes omitted/disabled custom members and every member of a disabled public gate; instrumented downstream discovery, cache, and judge callbacks receive zero calls for them." | diff-local |
| criterion | Story 2 negative: Given a declaration uses a built-in or retired id, a prototype key, an invalid id or field, a duplicate configuration key, or more than 32 custom declarations, when configuration is loaded, then it identifies the invalid declaration before any rubric invocation. | task-2 | covered | "The production configuration loader rejects each invalid-id, reserved/retired/prototype-id, duplicate-key, unknown-field, invalid-source/resource, and 33-member fixture before any rubric callback." | diff-local |
| criterion | Story 2 negative: Given custom review is enabled while aggregate adjudication is disabled, when configuration is loaded, then it refuses that combination before judging rather than permitting independent repair authority. | task-2 | covered | "Configuration integration rejects enabled custom review with adjudication disabled, while legacy unknown keys still error and retired keys still warn and contribute no member." | diff-local |
| criterion | Story 2 negative: Given a formerly enabled rubric has cached findings but is now disabled, when the next lap is evaluated, then its historical evidence remains inspectable but supplies no current blocker or new repair work. | task-23 | covered | "Reading removed/disabled/changed custom configurations preserves historic descriptors while excluding those old results from current membership; loading/invocation failures never claim unknown criteria were judged." | diff-local |
| criterion | Story 3 happy: Given complete valid installed-policy metadata with one eligible canonical match, when review resolves the policy, then it proceeds with that match and identifies the selected source. | task-3, task-4, task-5 | covered | "The installed-policy resolver selects the project, global, and plugin-qualified fixtures with exact canonical source/plugin identity; aliases of one installation collapse and original files remain unchanged." | diff-local |
| criterion | Story 3 happy: Given two discovered aliases resolve to the same canonical installation, when selection is evaluated, then it selects that one installation without reporting a false ambiguity. | task-3 | covered | "The installed-policy resolver selects the project, global, and plugin-qualified fixtures with exact canonical source/plugin identity; aliases of one installation collapse and original files remain unchanged." | diff-local |
| criterion | Story 3 negative: Given catalog output is partial, malformed, reports errors, or uses an unsupported format, when selection runs, then it reports a catalog failure rather than treating an empty or incomplete list as confirmed absence or successful coverage. | task-6 | covered | "Both metadata adapters classify partial-success, error-bearing, malformed, unsupported, unreadable, timeout, and cancellation fixtures as named loading failures rather than confirmed absence or judged coverage." | diff-local |
| criterion | Story 3 negative: Given catalog discovery times out, is canceled, or cannot read the selected source, when discovery terminates, then the candidate's discovery activity is stopped and the affected policy receives a loading failure with no judging call, cache hit, or cache write. | task-6 | covered | "Catalog-failure integration observes terminated child/session activity, zero policy judge/cache calls, and no provider-unavailable fallback for an otherwise available candidate." | diff-local |
| criterion | Story 3 negative: Given two byte-identical copies have different canonical installation origins, when an unqualified selection is resolved, then review reports ambiguity instead of deduplicating them by content. | task-3 | covered | "The resolver refuses distinct-origin ambiguity even for byte-equal copies, selects the explicitly qualified source, and names absent, unreadable, disabled, marketplace-only, or incomplete installations without substituting another copy." | diff-local |
| criterion | Story 3 negative: Given a required declared capability is unavailable to the selected reviewer, when preflight examines that policy, then it names the capability and recovery action before requesting its judgment; it does not activate unrelated plugin components. | task-11 | covered | "Policy preflight rejects declared unavailable capabilities/actions before judging and reports provider, capability, and recovery without activating unrelated components." | diff-local |
| criterion | Story 4 happy: Given a compatible installed skill with ordinary instructions and supporting criteria, when it participates in review, then the reviewer receives those criteria and returns attributable findings without requiring new harness-specific frontmatter or an installed-file edit. | task-10, task-16 | covered | "The review-contract renderer delivers unchanged ordinary skill criteria and supporting material with the shared findings schema, without requiring harness-specific frontmatter or installed-file edits." | diff-local |
| criterion | Story 4 happy: Given a skill describes a standalone presentation format but can apply its criteria in review mode, when it is invoked, then its review evidence follows the shared finding contract and its presentation instructions do not replace the aggregate verdict. | task-10, task-16 | covered | "A standalone-presentation fixture remains reviewable under the engine envelope and cannot supply the aggregate verdict, repair work order, or another output contract." | diff-local |
| criterion | Story 4 negative: Given declared required actions cannot run in the supported review role, when policy preflight runs, then the affected policy is refused before judging, with the incompatible requirement named and no reported coverage. | task-11, task-16 | covered | "Policy preflight rejects declared unavailable capabilities/actions before judging and reports provider, capability, and recovery without activating unrelated components." | diff-local |
| criterion | Story 4 negative: Given an undeclared dynamic dependency or incompatible instruction is discovered during review, when the policy reports that inability, then the result is explicitly unsupported rather than an empty successful judgment or permission to edit code, install dependencies, or publish comments. | task-11, task-16 | covered | "The custom result boundary maps a runtime unsupported response to visibly unjudged failed coverage; it neither accepts an empty PASS nor grants edits, installation, publishing, or repair authority." | diff-local |
| criterion | Story 5 happy: Given equivalent installed policy content for Claude Code and Codex, when each provider reviews the same declared policy and implementation input, then each receives the complete selected criteria and the same review obligations and output contract, with its own producing provenance; identical model wording is not required. | task-14, task-16 | covered | "Both production invoke adapters deliver the same full review envelope and captured support tree through the enforced read-only profile while retaining provider-specific producing identity and private bookkeeping." | diff-local |
| criterion | Story 5 happy: Given the preferred provider or model is unavailable and a configured fallback can load the selected policy, when fallback runs, then that actual candidate reviews its resolved policy under the same declaration and reports its own identity. | task-19 | covered | "Alternating early/late-unavailable candidates reuse only their own warm results without overwrite, borrowed preferred policy, another judging call, or duplicate token accounting; incompatible, legacy, or malformed entries re-judge." | diff-local |
| criterion | Story 5 negative: Given the fallback lacks the selected policy or resolves it ambiguously, when it prepares review, then it reports failed policy coverage without borrowing the preferred provider's policy or silently selecting another installation. | task-16, task-19 | covered | "Runner integration observes prepare → resolve/capture → lookup → optional judge/validated write → cleanup for each actual candidate; a load failure never reaches lookup/write and cannot buy a fallback." | diff-local |
| criterion | Story 5 negative: Given policy loading fails on an otherwise available candidate, when routing evaluates that failure, then it does not classify it as provider unavailability merely to obtain a different policy judgment. | task-6, task-16, task-19 | covered | "Runner integration observes prepare → resolve/capture → lookup → optional judge/validated write → cleanup for each actual candidate; a load failure never reaches lookup/write and cannot buy a fallback." | diff-local |
| criterion | Story 5 negative: Given a candidate fails authentication, is canceled, or returns a malformed review result, when that attempt ends, then it retains the existing cause-specific failure/fallback rules and records no invented successful judgment; all candidate-owned activity ends. | task-15, task-16 | covered | "Auxiliary lifecycle tests observe prepare before the candidate operation and exactly one cleanup after hit, invoke success, authentication failure, malformed result, timeout, or cancellation." | diff-local |
| criterion | Story 6 happy: Given a selected skill references supporting criteria inside its package, when review begins, then the reviewer can read the captured required content at its preserved relative locations, including safely resolved in-package links. | task-7 | covered | "The policy-bundle loader delivers SKILL.md, nested criteria, and admitted in-package link targets at preserved relative locations with bytes equal to the captured manifest." | diff-local |
| criterion | Story 6 happy: Given the installed package remains unchanged throughout loading, when review uses it, then the effective content identity describes exactly the captured definition and supporting package bytes delivered to that candidate. | task-7 | covered | "Bundle identity changes with every captured package-byte change, agrees with the delivered material, and remains unchanged when only its runtime parent path changes." | diff-local |
| criterion | Story 6 negative: Given a required resource is missing or unreadable, a local file reference is broken, or a link escapes the admitted package or cycles, when policy loading runs, then it identifies the resource defect and requests no informed judgment or cache lookup from that policy. | task-8 | covered | "The bundle loader rejects missing/unreadable resources, broken local references, escaping/cyclic links, and special-file fixtures with the offending resource named and no eligible bundle." | diff-local |
| criterion | Story 6 negative: Given the package exceeds 4,096 files or 64 MiB, or contains a special file, when loading runs, then it reports the breached limit or unsupported resource without truncating the policy into apparent coverage. | task-8 | covered | "Boundary fixtures accept 4096 files and 64 MiB but refuse either exceeded limit without a partial manifest, judging call, or cache lookup." | diff-local |
| criterion | Story 6 negative: Given package content changes during capture or storage cannot complete the captured material, when loading finishes, then it rejects the incomplete or inconsistent material without publishing an eligible judgment/cache entry; a later attempt must load again. | task-9 | covered | "The bundle capture rejects changed definition/resource bytes, added/removed members, and changed link targets between capture and final consistency check." | diff-local |
| criterion | Story 7 happy: Given a successful custom-policy judgment, when evidence is published, then it identifies the semantic skill, declaration, installation source, plugin/version when available, effective content, reviewed input, and producing provider/model policy. | task-23, task-40 | covered | "Artifact round-trip integration preserves original policy/source/plugin/version/content/input/provider/model attribution and a separate current-lap reuse reference with no invented execution or charge." | diff-local |
| criterion | Story 7 happy: Given a compatible prior judgment is reused, when the current lap is published, then it retains the original producing provenance and identifies the current reuse separately without claiming another model execution or token charge. | task-23, task-40 | covered | "Artifact round-trip integration preserves original policy/source/plugin/version/content/input/provider/model attribution and a separate current-lap reuse reference with no invented execution or charge." | diff-local |
| criterion | Story 7 negative: Given a policy fails loading or invocation, when its result is reported, then it is distinguishable from a judged result and does not claim that unavailable criteria were reviewed. | task-23, task-40 | covered | "Reading removed/disabled/changed custom configurations preserves historic descriptors while excluding those old results from current membership; loading/invocation failures never claim unknown criteria were judged." | diff-local |
| criterion | Story 7 negative: Given a reviewer supplies a forged rubric, lap, policy, provider, verdict, case identity, or out-of-input evidence reference, when its result is validated, then those claims cannot become authoritative evidence or an eligible cache result. | task-21, task-22 | covered | "Out-of-input/invalid references produce no eligible judged result, and a same-named changed policy yields a different exact finding identity while built-in identity fixtures remain unchanged." | diff-local |
| criterion | Story 7 negative: Given custom policy configuration is later removed or changed, when historical evidence is inspected, then it remains attributable to its original descriptor rather than being relabeled through today's configuration or rendered unreadable. | task-23, task-40 | covered | "Reading removed/disabled/changed custom configurations preserves historic descriptors while excluding those old results from current membership; loading/invocation failures never claim unknown criteria were judged." | diff-local |
| criterion | Story 8 happy: Given a valid prior result and unchanged effective policy, review input, contracts, engine content, and execution policy, when the same candidate reviews again, then it reuses that judgment without another judging call. | task-17, task-19 | covered | "Alternating early/late-unavailable candidates reuse only their own warm results without overwrite, borrowed preferred policy, another judging call, or duplicate token accounting; incompatible, legacy, or malformed entries re-judge." | diff-local |
| criterion | Story 8 happy: Given only temporary runtime paths, lap timing, or commit addresses change while all semantic inputs remain equivalent, when reuse is evaluated, then those incidental changes alone do not invalidate the judgment. | task-17, task-19 | covered | "The pure cache matcher hits for identical semantic identity and temporary-path, lap-time, publication-time, or commit-address-only changes, while retaining the original producing provenance." | diff-local |
| criterion | Story 8 negative: Given the skill definition, a supporting package file, declared question/source, reviewed content, contract, engine content, or resolved execution policy changes, when reuse is evaluated, then an incompatible prior judgment is not reused and the mismatch remains attributable. | task-17, task-19 | covered | "Independent changes to definition/support bytes, question/source/resources, input, contract/projection, execution policy, engine content stamp, actual provider, model, or effort produce attributable misses." | diff-local |
| criterion | Story 8 negative: Given a legacy entry lacks effective-policy evidence or a stored entry is malformed, when review encounters it, then it misses without fabricating provenance; only a newly valid result can replace it. | task-18, task-19 | covered | "Legacy missing-evidence entries miss with their staged cause, malformed entries miss as invalid, and interrupted/failed writes cannot publish an eligible partial result or destroy the other candidate entry." | diff-local |
| criterion | Story 8 negative: Given loading of the current effective policy fails or a cache write cannot complete, when that branch settles, then no partial entry becomes eligible for future reuse and the existing failure semantics remain effective. | task-18, task-19 | covered | "Runner integration observes prepare → resolve/capture → lookup → optional judge/validated write → cleanup for each actual candidate; a load failure never reaches lookup/write and cannot buy a fallback." | diff-local |
| criterion | Story 9 happy: Given the actual fallback has an eligible judgment for its own effective policy, when the preferred candidate is unavailable, then fallback reuses its own result and retains that producing identity. | task-19 | covered | "Alternating early/late-unavailable candidates reuse only their own warm results without overwrite, borrowed preferred policy, another judging call, or duplicate token accounting; incompatible, legacy, or malformed entries re-judge." | diff-local |
| criterion | Story 9 happy: Given both preferred and fallback candidates have different valid warm results, when routing alternates between them, then each can reuse its own compatible result without the other's write destroying its reusable candidate entry. | task-18, task-19 | covered | "Temporary-cache integration preserves separate warm preferred/fallback entries and only reads an entry whose full effective candidate identity and validated result match." | diff-local |
| criterion | Story 9 negative: Given a warm preferred-candidate result and different effective policy bytes for the fallback, when fallback is selected, then the preferred result cannot satisfy it, even if the declaration and implementation input are unchanged. | task-19 | covered | "Alternating early/late-unavailable candidates reuse only their own warm results without overwrite, borrowed preferred policy, another judging call, or duplicate token accounting; incompatible, legacy, or malformed entries re-judge." | diff-local |
| criterion | Story 9 negative: Given preferred-candidate preparation succeeds but invocation reports provider/model unavailability, when fallback prepares, then it performs policy resolution and reuse eligibility for itself rather than inheriting the earlier prepared identity. | task-19 | covered | "Runner integration observes prepare → resolve/capture → lookup → optional judge/validated write → cleanup for each actual candidate; a load failure never reaches lookup/write and cannot buy a fallback." | diff-local |
| criterion | Story 9 negative: Given candidate preparation or cancellation ends before policy resolution succeeds, when that attempt is cleaned up, then it leaves no claimed cache hit, judged artifact, or successful coverage for that candidate. | task-15, task-19 | covered | "Existing unavailability classification, session policy, attempt accounting, and non-review callers retain their behavior; cancellation/preparation failure cannot emit a claimed judgment or cache hit." | diff-local |
| criterion | Story 10 happy: Given a lap with enabled custom policies and a built-in peer, when the rubrics execute, then all observe the same frozen implementation input and receive only their own review context, regardless of completion order. | task-12 | covered | "Input-preparation integration gives every custom and built-in peer the same captured source identity, independent of completion order, with no sibling evidence in branch contexts." | diff-local |
| criterion | Story 10 happy: Given the selected provider needs writable bookkeeping during a supported custom review, when the reviewer runs, then it can write its private scratch while the reviewed source, original checkout, original policy installation, and engine evidence remain protected. | task-13, task-14 | covered | "The production containment adapter reaches its fake process boundary and admits scratch writes while refusing protected source/installation/engine-state writes and sibling-evidence reads under the generated mount profile." | diff-local |
| criterion | Story 10 negative: Given a reviewer attempts to modify protected input or read a sibling's private review evidence, when that access is attempted, then it cannot alter protected state or obtain the sibling evidence; no later rubric observes a changed input caused by that reviewer. | task-13, task-14 | covered | "The production containment adapter reaches its fake process boundary and admits scratch writes while refusing protected source/installation/engine-state writes and sibling-evidence reads under the generated mount profile." | diff-local |
| criterion | Story 10 negative: Given containment is unavailable, a protected-write or scratch-write probe fails, or the host's nested sandbox cannot support the boundary, when review prepares, then it names the provider, missing capability, and recovery action before judging, with no writable fallback. | task-13, task-14 | covered | "Missing bubblewrap, a successful protected write, failed scratch write, or unsupported nested sandbox yields a named unsupported-capability failure and zero reviewer launches with no writable fallback." | diff-local |
| criterion | Story 10 negative: Given the original checkout changes after a custom lap's input was captured, when remaining reviewers execute, then they still observe that lap's captured input and their evidence is not relabeled as reviewing the new checkout. | task-12 | covered | "Changing the original checkout after capture leaves later reviewer reads and evidence identity on the captured bytes; a built-in-only fixture creates no custom materialization prerequisite." | diff-local |
| criterion | Story 11 happy: Given enabled custom policies produce unresolved findings in either attended or daemon execution, when all branches settle, then one aggregate decision consumes their eligible findings and supplies the sole repair or decision-stop route for that lap. | task-32, task-33 | covered | "Attended and daemon entry integration each observes one settled custom aggregate judgment and one terminal repair or decision-stop route with no raw-finding kickback and no duplicate daemon adjudication." | diff-local |
| criterion | Story 11 happy: Given a valid custom finding and a sibling infrastructure failure, when the lap is evaluated, then both remain visible, content receives the one aggregate judgment, and an admitted consistent repair may proceed while the infrastructure result remains independently blocking. | task-34 | covered | "Mixed-lap coordinator integration sends eligible custom content to one judgment and retains the sibling closed infrastructure failure even when an admitted repair route is selected." | diff-local |
| criterion | Story 11 negative: Given one reviewer finishes early or instructs immediate repair, when other branches are unsettled, then no implementation work or semantic charge is authorized by that individual result. | task-32, task-33 | covered | "Early/raw rubric instructions authorize no effect or charge, and consuming an already recorded lap outcome never invokes a second adjudicator." | diff-local |
| criterion | Story 11 negative: Given an infrastructure-only lap, or content already settled by exact permitted dispositions with no live finding, when review evaluates it, then it preserves the corresponding existing no-judgment route rather than inventing new content work. | task-34 | covered | "Infrastructure-only, exact-settled, and confidence-suppressed/no-live-source fixtures do not dispatch a content judge or semantic charge; wholly unjudged waived laps cannot PASS." | diff-local |
| criterion | Story 11 negative: Given an attended compatibility path cannot provide the required custom-review capability, when it prepares the lap, then it refuses before judging rather than routing raw findings directly to BUILD; consuming a recorded aggregate result never invokes a second adjudicator. | task-32, task-33 | covered | "Unsupported scalar/compatibility wiring refuses custom review before judging, while no-custom routing retains the preexisting mode-specific behavior." | diff-local |
| criterion | Story 12 happy: Given two custom policies identify the same admitted defect, when the aggregate judgment merges their findings, then one repair case retains both original findings and the reason for their shared disposition. | task-29, task-32 | covered | "The remediation parser round-trips case-v2 duplicate/consistent-repair graphs with every original source, merge rationale, admission task id, and consistency verdict while retaining case-v1/non-case compatibility." | diff-local |
| criterion | Story 12 happy: Given two policies propose incompatible repairs and the aggregate judgment resolves them consistently within approved scope, when that judgment is accepted, then the worker receives only the selected consistent repair set and can inspect the disposition of both proposals. | task-29, task-32 | covered | "The shared outcome operation waits for settled branches, sends eligible current content to exactly one adjudicator, and returns only a validated consistent admitted repair set or inspectable decision stop." | diff-local |
| criterion | Story 12 negative: Given the conflict remains unresolved, when the aggregate reports blocked consistency, then no action from that adjudication reaches the worker and the stop identifies the implicated findings and rationale. | task-30, task-31 | covered | "Validation/application integration authorizes zero action effects for any invalid graph, blocked consistency, or escalation, including otherwise valid sibling actions." | diff-local |
| criterion | Story 12 negative: Given a decision omits consistency, has contradictory outcomes, or references nonexistent findings/cases, when it is checked, then none of its action effects is applied and the defect is reported. | task-30 | covered | "The case validator accepts exhaustive duplicate/consistent graphs but rejects omitted, duplicate, invented, unresolved-merge, nonexistent-reference, missing-consistency, contradictory-outcome, and missing-admission fixtures." | diff-local |
| criterion | Story 12 negative: Given complete current findings, relevant policy/scope context, or required prior-case history cannot fit within the supported bounds or cannot be loaded, when adjudication prepares, then it stops rather than judging a truncated account or delivering partially reconciled work. | task-28 | covered | "Unreadable/missing scope or case state and context overflow stop before model dispatch instead of truncating sources, criteria, or admission contracts." | diff-local |
| criterion | Story 13 happy: Given several current unresolved custom findings, when the aggregate settles, then every finding has one traceable permitted outcome, including a retained canonical target for merged findings, and only admitted action outcomes supply repair work. | task-29, task-30, task-35 | covered | "Work-order application publishes one durable prioritized repair set containing only admitted act outcomes, with real task ids, rationale, and complete custom/merged source links." | diff-local |
| criterion | Story 13 happy: Given rejected, legitimately deferred, operator-resolved, or confidence-suppressed custom findings, when the effective gate outcome is derived, then those findings remain inspectable without becoming autonomous repair work; suppression remains distinct from accepted risk. | task-24, task-34, task-35 | covered | "Effective-verdict integration retains rejected/deferred/operator-resolved/suppressed custom findings visibly outside repair work, leaves missing confidence unsuppressed, and never writes operator authority from suppression." | diff-local |
| criterion | Story 13 negative: Given a judgment omits, duplicates, or invents a source finding or leaves a merge target unresolved, when it is validated, then it cannot settle successfully or partially apply valid sibling actions. | task-30 | covered | "The case validator accepts exhaustive duplicate/consistent graphs but rejects omitted, duplicate, invented, unresolved-merge, nonexistent-reference, missing-consistency, contradictory-outcome, and missing-admission fixtures." | diff-local |
| criterion | Story 13 negative: Given a custom finding has absent confidence, invalid confidence, or an attempted self-awarded disposition, when its result is interpreted, then absent confidence is not suppressed, invalid confidence is malformed, and the reviewer cannot grant itself operator authorization. | task-21, task-24 | covered | "The descriptor-selected parser accepts bounded custom findings and absent confidence, but rejects invalid confidence and forged rubric/lap/policy/provider/verdict/case/effect/disposition fields." | diff-local |
| criterion | Story 13 negative: Given a current policy update changes a finding's effective identity, when historic settled outcomes are considered, then the new finding cannot be treated as an exact settled recurrence solely because its rubric name or wording matches; prior cases remain available for semantic judgment. | task-22, task-24 | covered | "The existing reconciler reuses an exact settled custom non-action without judging or charge but rejects exact reuse after policy identity changes and retains the original case history." | diff-local |
| criterion | Story 14 happy: Given a policy finding requires changing the approved product requirement, implementation plan, or architecture, when the aggregate identifies that need, then it records the appropriate decision owner and an actionable stop with the source findings preserved. | task-28, task-29, task-31 | covered | "Decision-routing integration records each owner stop with original source evidence and zero work publication, plan append, sealed-artifact mutation, automatic waiver, or semantic charge." | diff-local |
| criterion | Story 14 happy: Given an actionable finding can be repaired under an existing approved task, when the aggregate authorizes it, then the repair cites the admitting task and explains how the repair fits that approved scope. | task-28, task-29, task-35 | covered | "Work-order application publishes one durable prioritized repair set containing only admitted act outcomes, with real task ids, rationale, and complete custom/merged source links." | diff-local |
| criterion | Story 14 negative: Given a custom policy attempts to take over product-completion or architecture-choice authority, when findings are adjudicated, then it cannot override that authority or authorize an unapproved mechanism; the recorded outcome explains rejection or the required decision stop. | task-28, task-31 | covered | "Decision-routing integration records each owner stop with original source evidence and zero work publication, plan append, sealed-artifact mutation, automatic waiver, or semantic charge." | diff-local |
| criterion | Story 14 negative: Given an action cites a nonexistent task or lacks admission evidence, or any case requires escalation, when the result is checked, then no action from that adjudication is delivered and no plan task is appended or BUILD charge spent for the escalation. | task-30, task-31, task-35 | covered | "Validation/application integration authorizes zero action effects for any invalid graph, blocked consistency, or escalation, including otherwise valid sibling actions." | diff-local |
| criterion | Story 14 negative: Given a gap affects the current approved outcome, when a reviewer proposes treating it as an unrelated future improvement, then review cannot settle it through a non-blocking deferral; after an explicit owning decision changes the approved baseline, recovery re-evaluates without rewriting the old verdict into PASS. | task-31 | covered | "Current-outcome gaps cannot settle through deferral, restart alone retains decision/repeat stops, and an explicitly changed approved baseline causes new evaluation while preserving the old verdict." | diff-local |
| criterion | Story 15 happy: Given a new consistent set of admitted code repairs within the remaining allowance, when the aggregate authorizes repair and BUILD completes it, then one repair route is charged and all tests and reviews invalidated by that repair must supply current evidence before the feature proceeds. | task-35, task-36 | covered | "The repair/progression entry flow applies one admitted custom repair, reopens its invalidated test/review obligations, and proceeds only after all required evidence binds to the repaired input." | diff-local |
| criterion | Story 15 happy: Given a settled non-action custom finding recurs with the same effective identity, when the next lap is evaluated, then its permitted finalized outcome can be reused without a new adjudicator call or semantic repair charge, while retaining the original case evidence. | task-24, task-37 | covered | "The existing reconciler reuses an exact settled custom non-action without judging or charge but rejects exact reuse after policy identity changes and retains the original case history." | diff-local |
| criterion | Story 15 negative: Given an attempted or regressed case is reported again with equivalent substance despite wording or code movement, when the aggregate identifies that unrefuted recurrence and again proposes action, then it follows the existing repeated-case stop rather than granting a fresh repair allowance. | task-37 | covered | "Convergence integration charges one new aggregate action effect, reuses exact settled non-actions without charge, and stops attempted/regressed semantic recurrence or an exhausted bound." | diff-local |
| criterion | Story 15 negative: Given the cumulative bound is exhausted or a policy is renamed, updated, disabled, and re-enabled, when new repair routing is evaluated, then those changes do not reset the feature's accumulated charges or authorize an over-limit route. | task-37 | covered | "Policy rename/update/disable/re-enable, fallback, incidental movement, and restart cannot reset charges; existing genuine rebase invalidation, fresh-session, and operator-reset fixtures retain their authorized ledger transitions." | diff-local |
| criterion | Story 15 negative: Given repair completes but invalidated test or review evidence is stale, missing, or failing, when progression is attempted, then the feature cannot proceed on the pre-repair success and the known failure remains blocking. | task-36 | covered | "Progression integration refuses stale, missing, or failing post-repair test/review evidence and cannot consume the pre-repair PASS as current proof." | diff-local |
| criterion | Story 16 happy: Given an admitted custom-policy action is interrupted around decision persistence, work publication, or charge recording, when recovery runs, then it completes only missing authorized effects, retains original source attribution, and records at most one publication and one semantic charge for that action. | task-38 | covered | "Recovery integration at each decision/work-publication/charge boundary completes only missing effects and observes at most one publication and one semantic charge with original custom source attribution." | diff-local |
| criterion | Story 16 happy: Given a custom policy is removed or its installation changes after a decision was recorded, when recovery reads the old decision, then it retains that decision's original identity and complete unresolved history while evaluating any new lap against current policy. | task-23, task-38 | covered | "Removed/updated policy recovery preserves unresolved history and producing descriptors, while pending effects, decision stops, and attempted-repeat stops remain blocking until their existing resolution conditions hold." | diff-local |
| criterion | Story 16 negative: Given concurrent recovery attempts encounter the same pending effect, when they try to resume it, then only one can own its application and the other cannot duplicate work or charge. | task-39 | covered | "Concurrent recovery integration admits one effect owner and one publication/charge; a late exact operator disposition is re-read before application without stale overwrite." | diff-local |
| criterion | Story 16 negative: Given required durable state is unreadable, malformed, incomplete, or cannot be written, when recovery evaluates it, then it stops with the affected state named rather than assuming completion, dropping unresolved findings, or publishing a partial second effect. | task-39 | covered | "Unreadable, malformed, incomplete, or unwritable case/effect state stops with the affected state named, retaining blockers and preventing partial second publication or assumed completion." | diff-local |
| criterion | Story 16 negative: Given recovery resumes a decision stop or an already attempted repeated case, when the run restarts, then restart alone neither clears the stop nor buys another repair route; required operator decisions and existing bounds remain in force. | task-31, task-37, task-38 | covered | "Removed/updated policy recovery preserves unresolved history and producing descriptors, while pending effects, decision stops, and attempted-repeat stops remain blocking until their existing resolution conditions hold." | diff-local |
| criterion | Story 17 happy: Given an exact current operator accepted-risk decision covers a custom finding, when review evaluates it, then that finding remains visible as operator-resolved and does not create autonomous repair work. | task-25, task-27 | covered | "The accepted-risk matcher resolves only the exact current custom finding/declaration/content within the feature and leaves changed same-named findings blocking while preserving built-in matching." | diff-local |
| criterion | Story 17 happy: Given a valid custom declaration whose policy has never loaded, an exhausted loading failure, a healthy judged sibling, and an exact current operator reduced-coverage decision for that declaration and closed failure reason, when the aggregate is evaluated, then that decision applies without an effective content fingerprint and the failed branch remains visibly unjudged. | task-26, task-27, task-40 | covered | "Reduced-coverage integration applies an exact first-use loading-failure waiver without a digest and retains it across package/version, diagnostic, execution-policy, and restart changes while declaration and reason match." | diff-local |
| criterion | Story 17 happy: Given a custom reduced-coverage decision and the same validated declaration and closed failure reason, when the installed package changes or review restarts, then the decision still applies to that missing coverage and the current failure is reported without claiming the package was judged. | task-26, task-40 | covered | "Reduced-coverage integration applies an exact first-use loading-failure waiver without a digest and retains it across package/version, diagnostic, execution-policy, and restart changes while declaration and reason match." | diff-local |
| criterion | Story 17 negative: Given an automated rejection, deferral, merge, suppression, or escalation occurs, when it is recorded, then it cannot create, replace, or broaden an operator accepted-risk or reduced-coverage decision. | task-25, task-27 | covered | "Rejected, merged, deferred, suppressed, or escalated autonomous outcomes cannot create/replace risk records or substitute missing coverage for a judged finding." | diff-local |
| criterion | Story 17 negative: Given risk acceptance belongs to different effective policy content, or reduced coverage belongs to a different validated declaration or closed failure reason, when current evidence is evaluated, then it cannot authorize the new finding or failure merely because its rubric name matches. | task-25, task-26 | covered | "Changed declaration/resource/source/question/reason does not match; healed-policy findings remain blocking, invalid declarations are ineligible, and even fully waived infrastructure cannot make a zero-judgment lap PASS." | diff-local |
| criterion | Story 17 negative: Given the operator records a disposition after adjudication starts but before effects apply, when the result is applied, then the current exact disposition is respected and cannot be overwritten by stale autonomous work; unrelated unresolved findings remain blocking. | task-27, task-39 | covered | "A disposition recorded after judging begins is honored at application without stale overwrite or unrelated-finding clearance, and every applicable failure stays visibly unjudged." | diff-local |
| criterion | Story 17 negative: Given a previously covered policy now produces a judged finding, or every rubric remains unjudged, when the aggregate is evaluated, then reduced coverage cannot suppress that finding or make the entirely unjudged lap pass; invalid declarations and non-operator callers cannot gain waiver authority. | task-26, task-27, task-34 | covered | "Changed declaration/resource/source/question/reason does not match; healed-policy findings remain blocking, invalid declarations are ineligible, and even fully waived infrastructure cannot make a zero-judgment lap PASS." | diff-local |
| criterion | Story 18 happy: Given no custom declarations, when review runs, then existing built-in selection/defaults, empty-container and empty-scope behavior, permitted dispositions, and attended/daemon routing remain effective without requiring custom-policy containment. | task-20 | covered | "No-custom fixtures preserve attended/daemon, default/empty-container/empty-scope behavior and require no custom containment; generic custom findings cannot bypass testQuality vocabulary, scope, preflight, or anchor checks." | diff-local |
| criterion | Story 18 happy: Given an enabled built-in rubric and an unchanged effective policy on the actual candidate, when an eligible judgment exists, then review retains reuse; changed effective built-in criteria invalidate incompatible reuse without erasing existing built-in operator-risk binding. | task-20 | covered | "Built-in runner integration hits for unchanged candidate criteria, misses after effective criteria change, and refuses warm harness-root evidence when the actual candidate cannot load its policy, without changing built-in risk matching." | diff-local |
| criterion | Story 18 negative: Given an unknown key in the legacy rubric subtree or a retired rubric key, when configuration is loaded, then the unknown key retains rejection and the retired key retains its warning/no-op behavior rather than silently becoming a custom policy. | task-2 | covered | "Configuration integration rejects enabled custom review with adjudication disabled, while legacy unknown keys still error and retired keys still warn and contribute no member." | diff-local |
| criterion | Story 18 negative: Given a built-in candidate cannot load the effective criteria, when review considers a warm harness-root judgment, then it cannot use that cache entry to claim successful coverage; missing effective-policy evidence still requires a miss or loading failure. | task-20 | covered | "Built-in runner integration hits for unchanged candidate criteria, misses after effective criteria change, and refuses warm harness-root evidence when the actual candidate cannot load its policy, without changing built-in risk matching." | diff-local |
| criterion | Story 18 negative: Given a custom finding shape is supplied to the built-in rubric, when the result is validated, then the specialized built-in vocabulary, scope, preflight, and anchor rules remain intact rather than accepting the generic custom contract. | task-20, task-21 | covered | "No-custom fixtures preserve attended/daemon, default/empty-container/empty-scope behavior and require no custom containment; generic custom findings cannot bypass testQuality vocabulary, scope, preflight, or anchor checks." | diff-local |

## Cross-layer consistency and preserved behavior

The following comparisons were checked in both directions against the actual criteria and completion blocks; each is resolved by a scoped mechanism rather than by assuming a descriptive label grants authority.

- **Disabled/default membership versus new discovery and telemetry:** Tasks 1/2 exclude disabled declarations and a disabled public gate before discovery/cache/judge calls; Tasks 12/14/20/33 preserve no-custom behavior. Task 40 describes emitted policy occurrences and their existing consumers, not an unconditional discovery or model event. The cache correction remains intentionally applicable to built-ins.
- **Package completeness versus usable installed selection:** Tasks 3–11 and 16 accept unique local project/global/plugin installations without modifying them, capture the whole admitted package, and deliver its exact material. Catalog errors, resource limits, and incompatibility settle as coverage failure. FR-3's amendment admits runtime-discovered unsupported results; no task claims arbitrary prose compatibility can be decided before invocation.
- **Stable review input versus provider bookkeeping:** Tasks 12–16 make source/material read-only and private scratch writable through both actual invoke adapters. Failed protected/scratch/nested probes stop before review. Task 26's coverage decision changes failure evaluation only and cannot supply a missing access capability or suppress a judged finding.
- **Fallback versus content-grounded reuse:** Tasks 15–20 prepare each actual candidate before policy resolution and lookup. Only existing provider/model-unavailability classification permits fallback; catalog/load/auth/cancel/malformed outcomes do not borrow another policy or an unauthorized route. Exact equivalent semantics hit; incidental runtime paths/timing/commit addresses do not become identity.
- **Complete sources versus suppression and settled recurrence:** Tasks 21–24/28–30/34 retain raw and historic evidence while deriving the eligible source set through validated confidence, current membership, exact operator decisions, and finalized non-action recurrence. Suppressed entries remain separate non-blocking history; source completeness applies to the actual eligible set rather than demanding dispositions for intentionally excluded history.
- **One aggregate authority versus no-content routes:** Tasks 32/33 own shared application/navigation; Tasks 24/34 own no-judge settled/infrastructure routes. The additive Task 32 qualification limits its original repair-or-stop check to eligible unresolved content. Empty/settled/no-live-source paths cannot be forced into another model call by that check. Individual branches still authorize no work or charge.
- **Consistent repair versus owner decisions:** Tasks 28–35 carry full scope/ownership context, require every act to cite an existing admitting task, and prohibit all sibling effects when consistency is blocked, an owner escalation exists, or the graph is invalid. Current-outcome gaps cannot be hidden as unrelated future deferrals. The model owns semantic judgment; validation constrains graph/effect legality rather than deriving meaning from text.
- **New policy identity versus durable risk, coverage, and budgets:** Tasks 22/25 require exact effective content for custom finding acceptance; Task 26 intentionally excludes unavailable content from declaration/reason coverage. Package updates can invalidate judged cache and accepted-risk matching while leaving the same missing-coverage obligation waived. Tasks 24/37/38 retain semantic history and charges, so neither policy updates nor restarts buy another repair allowance; genuine preexisting refund/reset triggers remain.
- **Recovery versus current operator authority:** Tasks 27/35/38/39 re-read current operator state under the existing transaction/lease and apply only missing authorized effects. A pending effect, attempted recurrence, malformed state, or owner stop cannot become a settled PASS after restart. Removed configuration does not relabel historical descriptors.
- **Private accepted-risk details versus reduced-coverage publication:** The existing build-review-accepted-risk renderer deliberately publishes finding id/rubric only. The distinct existing reduced-coverage renderer publishes current closed failure, diagnostic, operator, rationale, and decision time. Task 40 names both consumer entry points and preserves these different existing contracts; it does not expand accepted-risk disclosure or add a telemetry channel.

## Corrections made in this DECIDE pass

Eight additive plan clarifications were made beside the original assertions on 2026-09-11, without changing scope, architecture, task count, or any accepted story: Task 7 complete-package enumeration; Task 11 total closed cause mapping; Tasks 21/22 integer confidence and non-identity semantics; Task 24 durable suppression history; Task 27 exact existing operator command names/current-lap and duplicate refusals; Task 32 conditional no-content routing; Task 40 retained-PR/shipped-record publication. These close completion-evidence omissions or shorthand ambiguity in already approved obligations. BUILD receives the corrected baseline rather than a task to amend DECIDE artifacts.

The original Task 32 repair-or-stop check could otherwise be read as unconditional, conflicting with Story 11's no-content no-judgment path. The dated qualification makes the eligible-content precondition explicit and requires the existing infrastructure/settled route otherwise. The command shorthand in Task 27 is resolved against the actual CLI definition; no command rename or new alias is authorized.

The existing architecture table has 43 distinct citable decisions across seven changed accepted ADRs (the unnumbered one-owner decision produces no invented D-number). Each was semantically reviewed under its accepted adjacent amendments and later supersessions. The infrastructure-only versus mixed-content distinction and the risk-versus-coverage identity split remain consistent across the old and new decisions.

> **Amended 2026-09-11 by #1986:** The land gate requires a citable identifier for each changed approved ADR, so the existing one-owner decision is now explicitly D1. Its substance and seven-ADR row pool are unchanged; the plan has 44 decision mappings. The earlier unnumbered description (including the one-owner row above) is historical. D1 is covered by Tasks 28/30/31/32/33/37: complete ownership/admission context, zero illegal action effects, owner stops, one aggregate authority, and bounded repair. The same substantive constraint was already reviewed; no new design decision or operator approval is introduced.

## Verification and limitations

- **Verified:** Named counterparts and exact completion quotes are present in the actual artifacts. PRD/story tie-out, task membership, dependencies, criterion grounding, and architecture-decision coverage are checked by the repository's parsers/validators, not by a separate invented rubric.
- **Semantic verdict, 95% confidence, inferred from the approved text and verified local boundaries:** The cited checks together can deliver each accepted criterion without contradicting an approved decision. This is a specification judgment, not measured runtime coverage. No unresolved load-bearing assumption is required for the PASS.
- **Operator-approved constraints retained:** Initial Linux/bubblewrap custom execution, complete installed-package capture, ordinary-skill review-role adaptation, separate custom accepted-risk and reduced-coverage subjects, full issue scope, and preserved built-in behavior.
- **Not claimed:** Live Claude/Codex semantic behavior, private Kotlin-package compatibility, or implemented feature test success. Ordinary BUILD tests fake all third-party boundaries; optional real-provider smoke remains separately opt-in.
- **Advisory overlap:** The plan's existing scan identifies shared configuration/conductor paths with daemon-self-host-guardrails and shared conductor/daemon rendering with self-host-phase6-wiring. This is a sequencing/rebase watchpoint, not a blocking conflict or authority to modify those branches.

**Verdict:** PASS. The operator approved this coherence output on 2026-09-11. It requires no scope exception, coherence waiver, or new architecture decision and is ready for composer land/handoff.

> **Amended 2026-09-11 by #1986 (main integration):** The single Git conflict placed the approved #2409 refuted-repeat amendment beside this feature’s consistency/owner-stop amendment. Both are retained. Story 15’s negative recurrence scenario now explicitly means an unrefuted repeat proposed as action; its exact plan/coherence criterion rows were updated together. Tasks 29/30/37 reuse the predecessor’s refute schema, validator, and no-charge terminal under an explicit build dependency on #2409, whose implementation is not yet in main. All other source completeness, operator authority, effect, and convergence guarantees remain. The 174-row coherence mapping and 44-decision table still apply; no new task, autonomous authority, or scope exception is added. The dependency prevents assuming the predecessor already exists.
