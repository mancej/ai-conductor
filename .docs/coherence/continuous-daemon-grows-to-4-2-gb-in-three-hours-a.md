# Coherence: Daemon memory observability and exit attribution

Date: 2026-09-22
Tier: M
Track: technical — no PRD, no FR rows.
Source: jstoup111/ai-conductor#2079; five staged intake outcomes in .pipeline/intake-outcomes.md.
Verdict: covered, with one waived gap (outcome-1)

The single table below maps the five staged outcomes, all seven stories, all 13 tasks, all 34 criteria, and the one amended ADR. Criterion rows carry six cells (class, exact criterion, task ids, verdict, exact Done-when quote, disposition); legacy rows carry five. outcome-1 (steady-state memory across a multi-hour run) is the memory-growth fix, excluded by the operator-confirmed scope boundary in `.docs/track/continuous-daemon-grows-to-4-2-gb-in-three-hours-a.md`; it is recorded as a gap and waived in `.docs/coherence-waivers/continuous-daemon-grows-to-4-2-gb-in-three-hours-a.md`, to return as a follow-on intake once the samples and heap dump this spec delivers identify the cause.

| Class | Id / Criterion | Cited ids | Verdict | Quote | Disposition |
| --- | --- | --- | --- | --- | --- |
| outcome | outcome-1 |  | gap | "A daemon driving a single feature at concurrency 1 holds steady-state memory across a multi-hour" |
| outcome | outcome-2 | story-1, story-2 | covered | "The daemon's own memory growth is observable while it runs, so the shape of the curve can be seen" |
| outcome | outcome-3 | story-3, story-4, story-5 | covered | "A daemon that dies from an external signal is distinguishable in its own log and status output" |
| outcome | outcome-4 | story-5, story-6 | covered | "A `session-up/process-dead` daemon surfaces itself without an operator having to run" |
| outcome | outcome-5 | story-7 | covered | "A feature interrupted this way resumes from its committed task progress rather than repeating" |
| story | story-1 | task-1, task-2, task-3 | covered | Story 1 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| story | story-2 | task-4, task-5 | covered | Story 2 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| story | story-3 | task-6, task-7 | covered | Story 3 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| story | story-4 | task-8, task-9 | covered | Story 4 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| story | story-5 | task-10, task-11 | covered | Story 5 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| story | story-6 | task-12 | covered | Story 6 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| story | story-7 | task-13 | covered | Story 7 is delivered by the cited tasks; each of its criteria has a criterion row below. |
| task | task-1 | story-1 | covered | Register the three daemon-origin event variants. |
| task | task-2 | story-1 | covered | Sample process memory at step boundaries on the root bus. |
| task | task-3 | story-1 | covered | Sampler stays quiet when idle and logs a ledger write failure once. |
| task | task-4 | story-2 | covered | Write one heap snapshot when a sample crosses the threshold. |
| task | task-5 | story-2 | covered | Heap dump is once per lifetime, retention-capped, and never partial. |
| task | task-6 | story-3 | covered | Add the daemon_heap_limit_mb config key with validation. |
| task | task-7 | story-3 | covered | Launch the daemon under --max-old-space-size. |
| task | task-8 | story-4 | covered | Add the exit-witness subverb that appends daemon_exited to its own ledger. |
| task | task-9 | story-4 | covered | Make the pane foreground a witnessing wrapper around the single daemon child. |
| task | task-10 | story-5 | covered | Read the exit ledger and the last memory sample, skipping malformed lines. |
| task | task-11 | story-5 | covered | Render the exit cause and last sample in daemon status. |
| task | task-12 | story-6 | covered | ensureRunning names the prior death it reclaims from. |
| task | task-13 | story-7 | covered | Prove a SIGKILLed feature resumes from committed task progress. |
| adr | adr-2026-06-29-daemon-supervisor-port-and-attachable-hosting | story-3, story-4 | covered | D8 heap cap is Task 7, D9 witness wrapper is Task 9, D10 sibling ledger is Tasks 8 and 9; D1–D7 are existing or no-change per the plan obligation table. |
| criterion | Story 1 happy: Given a `--continuous` daemon driving a feature, when a `step_started` or `step_completed` event passes the root bus, then one `daemon_memory_sample` record is appended to `.daemon/events.jsonl` carrying `rss`, `heapUsed`, `heapTotal`, `external`, the feature slug, the step name, the boundary kind, and the daemon pid | task-2 | covered | "Driving `step_started` then `step_completed` through the root bus with `startDaemonMemorySampler` attached emits exactly two `daemon_memory_sample` records carrying `rss`, `heapUsed`, `heapTotal`, `external`, `slug`, `step`, `boundary`, `pid`, and `dispatchSeq`, as asserted by the sampler unit test." | diff-local |
| criterion | Story 1 happy: Given a run of 25 build dispatches, when the ledger is read, then the samples can be ordered by timestamp and by dispatch count so growth per dispatch and growth per hour are both computable from the ledger alone | task-2 | covered | "Each sample carries a timestamp and a monotonically increasing `dispatchSeq`, so ordering by time and by dispatch count is asserted from the recorded array alone." | diff-local |
| criterion | Story 1 negative: Given the daemon is idle with no feature in flight, when a poll tick passes, then no `daemon_memory_sample` is written on that tick | task-3 | covered | "Emitting a `daemon_backlog_snapshot` on the root bus produces no `daemon_memory_sample`, as asserted by the idle-tick test." | diff-local |
| criterion | Story 1 negative: Given a feature worktree's `.pipeline/events.jsonl`, when the daemon samples memory, then no `daemon_memory_sample` appears in the feature ledger because the record is daemon-origin, not feature-origin | task-2 | covered | "A feature `EventPersister` attached to a `ForwardingEventEmitter` over the same root bus records zero `daemon_memory_sample` lines while the daemon persister records both, as asserted by the two-ledger test." | diff-local |
| criterion | Story 1 negative: Given `.daemon/events.jsonl` is unwritable, when a sample is due, then the step proceeds unaffected and one `daemon.log` line names the write failure once rather than on every boundary | task-3 | covered | "With an appender that throws on every write, three step boundaries produce exactly one `daemon.log` line naming the ledger path and the error, and the `step_completed` listener registered after the sampler still runs, as asserted by the unwritable-ledger test." | diff-local |
| criterion | Story 2 happy: Given a configured RSS threshold in megabytes, when a memory sample first crosses it, then one heap snapshot is written under `.daemon/heap/` with a name carrying the timestamp and pid and a `daemon_heap_dump_written` record naming the path, the byte size, and the triggering `rss` is appended to `.daemon/events.jsonl` | task-4 | covered | "With threshold 100 MB, samples of 50 then 150 MB rss cause exactly one snapshot write under `.daemon/heap/` named by timestamp and pid and exactly one `daemon_heap_dump_written` record carrying `path`, `bytes`, `rss`, and `pid`, as asserted by the threshold test." | diff-local |
| criterion | Story 2 happy: Given no threshold configured, when samples are taken, then a documented default threshold applies and a crossing still produces exactly one dump | task-4 | covered | "With no threshold configured, `DEFAULT_HEAP_DUMP_THRESHOLD_MB` is applied and a crossing writes exactly one dump and one record, as asserted by the default-threshold test." | diff-local |
| criterion | Story 2 negative: Given a dump was already written in this daemon lifetime, when later samples also exceed the threshold, then no second snapshot is written and no second `daemon_heap_dump_written` record is appended | task-5 | covered | "After one dump, two further samples above threshold write no file and emit no second `daemon_heap_dump_written`, as asserted by the once-per-lifetime test." | diff-local |
| criterion | Story 2 negative: Given `.daemon/heap/` already holds the configured maximum number of snapshots, when a new dump is written, then the oldest snapshot is removed first so the count never exceeds the cap | task-5 | covered | "With retention 3 and three existing snapshots, a new dump leaves exactly three files in `.daemon/heap/` and the oldest by mtime is the one removed, as asserted by the retention test." | diff-local |
| criterion | Story 2 negative: Given the snapshot write fails midway, when the trigger returns, then no partial file remains at the target path, one `daemon.log` line names the failure, and the step in flight is unaffected | task-5 | covered | "A `writeHeapSnapshot` that throws leaves no file at the target path and no `.tmp` sibling, produces exactly one `daemon.log` line naming the failure, and does not throw into the step listener, as asserted by the failed-write test." | diff-local |
| criterion | Story 3 happy: Given no `daemon_heap_limit_mb` in config, when the supervisor builds the pane foreground command, then the launched daemon carries `--max-old-space-size` with the documented default value | task-7 | covered | "`buildDaemonForegroundCommand` returns a command containing `--max-old-space-size=4096` with no key set and `--max-old-space-size=6144` with `daemon_heap_limit_mb: 6144`, as asserted by the foreground-command test." | diff-local |
| criterion | Story 3 happy: Given `daemon_heap_limit_mb: 6144` in config, when the supervisor builds the pane foreground command, then the launched daemon carries `--max-old-space-size=6144` | task-7 | covered | "`buildDaemonForegroundCommand` returns a command containing `--max-old-space-size=4096` with no key set and `--max-old-space-size=6144` with `daemon_heap_limit_mb: 6144`, as asserted by the foreground-command test." | diff-local |
| criterion | Story 3 negative: Given `daemon_heap_limit_mb: 0` or a non-integer or a negative value, when config is validated, then validation fails with a message naming `daemon_heap_limit_mb` and the accepted range, and no daemon is spawned | task-7 | covered | "`supervisor.start` builds the command through `buildDaemonForegroundCommand(loadConfig(root))`, and an invalid `daemon_heap_limit_mb` makes it throw the validation message before any `tmux new-session` or `respawn-pane` call is made, as asserted by the invalid-config start test with a recording tmux runner." | diff-local |
| criterion | Story 3 negative: Given the daemon exceeds the cap, when V8 aborts, then `.daemon/daemon.log` contains the `JavaScript heap out of memory` fatal error text and the exit witness of Story 4 records a non-zero exit for that pid | task-7, task-9 | covered | "A daemon exceeding the cap leaves the `JavaScript heap out of memory` fatal text in `.daemon/daemon.log` and a non-zero exit for its pid, as asserted by the heap-abort test that starts a child with a 16 MB cap and an allocation loop." | diff-local |
| criterion | Story 4 happy: Given a tmux-hosted daemon, when the daemon process exits with code 0, exits with a non-zero code, or is killed by `SIGKILL`, then the pane foreground appends exactly one `daemon_exited` record to `.daemon/exit-events.jsonl` carrying the daemon pid, the exit code or the signal name, and a timestamp, before the pane foreground itself exits | task-9 | covered | "On a fixture-owned private tmux socket, `SIGKILL` of the daemon child yields exactly one `daemon_exited` record with `signal: SIGKILL` and `code: null` in `.daemon/exit-events.jsonl`, zero such records in `.daemon/events.jsonl`, and the record is written before the wrapper exits, as asserted by the real-tmux test." | diff-local |
| criterion | Story 4 happy: Given the daemon is killed with `SIGKILL`, when the witness runs, then the record carries `signal: SIGKILL` and a null exit code | task-9 | covered | "On a fixture-owned private tmux socket, `SIGKILL` of the daemon child yields exactly one `daemon_exited` record with `signal: SIGKILL` and `code: null` in `.daemon/exit-events.jsonl`, zero such records in `.daemon/events.jsonl`, and the record is written before the wrapper exits, as asserted by the real-tmux test." | diff-local |
| criterion | Story 4 happy: Given the daemon exits with code 134 after a V8 heap abort, when the witness runs, then the record carries `code: 134` and `signal: SIGABRT` | task-9 | covered | "A daemon exiting 134 after a heap abort yields a `daemon_exited` record with `code: 134` and `signal: SIGABRT`, as asserted by the real-tmux heap-abort case." | diff-local |
| criterion | Story 4 negative: Given the witness cannot write `.daemon/exit-events.jsonl`, when the daemon exits, then the witness writes the same record as one line to `.daemon/daemon.log` and exits non-zero without hanging the pane | task-8 | covered | "With an unwritable `.daemon/exit-events.jsonl`, the witness writes the same record as one line to `.daemon/daemon.log` and exits non-zero, as asserted by the fallback test." | diff-local |
| criterion | Story 4 negative: Given a daemon restarted in place while an older wrapper is still writing its record, when both proceed, then `.daemon/events.jsonl` is never opened for writing by the witness and `.daemon/exit-events.jsonl` has exactly one line per exited pid with no torn line | task-9 | covered | "A respawn while an older wrapper is still writing leaves `.daemon/exit-events.jsonl` with exactly one line per exited pid and every line parseable, and the witness never opens `.daemon/events.jsonl`, as asserted by the overlapping-respawn test." | diff-local |
| criterion | Story 4 negative: Given the daemon is run bare with no tmux session, when it exits, then no witness runs and no `.daemon/exit-events.jsonl` is created by that exit | task-9 | covered | "A bare `daemon --continuous` run outside tmux exits without creating `.daemon/exit-events.jsonl`, as asserted by the bare-run test." | diff-local |
| criterion | Story 4 negative: Given `ai-conductor daemon exit-witness` is invoked without `--pid` or without an exit status, when it parses arguments, then it exits non-zero with a usage message and writes nothing | task-8 | covered | "Invoking the witness without `--pid` or without an exit status exits non-zero with a usage message and creates no file, as asserted by the argument test." | diff-local |
| criterion | Story 5 happy: Given a session whose daemon pid is dead and `.daemon/exit-events.jsonl` holds a `daemon_exited` for that pid, when `daemon status` runs, then the `⚠ session-up/process-dead` row also renders the signal or exit code and the exit timestamp on the same row | task-11 | covered | "A `dead-pane` row whose dead pid has a matching `daemon_exited` renders the signal or exit code and the exit timestamp on the same line as `⚠ session-up/process-dead`, as asserted by the status render test." | diff-local |
| criterion | Story 5 happy: Given `.daemon/events.jsonl` holds `daemon_memory_sample` records, when `daemon status` runs, then the row renders the most recent sample's `rss` in megabytes and its timestamp | task-11 | covered | "A row renders the most recent `daemon_memory_sample` as `mem <n> MB at <iso>`, as asserted by the status render test." | diff-local |
| criterion | Story 5 negative: Given a dead pid with no `daemon_exited` record for it, when `daemon status` runs, then the row renders `exit cause unknown` rather than the cause of an older pid | task-11 | covered | "A dead pid with no matching record renders `exit cause unknown` rather than an older pid's cause, as asserted by the stale-record test." | diff-local |
| criterion | Story 5 negative: Given `.daemon/exit-events.jsonl` contains one malformed line among valid ones, when `daemon status` runs, then the valid records are still used and the malformed line is counted in a single `skipped N unparseable` note | task-11 | covered | "One malformed exit-ledger line produces a single `(skipped 1 unparseable)` note and the row still renders the valid record, as asserted by the malformed-line render test." | diff-local |
| criterion | Story 5 negative: Given a healthy running daemon, when `daemon status` runs, then no exit cause is rendered and the last memory sample is still shown | task-11 | covered | "A healthy running daemon renders the last sample and no exit cause, as asserted by the healthy-row test." | diff-local |
| criterion | Story 6 happy: Given a stale pidfile whose pid is dead and a matching `daemon_exited` record, when `ensureRunning` reclaims the lock and respawns, then its log line names the dead pid, the signal or exit code, and the exit timestamp before the respawn line | task-12 | covered | "With a dead pidfile owner and a matching `daemon_exited`, the `ensureRunning` reclaim log line contains the dead pid, the signal or exit code, and the exit timestamp, and is emitted before the respawn line, as asserted by the reclaim-report test." | diff-local |
| criterion | Story 6 negative: Given a stale pidfile whose pid has no `daemon_exited` record, when `ensureRunning` reclaims, then the log line names the dead pid with `exit cause unknown` and the respawn proceeds | task-12 | covered | "With no matching record the reclaim log line contains `exit cause unknown` and the respawn still proceeds, as asserted by the unknown-cause test." | diff-local |
| criterion | Story 6 negative: Given the exit ledger is unreadable, when `ensureRunning` reclaims, then the respawn still proceeds and the log line notes the ledger could not be read | task-12 | covered | "With an unreadable exit ledger the reclaim log line notes the ledger could not be read and the respawn still proceeds without throwing, as asserted by the unreadable-ledger test." | diff-local |
| criterion | Story 7 happy: Given a feature at task 19 of 25 with tasks 1–18 carrying `Task:` trailers on commits and `completed` rows in its worktree `task-status.json`, when the daemon is killed with `SIGKILL` and a new daemon dispatches the feature, then the build resumes at task 19 and tasks 1–18 are neither re-dispatched nor re-marked | task-13 | covered | "The resume acceptance test seeds 18 trailered commits and completed rows, re-seeds as a new dispatch, and asserts the next task index is 19 with rows 1–18 unchanged." | diff-local |
| criterion | Story 7 happy: Given the same interruption, when the new daemon dispatches, then the worktree `.pipeline/events.jsonl` and `conduct-state.json` from before the kill are intact and the completed steps before `build` are not re-run | task-13 | covered | "The same test asserts `.pipeline/events.jsonl` and `conduct-state.json` retain their pre-kill content and no DECIDE step status changes on redispatch." | diff-local |
| criterion | Story 7 negative: Given a completed task whose commit carries a `Task:` trailer but whose `task-status.json` row was lost, when the feature is re-dispatched, then the row is restored as `completed` from the trailer and the task is not redone | task-13 | covered | "A trailer-only completed task is restored as `completed` when `task-status.json` was lost, and an uncommitted mid-flight task keeps its preserved `in_progress` row and is re-dispatched rather than treated as complete, as asserted by the re-seed cases." | diff-local |
| criterion | Story 7 negative: Given the daemon was killed while task 19 was mid-flight with no commit, when the feature is re-dispatched, then task 19 keeps its preserved `in_progress` row and is dispatched again, and no half-finished state is treated as complete | task-13 | covered | "A trailer-only completed task is restored as `completed` when `task-status.json` was lost, and an uncommitted mid-flight task keeps its preserved `in_progress` row and is re-dispatched rather than treated as complete, as asserted by the re-seed cases." | diff-local |
| criterion | Story 7 negative: Given the feature had no `.pipeline/HALT` written because the daemon died abruptly, when the new daemon scans the backlog, then the feature is re-dispatched on the next poll without an operator clearing anything | task-13 | covered | "A feature worktree with no `.pipeline/HALT` is returned as dispatchable by the backlog scan on the next poll, as asserted by the no-halt case." | diff-local |

