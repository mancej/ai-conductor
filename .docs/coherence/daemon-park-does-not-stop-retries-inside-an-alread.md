# Coherence: Park stops retries inside an already-dispatched step (#2103)

**Date:** 2026-09-22
**Tier:** M
**Track:** product
**Outcome source:** jstoup111/ai-conductor#2103, with five staged Desired-outcome bullets in `.pipeline/intake-outcomes.md`.

## Consistency pass

Each staged outcome maps to the stories that deliver it. Outcome 1 (no new attempts) maps to Stories 1, 2, and 7. Outcome 2 (park output tells whether work stopped) maps to Stories 9–11. Outcome 3 (no daemon stop needed) maps to Story 8. Outcome 5 (a running provider that park cannot interrupt is reported) maps to Stories 9 and 11. Outcome 4 (runbook documentation) is documentation. The stories and plan skills forbid documentation stories and tasks, so it is a `gap` row waived to the gating `maintain-documentation` step (see `.docs/coherence-waivers/daemon-park-does-not-stop-retries-inside-an-alread.md`), by operator decision on 2026-09-22.

The amended ADR `adr-2026-07-29-operator-park-scheduling-unit-boundary` is adjudicated by the `adr` row below. Its per-decision obligations D1–D14 are tabled in the plan's Architecture Obligation Coverage section.

| Row class | Cited id | Counterpart id(s) | Verdict | Quote | Disposition |
|---|---|---|---|---|---|
| outcome | outcome-1 | story-1, story-2, story-7 | covered | A parked feature stops launching new provider attempts, including retries inside a step that was |
| outcome | outcome-2 | story-9, story-10, story-11 | covered | The operator can tell from the park command's own output whether the feature actually stopped or |
| outcome | outcome-3 | story-8 | covered | Stopping an in-flight parked feature does not require stopping the daemon, so unrelated queued |
| outcome | outcome-4 | | gap | Whatever the in-flight behaviour ends up being, it is documented where an operator looks during an |
| outcome | outcome-5 | story-9, story-11 | covered | An already-running provider that a park cannot interrupt is at least reported as still running, |
| fr | fr-1 | story-1 | covered | Story 1 cites FR-1. |
| fr | fr-2 | story-3 | covered | Story 3 cites FR-2. |
| fr | fr-3 | story-4 | covered | Story 4 cites FR-3. |
| fr | fr-4 | story-5 | covered | Story 5 cites FR-4. |
| fr | fr-5 | story-6 | covered | Story 6 cites FR-5. |
| fr | fr-6 | story-2 | covered | Story 2 cites FR-6. |
| fr | fr-7 | story-7 | covered | Story 7 cites FR-7. |
| fr | fr-8 | story-8 | covered | Story 8 cites FR-8. |
| fr | fr-9 | story-9 | covered | Story 9 cites FR-9. |
| fr | fr-10 | story-11 | covered | Story 11 cites FR-10. |
| fr | fr-11 | story-10 | covered | Story 10 cites FR-11. |
| fr | fr-12 | story-12 | covered | Story 12 cites FR-12. |
| fr | fr-14 | story-13 | covered | Story 13 cites FR-14. |
| story | story-1 | task-2 | covered | A parked serial step launches no further attempt — every criterion is quoted from a cited task below. |
| story | story-2 | task-3 | covered | Every retry path honors park — every criterion is quoted from a cited task below. |
| story | story-3 | task-4 | covered | The running attempt is never interrupted — every criterion is quoted from a cited task below. |
| story | story-4 | task-5 | covered | A declined attempt costs nothing — every criterion is quoted from a cited task below. |
| story | story-5 | task-6 | covered | Unpark resumes the parked step — every criterion is quoted from a cited task below. |
| story | story-6 | task-7 | covered | Park races fail safely — every criterion is quoted from a cited task below. |
| story | story-7 | task-8, task-9 | covered | A parked parallel-group member settles as parked — every criterion is quoted from a cited task below. |
| story | story-8 | task-10 | covered | Parking one feature leaves others running — every criterion is quoted from a cited task below. |
| story | story-9 | task-11, task-12 | covered | The park command reports running work — every criterion is quoted from a cited task below. |
| story | story-10 | task-11, task-12 | covered | The park command reports a fully stopped feature — every criterion is quoted from a cited task below. |
| story | story-11 | task-11, task-12 | covered | Unknown state is reported as unknown — every criterion is quoted from a cited task below. |
| story | story-12 | task-1, task-2, task-9 | covered | The declined attempt is visible in daemon reporting — every criterion is quoted from a cited task below. |
| story | story-13 | task-13 | covered | Interactive runs are unchanged — every criterion is quoted from a cited task below. |
| task | task-1 | story-12 | covered | Attempt-level operator-park boundary on the existing event. |
| task | task-2 | story-1, story-12 | covered | Per-attempt park gate on the ordinary serial dispatch path. |
| task | task-3 | story-2 | covered | Every free and budgeted retry path passes the attempt gate. |
| task | task-4 | story-3 | covered | The attempt already running drains uninterrupted. |
| task | task-5 | story-4 | covered | A declined attempt spends no budget and writes no HALT. |
| task | task-6 | story-5 | covered | Unpark resumes the parked step with a fresh budget. |
| task | task-7 | story-6 | covered | Park races between attempts fail toward parked. |
| task | task-8 | story-7 | covered | Group members settle as parked instead of retrying. |
| task | task-9 | story-7, story-12 | covered | A join with a parked member stops the feature as parked. |
| task | task-10 | story-8 | covered | Parking one feature leaves the daemon pool running. |
| task | task-11 | story-9, story-10, story-11 | covered | Classify a feature's running work from the persisted spine. |
| task | task-12 | story-9, story-10, story-11 | covered | The park command reports running, stopped, or unknown work. |
| task | task-13 | story-13 | covered | Interactive conduct keeps every retry. |
| adr | adr-2026-07-29-operator-park-scheduling-unit-boundary | task-1, task-2, task-5, task-8, task-9, task-12 | covered | Amended D11–D14 are delivered by the cited tasks; D1–D10 are existing or no-change per the plan obligation table. |
| criterion | Story 1 happy: Given a daemon-run build step whose first provider attempt fails and whose retry budget allows another attempt, When the feature is parked before that attempt ends, Then no second provider attempt is dispatched and the conductor returns an operator-parked termination. | task-2 | covered | "On the ordinary dispatch path, the per-attempt park check in conductor.ts consults the injected operatorParkBoundary before `stepRunner.run` and, when it returns true, dispatches nothing and the run returns an `operator-parked` termination, as asserted by the ordinary-path park test that records exactly one runner dispatch for a step allowed three attempts when the park lands during attempt 1." | diff-local |
| criterion | Story 1 happy: Given a daemon-run build step on the ordinary (non-self-host) dispatch path with no park active, When its first attempt fails, Then the next attempt is dispatched exactly as today. | task-2 | covered | "With no park active, or with a park removed before the next attempt's check, the per-attempt check lets the next attempt dispatch and no `operator-parked` termination is returned, as asserted by the no-park and removed-park retry tests that record the second runner dispatch." | diff-local |
| criterion | Story 1 negative: Given a daemon-run build step whose attempt fails, When the park-state read throws an error before the next attempt, Then no further attempt is dispatched and the conductor returns an operator-parked termination. | task-2 | covered | "When operatorParkBoundary rejects with an error at the per-attempt check, the feature is treated as parked, no runner dispatch follows the failed attempt, and the run returns an `operator-parked` termination, as asserted by the throwing-predicate test." | diff-local |
| criterion | Story 1 negative: Given a feature whose park was removed before the next attempt began, When the retry loop reaches that attempt, Then the attempt is dispatched and no operator-parked termination is returned. | task-2 | covered | "With no park active, or with a park removed before the next attempt's check, the per-attempt check lets the next attempt dispatch and no `operator-parked` termination is returned, as asserted by the no-park and removed-park retry tests that record the second runner dispatch." | diff-local |
| criterion | Story 2 happy: Given a parked feature whose running attempt ended with a rate-limit wait, a stale session, an auth refresh, a finish-publication progress retry, a test_suite infrastructure retry, or a build_review mechanical-fault retry, When the conductor would re-enter the attempt loop for that free retry, Then no provider attempt is dispatched and the run returns an operator-parked termination. | task-3 | covered | "For each free-retry path of the serial retry loop (rate-limit wait, stale-session reset, auth refresh, finish-publication progress retry, test_suite infrastructure retry, and build_review mechanical-fault retry), a parked-feature conductor test records zero runner dispatches after the park and an `operator-parked` termination." | diff-local |
| criterion | Story 2 happy: Given a self-host daemon build whose attempt fails while the feature is parked, When the next attempt reaches self-host dispatch admission, Then it is cancelled before dispatch exactly as it is today. | task-3 | covered | "A self-host attempt reached while parked is still cancelled before dispatch with a `self_host_dispatch_admission` event in state `cancelled` and no runner call, as asserted by the existing self-host admission park test." | diff-local |
| criterion | Story 2 negative: Given a parked feature whose running attempt produced a completion-check miss that would normally count as a budgeted retry, When the loop continues, Then no provider attempt is dispatched even though retry budget remains. | task-3 | covered | "A parked-feature conductor test whose attempt ends in a completion-check miss with retry budget remaining records zero runner dispatches after the park even though the budget would allow another attempt." | diff-local |
| criterion | Story 2 negative: Given a parked feature whose running attempt ended in a free retry that does not consume budget, When the loop re-enters, Then the park is still consulted before dispatch and the free retry does not bypass it. | task-3 | covered | "The free-retry tests assert that the park predicate is consulted on each re-entry of the retry loop, so a free retry never bypasses the per-attempt check." | diff-local |
| criterion | Story 3 happy: Given a provider attempt that is running when the feature is parked, When the attempt later completes successfully, Then its result is recorded normally and the step reaches its natural terminal status before the feature stops. | task-4 | covered | "A conductor test whose runner resolves successfully after the park is written observes that attempt's result persisted, the step's persisted status set to its natural terminal status, and the run stopping only afterwards at the next unit boundary." | diff-local |
| criterion | Story 3 negative: Given a provider attempt that is running when the feature is parked, When the park lands, Then the attempt receives no abort, cancellation, or kill and its runner call returns only when the provider finishes. | task-4 | covered | "The same drain test asserts that the runner receives no abort signal, cancellation, or kill, and that its runner call returns only when the fake provider resolves." | diff-local |
| criterion | Story 3 negative: Given a running attempt that fails after the park lands, When it returns, Then its failure output is recorded as the attempt's result and the park stops only the next attempt. | task-4 | covered | "A conductor test whose running attempt fails after the park lands observes that failure output recorded as the attempt's result and zero further runner dispatches." | diff-local |
| criterion | Story 4 happy: Given a step whose next attempt is declined for a park, When the conductor returns, Then the termination is operator-parked and no step_failed, step_retry, or retry-exhaustion event is emitted after the operator-park boundary. | task-5 | covered | "After a declined attempt the conductor run returns an `operator-parked` termination, and the event stream ends with the `operator_park_boundary` event and contains no step_failed, step_retry, or retry-exhaustion event after it, as asserted by the declined-attempt event test." | diff-local |
| criterion | Story 4 negative: Given a step whose next attempt is declined for a park, When the conductor returns, Then no HALT marker file is written to the feature worktree. | task-5 | covered | "After a declined attempt the feature worktree contains no HALT marker file and persisted conduct state shows the step `in_progress`, not failed, as asserted by the declined-attempt state test." | diff-local |
| criterion | Story 4 negative: Given a build step whose declined attempt would have been its third, When the conductor returns, Then no escalation-ladder model or effort rung is recorded for that attempt and the no-evidence attempt count is unchanged from before the park. | task-5 | covered | "For a build step whose declined attempt would have been its third, the escalation ladder records no model or effort rung for attempt 3 and the persisted no-evidence attempt count is identical before and after the declined attempt, as asserted by the declined-build-accounting test." | diff-local |
| criterion | Story 4 negative: Given a step whose next attempt is declined for a park, When the persisted conduct state is read, Then the step's status is in_progress, not failed. | task-5 | covered | "After a declined attempt the feature worktree contains no HALT marker file and persisted conduct state shows the step `in_progress`, not failed, as asserted by the declined-attempt state test." | diff-local |
| criterion | Story 5 happy: Given a feature stopped by a declined attempt with its step left in_progress, When the park is removed and the daemon resumes the feature, Then the conductor resumes at that same step and dispatches its first attempt with a fresh attempt budget. | task-6 | covered | "A resume test that removes the park after a declined attempt observes the conductor's resume index landing on the parked `in_progress` step and its first dispatched attempt numbered 1, the start of a fresh attempt budget." | diff-local |
| criterion | Story 5 happy: Given a feature parked mid-build after some plan tasks were committed complete, When it resumes, Then those tasks remain complete and are not dispatched again. | task-6 | covered | "A resume test for a build parked after some plan tasks were committed complete observes those task statuses still complete after resume and no dispatch for those tasks." | diff-local |
| criterion | Story 5 negative: Given a feature stopped by a declined attempt, When it resumes while still parked, Then the pre-unit gate stops it before any attempt and the step remains in_progress. | task-6 | covered | "A resume test that resumes while the park is still active observes the existing pre-unit gate returning `operator-parked` before any runner dispatch and the step still `in_progress`." | diff-local |
| criterion | Story 6 happy: Given an attempt that has returned and a park written before the next attempt's dispatch check, When the check runs, Then it observes the park and no attempt is dispatched. | task-7 | covered | "A test that writes the park between attempt N and attempt N+1 records exactly N runner dispatches, because the per-attempt park check observes the park before the next dispatch." | diff-local |
| criterion | Story 6 negative: Given a park-state read that rejects with an error at the attempt check, When the check completes, Then it treats the feature as parked and no attempt is dispatched. | task-7 | covered | "A test whose park predicate rejects with an error at the per-attempt check records zero runner dispatches at that check and an `operator-parked` termination, treating the feature as parked." | diff-local |
| criterion | Story 6 negative: Given a park written after the attempt check passed and while that attempt's dispatch is starting, When the attempt runs, Then it runs to completion (per Story 3) and the following attempt is declined. | task-7 | covered | "A test that writes the park after the per-attempt check passed and while that attempt's dispatch is starting observes that attempt running to completion with its result recorded and the following attempt declined." | diff-local |
| criterion | Story 7 happy: Given a daemon-run parallel group whose member's first attempt fails while the feature is parked, When that member would retry, Then it dispatches no further attempt and settles with a parked outcome. | task-8 | covered | "In `runGroupBranchInner`, a member whose first attempt fails while the injected park predicate returns true makes no further `stepRunner.run` call and settles with the `parked` outcome, as asserted by the member-retry-park test that records one dispatch." | diff-local |
| criterion | Story 7 happy: Given a parallel group whose members are passing, parked, or still running when the park lands, When every started member settles, Then the group join returns an operator-parked termination rather than a pass or a failure. | task-9 | covered | "A join over passing, parked, and still-running members waits for every started member to settle, and then the conductor returns an `operator-parked` termination rather than a group pass or failure, as asserted by the mixed-join park test." | diff-local |
| criterion | Story 7 negative: Given a parallel group with one parked member and one failed member, When the group joins, Then the failed member's genuine failure and diagnostics are reported and are not overwritten by the park. | task-9 | covered | "A join with one parked and one failed member emits the failed member's parallel_failure event with its original error and diagnostics unchanged, and the conductor's group result is the existing failure outcome, not an `operator-parked` termination, as asserted by the parked-plus-failed join test." | diff-local |
| criterion | Story 7 negative: Given a parallel group with one parked member, When the group joins, Then no parallel_failure event is emitted for the parked member. | task-9 | covered | "`buildParallelFailureEvents` emits no parallel_failure event for a `parked` member, as asserted by the join test that counts zero parallel_failure events for the parked member." | diff-local |
| criterion | Story 7 negative: Given a parked member, When its outcome is classified, Then it is classified as parked and never as no-verdict or aborted. | task-8 | covered | "`BranchOutcome` in group-core.ts gains a `parked` kind, and `classifyOutcome` returns `parked` for it through its exhaustive switch with no default branch, as asserted by the classify-parked test, which also asserts the result is neither `no-verdict` nor `aborted`." | diff-local |
| criterion | Story 8 happy: Given a daemon pool running feature A and feature B, When A is parked and its next attempt is declined, Then B's attempts continue to dispatch and B can finish. | task-10 | covered | "A daemon pool test in which feature A's conductor returns an `operator-parked` termination from a declined attempt observes feature B's dispatch count still increasing afterwards and B reaching completion." | diff-local |
| criterion | Story 8 negative: Given a daemon pool with feature A parked mid-step and feature C queued, When A returns its operator-parked termination, Then C is dispatched into the freed slot and the daemon process keeps running. | task-10 | covered | "The same pool test observes queued feature C dispatched into the slot A freed, with no daemon shutdown event emitted." | diff-local |
| criterion | Story 9 happy: Given a live daemon and a feature whose latest persisted provider attempt for its current step is running, When the operator parks the feature, Then the output states that an attempt is still running and names the step and attempt. | task-12 | covered | "After writing the park marker, `daemon park` prints a report stating that an attempt is still running and naming the step and attempt when the daemon pidfile is live per `readPidRecord` and `isLive` and `classifyRunningWork` returns `running` for a running or preparing attempt, as asserted by the park CLI running tests." | diff-local |
| criterion | Story 9 happy: Given a live daemon and a feature whose latest persisted provider attempt is preparing, When the operator parks the feature, Then the output states that an attempt is still running. | task-12 | covered | "After writing the park marker, `daemon park` prints a report stating that an attempt is still running and naming the step and attempt when the daemon pidfile is live per `readPidRecord` and `isLive` and `classifyRunningWork` returns `running` for a running or preparing attempt, as asserted by the park CLI running tests." | diff-local |
| criterion | Story 9 negative: Given a feature already parked, When the operator parks it again, Then the output still carries the running-work report alongside the existing already-parked notice. | task-12 | covered | "Re-parking an already-parked feature prints the existing already-parked notice together with the running-work report, as asserted by the already-parked report test." | diff-local |
| criterion | Story 10 happy: Given a feature whose latest persisted provider attempt has settled, When the operator parks it, Then the output states that the feature is fully stopped. | task-12 | covered | "`daemon park` prints that the feature is fully stopped when `classifyRunningWork` returns `stopped`, when no daemon pidfile is live, and when the feature has no worktree and no persisted events, and in each case the park marker is written, as asserted by the park CLI stopped tests." | diff-local |
| criterion | Story 10 happy: Given no running daemon for the repository, When the operator parks a feature, Then the output states that the feature is fully stopped. | task-12 | covered | "`daemon park` prints that the feature is fully stopped when `classifyRunningWork` returns `stopped`, when no daemon pidfile is live, and when the feature has no worktree and no persisted events, and in each case the park marker is written, as asserted by the park CLI stopped tests." | diff-local |
| criterion | Story 10 negative: Given a feature with no worktree and no persisted events, When the operator parks it, Then the output states that the feature is fully stopped and the park is still written. | task-12 | covered | "`daemon park` prints that the feature is fully stopped when `classifyRunningWork` returns `stopped`, when no daemon pidfile is live, and when the feature has no worktree and no persisted events, and in each case the park marker is written, as asserted by the park CLI stopped tests." | diff-local |
| criterion | Story 11 happy: Given a live daemon and a feature whose events file exists but cannot be read, When the operator parks it, Then the output states that running work is unknown and the park is still written. | task-12 | covered | "`daemon park` prints that running work is unknown, and never the fully-stopped text, when the events file is unreadable, when the provider-attempt lines are all malformed, or when pidfile liveness cannot be determined, and the park marker is still written, as asserted by the park CLI unknown tests." | diff-local |
| criterion | Story 11 negative: Given a feature whose events file contains only malformed provider-attempt lines, When the operator parks it, Then the output states that running work is unknown and does not state that the feature is stopped. | task-12 | covered | "`daemon park` prints that running work is unknown, and never the fully-stopped text, when the events file is unreadable, when the provider-attempt lines are all malformed, or when pidfile liveness cannot be determined, and the park marker is still written, as asserted by the park CLI unknown tests." | diff-local |
| criterion | Story 11 negative: Given a daemon pidfile whose liveness cannot be determined, When the operator parks a feature, Then the output states that running work is unknown. | task-12 | covered | "`daemon park` prints that running work is unknown, and never the fully-stopped text, when the events file is unreadable, when the provider-attempt lines are all malformed, or when pidfile liveness cannot be determined, and the park marker is still written, as asserted by the park CLI unknown tests." | diff-local |
| criterion | Story 11 negative: Given any unknown report, When the park command exits, Then it exits successfully because the park itself was written. | task-12 | covered | "Every unknown-report park CLI test asserts exit status 0, because the park itself was written." | diff-local |
| criterion | Story 12 happy: Given a serial step whose next attempt is declined for a park, When the operator-park boundary event is emitted, Then it identifies the feature, the step, and an attempt-level boundary, and the daemon log renders a line naming them. | task-1, task-2 | covered | "The daemon-log renderer in daemon-cli.ts renders an attempt boundary as a line naming the feature, the step, and that an attempt was declined for an operator park, and a member boundary as a line naming the group step and the member, as asserted by the attempt and member render tests." | diff-local |
| criterion | Story 12 happy: Given a parallel-group member whose retry is declined, When the boundary event is emitted, Then it names the group step and the member. | task-1, task-9 | covered | "The daemon-log renderer in daemon-cli.ts renders an attempt boundary as a line naming the feature, the step, and that an attempt was declined for an operator park, and a member boundary as a line naming the group step and the member, as asserted by the attempt and member render tests." | diff-local |
| criterion | Story 12 negative: Given a park observed at a scheduling-unit boundary with no attempt declined, When the boundary event is emitted, Then it keeps the existing unit-level shape and rendering unchanged. | task-1 | covered | "The existing unit-boundary render test in daemon-render.test.ts passes unchanged, so step, group, and pre-first-unit boundaries keep their existing shape and rendered text." | diff-local |
| criterion | Story 12 negative: Given a declined attempt, When the event is persisted, Then exactly one operator-park boundary event is recorded for that stop, not one per retry path. | task-2 | covered | "A declined attempt emits exactly one `operator_park_boundary` event, whose attempt boundary carries the feature slug, the step name, and the attempt number, as asserted by the single-boundary-event test." | diff-local |
| criterion | Story 13 happy: Given an interactive conduct run with no daemon park predicate, When a step retries, Then every retry is dispatched exactly as before. | task-13 | covered | "A conductor constructed without an operatorParkBoundary option dispatches every retry of a failing step, as asserted by the interactive-retry test that records the full retry count." | diff-local |
| criterion | Story 13 negative: Given an interactive conduct run and a park marker present on disk for its slug, When a step retries, Then the marker is not consulted and the retry is dispatched. | task-13 | covered | "The same interactive test with a park marker present on disk for its slug records every retry dispatched and the park marker never read, as asserted by a park-read spy with zero calls." | diff-local |

## Verify-Claims Ledger

- [verified] There are five staged outcomes; their text is copied from `.pipeline/intake-outcomes.md` into the outcome rows.
- [verified] FR-1 through FR-12 and FR-14 exist in the approved PRD; every FR is cited by a story's Requirement line.
- [verified] Stories 1–13 are each cited by at least one plan task's Story line, and every criterion row quotes a Done-when bullet of a cited task.
- [verified] The amended ADR parses to citable decisions 1–14, and each has one obligation row in the plan.
