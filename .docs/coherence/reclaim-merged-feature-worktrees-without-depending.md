# Coherence: Reclaim merged feature worktrees without depending on the mergeable watch registry

**Result:** PASS with one waived outcome gap
**Date:** 2026-09-14
**Tier:** M
**Track:** technical
**Source:** jstoup111/ai-conductor#1510
**Plan:** .docs/plans/reclaim-merged-feature-worktrees-without-depending.md
**Stories:** .docs/stories/reclaim-merged-feature-worktrees-without-depending.md

## Scope and inputs

Reviewed the four staged desired outcomes (rewritten by hand from the issue body because the reaped stale claim staged an empty section; no neutralizations reported), all eight accepted stories and 55 criteria, eleven approved plan tasks, and the three changed approved ADRs with 8, 9, and 8 citable decisions. No product FR layer applies. The conflict-check amendment to the older parked-feature reconciliation stories is context only; it ships as a companion PR and is not part of this row pool. Gap outcome-4 is waived at the sibling coherence-waivers file.

## Mapping

| Row class | Cited id(s) | Counterpart id(s) | Verdict | Notes / exact outcome quote |
| --- | --- | --- | --- | --- |
| outcome | outcome-1 | story-1, story-3, story-4, story-5 | covered | A worktree whose PR is merged and whose shipped record is on main is reclaimed automatically, whether or not its PR was ever recorded in the mergeable watch registry. |
| outcome | outcome-2 | story-3, story-4 | covered | Reclamation covers worktrees whose branch does not follow the `feat/daemon-<slug>` naming convention. |
| outcome | outcome-3 | story-2, story-6, story-8 | covered | A worktree for in-progress, parked, or unmerged work is never reclaimed by this path, and an operator can see from the daemon log why each retained worktree was kept. |
| outcome | outcome-4 | | gap | An operator can determine the reclaimability of every worktree in a checkout from one command, without hand-cross-referencing branches against GitHub. |
| adr | adr-2026-08-01-multi-proof-park-deletion-authority | story-1, story-3, story-4, story-5 | covered | Amended D6 uses Tasks 1 and 5 (listing-fed candidate set), D7 Task 2 (listed-branch evidence), D8 Task 3 (branch-kind record rule); D1–D5 existing or no-change per the plan obligation table, each verified against park-reconciliation.ts. |
| adr | adr-2026-07-29-defer-feature-worktree-reap-to-shipped-record-on-main | story-1, story-2, story-6, story-7 | covered | Amended D8 uses Tasks 5, 6, 10 (enumeration-fed path through the single-slug helper, gated by reclaim_merged_worktrees); D9 uses Tasks 5, 6, 8 (named retention reasons on the spine); D1–D7 existing or no-change, verified against daemon-runner.ts, mergeable-sweep.ts, shipped-record-on-main.ts, daemon-park-cli.ts. |
| adr | adr-2026-07-27-ancestry-proven-park-reconciliation | story-2, story-4, story-5, story-8 | covered | Additive notes under D1 and D4 point at adr-2026-08-01 D6–D8. D1 Task 5, D4 Task 3, D6 Task 4. D5 names the helper as the in-flight refuser; the shipped helper deliberately dropped that check before this spec and the invariant (an in-flight worktree is never removed) is delivered by Task 5 at the sweep layer, judged as satisfying the obligation; recorded here so a reader need not re-derive it. |
| story | story-1 | task-1, task-5, task-6 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-2 | task-5, task-10, task-11 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-3 | task-2 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-4 | task-3 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-5 | task-4, task-8, task-11 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-6 | task-7, task-8, task-10 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-7 | task-6, task-9, task-10 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| story | story-8 | task-5, task-11 | covered | Task citations checked against the plan's Story lines; criterion proof follows below. |
| task | task-1 | story-1 | covered | Enumerate registered worktrees from the porcelain listing. Completion checks inspected against these stories. |
| task | task-2 | story-3 | covered | Key merge evidence on the listed branch. Completion checks inspected against these stories. |
| task | task-3 | story-4 | covered | Scope the shipped-record precondition to daemon branches. Completion checks inspected against these stories. |
| task | task-4 | story-5 | covered | Unpark only when a park marker exists. Completion checks inspected against these stories. |
| task | task-5 | story-1, story-2, story-8 | covered | Widen the sweep candidate set and exclude non-reclaimable candidates. Completion checks inspected against these stories. |
| task | task-6 | story-1, story-7 | covered | Retain the whole pass on listing failure and honour the reclaim gate. Completion checks inspected against these stories. |
| task | task-7 | story-6 | covered | Declare the reclaim event variants and their sinks. Completion checks inspected against these stories. |
| task | task-8 | story-5, story-6 | covered | Emit reclaim outcomes from the sweep. Completion checks inspected against these stories. |
| task | task-9 | story-7 | covered | Validate and register the reclaim_merged_worktrees config key. Completion checks inspected against these stories. |
| task | task-10 | story-2, story-6, story-7 | covered | Wire the widened sweep at the daemon composition root. Completion checks inspected against these stories. |
| task | task-11 | story-2, story-5, story-8 | covered | Prove single removal and parked-path preservation. Completion checks inspected against these stories. |
| criterion | Story 1 happy: Given a checkout whose `git worktree list --porcelain` reports three worktrees directly under `.worktrees/` and no park markers, when the sweep runs, then all three are evaluated as candidates and the summary reports three candidates | task-5 | covered | "`reconcileParkedFeatures` evaluates the union of parked slugs and depth-1 registered worktrees once per slug, with the listed branch attached, no dependence on the mergeable watch registry, and the summary line reporting the enumerated candidate count, as asserted by the union, once-per-slug, and missing-registry tests" | diff-local |
| criterion | Story 1 happy: Given a checkout with one parked slug that has no worktree on disk and two registered worktrees that are not parked, when the sweep runs, then the candidate set is the union of the three and no slug appears twice | task-5 | covered | "`reconcileParkedFeatures` evaluates the union of parked slugs and depth-1 registered worktrees once per slug, with the listed branch attached, no dependence on the mergeable watch registry, and the summary line reporting the enumerated candidate count, as asserted by the union, once-per-slug, and missing-registry tests" | diff-local |
| criterion | Story 1 happy: Given a registered worktree at `.worktrees/hotfix-x` whose listed branch is `hotfix/x`, when the sweep builds its candidate for it, then the candidate carries slug `hotfix-x` and branch `hotfix/x` exactly as listed | task-1 | covered | "`listRegisteredWorktrees` returns one `{ slug, branch }` per depth-1 registered worktree under `.worktrees/` with the branch read from the porcelain `branch` line, as asserted by the depth-1 listing test" | diff-local |
| criterion | Story 1 happy: Given a checkout with no `.daemon/mergeable-watch.jsonl` at all, when the sweep runs, then candidates are still enumerated and the registry's absence is not logged as an error | task-5 | covered | "`reconcileParkedFeatures` evaluates the union of parked slugs and depth-1 registered worktrees once per slug, with the listed branch attached, no dependence on the mergeable watch registry, and the summary line reporting the enumerated candidate count, as asserted by the union, once-per-slug, and missing-registry tests" | diff-local |
| criterion | Story 1 negative: Given a registered worktree at a nested path `.worktrees/feat/daemon-x`, when the sweep enumerates, then it is not treated as a candidate named `feat` and is retained with reason `invalid-slug` | task-1 | covered | "`listRegisteredWorktrees` excludes a nested path, a path outside `.worktrees/`, and a detached worktree, as asserted by the exclusion test, so none of them can become a candidate" | diff-local |
| criterion | Story 1 negative: Given a plain directory under `.worktrees/` that `git worktree list` does not report, when the sweep enumerates, then it is not a candidate and nothing under that directory is removed | task-1 | covered | "`listRegisteredWorktrees` excludes a nested path, a path outside `.worktrees/`, and a detached worktree, as asserted by the exclusion test, so none of them can become a candidate" | diff-local |
| criterion | Story 1 negative: Given a registered worktree outside `.worktrees/` (for example the project root or `.claude/worktrees/x`), when the sweep enumerates, then it is never a candidate | task-1 | covered | "`listRegisteredWorktrees` excludes a nested path, a path outside `.worktrees/`, and a detached worktree, as asserted by the exclusion test, so none of them can become a candidate" | diff-local |
| criterion | Story 1 negative: Given `git worktree list --porcelain` fails, when the sweep runs, then every candidate for that pass is retained with reason `listing-unavailable` and no removal is attempted | task-6 | covered | "a failed worktree listing retains every candidate with reason `listing-unavailable` and makes zero helper calls, as asserted by the listing-failure sweep test" | diff-local |
| criterion | Story 2 happy: Given a candidate slug for which the sweep context reports `isFeatureInFlight` true, when the sweep evaluates it, then the guarded helper is not invoked for it and it is retained with reason `in-flight` | task-5 | covered | "an in-flight candidate is retained with reason `in-flight` before the helper is called even when fully proven, as asserted by the in-flight-wins test" | diff-local |
| criterion | Story 2 happy: Given a candidate whose directory name starts with `engineer-` or `resolve-`, when the sweep evaluates it, then the guarded helper is not invoked and it is retained with reason `foreign-lifecycle` | task-5 | covered | "`engineer-*`/`resolve-*`, invalid or nested slugs, and a present or unreadable `.pipeline/HALT` are each retained with reasons `foreign-lifecycle`, `invalid-slug`, and `halted` before the helper is called, as asserted by the exclusion tests" | diff-local |
| criterion | Story 2 happy: Given a candidate whose worktree contains a live `.pipeline/HALT`, when the sweep evaluates it, then the guarded helper is not invoked and it is retained with reason `halted` | task-5 | covered | "`engineer-*`/`resolve-*`, invalid or nested slugs, and a present or unreadable `.pipeline/HALT` are each retained with reasons `foreign-lifecycle`, `invalid-slug`, and `halted` before the helper is called, as asserted by the exclusion tests" | diff-local |
| criterion | Story 2 happy: Given a candidate whose slug fails the single-slug pattern, when the sweep evaluates it, then it is retained with reason `invalid-slug` | task-5 | covered | "`engineer-*`/`resolve-*`, invalid or nested slugs, and a present or unreadable `.pipeline/HALT` are each retained with reasons `foreign-lifecycle`, `invalid-slug`, and `halted` before the helper is called, as asserted by the exclusion tests" | diff-local |
| criterion | Story 2 negative: Given a candidate that is in flight and also carries every merge proof and a shipped record, when the sweep evaluates it, then it is still retained with reason `in-flight` and no path is removed | task-5 | covered | "an in-flight candidate is retained with reason `in-flight` before the helper is called even when fully proven, as asserted by the in-flight-wins test" | diff-local |
| criterion | Story 2 negative: Given a candidate whose `.pipeline/HALT` is unreadable for a reason other than absence, when the sweep evaluates it, then it is retained with reason `halted` rather than proceeding to the helper | task-5 | covered | "`engineer-*`/`resolve-*`, invalid or nested slugs, and a present or unreadable `.pipeline/HALT` are each retained with reasons `foreign-lifecycle`, `invalid-slug`, and `halted` before the helper is called, as asserted by the exclusion tests" | diff-local |
| criterion | Story 2 negative: Given a candidate whose slug is operator-parked and whose park marker read throws, when the sweep evaluates it, then it is retained and no removal is attempted | task-5 | covered | "a throwing park-marker read retains the candidate with no removal, as asserted by the marker-throw test" | diff-local |
| criterion | Story 2 negative: Given a sweep over N candidates of which exactly one is proven merged and eligible, when the sweep completes, then exactly one worktree path is removed and N-1 candidates are retained with named reasons | task-11 | covered | "a sweep over five candidates with one eligible slug issues exactly one `git worktree remove` for that path and retains the other four with named reasons, as asserted by the single-removal test" | diff-local |
| criterion | Story 3 happy: Given a candidate at `.worktrees/hotfix-x` on branch `hotfix/x` that is an ancestor of `origin/main`, when the helper runs with that branch, then ancestry proves it merged without consulting `gh` | task-2 | covered | "`gatherMergeEvidence` evaluates the supplied listed branch, so `hotfix-x`/`hotfix/x` is proven by ancestry and `<slug>`/`feat/daemon-<slug>` by merged-PR head identity, as asserted by the listed-branch evidence tests where the slug-segment lookup found no branch" | diff-local |
| criterion | Story 3 happy: Given a candidate at `.worktrees/<slug>` on branch `feat/daemon-<slug>` that is not an ancestor but whose MERGED PR `headRefOid` equals the branch tip, when the helper runs with that branch, then head identity proves it merged | task-2 | covered | "`gatherMergeEvidence` evaluates the supplied listed branch, so `hotfix-x`/`hotfix/x` is proven by ancestry and `<slug>`/`feat/daemon-<slug>` by merged-PR head identity, as asserted by the listed-branch evidence tests where the slug-segment lookup found no branch" | diff-local |
| criterion | Story 3 happy: Given a candidate whose listed branch is proven merged, when the helper completes, then the proven branch is the one deleted, not a branch that merely shares the slug's final path segment | task-2 | covered | "`reconcileMergedPark` deletes exactly the listed branch after proof, as asserted by the shared-final-segment test" | diff-local |
| criterion | Story 3 negative: Given a candidate whose listed branch is not an ancestor and has no merged PR, when the helper runs, then it refuses with `no-merge-proof` and nothing is removed | task-2 | covered | "each of `no-merge-proof`, `unmerged-commits`, `branch-missing`, and the gh-capability refusal returns before any removal, as asserted by the refusal tests that check every path and branch is untouched" | diff-local |
| criterion | Story 3 negative: Given a candidate whose listed branch has advanced past its merged PR head, when the helper runs, then it refuses with `unmerged-commits` naming the dropped commits and nothing is removed | task-2 | covered | "each of `no-merge-proof`, `unmerged-commits`, `branch-missing`, and the gh-capability refusal returns before any removal, as asserted by the refusal tests that check every path and branch is untouched" | diff-local |
| criterion | Story 3 negative: Given a candidate whose listed branch no longer exists locally and no shipped record is on `origin/main`, when the helper runs, then it refuses with `branch-missing` and nothing is removed | task-2 | covered | "each of `no-merge-proof`, `unmerged-commits`, `branch-missing`, and the gh-capability refusal returns before any removal, as asserted by the refusal tests that check every path and branch is untouched" | diff-local |
| criterion | Story 3 negative: Given `gh` reports a capability error while proving head identity, when the helper runs, then it refuses with `no-merge-proof` and nothing is removed | task-2 | covered | "each of `no-merge-proof`, `unmerged-commits`, `branch-missing`, and the gh-capability refusal returns before any removal, as asserted by the refusal tests that check every path and branch is untouched" | diff-local |
| criterion | Story 4 happy: Given a proven-merged candidate on branch `feat/daemon-<slug>` with `.docs/shipped/<slug>.md` on `origin/main`, when the helper runs, then the worktree is reclaimed | task-3 | covered | "`reconcileMergedPark` reclaims a proven-merged candidate on a non-`feat/daemon-*` branch with no shipped record and requests no record repair, and still returns `record-missing` with `deferred: true` for a `feat/daemon-*` candidate without one, as asserted by the branch-kind record tests" | diff-local |
| criterion | Story 4 happy: Given a proven-merged candidate on branch `hotfix/x` with no shipped record on `origin/main`, when the helper runs, then the worktree is reclaimed | task-3 | covered | "`reconcileMergedPark` reclaims a proven-merged candidate on a non-`feat/daemon-*` branch with no shipped record and requests no record repair, and still returns `record-missing` with `deferred: true` for a `feat/daemon-*` candidate without one, as asserted by the branch-kind record tests" | diff-local |
| criterion | Story 4 happy: Given a proven-merged candidate on branch `spec/<slug>` with no shipped record, when the helper runs, then the worktree is reclaimed and no record repair is requested | task-3 | covered | "`reconcileMergedPark` reclaims a proven-merged candidate on a non-`feat/daemon-*` branch with no shipped record and requests no record repair, and still returns `record-missing` with `deferred: true` for a `feat/daemon-*` candidate without one, as asserted by the branch-kind record tests" | diff-local |
| criterion | Story 4 negative: Given a proven-merged candidate on branch `feat/daemon-<slug>` with no shipped record on `origin/main`, when the helper runs, then it refuses with `record-missing`, defers, and requests record repair exactly as before | task-3 | covered | "`reconcileMergedPark` reclaims a proven-merged candidate on a non-`feat/daemon-*` branch with no shipped record and requests no record repair, and still returns `record-missing` with `deferred: true` for a `feat/daemon-*` candidate without one, as asserted by the branch-kind record tests" | diff-local |
| criterion | Story 4 negative: Given a candidate on branch `hotfix/x` with no merge proof and no shipped record, when the helper runs, then the absence of the record requirement does not make it reclaimable and it refuses with `no-merge-proof` | task-3 | covered | "a non-daemon candidate with no merge proof is refused `no-merge-proof` regardless of the record rule, as asserted by the no-proof hotfix test" | diff-local |
| criterion | Story 4 negative: Given the `origin/main:.docs/shipped` listing cannot be read, when the helper evaluates a `feat/daemon-*` candidate, then it refuses with `ancestry-check-failed` rather than treating the record as absent | task-3 | covered | "an unreadable shipped-record listing yields `ancestry-check-failed` for a `feat/daemon-*` candidate rather than treating the record as absent, as asserted by the unreadable-listing test; the existing parked record-missing deferral tests pass unchanged" | diff-local |
| criterion | Story 5 happy: Given a proven, eligible candidate, when the sweep reclaims it, then project teardown runs on the worktree path before `git worktree remove --force` of that single path | task-4 | covered | "`reconcileMergedPark` runs project teardown then removes the single worktree path and, for a non-parked candidate, returns reconciled with no `unparked` step and no unpark dispatch, as asserted by the non-parked reclaim test" | diff-local |
| criterion | Story 5 happy: Given a proven, eligible candidate that is not parked, when the helper completes, then it reports reconciled without attempting an unpark | task-4 | covered | "`reconcileMergedPark` runs project teardown then removes the single worktree path and, for a non-parked candidate, returns reconciled with no `unparked` step and no unpark dispatch, as asserted by the non-parked reclaim test" | diff-local |
| criterion | Story 5 happy: Given a proven, eligible candidate that is parked, when the helper completes, then the park marker is removed last, as today | task-4 | covered | "a parked candidate still ends with `unparked` as its last step, as asserted by the parked reclaim test" | diff-local |
| criterion | Story 5 happy: Given two proven, eligible candidates, when the sweep reclaims them, then two separate helper invocations occur, each with exactly one slug | task-11 | covered | "two eligible candidates produce two helper invocations each carrying exactly one slug, as asserted by the two-candidate test" | diff-local |
| criterion | Story 5 negative: Given a candidate whose `git worktree remove` fails on a path git owns, when the helper runs, then it refuses with `worktree-remove-failed` and the branch is not deleted | task-4 | covered | "`worktree-remove-failed` stops before branch deletion and `invalid-slug` is returned before any git call, as asserted by the refusal tests" | diff-local |
| criterion | Story 5 negative: Given a candidate whose branch deletion fails after the worktree was removed, when the helper runs, then it refuses with `branch-delete-failed` and the outcome is reported as failed, not reclaimed | task-8 | covered | "a `branch-delete-failed` or `worktree-remove-failed` outcome emits one `worktree_reclaim_failed` carrying the refusal and is reported as failed rather than reclaimed, as asserted by the failed-event test" | diff-local |
| criterion | Story 5 negative: Given the helper is invoked with a list or a glob instead of one slug, when it validates input, then it refuses with `invalid-slug` before any evidence is gathered | task-4 | covered | "`worktree-remove-failed` stops before branch deletion and `invalid-slug` is returned before any git call, as asserted by the refusal tests" | diff-local |
| criterion | Story 5 negative: Given a candidate whose evidence was classified merged by the sweep but changes before the helper re-derives it, when the helper runs, then the helper's own re-derived evidence decides and the stale classification is not trusted | task-4 | covered | "the helper's own re-derived evidence refuses a slug the caller classified merged but which changed before the destructive step, as asserted by the stale-classification test" | diff-local |
| criterion | Story 6 happy: Given a candidate is reclaimed, when the sweep completes it, then a `worktree_reclaim_reclaimed` event carrying the slug, plus the branch and its proof kind when the candidate had a branch, is emitted on the daemon's emitter | task-8 | covered | "the sweep emits exactly one `worktree_reclaim_reclaimed` per reclaimed candidate carrying slug plus the branch and its ancestry or merged-pr-head proof kind when a branch existed, and neither field when none did, as asserted by the reclaimed-event test" | diff-local |
| criterion | Story 6 happy: Given a candidate is retained, when the sweep completes it, then a `worktree_reclaim_retained` event carrying the slug and a closed-union reason is emitted | task-8 | covered | "the sweep emits exactly one `worktree_reclaim_retained` per retained candidate carrying its closed-union reason, as asserted by the retained-event test" | diff-local |
| criterion | Story 6 happy: Given a removal or branch deletion fails, when the sweep completes it, then a `worktree_reclaim_failed` event carrying the slug and the refusal is emitted | task-8 | covered | "a `branch-delete-failed` or `worktree-remove-failed` outcome emits one `worktree_reclaim_failed` carrying the refusal and is reported as failed rather than reclaimed, as asserted by the failed-event test" | diff-local |
| criterion | Story 6 happy: Given the three variants exist, when the sink registry is read, then each has a declaration with `persist: true`, `retained` has `render: false`, and `reclaimed` and `failed` have `render: true` | task-7 | covered | "`EVENT_SINKS` declares `worktree_reclaim_reclaimed` and `worktree_reclaim_failed` with `render: true, persist: true` and `worktree_reclaim_retained` with `render: false, persist: true`, as asserted by the sink declaration test, and the union exhaustiveness guard passes with the three variants appended at the end of the union" | diff-local |
| criterion | Story 6 negative: Given a `worktree_reclaim_retained` event, when the daemon renders its log, then no per-slug line is printed for it and the sweep's existing summary line still reports counts | task-7 | covered | "`renderDaemonEvent` prints one line for a reclaimed or failed event and nothing for a retained event, as asserted by the renderer test" | diff-local |
| criterion | Story 6 negative: Given a new `ConductorEvent` variant is added without a sink declaration, when the exhaustiveness guard runs, then it fails naming the variant | task-7 | covered | "`EVENT_SINKS` declares `worktree_reclaim_reclaimed` and `worktree_reclaim_failed` with `render: true, persist: true` and `worktree_reclaim_retained` with `render: false, persist: true`, as asserted by the sink declaration test, and the union exhaustiveness guard passes with the three variants appended at the end of the union" | diff-local |
| criterion | Story 6 negative: Given the emitter is absent (no root emitter injected), when the sweep runs, then it completes without throwing and the summary line is still logged | task-8 | covered | "the sweep completes and logs its summary line when no emitter is injected, as asserted by the missing-emitter test" | diff-local |
| criterion | Story 6 negative: Given a reclaim removes the worktree, when the pass completes, then the reclaim event is present in the daemon-root ledger even though the worktree's own `.pipeline/events.jsonl` no longer exists | task-10 | covered | "a merged non-daemon worktree in a temp repo is removed by one sweep pass and its `worktree_reclaim_reclaimed` record is present in the daemon-root `events.jsonl` after the worktree is gone, as asserted by the integration test" | diff-local |
| criterion | Story 7 happy: Given `reclaim_merged_worktrees` is absent from config, when the daemon reads its config, then the enumerated path is enabled | task-9 | covered | "`validateConfig` rejects a non-boolean `reclaim_merged_worktrees` with an error naming the key, defaults an absent key to `true`, and preserves `false`, as asserted by the config validation tests" | diff-local |
| criterion | Story 7 happy: Given `reclaim_merged_worktrees: true`, when the sweep runs, then enumerated candidates are evaluated and eligible ones are reclaimed | task-10 | covered | "a merged non-daemon worktree in a temp repo is removed by one sweep pass and its `worktree_reclaim_reclaimed` record is present in the daemon-root `events.jsonl` after the worktree is gone, as asserted by the integration test" | diff-local |
| criterion | Story 7 happy: Given `reclaim_merged_worktrees: false`, when the sweep runs, then enumerated candidates are evaluated and every eligible one is retained with reason `disabled` and emitted, and none is removed | task-6 | covered | "`reclaimMergedWorktrees: false` evaluates enumerated candidates, retains eligible ones with reason `disabled`, and removes nothing, as asserted by the disabled-gate test" | diff-local |
| criterion | Story 7 negative: Given `reclaim_merged_worktrees: "yes"` (a non-boolean), when config validation runs, then it returns an error naming the key and the daemon does not start | task-9 | covered | "`validateConfig` rejects a non-boolean `reclaim_merged_worktrees` with an error naming the key, defaults an absent key to `true`, and preserves `false`, as asserted by the config validation tests" | diff-local |
| criterion | Story 7 negative: Given `reclaim_merged_worktrees: false` and `reconcile_parked_auto_cleanup: true`, when the sweep runs, then parked merged slugs are still reclaimed exactly as today and only the enumerated non-parked candidates are held | task-6 | covered | "with the gate off, parked merged slugs are still reclaimed under `autoCleanup` while enumerated non-parked candidates are held, as asserted by the gate-off-parked test" | diff-local |
| criterion | Story 7 negative: Given the key is added to the documented key list, when the config-key consumer registry test runs, then it fails unless the key declares its consumer | task-9 | covered | "`reclaim_merged_worktrees` is declared in the config-key consumer registry with consumer `daemon-cli.ts`, so the registry test passes with the key documented" | diff-local |
| criterion | Story 8 happy: Given only parked slugs on `feat/daemon-*` branches and no registered worktrees beyond them, when the sweep runs, then classifications, counts, refusal breakdown, and the de-duplicated summary line are identical to the pre-change behavior, except that a parked slug retained by a Story 2 guard (in-flight, or a live or unreadable `.pipeline/HALT`) keeps its named reason instead of being reclaimed | task-11 | covered | "with only `feat/daemon-*` parked slugs, whether the worktree listing is empty or names only those slugs' own worktrees, an unmerged park never reaches the helper and classifications, counts, refusal breakdown, and the summary line match the pre-change expectations except that a parked slug retained by a Story 2 guard keeps its named reason instead of being reclaimed, an orphaned park is annotated `orphan` and never deleted, and an unreadable record listing yields `unclassified` with no removal, as asserted by the preservation tests" | diff-local |
| criterion | Story 8 happy: Given a parked slug that is orphaned (closed source issue, not merged), when the sweep runs, then it is annotated `orphan` and never deleted, as today | task-11 | covered | "with only `feat/daemon-*` parked slugs, whether the worktree listing is empty or names only those slugs' own worktrees, an unmerged park never reaches the helper and classifications, counts, refusal breakdown, and the summary line match the pre-change expectations except that a parked slug retained by a Story 2 guard keeps its named reason instead of being reclaimed, an orphaned park is annotated `orphan` and never deleted, and an unreadable record listing yields `unclassified` with no removal, as asserted by the preservation tests" | diff-local |
| criterion | Story 8 negative: Given the record listing is unreadable, when the sweep runs over parked slugs, then each is classified `unclassified` and no removal occurs, as today | task-11 | covered | "with only `feat/daemon-*` parked slugs, whether the worktree listing is empty or names only those slugs' own worktrees, an unmerged park never reaches the helper and classifications, counts, refusal breakdown, and the summary line match the pre-change expectations except that a parked slug retained by a Story 2 guard keeps its named reason instead of being reclaimed, an orphaned park is annotated `orphan` and never deleted, and an unreadable record listing yields `unclassified` with no removal, as asserted by the preservation tests" | diff-local |
| criterion | Story 8 negative: Given a parked slug whose worktree was already removed by the enumerated path in the same pass, when the parked classification reaches it, then it is not evaluated a second time and is not double-counted | task-5 | covered | "`reconcileParkedFeatures` evaluates the union of parked slugs and depth-1 registered worktrees once per slug, with the listed branch attached, no dependence on the mergeable watch registry, and the summary line reporting the enumerated candidate count, as asserted by the union, once-per-slug, and missing-registry tests" | diff-local |

## Consistency and achievability judgment

Every story is cited by at least one task's Story line and every task cites at least one story. Each criterion row quotes a completion check from a cited task's Done-when block naming the mechanism that produces the Then-clause.

Preserved-behavior sweep: Story 8's "identical to the pre-change behavior" promise names classifications, counts, refusal breakdown, and the summary line as the protected observables. Task 8 adds event emission on the same path; events are not among the named observables, so they do not defeat the promise. Task 5 extends the summary line; its fifth completion check confines the new fields to passes with at least one enumerated non-parked candidate, so a parked-only pass prints today's line byte-for-byte, and Task 11 pins that. No contradiction or oscillation was found across outcome↔task, ADR↔story, or story↔task pairs: the in-flight exclusion (Story 2) and reclamation (Story 4) are ordered by Task 5's exclusion-before-helper check; the gate (Story 7) holds only enumerated candidates while parked reclamation is unchanged (Task 6).

Outcome-4 gap: the operator excluded an on-demand reclaimability command from scope during explore; retained and reclaimed events plus the summary line make each disposition observable, but not from one command. Gap id: outcome-4, waived.

## ADR decision tie-out

adr-2026-08-01 D6 → Tasks 1, 5; D7 → Task 2; D8 → Task 3. adr-2026-07-29 D8 → Tasks 5, 6, 10; D9 → Tasks 5, 6, 8. adr-2026-07-27 D1 → Task 5; D4 → Task 3; D5 → Task 5 (see the adr row note); D6 → Task 4. All other decisions are existing or no-change with cited evidence in the plan's obligation table, each checked against the named source file.

## Observation declaration

No observation-only rows. Every criterion is diff-local: its truth is decided by code and deterministic fakes inside this feature's diff.

## Verify-claims ledger

- Story-to-task citations were derived from the plan's Story lines and every cited task exists in the plan, 100%.
- Criterion text is the extractor's prefixed form and every quote is a verbatim Done-when bullet of a cited task, checked by substring, 100%.
- Outcome-4's gap is an operator decision recorded in the track marker, 100%.
