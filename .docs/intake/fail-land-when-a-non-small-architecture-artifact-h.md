# Intake origin: fail-land-when-a-non-small-architecture-artifact-h

Source-Ref: jstoup111/ai-conductor#729
Owner: jstoup111

## Desired outcome
A deterministic check fails (or loudly flags) a non-Small architecture artifact containing zero mermaid fences, at the moment it is produced — Small tier and legitimately diagram-free docs (if any class exists) explicitly exempted.
