# Intake origin: reaped-stale-claim-jstoup111-ai-conductor-2054

Source-Ref: jstoup111/ai-conductor#2054
Owner: jstoup111

## Desired outcome

- An ADR authored per the project's own template can always have its decisions cited as governing clauses — authoring a valid ADR never produces an uncitable decision.
- A decision shape that no consumer can cite is rejected when the ADR is authored or landed, naming the offending decision, rather than surfacing later as a halt during validation.
- The two APPROVED ADRs with no `## Decision` heading are either corrected or explicitly recognized as a form clause resolution accepts.
- Adding a new consumer of ADR decisions does not require rediscovering which shapes exist in the corpus.
