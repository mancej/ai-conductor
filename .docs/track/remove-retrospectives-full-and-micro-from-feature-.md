# Track: remove retrospectives (full and micro) from feature delivery

Track: technical

Scope boundary: Full purge — the `retro` engine step (rebase rewired to `architecture_review_as_built`, preserving the #922 serial ship-tail fence), the daemon-completion retro narrative provider call in engineer-store (halt narratives stay), the `micro-retro` closeout obligation, `skills/retro/` and all embedded skill/doc/template/test references, config/model-table entries. Historical `.docs/retros/` reports and retro-era spec artifacts stay as records. Recovery anchor: git tag `retro-last` (created by the operator session, not a plan task). #717 closes as obsoleted; #939 re-scoped.

Rationale: machinery removal with no new product capability; acceptance criteria live in stories.
