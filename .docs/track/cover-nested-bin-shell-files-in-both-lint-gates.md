# Track: Cover nested bin shell files in both lint gates

Track: technical

Scope boundary: Small fix for #2161, approved by the operator on 2026-09-06 (delegated). Make the shell-script enumeration that feeds both the `bash -n` syntax gate and the shellcheck gate recurse under `bin/`, so `bin/lib/harness-common.sh` and any future nested shell file are checked; make the two gates read one enumeration instead of two hand-maintained ones; and give deliberate exclusions a declared list. Raising the shellcheck severity floor, reworking the assertion helper, widening the enumerated roots beyond `bin/`, `hooks/`, `test/`, and `.github/scripts/`, and changing any script the gates newly cover are outside this slice.

This is a repository validation-gate correction; acceptance criteria live in technical stories rather than a PRD.

The operator-delegated decision on 2026-09-06 was to make the existing enumerator in `test/lint_shell.sh` recursive and let the integrity suite's syntax check consume that same enumeration, rather than adding a second recursive `find` to the integrity script. The filer's hypothesis proposed a recursive `find` in both scripts; that was weighed and narrowed, because two independently-maintained recursive enumerations reproduce the drift this issue is about — the two gates already claim a shared surface in `test/lint_shell.sh`'s own header and only one of them enforces it.

Scope check: A — harness-repo-only; step 1 signal 2 fires directly (the change touches `test/test_harness_integrity.sh` and `test/lint_shell.sh`, this repository's own validation gates). B — n/a (no new skill). C — provider-agnostic (no host, model, or provider surface is touched). No catalog registration is required. The rule this change enforces is purely additive to this repository's own gates and narrows no shared convention, so no "for consumer projects … remains unchanged" sentence is owed.

Verified foundation: `test/lint_shell.sh:36-44` enumerates `"${HARNESS_DIR}"/bin/*` with a `[ -f ]` guard, so the `bin/lib/` directory is skipped, and its `find` covers only `hooks/`, `test/`, and `.github/scripts/`; `test/test_harness_integrity.sh:70-102` runs four separate non-recursive loops of the same shape. `bin/lib/harness-common.sh` is tracked with a `#!/usr/bin/env bash` shebang and is absent from `test/lint_shell.sh --list`. `bin/conduct-ts` is a symlink (git mode 120000) to `bin/ai-conductor`, which today's `[ -f ]` guard follows, so the replacement enumeration must follow symlinks to keep it. `bin/lib/migration_fences.py` opens with a docstring and no shebang, so the existing shebang filter already excludes it. `bin/lib/harness-common.sh` is clean under `shellcheck --severity=error` (and at `warning`) and parses under `bash -n` today, so widening the gates lands green.
