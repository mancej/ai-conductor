# Track: Accurate paths in the land cleanliness refusal

Track: technical

Scope boundary: Small fix for #1300, approved by the operator on 2026-09-06 (delegated). Only the refusal diagnostic is in scope: the message that `landSpec` raises when its worktree-cleanliness guard rejects a land must name each blocking path in its true category, and the canonical guide entry describing that refusal must match. The guard's accept/reject decision is unchanged, and the reader-facing guide bullet that repeats the same inaccuracy is corrected in the same change.

Deliberately excluded: the issue's first, second, fourth, and fifth desired outcomes, which together ask for a supported route to revise an already-landed spec on its own spec branch. That half is an open design decision — whether a revision is signalled by a flag, by the `reconcile` state of an attached worktree, or by a separate verb; how it keeps the stale-leftover protection intact; and how it interacts with the branch-deletion hook. Nothing in the repository decides it: there is no `reland` or `revise` entry point anywhere in the engine, and the one precedent, the merged revision of the daemon/engineer rename spec, went through the issue's own scratch-worktree workaround rather than a supported command. The runbook for a no-longer-wanted spec covers abandonment, not revision. #1300 therefore stays open after this change, carrying only the undecided revise-route half.

This is an internal engine diagnostic and its reference documentation; acceptance criteria live in technical stories rather than a PRD.

Approaches weighed: (A) correct only the sentence at the throw site, leaving one flat list; (B) classify the blocking entries into the two conditions the guard actually enforces and render one labelled line per condition, with a remedy naming only the applicable actions; (C) defer the message entirely until the revise route is designed. B was selected and recorded as approved by the operator on 2026-09-06 (delegated): A still leaves a reader unable to tell which of two different problems each listed path represents, and C leaves a verified wrong diagnostic in place for an undecided amount of time. C's concern is answered by keeping the guard's decision byte-for-byte unchanged, so the revise-route design still starts from the same behavior it would have started from.

Scope check: A — consumer-facing; the deciding signal is that `landSpec` is engine code every installed repository runs through `ai-conductor compose land`, and no repo-only signal fired (no self-host, sandbox, integrity-suite, release-gate, CI, or repo-private-convention surface is touched). B — n/a; no skill is added. C — provider-agnostic; the guard reads `git status --porcelain` and raises an Error, with no provider path, variable, or capability on either side. Registration: none beyond this repository's documentation-upkeep rule, which the guide correction satisfies.

Verified foundation: the guard lives at `src/conductor/src/engine/engineer/land-spec.ts`, step 2, and filters `git status --porcelain` lines — `??` entries are kept only when the path does not start with the artifact directory prefix, and every other prefix is kept unconditionally. The throw that follows reports the whole filtered set as "changes outside" that directory, which is false for the tracked-modification half. The engineer-loop guide's `land` refusal list repeats the same claim in one bullet. The composer skill's own summary says only "a dirty worktree" and is already accurate, so it needs no edit. No `reland`, `revise`, or second land entry point exists in the engine.
