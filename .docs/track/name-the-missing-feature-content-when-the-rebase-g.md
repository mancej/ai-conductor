# Track: Name the missing feature content when the rebase guard rejects

Track: technical

Scope boundary: Small fix for #1497, approved by the operator on 2026-09-06 (delegated). Two of the issue's four desired outcomes are already delivered on main and are only regression-pinned here. This slice delivers the remaining two: the work-preservation guard's rejection must name which feature content is missing rather than reporting that a commit subject disappeared, and a HALT raised by that guard must carry a resume procedure matching the actual repository state. Widening or loosening the guard's supersession judgement, changing the conflict resolver, changing halt classification, changing the halt record schema, and rewriting operator runbook prose are outside this slice.

This is an internal engine correction to rebase acceptance-guard diagnostics; acceptance criteria live in technical stories rather than a PRD.

The operator approved carrying the missing-content evidence on the guard's return value (a discriminated verdict) over recomputing it at the halt-writing site on 2026-09-06 (delegated). The guard is the only place that already knows which subject failed and why the content check rejected it; recomputing later would re-run the same git inspection against a tree that may have moved.

Scope check: A — consumer-facing engine behavior (the rebase step runs in any repository that installs the harness; no self-host, release-gate, or repo-local-convention signal fires), but it introduces no behavioral rule and no documentation contract, so no HARNESS.md entry is required. B — n/a (no new skill, so no catalog registration). C — provider-agnostic: the change is pure git inspection and string construction in the engine, with no provider dispatch, prompt, or model selection.

Verified foundation: `supersededByBase` at src/conductor/src/engine/rebase.ts:922 already performs the content-based check that closes the issue's first and third outcomes, and `featureCommitsPreserved` at rebase.ts:985 routes every missing subject through it; that work landed as commit 865167e9e ("fix(rebase): stop halting when the base already carries a dropped commit (#1544)"). Both return bare booleans, so the FR-9 rejection at rebase.ts:1130-1132 emits the fixed string `rebase resolution dropped feature commit(s)` and the parallel guard at src/conductor/src/engine/autoresolve.ts:487-497 emits the first three PRE-rebase subjects — neither names the content actually missing. `writeHalt` at rebase.ts:454 templates one unconditional resume procedure ("Resolve the conflicts in the listed file(s)" then `git rebase --continue`), and the FR-8/FR-9 rejections at rebase.ts:1121 and rebase.ts:1130 are reached only after `resolveRebaseConflicts` has confirmed at rebase.ts:1108-1112 that no rebase is active and no conflicted paths remain, so that procedure is wrong exactly on this path.

Decision-record sweep: all 559 approved decision records were read on 2026-09-06. None types or governs the rebase outcome union, and none fixes the halt resume procedure as immutable text. The decision that created these guards leaves an open follow-up asking for the preservation check to be defined precisely, and its architecture review's condition 2 asks for the same, so this slice discharges the diagnostic half of both. It conforms to the approved constraints that the guard stays engine-native and prompt-free, that the guard predicate itself is unchanged, that the halt keeps the single sanctioned marker writer and the needs-human class, that each gate emits a distinct and actionable halt reason, that a halt note may not assert a state that is not true, that routing keys on result kind rather than reason text, and that the halt body renders deterministically because it is committed and pushed. No amendment to any approved record is required and none is authored.
