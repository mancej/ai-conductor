# Track: Harness integrity verification runs in BUILD, not SHIP

Track: technical

Scope boundary: Small fix for #658, re-scoped and approved by the operator on 2026-09-06. Declare the harness integrity suite as a second verification entry of this repository's BUILD test_suite step, and delete the integrity sub-gate (TR-8) from the finish-plane release gate so no test executes in SHIP. The migration-block sub-gate (TR-10), its waiver evaluation, and the VERSION-approval gate are untouched. The engine support for an ordered list of test-suite commands is a separate feature owned by issue #2358; this slice only consumes the key that feature introduces. The earlier self-heal/remediation-routing lane explored for this issue is withdrawn and out of scope.

This is a self-host and repository-validation correction, so acceptance criteria live in technical stories rather than a PRD.

The operator chose relocation over self-healing on 2026-09-06: an integrity failure whose remediation the check already prints belongs in the build loop that can run it, not in a finish-time halt. That choice makes SHIP test-free rather than adding a second remediation mechanism to the finish plane.

Scope check: A — harness-repo-only. Three repo-only signals fire together: the change touches self-host machinery under the engine's self-host directory, it touches this repository's own validation and release gates, and it touches this repository's own project configuration. No shared behavioral rule is narrowed or replaced, so no consumer-convention sentence is owed. B — n/a, no new skill. C — provider-agnostic: the gate deletion and the configuration entry are deterministic engine and config surface with no provider-specific code path. No catalog registration is required.

Verified foundation: this repository's project configuration declares a single `command: npm test` with `working_directory: src/conductor` and `verification.mode: aggregate`, and its own comment still calls harness integrity "a separate self-host gate". The engine's self-host release-gate module defines the integrity script constant, a default timeout, an exec seam, and the suite function, and the composed gate runs the suite before it classifies breaking surfaces. The gate's callers pass no integrity field: the guardrail bundle only forwards the composed gate, and the conductor's finish-gate method supplies project root, harness root, a text reader, a change-set enumerator, release metadata, and a halt writer. The continuous-integration workflow already runs the same script as its own required job on every non-documentation diff. The full-suite fingerprint hashes the suite's command, working directory, timeout, declared inputs, and environment, and includes the counterfactual scoped command only when the verification mode is scoped — which this repository's is not.
