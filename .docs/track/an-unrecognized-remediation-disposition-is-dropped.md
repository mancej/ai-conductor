# Track: an-unrecognized-remediation-disposition-is-dropped

Track: technical

Scope boundary: reporting only — a typed rejection from `readRemediationPlan`, one spine event per dropped disposition, and a halt that names the word, the finding, and the accepted vocabulary; mixed outputs still route the recognized gaps. Skill/engine vocabulary ownership (who defines the disposition set, how a self-hosted feature that widens it is dispatched) is out of scope and gets its own intake.

Engine routing/telemetry fix for #2187 with no user-facing product capability; acceptance criteria live in stories.
