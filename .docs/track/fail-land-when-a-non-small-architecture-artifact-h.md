# Track: Fail land when a non-Small architecture artifact has no mermaid diagram

Track: technical

Scope boundary: Small fix for #729, approved by the operator on 2026-09-06 (delegated). Add one deterministic presence check at the existing engineer land seam: a non-Small spec's own architecture artifact must contain at least one fenced mermaid block. Out of scope — auditing or backfilling architecture documents already merged on the base branch, any new CLI flag or config key, changing how diagrams are rendered or parsed, extending the check to ADRs, review artifacts, plans, or the architecture sequences subdirectory, and any change to the build-time architecture-review steps.

This is internal harness tooling that hardens an existing gate; acceptance criteria live in technical stories rather than a PRD.

The operator's issue offered "fails (or loudly flags)". Fail-closed was chosen on 2026-09-06 (delegated) to match the posture of the mermaid render gate already sitting three statements away in the same function, which refuses rather than warns. A warning is exactly the prompt-adherence outcome the issue reports as insufficient — twice.

Scope check: A — consumer-facing. No repo-only signal fires: the change touches no self-host, daemon or sandbox path, no repository validation or release gate, no workflow, and no convention private to this repository. Step 2 answers YES — a repository that installed the harness, has no daemon and no inherited `.docs/` history, still authors non-Small specs through `engineer land` and benefits identically. B — n/a, no new skill; the one-sentence skill-text edit lands in the existing shipped `skills/architecture-diagram/SKILL.md`. C — provider-agnostic; the check is engine code reached identically from every host, and the skill sentence names no provider mechanism. Registration required: the canonical affected documentation pages only (`docs/explanation/gates.md` land-time gate table, `docs/reference/artifacts.md` architecture row). No model-table row, no catalog symlink, no `HARNESS.md` rule.

Verified foundation: `src/conductor/src/engine/engineer/land-spec.ts` step 4d resolves `tier` from `.docs/complexity/` via `parseComplexityTier`, and for `tier && tier !== 'S'` already resolves `architectureFile` through `pickIdeaFile` and refuses when it is absent — so the tier discrimination, the artifact pick, and the refusal shape the fix needs all exist. Step 4f then runs `checkDiagramsForFile` over the git-changed `.docs/**/*.md` set; `src/conductor/src/engine/mermaid-renderer.ts:258` returns `no-diagrams` for a fence-less file and land treats that as a pass, which is the reported defect. `extractMermaidBlocks` is already exported from the same module and anchors its fences to line starts. `resolveIdeaFiles` builds the attribution set from the branch diff against the default branch plus untracked files, so `pickIdeaFile` can never return a document inherited from the base branch — no grandfathering clause is needed, and a sweep of the 230 committed architecture documents found exactly one fence-less file, which the gate will never see.
