# Track: Support Astra in the daemon

Track: product

Scope boundary: Make `gpt-6-astra` a first-class opt-in model everywhere daemon model configuration is supported, including configuration, dispatch and fallback behavior, and automatic cost-rate coverage. Do not change any default model selection or Claude policy.

This is product work because it adds an operator-visible daemon configuration capability with observable execution and metering behavior.
