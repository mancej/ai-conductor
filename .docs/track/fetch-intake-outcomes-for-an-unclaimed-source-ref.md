# Track: Stage intake outcomes for an unclaimed source ref

Track: technical

Scope boundary: Small fix for #1340, approved by the operator on 2026-09-06 (delegated). Resolve the Desired-outcome body for a by-ref idea from the originating issue when no claim record and no explicit body exist, report at worktree-creation time when no outcome layer could be staged, and make the coherence refusal distinguish a never-staged outcome layer from a genuinely absent id. Reconciling the message-accuracy work with the neighbouring land-message defect (#1300), revising the coherence gate's own design, changing the committed intake marker's authorship, and any tracker backend other than the existing GitHub seam are outside this slice.

This is internal engine and CLI tooling behavior with no product requirement; acceptance criteria live in technical stories rather than a product requirements document.

The operator's delegate chose "resolve the body from the issue" over "refuse the command" on 2026-09-06 (delegated). The issue's desired outcome allows either, and refusing would make tracker reachability a hard precondition of creating a worktree — a regression for offline work and for a reference grammar the GitHub seam cannot read. Resolution is attempted first; when it yields nothing, the command still succeeds and says so immediately, which is the part of the desired outcome that actually removes the late surprise. The filer's fourth hypothesis (documenting the body flag in the loop's skill text) was rejected as prompt discipline where machinery decides: once the ref itself supplies the body, the loop's already-documented step is the discoverable mechanism and no new prose is owed.

Scope check: A — consumer-facing (no self-host, daemon, release-gate, or repository-private convention is touched; the composer loop and its outcome staging ship to every repository that installs the harness, so the engine and its command reference are the surfaces); B — n/a (no new skill); C — provider-agnostic (engine-side behavior with no host-specific language). No catalog registration is required. Event spine: no channel is added — the existing staging writer and the existing gate refusal are reused, and the new diagnostic is operator-facing command output, not telemetry.

Verified foundation: read in the worktree. `engineer-cli.ts:915-924` resolves the by-ref body only from a persisted claim record and leaves it undefined when none exists; `worktree-authoring.ts` stages nothing unless both a ref and a body are present; `outcome-staging.ts:50-69` returns null on an empty body, and its reader then reports the outcome layer as not required; the committed-marker fallback cannot help on a first land because that marker is written by land itself. `land-spec.ts:429-451` therefore passes zero bullets into the gate, and `coherence-validator.ts:1650-1664` rejects every outcome row with a message that names the coherence artifact. `tracker-client.ts` already exposes `getIssueBody(repo, issueRef, cwd)` on the canonical `TrackerClient` seam, returning `null` on a 404, and `engineer-cli.ts:731` already holds the injectable `gh` runner that seam is built from.
