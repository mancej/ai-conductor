# Track: Name the directing text when remediation redirects a gap away from build

Track: technical

Scope boundary: Small diagnostic completion of jstoup111/ai-conductor#1851, approved by the operator on 2026-09-06 (delegated). The detection half of that issue already shipped in PR #2150, which gave remediation task titles the same directed-edit clause test the rationale prose already used. What remains is the issue's fourth desired outcome: when a redirect does fire, the operator must be able to tell a true positive from a false one without reading engine source. This slice makes the redirect carry the exact text it read as directing the edit, through the redirect event, the daemon renderer, and the DECIDE-entry halt body that the operator actually reads. Changing the detector's verb vocabulary or clause boundaries, revisiting whether a redirect to an ungrantable DECIDE step is the right routing shape at all (the issue's second hypothesis), and any new grant or recovery path are outside this slice.

This is an internal engine diagnostics correction with no product requirement behind it; acceptance criteria live in technical stories rather than a PRD.

The operator approved carrying the directing text on the existing redirect event and annotating the existing remediation evidence string, over the alternative of a bespoke diagnostic sidecar written beside the halt marker, on 2026-09-06 (delegated). The existing spine already emits an occurrence for this exact moment; a second channel would be invisible to the renderer, the persisted ledger, and the audit trail.

Event spine: Channel? no — an optional field is added to the existing `remediation_sealed_artifact_redirect` variant. Concern: occurrence — the redirect is a moment in time a consumer needs to know about. Verdict: extend the union. Additive optional fields on an existing variant are the sanctioned shape; no sibling ledger and no ADR are required.

Scope check: A — consumer-facing engine behavior (the redirect and its halt exist in every repository that installs the harness engine, not only in this one); B — n/a (no new skill); C — provider-agnostic (no provider name, model, or host facility is referenced). No skill-catalog registration, no model-table row, and no HARNESS.md rule follow from a code-only diagnostics change. The operator-facing runbook mention is ordinary documentation upkeep carried by the implementation PR, not a plan task.

Verified foundation: `remediationGapTargetsAnotherFeatureSealedArtifact` at `src/conductor/src/engine/conductor.ts:12949` now runs each task title and the gap rationale through the same private `directedProtectedTarget` helper at `:12976`, which returns only a path string and discards the clause it matched. `planRemediation` collects those paths into `sealedArtifactsByGapId` at `:3895`, emits one redirect event per entry at `:3904` carrying only `gapId` and `artifact`, and rewrites the gap disposition to `plan` at `:3907`. The same method builds its operator-facing evidence string at `:4629` as bare `id→disposition` pairs, hands it to `resolveDecideEntryDisposition`, and renders it into the halt body through `renderDecideEntryHalt` at `src/conductor/src/engine/decide-entry-policy.ts:112-124`, whose `Evidence:` line is what the operator reads. `plan` is ungrantable at `decide-entry-policy.ts:29`, so this halt is terminal and its body is the operator's only diagnostic. The event variant is declared at `src/conductor/src/types/events.ts:555` and rendered at `src/conductor/src/daemon-cli.ts:2873`; `src/conductor/src/engine/event-sinks.ts:78` already persists and renders it.
