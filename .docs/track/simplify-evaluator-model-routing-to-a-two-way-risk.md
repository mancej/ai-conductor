# Track: Simplify evaluator model routing to a two-way risk switch

Track: technical

Scope boundary: Small routing-policy simplification for #193, approved by the operator on 2026-09-06 (delegated). Replace the evaluator's multi-category content classification with one risk-domain criterion in the typed model-table metadata, the generated HARNESS.md row, the hand-synced reference mirror row, and the shipped evaluator dispatch guidance. Evaluator fresh-context isolation, dispatch frequency, batch diff scope, the closeout-obligation gate, the domain-reviewer diff-size routing, and every other model-table row are outside this slice.

This is an internal routing-policy correction to shipped harness guidance; acceptance criteria live in technical stories rather than a PRD.

The operator approved a two-way switch with the top Claude tier on risk domains over keeping a graded three-way table on 2026-09-06 (delegated). A graded table reintroduces the classification cost the issue exists to remove; the two-way switch leaves exactly one question per dispatch.

Scope check: A — consumer-facing (no repo-only signal fires; the evaluator dispatch guidance and the model-selection table install into every repository that uses the harness, and a consumer with no daemon and no `.docs/` history still dispatches this evaluator); B — n/a (no new skill); C — scoped-with-both-seams (every Claude model parameter stays on a line that names Claude Code, the existing Codex delegation paragraph in `skills/code-review/SKILL.md` continues to own the other host seam, and the row's Codex model/effort columns keep their inheritance placeholder). No catalog registration is required.

Verified foundation: `src/conductor/src/engine/model-table-metadata.ts:218-224` holds the evaluator entry in `EXTRA_MODEL_TABLE_ROWS`, whose `claudeModel` string is the multi-category list; `bin/generate-model-table` renders it into the `<!-- BEGIN GENERATED: model-selection-table -->` region of `HARNESS.md` (row at `HARNESS.md:269`, markers at `:236` and `:291`), and integrity check 5a fails on hand-edited drift. `docs/reference/models.md:223` carries a hand-synced mirror of the same cell. `skills/code-review/SKILL.md:60-64` states the same list as two dispatch bullets under an already Claude-scoped delegation paragraph (`:24-29`). `agents/evaluator.md` names no model and needs no change. `skills/pipeline/SKILL.md:357-363` carries a separate tier/batch-position table whose frequency columns this slice leaves untouched. `src/conductor/src/tools/generate-model-table.ts:340-362` constrains only the Codex columns, so the Claude cell is free-form; `src/conductor/src/engine/provider-model-policy.ts:148-149` and `HARNESS.md:336-344` already document the `fable→opus→sonnet` availability ladder delivered by the now-closed #186.
