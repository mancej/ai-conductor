# Track: Refuted build_review finding cycles to a needs-human halt instead of settling

Track: technical

Scope boundary: Full jstoup111/ai-conductor#2409 outcome, operator-confirmed 2026-09-09 as the
bounded refutation lane. The `remediate` case-v1 judge may settle a re-raised, already-attempted
`act` case as refuted through a schema-constrained record that the engine admits only under
mechanical bounds (attempted case, once per case, high confidence, resolvable evidence anchors);
the refutation rationale is persisted and rendered by `build-review findings`; an unrefuted repeat
still halts as a semantic case repeat. The narrow true remainder of a refuted finding routes through
the existing `defer` effect (intake issue), not a new build_review-to-simplify seam. Excluded:
prompt-only rerouting of refutations into fresh reject cases, mechanically verified refutation
(mutation probes), and the no-new-behavior lane tracked by #1790.

Engine adjudication internals plus the remediate skill contract; no product requirements, acceptance
criteria live in stories.
